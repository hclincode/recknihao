# Iter53 Q2 Score — Storage Sizing: Cost-per-Event Measurement for Budget Planning

**Topic**: Storage sizing and growth estimation for lakehouse workloads
**Question angle (new for this topic)**: Per-event-type bytes-per-row measurement via `$files`, simple GB-per-month forecasting formula, MinIO EC overhead, and Zstd default codec.

| Dimension | Score | Reasoning |
|---|---|---|
| Technical accuracy | 4 | Core mechanic (query `$files` for `file_size_in_bytes` / `record_count`, divide for bytes-per-row) is correct. Trino syntax `iceberg.analytics."events$files"` with double-quotes is correct. Per-column compression breakdown accurate (UUIDs/JSON low ratio, low-cardinality strings high ratio). EC:4+2 = 1.5x raw verified correct. Zstd default in Iceberg 1.4.0+ correct, and `ALTER TABLE ... SET PROPERTIES "write.parquet.compression-codec" = 'zstd'` Trino syntax is correct. **Bug**: the per-event-type query `SELECT event_type ... FROM "events$files" GROUP BY event_type` is INVALID — `$files` is file-level metadata and does not expose row-level columns like `event_type`. To break out bytes/row by event type, you either (a) partition the table by event_type and use the `partition` column from `$files`, (b) sample rows from the base table with `pg_column_size`-style measurement, or (c) compute per-file bytes-per-row then average over files in the relevant partition. An engineer copying this query will get a "Column 'event_type' cannot be resolved" error. |
| Beginner clarity | 5 | Excellent structure: TL;DR formula up front, worked example with concrete numbers (50M page views × 6.2 bytes = 310 MB/month), tabular breakdown of compression ratios with plain-English "why" column, and a final spreadsheet checklist. The forecasting math is shown end-to-end with units annotated. No unexplained jargon left dangling. |
| Practical applicability | 4 | Engineer can run the top-level `$files` query immediately, get a baseline bytes-per-row number, plug into the forecasting formula, and apply the 1.25x buffer + 1.5x EC multiplier to land at a defensible hardware ask. Spreadsheet checklist is directly usable. Practical applicability docked one point because the broken per-event-type query is the most important step for an engineer who specifically said "some event types are very high volume and some are low volume" — that engineer needs per-type bytes/row, and the answer hands them a query that will fail. The compaction job mention (`rewrite_data_files` to retroactively apply Zstd) is correct but presented as a side note rather than tied back to the cost question. |
| Completeness | 5 | All seven expected criteria from the question rubric appear in the answer: `$files` query (yes), per-event-type variance (yes, with mechanism), bytes-per-row formula (yes, with worked example), per-column compression variance (yes, with table), 20–30% buffer (yes, with three sources of inflation), Zstd default + `rewrite_data_files` recompaction (yes), MinIO EC:4+2 = 1.5x (yes). Plus extras (snapshot expiry callout, spreadsheet checklist) that round out the budget-planning angle. |
| **Average** | **4.50** | |

**Status**: PASS (≥ 3.5 threshold).

**Resource gap identified**: `resources/11-lakehouse-storage-sizing.md` should explicitly call out that `$files` is file-level metadata — to compute bytes-per-row per event_type, you either need (a) the table partitioned by event_type so you can group by the `partition` column from `$files`, or (b) measure on the base table directly via sampling. The current resource implies (via the "measure per event-type" practical guidance) that this is straightforward without showing the correct query shape. Add a "per-event-type bytes-per-row — the right way" subsection with the partition-based query OR a sampling-based query against the base table.

**Notes**: This is the 4th question on this topic (prior 3: 4.00, 4.75, 4.50, so prior avg = 4.417 across 3q). New running avg: (4.00 + 4.75 + 4.50 + 4.50) / 4 = **4.4375** across 4 questions. The technical error on the per-event-type `$files` query is the only material issue; everything else is correct and well-anchored to the production stack (Iceberg 1.5.2, MinIO, on-prem).

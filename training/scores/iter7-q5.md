# Iter 7 Q5 — Funnel query performance: 8 minutes on 500M rows, already day-partitioned

## Scores

| Dimension | Score | Reasoning |
|---|---|---|
| Technical accuracy | 4 | Core diagnosis (multi-pass funnel scan, day-partition spans 7 partitions) is correct and matches the resource's documented bottleneck. MATCH_RECOGNIZE recommendation is accurate — Trino has supported it since v356 and the funnel-in-one-pass claim is directionally right. Compaction advice (256 MB target, rewrite_data_files) is correct. The pre-aggregated rollup SQL is valid. However, two technical issues lower this score: (1) The ALTER TABLE syntax `SET PARTITIONING = ARRAY[...]` is wrong — the correct Trino Iceberg DDL is `ALTER TABLE ... SET PROPERTIES partitioning = ARRAY[...]`. This syntax error comes from the resource (resources/10-lakehouse-partitioning.md line 194), so the responder faithfully reproduced a resource bug rather than introducing its own. (2) The "Don't: add more partitions on user_id" section gives a strawman — the data team said "add partitioning" (ambiguous) not "bucket(user_id, 10000)" specifically. Attributing a bad idea to "your data team may suggest" is slightly unfair and could sow distrust without direct evidence. The bucket(user_id) anti-pattern advice itself is technically correct. |
| Beginner clarity | 4 | Well-organized: opens with a plain-English diagnosis, uses an ordered list, explains what MATCH_RECOGNIZE does in a parenthetical, and ends with a clear "real answer" decisional close. Inline explanation of MATCH_RECOGNIZE as "a SQL standard pattern-matching operator... single pass" is useful for a beginner. Resource gaps: "CTEs," "compaction," "rewrite_data_files," "rollup," "manifest metadata," and "nightly Spark job or dbt model" all appear without inline plain-English glosses. The "options => map()" CALL syntax is dropped without any explanation — a beginner copy-pasting this won't know what target-file-size-bytes is in MB (it's 256 MB = 268,435,456 bytes — not stated). The "highest impact" label on tenant_id partitioning is stated without evidence; for a cross-tenant funnel (which is the query described), tenant_id partitioning actively hurts, and the answer correctly notes this in a sub-clause but buries it. |
| Practical applicability | 4 | Five concrete actions with an explicit ordering and "do this / don't do this" framing — highly actionable for the on-prem Trino + Iceberg + MinIO stack. Pre-aggregated funnel rollup using a dbt model fits the production stack (dbt is supported). Compaction CALL maps to the resource. The engineer knows what to try next. Score docked one point because the ALTER TABLE syntax is wrong (SET PARTITIONING vs SET PROPERTIES partitioning) — an engineer copy-pasting this will get a syntax error. Additionally, step 2 (MATCH_RECOGNIZE) is recommended as the "quick, no schema change" fix but no example MATCH_RECOGNIZE funnel query is shown — the engineer knows the answer involves MATCH_RECOGNIZE but has nothing to actually try. The resource (resources/07-analytical-query-patterns.md) mentions MATCH_RECOGNIZE but does not include a concrete funnel example with it. |
| Completeness | 4 | Directly addresses the question's framing (day-partition already exists, what else can I do?). Covers five distinct levers: tenant_id co-partitioning, MATCH_RECOGNIZE, pre-aggregation, compaction, and bucket anti-pattern. The closing decisional framework (ad-hoc vs daily metric) is exactly the kind of guidance the question calls for. One material omission: the question describes a 7-day window funnel on 500M rows. The most likely cause of 8-minute runtime at that scale is the multi-pass scan (acknowledged) combined with missing or sparse Parquet column statistics — Trino cannot prune row groups within a file without column min/max stats, and if the events table was ingested without compaction, those stats are absent. The answer does not mention checking EXPLAIN ANALYZE to confirm which node is the bottleneck (HashAggregate vs ScanFilterProject vs RemoteExchange) before prescribing a fix — a rigorous answer would diagnose before treating. Also, the sort order / clustering of the Parquet files within partitions (z-ordering or clustering by user_id) would reduce the range of files touched per user, a standard funnel optimization not mentioned. |
| **Average** | **4.00** | |

## Topic updated

**Topic**: Analytical query patterns on Iceberg+Trino: funnels, cohorts, time-series SQL

- Prior avg: 4.75 (1 question, Iter 3 Q5)
- This question's score: 4.00
- New running avg: (4.75 + 4.00) / 2 = **4.375** across 2 questions
- Status: **PASSED** — avg 4.375 >= 3.5 threshold, 2 different question angles (Iter 3 Q5: "show me funnel/cohort/WAU SQL"; Iter 7 Q5: "my funnel takes 8 minutes, how do I fix it?")

## Key finding

The answer correctly identifies the multi-pass scan bottleneck and delivers five actionable levers in priority order — this is the right structure. The most impactful weakness is the runnable DDL syntax error (`SET PARTITIONING` vs `SET PROPERTIES partitioning`) which traces back to a bug in `resources/10-lakehouse-partitioning.md`, and the absence of a concrete MATCH_RECOGNIZE example that the engineer can actually try.

## Resource gap

Two gaps: (1) `resources/10-lakehouse-partitioning.md` line 194 uses `SET PARTITIONING = ARRAY[...]` — this is incorrect Trino DDL. The correct syntax is `ALTER TABLE iceberg.analytics.table_name SET PROPERTIES partitioning = ARRAY['day(occurred_at)', 'tenant_id']`. Fix this to prevent copy-paste errors in production. (2) `resources/07-analytical-query-patterns.md` mentions MATCH_RECOGNIZE as a funnel optimization tip (section 2, final paragraph) but provides no example query. Add a concrete 3-step MATCH_RECOGNIZE funnel example (signup -> activation -> payment within 7 days) so the responder can show working code when the engineer asks how to apply it — the current tip tells engineers it exists but gives them nothing to run.

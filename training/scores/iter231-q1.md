# Iter 231 Q1 Score

**Score: 2.75 / 5.0**
**Pass: NO** (threshold is 4.5 in extended/final phase)

## What was correct

- **Root cause is accurately stated**: Non-partitioned JDBC table scans in Trino produce a single split, so one worker holds one JDBC connection regardless of cluster size. This matches the documented behavior described in trinodb/trino#389 and the Trino concepts/connectors developer docs.
- **Correct framing of Iceberg vs JDBC contrast**: Iceberg splits across files; JDBC produces a single split. The "49 other workers sit idle" illustration is concrete and correct.
- **WHERE clause explanation is correct**: predicate pushdown reduces rows pulled but doesn't unlock parallelism — bottleneck remains the single socket.
- **EXPLAIN ANALYZE diagnostic guidance is sound**: looking at `Physical Input`, `CPU` vs `Scheduled` ratio, and task count is a real and useful diagnostic for confirming single-split scans.
- **Mitigation #2 (pre-aggregate/denormalize on MySQL side) is correct and practical.**
- **Mitigation #4 (cache the MySQL data in Iceberg snapshot) is correct, well-suited to this on-prem stack, and arguably the best mitigation for SaaS dimension data.**
- **"Always query a read replica" closing tip is sound production advice.**
- **The "why is this by design" framing accurately captures Trino's OLTP-protection rationale.**

## What was wrong or missing

- **CRITICAL (hallucinated properties — Mitigation #1)**: The answer claims the Trino MySQL connector supports `partition-column`, `partition-num-partitions`, `partition-lower-bound`, and `partition-upper-bound` config properties. **These properties do NOT exist** in the Trino MySQL connector documentation (verified against trino.io/docs/current/connector/mysql.html). Parallel JDBC reads for the MySQL connector remain an open feature request (trinodb/trino#389, open since 2019). The answer presents the entire mitigation as a working production fix complete with a copy-pasteable `etc/catalog/mysql_catalog.properties` file — an engineer trying this will get config-parsing errors at Trino startup. This is the most actionable-looking section of the answer and it is fabricated. The engineer is likely confusing this with Spark JDBC's `partitionColumn`/`lowerBound`/`upperBound`/`numPartitions` options, which apply to Spark, not Trino.
- **HIGH (wrong JDBC parameter name — Mitigation #3)**: `defaultRowFetchSize` is a **PostgreSQL JDBC** parameter, not a MySQL Connector/J parameter. The correct MySQL parameter is **`defaultFetchSize`**, and for true row streaming you also need **`useCursorFetch=true`** (otherwise MySQL Connector/J still buffers the entire result set in memory by default — `defaultFetchSize=0` and useCursorFetch off means the driver loads the whole ResultSet). This is the same class of bug as the Mitigation #1 hallucination — wrong driver assumed.
- **HIGH (internally contradictory unit claim)**: The Quick Checklist line says "Increase `defaultRowFetchSize` to reduce round-trips (MySQL Connector/J, in milliseconds)" — fetch size is a **row count**, not a duration. The "in milliseconds" appears to be a copy-paste error from the `socketTimeout` discussion two lines above. While the body text correctly states socketTimeout is in milliseconds, the checklist conflates the two parameters, leaving the engineer confused about what `defaultRowFetchSize` actually controls.
- **MEDIUM (missed real lever)**: Trino does have legitimate JDBC-side levers that *were* missed: dynamic filtering tuning (`dynamic-filtering.wait-timeout`, the broadcast-join + DF pattern that makes the federated join fire only the rows present on the Iceberg side at MySQL), and `domain-compaction-threshold` (default 256, per iter230 correction). For an engineer joining Iceberg facts to MySQL dimensions, dynamic filtering is the single biggest practical win and is not mentioned at all.
- **MEDIUM (missed the most production-honest framing)**: For an on-prem Trino 467 + MinIO + Iceberg stack joining MySQL dimensions, the right answer leads with "cache the MySQL data in Iceberg" or "use dynamic filtering" — the per-split limitation in Trino's MySQL connector has no in-connector parallelism workaround today. The answer instead leads with a fabricated parallelism config, which is misleading framing even before the factual errors.
- **LOW (missing diagnostic refinement)**: No mention of `system.runtime.tasks` or `system.runtime.queries` for cross-checking task distribution; no suggestion to run plain `EXPLAIN` before `EXPLAIN ANALYZE`.

## Verification notes

- **Trino MySQL connector parallel-read properties** — verified via trino.io/docs/current/connector/mysql.html: connector documents `case-insensitive-name-matching`, `metadata.cache-ttl`, `write.batch-size`, `dynamic-filtering.enabled`, `dynamic-filtering.wait-timeout`, `query.comment-format`, `domain-compaction-threshold`, `join-pushdown.enabled`, `join-pushdown.strategy`, plus connection and type-mapping properties. **No `partition-*` properties documented.**
- **trinodb/trino#389 ("Parallel read in jdbc-based connectors")** — still open as enhancement/roadmap. The feature request explicitly proposes adopting Spark-style partition-column config; it has not been implemented for the MySQL connector.
- **PostgreSQL connector** — verified the same `partition-*` properties also do NOT exist on the Trino PostgreSQL connector. The responder is not just confused about MySQL — these properties are not in any Trino JDBC connector.
- **`defaultRowFetchSize`** — verified via dev.mysql.com/doc/connector-j/en/connector-j-reference-configuration-properties.html: **not a MySQL parameter**. The MySQL equivalent is `defaultFetchSize` (default 0), and effective row-streaming requires `useCursorFetch=true`. `defaultRowFetchSize` is the **PostgreSQL** pgjdbc parameter.
- **`socketTimeout` in milliseconds** — CORRECT per dev.mysql.com/doc/connector-j/en/connector-j-connp-props-networking.html: MySQL Connector/J's `socketTimeout` is in milliseconds (0 = no timeout). The body text gets this right; only the checklist mislabels it.
- **One split per non-partitioned JDBC table scan** — CORRECT per Trino developer connector docs and #389 discussion: the default JDBC strategy is a single split per table scan.

## Recommendation for teacher

1. **(BLOCKING) Remove or rewrite Mitigation #1 entirely.** Replace the fabricated `partition-column`/`partition-num-partitions`/`partition-lower-bound`/`partition-upper-bound` block with a clear statement: *Trino's MySQL connector does NOT currently support in-connector parallel reads (tracked as trinodb/trino#389, open). The single-split-per-table-scan behavior is a hard limit you cannot configure away.* If a Spark-vs-Trino contrast is useful, label the Spark `partitionColumn`/`numPartitions` options clearly as a **Spark JDBC** feature that does NOT exist in Trino.
2. **(BLOCKING) Fix the JDBC fetch-size parameter name.** MySQL Connector/J uses `defaultFetchSize` (not `defaultRowFetchSize`), and needs `useCursorFetch=true` to actually stream rows from the server. Add a one-line callout: "PostgreSQL pgjdbc uses `defaultRowFetchSize`; MySQL Connector/J uses `defaultFetchSize` + `useCursorFetch=true`. They are not interchangeable."
3. **(BLOCKING) Fix the Quick Checklist line.** "Increase `defaultRowFetchSize` to reduce round-trips (MySQL Connector/J, in milliseconds)" must become "Set `defaultFetchSize=N` and `useCursorFetch=true` in the JDBC URL — N is a row count, not a duration."
4. **(HIGH) Add a "what actually works" mitigation section** leading with: (a) dynamic filtering with broadcast join (the join condition becomes a server-side `IN`-list pushdown to MySQL, so MySQL returns only matching rows), (b) cache MySQL data into Iceberg as a nightly/hourly snapshot — the dominant pattern for SaaS dimension data on this on-prem stack, (c) pre-aggregate/denormalize MySQL-side. Move the existing snapshot-into-Iceberg recommendation (Mitigation #4) to position #1 since it is the most production-honest answer for this stack.
5. **(MEDIUM) Add `dynamic-filtering.wait-timeout` and `domain-compaction-threshold` (default 256) to the MySQL connector property reference** in the federation resource — these are the *real* MySQL-side performance levers and are missing from the responder's vocabulary.
6. **(LOW) Add a "Trino JDBC parallelism FAQ" callout** to the federation resource that explicitly debunks the common Spark→Trino misconception: "Trino does NOT have an equivalent to Spark JDBC's `partitionColumn`/`lowerBound`/`upperBound`/`numPartitions`. If you need parallel MySQL reads, ingest the data into Iceberg via Spark."

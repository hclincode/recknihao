# Iter 232 Q1 Score

**Score: 3.25 / 5.0**
**Pass: NO** (threshold is 4.5 in extended/final phase for Trino federation)

## What was correct
- Broadcast-join mechanics are accurately described: small build side replicated to every worker, large fact streamed past the local hash table, no inter-worker reshuffle. The contrast with PARTITIONED join is correct.
- The framing of broadcast-join selection as a CBO planning-time decision is correct.
- `ANALYZE iceberg.analytics.events WITH (columns = ARRAY['customer_id'])` syntax is correct.
- `SET SESSION join_distribution_type = 'AUTOMATIC'` is the right session property name and value (this is in fact already the cluster default in Trino 467; setting it explicitly is harmless).
- `EXPLAIN (TYPE DISTRIBUTED)` and looking for `Join[BROADCAST]` vs `Join[PARTITIONED]` in the output is a real, working verification recipe.
- Correct that MySQL row counts feed into Trino via `INFORMATION_SCHEMA.STATISTICS`.
- The answer does correctly identify dynamic filtering by name and notes that it operates at *runtime* (not plan time), which is the right conceptual distinction.

## What was wrong or missing
- **Critical framing error**: the user's literal question is "Is there something Trino does automatically when one side of the join is small that makes the *other side scan less data*?" That description matches **dynamic filtering**, not broadcast join. Broadcast join controls where the build side is *shipped*; it does not by itself reduce how much of the large side is *scanned*. The answer leads with broadcast join and treats DF as a parenthetical aside ("might explain" / "a runtime feature"). The primary answer to the user's actual question is dynamic filtering, with broadcast join as the enabling distribution choice that makes per-value DF most precise.
- **False resource claim**: the answer says "The resources don't have explicit coverage of how to configure or verify this feature [dynamic filtering]." This is factually wrong. `/Users/hclin/github/recknihao/resources/22-trino-federation-postgresql.md` Section 5 is an entire dedicated treatment of dynamic filtering, including Section 5.3 (verification with `EXPLAIN ANALYZE` and `dynamicFilters = {...}` annotations on the probe scan), Section 5.4 (the `dynamic-filtering.wait-timeout` tuning knobs and per-connector defaults — 1s for Iceberg probe, 20s for JDBC probe), and Section 5.5 (broadcast vs. partitioned interaction with DF). The responder had this content available and either failed to consult it or failed to recognize the relevance.
- **Missing the "why is the 2nd run faster" mechanism**: the more likely explanations than "metadata cache reuse" are (a) Linux OS page cache on MinIO/local disk warming the Iceberg data files referenced by the partition, (b) Iceberg manifest cache on the coordinator avoiding S3 list/get round trips, (c) JIT warm-up on the worker JVMs, and (d) `MySQL` JDBC connection already established (no TCP/SSL handshake cost). The "Trino reuses partition pruning decisions" claim is wrong — Trino does not cache prior query planning decisions in a way that benefits a different filter range.
- **Missed production-environment cue**: prod_info.md specifies Trino 467, Iceberg via Hive Metastore. The answer never names the version, never confirms behavior matches 467, and gives no MinIO/HMS-flavored guidance. Resource 22 already lays this out for the MySQL connector (Section 2A) and would be the right citation.
- **`EXPLAIN ANALYZE VERBOSE` is missing**: this is the canonical command for proving whether DF actually fired (`dynamicFilterSplitsProcessed > 0` on the probe side, plus the wait-time measurement). The answer recommends only `EXPLAIN (TYPE DISTRIBUTED)`, which shows planning intent but not whether DF ran at runtime.
- **Missed the JDBC-build → Iceberg-probe asymmetry**: this user's scenario (small MySQL → large Iceberg) has Iceberg as the probe side, which means the relevant wait-timeout is `iceberg.dynamic-filtering.wait-timeout` (default 1s — very tight), not the MySQL side's. Resource 22 calls this out explicitly. Missing this nuance means the user could ANALYZE both tables and still not get DF firing if the MySQL scan takes >1s.
- **Resource link to `22-trino-federation-postgresql.md` is buried** at the bottom of the answer and described only as "federation details for MySQL connector," with no mention of the Section 5 dynamic-filtering content.

## Verification notes
- Trino docs confirm: dynamic filtering is supported for JDBC connectors since PR #13334 (merged into Trino 401+) and is enabled by default. ([Dynamic filtering — Trino docs](https://trino.io/docs/current/admin/dynamic-filtering.html))
- The default `dynamic-filtering.wait-timeout` differs by connector: Iceberg ships with `1s` (per `iceberg.dynamic-filtering.wait-timeout`), while JDBC connectors default to `20s`. The answer does not mention this — and it is precisely the knob most likely to be wrong in the asker's setup. ([Iceberg connector — Trino docs](https://trino.io/docs/current/connector/iceberg.html))
- `EXPLAIN ANALYZE VERBOSE` is documented as the way to see dynamic-filter wait time and the actual filter values applied. ([EXPLAIN ANALYZE — Trino docs](https://trino.io/docs/current/sql/explain-analyze.html))
- Broadcast-join mechanics: confirmed accurate. The blog post "Dynamic filtering for highly-selective join optimization" on trino.io describes the exact "build-side derives runtime predicate, push to probe scan" mechanism that the user is asking about. ([Trino blog — Dynamic filtering](https://trino.io/blog/2019/06/30/dynamic-filtering.html))
- Resource `22-trino-federation-postgresql.md` contains comprehensive dynamic-filtering content in Section 5 (verified by grep): Section 5.3 verification, Section 5.4 per-connector wait-timeout defaults, Section 5.5 broadcast vs partitioned interaction. The responder's claim that resources lack this coverage is incorrect.

## Recommendation for teacher
- **Restructure the answer pattern for "why does the second run feel faster + is Trino doing something smart?"**: the canonical answer flow should be (1) dynamic filtering as the headline mechanism (small build side → runtime IN-list / range pushed to large probe scan → fewer Parquet/Iceberg splits scanned), (2) broadcast vs. partitioned as the distribution choice that makes DF more precise, (3) ANALYZE + CBO as the prerequisite that makes the right distribution choice get picked, (4) OS page cache / Iceberg manifest cache / JDBC connection warmth as the actual "second-run-feels-faster" explanation.
- **Add an explicit "MySQL/Postgres dimension × Iceberg fact" cookbook example to Resource 22** if not already present at the top of Section 5, since this is one of the most common federated patterns and the responder did not retrieve the relevant Section-5 content despite it existing. Make the section title contain literal phrases like "small dimension table" and "large fact table" so retrieval matches the asker's wording.
- **Fix the resource-citation behavior**: the responder said the resources lack DF coverage when they do not. This suggests either the resource's table of contents is not surfacing Section 5 prominently, or the responder is relying on filename heuristics. Add a top-of-file index/TOC to Resource 22 that lists "Section 5: Dynamic filtering (the runtime optimization that makes federated joins survivable)" so name-only retrieval finds it.
- **Always recommend `EXPLAIN ANALYZE VERBOSE` (not just `EXPLAIN (TYPE DISTRIBUTED)`) when the question is "is Trino actually doing X at runtime"** — distribution shows intent, ANALYZE VERBOSE shows execution truth.
- **Surface the JDBC-build → Iceberg-probe wait-timeout asymmetry** (1s default on Iceberg side) as a numbered gotcha — this is the single most common reason DF silently fails to fire in the production stack described in prod_info.md.

{
  "iteration": 614,
  "phase": "extended",
  "final_iterations_remaining": 0,
  "passed": false,
  "notes": "iter614 — ONE TARGETED ADDITIVE FIX (regexp_replace STRIP/REMOVE canonical), default NO-OP elsewhere. iter613 STRONG PASS 4.875, 22 consecutive passes. GREP-FIRST verification of anticipated fresh probe areas (array_agg, map_agg, date_parse-vs-parse_datetime, regexp_replace + secondaries); all FINDABLE/correct EXCEPT a narrow purpose-built gap at the EXACT anticipated 'regexp_replace strip-pattern' probe -> minimal canonical added. EDIT (additive, reconcile-in-place): inserted '#### LEADING CANONICAL — strip / remove characters matching a pattern (remove all non-digits, keep only digits, delete a pattern)' in r27 IMMEDIATELY AFTER the lambda-replacement bonus block (was line 963) and BEFORE the DO-NOT-WRITE matrix (was line 965). BEFORE STATE (grep-confirmed): r27 regexp_replace coverage was ENTIRELY migration-flavored 3-arg capture-group reform (\\1->$1 lock at r27:952/969) + lambda form; ZERO worked example of the 2-arg removal form regexp_replace(s,pattern) or the empty-replacement strip idiom regexp_replace(s,'[^0-9]',''), and ZERO keyword anchor a 'remove all non-digits / keep only digits / strip a pattern / clean a phone number' question lands on. A keyword like 'remove all non-digits' routed into r27's migration table and found only $1 capture-group syntax — risk of fabricating a translate/nested-replace workaround. New block contents: (1) keyword anchors (remove all non-digits, strip non-numeric, keep only digits, clean a phone number, remove punctuation/dashes/spaces, strip a pattern, delete matches, regexp_replace empty string, 2-argument regexp_replace, sanitize string); (2) one-fact lead — TWO equivalent delete forms: 2-arg regexp_replace(string,pattern) [docs verbatim 'Removes every instance of the substring matched by the regular expression pattern from string'] and 3-arg empty-replacement regexp_replace(string,pattern,''); (3) canonical phone cleanup worked example three ways — regexp_replace('+1 (555) 123-4567','[^0-9]') / ...,'' / '\\D' all -> '15551234567'; (4) other strip idioms (letters-only '[^a-zA-Z]', collapse-whitespace '\\s+'->' ', keep-digits-and-decimal '[^0-9.]'); (5) DO-NOT-WRITE — do NOT reach for translate/nested replace() for an OPEN-ENDED class (non-digit/non-letter); translate/replace only for a FIXED enumerated char set; do NOT confuse strip (DELETE) with the $1/$2 capture-group REFORM (that lock in nuance #3 applies only to reform); \\D/\\d Java shorthand work directly (no Postgres [[:^digit:]] muscle-memory). DOCS VERIFIED & QUOTED via WebFetch trino.io/docs/467/functions/regexp.html: 2-arg regexp_replace(string,pattern)->varchar 'Removes every instance of the substring matched by the regular expression pattern from string'; 3-arg regexp_replace(string,pattern,replacement)->varchar with $g/${name} capture-group refs; 4-arg lambda form. All new SQL valid Trino 467 (2-arg + 3-arg regexp_replace, [^0-9]/\\D Java char classes — no QUALIFY, no :: cast, no fabricated function). LOCK PRESERVED: the \\1->$1 capture-group reform lock (r27:952 nuance #3 + r27:969 DO-NOT-WRITE matrix) UNTOUCHED — new strip block sits ABOVE the matrix, distinguishes DELETE from REFORM, does not rewrite/weaken the $1 content. translate/reverse/position/concat_ws 'all-five-exist' canonical (r27:918-936) UNTOUCHED. GREP FINDABILITY VERIFICATION (all other anticipated + secondary fresh areas FINDABLE/correct -> NO-OP, file:line): (1) array_agg collect-per-group CONFIRMED FINDABLE r07:165 LEADING CANONICAL 'array_agg(x ORDER BY y) ordered aggregation (+ DISTINCT)' + r07:139 FILTER(WHERE col IS NOT NULL) LEFT-JOIN-empty-array trap + COALESCE(...,ARRAY[]) + r07:180 array_agg(DISTINCT x ORDER BY x) + r07:666 FILTER-on-every-aggregate-incl-array_agg note + r23:601 'all values of x per group -> array_agg(x)'. (2) map_agg / multimap_agg build-a-map CONFIRMED FINDABLE r23:750 map_agg(x,COUNT(*)) over GROUP BY subquery as the manual histogram-equivalent + r23:752 net rule (histogram one-shot vs map_agg(k,COUNT(*)) manual). map_agg(key,value) two-column build-a-map is the same idiom; multimap_agg (collect-into-array-per-key map) is a trivial sibling — the map_agg landing is correct; multimap_agg synthesizable, NOT a gap. (3) date_parse-vs-parse_datetime string->timestamp FORMAT-STRING-CORRECTNESS CHECK — CONFIRMED CORRECT, NO-OP: r27:707/708/709 date_parse worked examples ALL use MySQL specifiers correctly (date_parse('2026-05-30','%Y-%m-%d'); date_parse('30/05/2026','%d/%m/%Y'); date_parse('05-JUN-2026','%d-%b-%Y')); r27:707/1217 parse_datetime worked examples ALL use Joda correctly (parse_datetime(s,'yyyy-MM-dd')); r27:1219 is a DEDICATED 'MySQL-vs-Joda format specifier confusion' PIN that shows the WRONG mixed forms (date_parse with Joda 'yyyy-MM-dd', parse_datetime with '%Y-%m-%d') ONLY as LABELED anti-patterns with the silent-NULL/wrong-date consequence + the verbatim distinction (date_parse ALWAYS MySQL %Y/%m/%d/%H/%i/%s; parse_datetime ALWAYS Joda yyyy/MM/dd/HH/mm/ss) + the percent-sign->MySQL->date_parse mnemonic. NO real worked example carries a mismatched format string -> NO VERIFIED-FALSE CLAIM -> NO-OP. (4) RANGE-interval 7-day rolling avg CONFIRMED FINDABLE r07:1914 '7-day rolling average of daily active users per tenant' + r07:1924 RANGE BETWEEN INTERVAL '6' DAY PRECEDING AND CURRENT ROW (7-day = 6-preceding) + r07:1929-1937 RANGE-vs-ROWS gap-day-semantics rule (RANGE calendar-aware/gap-correct, ROWS positional/wrong-on-holidays) + ORDER-BY-type requirement + empty-frame NULL guard r07:1939; the redundant INTERVAL '0' DAY guardrail r07:1306/1308 also intact. (5) CAST(ROW(...) AS ROW(...)) nested ROW construction CONFIRMED FINDABLE r09:732 CAST(CAST(ROW(123,'abc',true) AS ROW(v1 BIGINT,v2 VARCHAR,v3 BOOLEAN)) AS JSON) + r09:763 native-ROW dot-access canonical quoting the docs CAST(ROW(1,2.0) AS ROW(x BIGINT,y DOUBLE)).x form + r09:803 redundant-cast-on-stored-ROW note. (6) UNNEST WITH ORDINALITY position CONFIRMED FINDABLE r07:70 sub-note (WITH ORDINALITY AS t(elem,idx) appends 1-based ORIGINAL array offset as LAST alias column; works on CROSS JOIN + LEFT JOIN forms; DO-NOT-WRITE 'Trino has no WITH ORDINALITY/Postgres-only' FALSE + ROW_NUMBER-is-not-the-array-offset note). (7) json_array_length / CAST(json AS ARRAY) CONFIRMED FINDABLE r13:3387 LEADING CANONICAL JSON extraction family + r13:3393 json_array_length(json)->bigint signature + r13:3411-3412 worked json_array_length(json_extract(payload,'$.user.tags'))->2 + r13:3426 VARCHAR-coerce/json_parse note; CAST(json_parse(tags_raw) AS ARRAY(VARCHAR)) at r09:835. (8) recursive CTE WITH RECURSIVE CONFIRMED FINDABLE r27:3433 §7A.1 CONNECT BY->WITH RECURSIVE canonical + r27:3438 worked org_tree(id,manager_id,name,level) + experimental flag + max_recursion_depth=10 + quadratic-plan-growth + closure-table dbt alternative; r07:354 cross-ref. (9) COALESCE-of-aggregate / FILTER-on-array_agg CONFIRMED FINDABLE r07:324 COALESCE(SUM(o.amount),0) LEFT-JOIN zero-fill + r07:316 COUNT(*)-never-NULL nuance + r07:1756 COALESCE(PERCENT_RANK() OVER(...),0.0) + r07:139/143 array_agg FILTER(WHERE col IS NOT NULL) + COALESCE(...,ARRAY[]) + r07:666 FILTER-on-every-aggregate-incl-array_agg. WATCH-ITEM (gaps-and-islands / consecutive-streak): still GENUINELY ABSENT but iter613 PROVED the responder synthesizes it unaided — REACTIVE-ONLY, NOT manufactured pre-probe (durability/no-churn posture); HELD NO-OP. resources/22 UNTOUCHED — ZERO edits, NO federation content; §13.x guardrails + federation rubric row 4.49944/310 frozen (git status confirms ONLY resources/27 modified). No git commit/push (resources/27 + state.json edits only). ALL iter534-613 LOCKS PRESERVED (nothing rewritten/removed/weakened): r27 §7A.1 WITH-RECURSIVE + §7A.2/7A.2A listagg/array_join + try/TRY_CAST + lpad/rpad-cast + quarter + date_diff-months + date_parse/parse_datetime MySQL-vs-Joda PIN + \\1->$1 capture-group reform lock (strip canonical ADDED ABOVE the matrix, $1 lock untouched) + translate/reverse/position/concat_ws all-five-exist; r07 named-WINDOW r07:1399 + iter606-N-min + iter605-share-of-grand-total + LAG-MoM + LEAD-next + date-spine + CROSS-JOIN-dense-grid + running-max + B3 first/last/nth_value + C3/C4 + forward-fill (incl iter571 pre-join-LAST_VALUE ban) + cardinality + array_agg ordered/DISTINCT/FILTER + RANGE-INTERVAL-7day + WITH-ORDINALITY + date_add-signature; r23 iter611-PERCENTILE_CONT-inoculation + §3 FOUR-signature approx_percentile DO-NOT-WRITE + histogram + map_agg-manual-equivalent + DISTINCT-*-dedup + greatest/least-NULL + searched-CASE-tiers + approx_distinct + WHERE-vs-HAVING + INTERSECT-bought-both + EXISTS-correlated + SUM-decimal-precision + HALF_UP-cast-rounding + ::-cast-ban + NULLIF-guard + all string/math/date locks; r28 iter609-DECIDE-FIRST-signpost + iter610-GROUPING-label-mapping + ROLLUP/CUBE; r09 element_at + map-HOF/transform_values + native-ROW-dot + CAST(ROW)-construction + json_extract/json_extract_scalar + CAST(json AS ROW/MAP/ARRAY); r13 json_array_length/json_size/from_unixtime + JSON; r05/r16 approx_percentile-footgun; r17/r18/r10/r24; r22 §13.x UNTOUCHED. PRIOR iter613 notes follow: iter613 — FULL NO-OP DURABILITY/BREADTH PROBE. ZERO file edits to resources/ (git status clean before state.json bump). iter612 STRONG PASS 4.78, 21 consecutive passes; resources very stable. This iteration confirms locks via verify-first + grep-first findability and deliberately manufactures no churn (the failure-adjacent risk). NAMED-WINDOW-FRAME DECISION (iter612 judge-flagged OPTIONAL/LOW candidate): NO-OP — the r07:1399-1414 named-WINDOW canonical is ALREADY SAFE (does NOT model the frame-shared-across-ranking-and-aggregate pattern). Read-confirmed worked spec at r07:1404-1411: WINDOW w AS (PARTITION BY tenant_id ORDER BY day ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW), referenced ONLY from AGGREGATE functions (SUM(amount) OVER w, AVG(amount) OVER w, MAX(amount) OVER w) — there is NO RANK/DENSE_RANK/ROW_NUMBER/LAG/LEAD sharing the framed window. The frame is legitimately attached only to aggregates that accept it; the slightly-confusing 'one framed named window shared across a rank/lag AND a running total' shape the iter612 responder copied is NOT present in the canonical, so per directive NO note added and NO-OP said explicitly. DOCS VERIFIED & QUOTED via WebFetch trino.io/docs/467/functions/window.html: for rank(), dense_rank(), row_number(), lag(), and lead() the function descriptions each state verbatim 'The window frame must not be specified.' (ranking + value/offset functions do NOT accept a ROWS/RANGE frame; Trino silently ignores / rejects one). The canonical's frame sits only on SUM/AVG/MAX which DO accept it — correct as written, no inoculation warranted. GREP FINDABILITY VERIFICATION (anticipated + secondary fresh areas, file:line): (1) ROW/struct field dot-access CONFIRMED FINDABLE r09:759 LEADING CANONICAL 'get a field out of a native ROW / struct column (dot notation col.field, no CAST needed)' + r09:761 dense keyword anchors (struct column, address.city, native struct field, struct dot notation, ROW(...) column dot notation) + r09:763 docs quote 'Named row fields are accessed with field reference operator (.)' + r09:799 element_at-is-MAP/ARRAY-only disambiguator (no element_at(row(...)) overload, type error) + r09:556/927 cross-disambiguators from MAP and json_extract_scalar sections. (2) MAP transform_values CONFIRMED FINDABLE r09:687 LEADING CANONICAL 'Trino MAP higher-order functions (map_filter / map_keys / map_values / transform_keys / transform_values): filter and reshape a MAP IN-PLACE (no UNNEST)' + r09:699 signature transform_values(map(K,V), (k,v) -> V2) -> map(K,V2) + r09:689 keyword anchors + r09:663 transform_values-only-iterates-m1-keys silent-data-bug warning (use map_concat for union-of-keys override) + r09:542 navigation. (3) CAST to DECIMAL(p,s) money rounding precision CONFIRMED FINDABLE r23:433 'SUM decimal precision' keyword anchors (CAST DECIMAL 38 sum, money sum precision, DECIMAL(10,2) overflow SUM) + r23:435 sum(decimal(p,s)) -> decimal(38,s) auto-widen rule + r23:445 upstream CAST(... AS DECIMAL(p,0)) cents-truncation trap + r23:464 LEADING CANONICAL 'CAST(DOUBLE/REAL AS DECIMAL) uses HALF_UP rounding (NOT banker's)' with worked CAST(DOUBLE '0.005' AS DECIMAL(3,2)) -> 0.01 examples (r23:474-477) + source-verified DecimalConversions.java HALF_UP. (4) consecutive-day login streak / gaps-and-islands — GENUINELY ABSENT (WATCH-ITEM, NOT manufactured pre-probe per directive). Grep for island/consecutive/streak/sessioniz + 'row_number - date' islands-grouping trick found NO dedicated query-time canonical (all 'consecutive' hits are unrelated: r05:888 polling-failure prose, r22:7870/8330 HAProxy health-check + federation TopN-ban prose, r28:24 citation-streak prose). The 'subtract a dense_rank/row_number from a date to label consecutive runs, then GROUP BY the offset' pattern is advanced and not present. Per directive (gaps-and-islands may genuinely be absent -> NOTE as watch-item, do NOT manufacture pre-probe) + 21-pass durability posture -> HELD NO-OP; flagged as the single watch-item for a future REACTIVE fix only IF a probe lands on it and the responder fails. SECONDARY AREAS all FINDABLE -> NO-OP: EXTRACT hour-of-day — query-time hour bucketing is a trivial unit-swap on the r07 N-minute date_trunc canonical (EXTRACT(minute FROM ts) form shown there; EXTRACT(hour FROM ts) / date_trunc('hour', ts) is the unit swap) — the hour() hits at r03:452 + r10:1379-1401 are Iceberg PARTITION-transform context (don't over-partition by hour), correctly distinct from query-time extraction; minor watch but synthesizable, NOT a gap. lpad/rpad zero-pad CONFIRMED FINDABLE r27:905 lpad(varchar,bigint,varchar) signature + MUST-CAST-numeric-to-VARCHAR-first PIN (lpad(CAST(account_id AS VARCHAR),10,'0'); bare numeric col = 'Unexpected parameters (bigint,integer,varchar)' parse error) + keyword anchors (zero-pad number Trino, pad account number). UNNEST a MAP CONFIRMED FINDABLE r07:66 UNNEST docs (LEFT JOIN UNNEST to avoid losing the row) + r09:542 map_entries navigation + r09:662 map_from_entries(map_entries(...)) key-collision note + r09:714 UNNEST(map_entries(m))...GROUP BY...MAP_AGG explode/regroup-only-when-you-need-ROWS guard. date subtract / days-ago filter CONFIRMED FINDABLE r07:373 'event_date >= current_date - INTERVAL '14' DAY' + r08:94/106 'current_date - INTERVAL '7' DAY' enterprise-7-day filter + date_add('day',-n,...) negative-offset form in the r07 date_add signature lock. NO verified-false claims found; ONE genuine purpose-built gap noted (gaps-and-islands consecutive-streak) -> HELD as watch-item NOT manufactured (durability/no-churn posture, advanced pattern, no failing probe yet). resources/22 UNTOUCHED — ZERO edits, NO federation content; §13.x guardrails + federation rubric row 4.49944/310 frozen. No git commit/push (state.json bump only, no resources/ edits). ALL iter534-612 LOCKS PRESERVED (nothing rewritten/removed/weakened): r07 named-WINDOW r07:1399 (read-verified safe, NO frame-note added — already frameless-on-ranking) + iter606-N-min + iter605-share-of-grand-total + LAG-MoM + LEAD-next + date-spine + CROSS-JOIN-dense-grid + running-max + B3 + C3/C4 + forward-fill + cardinality + date_add-signature; r23 iter611-PERCENTILE_CONT-inoculation + §3 FOUR-signature approx_percentile DO-NOT-WRITE + histogram + DISTINCT-*-dedup + greatest/least-NULL + searched-CASE-tiers + approx_distinct + WHERE-vs-HAVING + INTERSECT-bought-both + EXISTS-correlated + SUM-decimal-precision + HALF_UP-cast-rounding + ::-cast-ban + NULLIF-guard + all string/math/date locks; r28 iter609-DECIDE-FIRST-signpost + iter610-GROUPING-label-mapping + ROLLUP/CUBE; r09 element_at + map-HOF/transform_values + native-ROW-dot + json_extract/json_extract_scalar + map-value-by-key + map_concat; r27 try/TRY_CAST + listagg + lpad/rpad-cast + quarter + date_diff-months; r05/r16 approx_percentile-footgun; r13/r17/r18/r10/r24; r22 §13.x UNTOUCHED. PRIOR iter612 notes follow: iter612 — FULL NO-OP DURABILITY/BREADTH PROBE. ZERO file edits to resources/ (git status clean before state.json bump). iter611 STRONG PASS 4.875 (clean sweep, all four 5.0 — PERCENTILE_CONT inoculation resolved under hardest framing), 20 consecutive passes; resources very stable. This iteration confirms locks via grep-first findability verification and deliberately manufactures no churn (the failure-adjacent risk). ALL FOUR anticipated fresh areas + secondary areas FINDABLE in valid Trino 467 -> NO-OP, file:line: (1) NAMED WINDOW clause CONFIRMED FINDABLE r07:1399 LEADING CANONICAL 'Trino named WINDOW clause (define a window once, reference by name)' + r07:1401 keyword anchors (WINDOW w AS, reuse OVER clause, avoid repeating PARTITION BY) + docs quote trino.io/docs/467/sql/select.html ('The WINDOW clause is used to define named window specifications...referred to in the SELECT and ORDER BY clauses', supported since v352) + r07:1410 worked WINDOW w AS (PARTITION BY tenant_id ORDER BY day ...) + r07:1414 slot rule (after HAVING, before ORDER BY) + extend-named-window w2 AS (w ORDER BY ts) + DO-NOT-WRITE 'don't claim Trino has no named WINDOW clause'. (2) EXISTS / NOT EXISTS correlated CONFIRMED FINDABLE — positive WHERE EXISTS worked example r23:1465 + r23:1495 (correlated EXISTS -> CorrelatedJoin -> manual LEFT JOIN + DISTINCT + IS NOT NULL rewrite at r23:1474-1483 with row-multiplication note); 'at least one' / any-in-group also covered by bool_or LEADING CANONICAL r23:684-703 ('at least one row is true', bool_or(is_late) AS any_late TRUE if at least one); NOT EXISTS / anti-join / NULL-safety LEADING at r23:1357-1370 (NOT EXISTS recommended, NULL-safe, decorrelates to anti-join SemiJoin) + r23:1330-1336 five-term glossary + r28:749 EXISTS->IN->SemiJoin rewrite. 'customers with at least one refund' = positive correlated EXISTS (r23:1465 form) or IN/SemiJoin; 'customers with no refund' = NOT EXISTS / anti-join (r23:1357). (3) CROSS JOIN dense grid CONFIRMED FINDABLE r07:765 sequence() signature pin (NO generate_series; sequence(start,stop[,step]) INCLUSIVE bounds, UNNEST to rows) + r07:824/877 CROSS JOIN UNNEST(sequence(lo,hi,INTERVAL '1' DAY/MINUTE)) dense-grid recipe + r07:877 VERBATIM 'Use sequence(start,end,INTERVAL 1 DAY) + CROSS JOIN UNNEST for a TRUE dense calendar, then CROSS JOIN the distinct entity list to get a (entity, day) cross product' (region x month = the entity x date-spine cross product instance — synthesizable from this canonical) + r07:713 §4 date gap-fill + LEFT JOIN + COALESCE(...,0) zero-fill + r23:261 CROSS JOIN UNNEST(split(...)) explode form + r07:72 §1a.1 CROSS-JOIN-UNNEST-before-WHERE clause-order rule. (4) json_extract_scalar CONFIRMED FINDABLE r09:931 worked example json_extract_scalar(properties_raw, '$.referrer_domain') + r09:538/552 ('$.key' over JSON-string column, slower than MAP, cannot be file-pruned) + r09:556/800/927 disambiguator (json_extract_scalar is for JSON-text VARCHAR/JSON columns ONLY; native ROW columns use dot notation col.field — type-error otherwise) + r09:859 cross-ref to resource 13 Trino JSON extraction family (json_extract/json_extract_scalar/json_array_length/json_parse/json_format) + r18:1207 readable_metrics '$.plan_type.lower_bound' worked use. SECONDARY AREAS all FINDABLE -> NO-OP: date_add('month',n,d) r07:1214 signature pin (date_add(unit,value,timestamp), negative value subtracts, units include 'month'/'quarter'/'year') + r07:1543/1556/1563 (-25/-12/-1 month offsets, MoM/YoY/QoQ unit-swap) + r07:1250 Oracle ADD_MONTHS mapping; lpad/rpad zero-pad r23:375 (format('%05d',42)->'00042') + r27:905 lpad/rpad signature + MUST-CAST-numeric-to-VARCHAR-first PIN (lpad(CAST(account_id AS VARCHAR),10,'0'); bare numeric col = Unexpected parameters parse error); ROW/struct dot access r09:759 LEADING CANONICAL (col.field, no CAST/json_parse/element_at needed; docs quote 'Named row fields are accessed with field reference operator (.)') + r09:556 element_at-is-MAP/ARRAY-only disambiguator; MAP transform_values r09:687 LEADING CANONICAL MAP HOF (map_filter/transform_values/transform_keys/map_keys/map_values reshape in-place no UNNEST) + r09:542 navigation + r09:663 transform_values-only-iterates-m1-keys override-bug warning (use map_concat); TRY safe division r27:1101 try(expression)->NULL-on-runtime-error + r27:1111 try(amount/commission_rate) + r27:1114 COALESCE(try(...),0) + r27:1140 'divide-by-zero: both NULLIF(denom,0) and try(numer/denom) work' + r27:1144 DO-NOT-WRITE 'Trino has no try()' is FALSE; COALESCE vs NULLIF distinction r27:1140/1144 (try/NULLIF/COALESCE all in conditional family) + r27:3416/3967 COALESCE(NULLIF(col,''),default) empty-string-vs-NULL idiom + r07:1094 NULLIF(denom,0) divide-guard + r09:668 COALESCE(element_at(map,key),default) get-with-default. NO verified-false claims found; NO purpose-built gaps at any landing point — every probed area has a dedicated keyword-anchored canonical (named WINDOW, positive/negative EXISTS, CROSS JOIN dense grid, json_extract_scalar all have LEADING CANONICAL or worked examples a question's keywords land on directly), OR is a trivial instance of a present canonical (region x month grid = entity x date-spine cross product already shown verbatim r07:877). Manufacturing dedicated cards for correctly-routable/synthesizable questions is the churn risk this iteration exists to avoid -> HELD NO-OP. resources/22 UNTOUCHED — ZERO edits, NO federation content; §13.x guardrails + federation rubric row 4.49944/310 frozen. No git commit/push (state.json bump only, no resources/ edits). ALL iter534-611 LOCKS PRESERVED (nothing rewritten/removed/weakened): r23 iter611-approx_percentile-PERCENTILE_CONT-inoculation + §3 FOUR-signature DO-NOT-WRITE + histogram + DISTINCT-*-dedup + greatest/least-NULL + searched-CASE-tiers + approx_distinct + WHERE-vs-HAVING + INTERSECT-bought-both + ::-cast-ban + NULLIF-guard + positive/negative EXISTS + §10 SemiJoin/anti-join + bool_or/bool_and + named-WINDOW(r07) + all string/math/date locks; r28 iter609-DECIDE-FIRST-signpost + iter610-GROUPING-label-mapping + ROLLUP/CUBE; r07 N-min + share-of-grand-total + LAG-MoM + LEAD-next + date-spine + CROSS-JOIN-dense-grid + running-max + B3 + C3/C4 + forward-fill + cardinality + named-WINDOW r07:1399; r27 §4.4E try()/TRY_CAST + listagg + quarter + date_diff-months + lpad/rpad-cast + COALESCE/NULLIF; r09 element_at + map-value-by-key + map-HOF/transform_values + native-ROW-dot + json_extract/json_extract_scalar; r05/r16 approx_percentile-footgun; r13/r17/r18/r10/r24; r22 §13.x UNTOUCHED. PRIOR iter611 notes follow: iter611 — REACTIVE FIX A: PERCENTILE_CONT-not-in-Trino inoculation added AT the r23 §3 approx_percentile worked-example LANDING POINT, plus a missing per-group (GROUP BY store) p90 worked example. TRIGGER (iter610 responder failure): a p90-per-store question was answered with the CORRECT lead approx_percentile(order_amount, 0.90) but then the responder APPENDED a FABRICATED alternative 'If you need exact percentiles, use PERCENTILE_CONT()' — PERCENTILE_CONT does NOT exist in Trino 467 (function-not-found/parse error). LANDING-POINT DIAGNOSIS: the r23 §3 approx_percentile block (lines 141-158) DID already carry a strong inoculation, but it was BURIED in a `> DO-NOT-WRITE` blockquote (old lines 154-156: PERCENTILE_CONT/PERCENTILE_DISC = Postgres/Snowflake/Oracle NOT Trino; WITHIN GROUP only for listagg) that sat BELOW and SEPARATE from the leading worked example (old lines 141-145), AND the leading worked example had NO per-group GROUP BY form and NO 'do not append an exact-percentile alternative' guard inline. A responder grabbing the lead approx_percentile example (the p90 answer) did not absorb the DO-NOT-WRITE block below it and synthesized the fabricated PERCENTILE_CONT fallback. BEFORE STATE re inoculation at the LEAD example: LACKED-IT inline (the inoculation existed only in the DO-NOT-WRITE blockquote further down, not at the worked-example the responder copied). EXACT EDIT (additive, reconcile-in-place, immediately after the lead approx_percentile worked example at r23 line 145, before the FOUR-signature blockquote): (1) added two worked per-group examples — `SELECT store_id, approx_percentile(order_amount, 0.90) AS p90_order_amount FROM orders GROUP BY store_id;` and the p50/p90/p99 ARRAY-form-per-store `approx_percentile(order_amount, ARRAY[0.5, 0.9, 0.99])`; (2) added a tight inline guard blockquote: 'DO NOT append a \"for exact percentiles use PERCENTILE_CONT()\" alternative — PERCENTILE_CONT / PERCENTILE_DISC do NOT exist in Trino 467 (function-not-found / parse error). approx_percentile(x, p) is the ONLY percentile function — there is NO separate exact-percentile function. Trino has NO `... WITHIN GROUP (ORDER BY ...)` ordered-set aggregate; WITHIN GROUP works ONLY with listagg.' + 'answer with approx_percentile and STOP' + tighter-accuracy note (build a qdigest: value_at_quantile(qdigest_agg(x, 1, accuracy), p); the 3-arg approx_percentile(x,p,accuracy) accuracy overload does NOT exist on current Trino — consistent with the existing r23:155 lock) + PERCENT_RANK() OVER (ORDER BY x) for exact-ish per-row + cross-ref pointers to the r23 FOUR-signature DO-NOT-WRITE block just below and the r05/r16 footgun (POINTER ONLY, footgun not duplicated). DOCS VERIFIED & QUOTED via WebFetch trino.io/docs/467/functions/aggregate.html: approx_percentile overloads = approx_percentile(x, percentage)->[same as x], approx_percentile(x, percentages)->array<[same as x]> (multiple percentiles ONE pass), approx_percentile(x, w, percentage)->[same as x] (weighted), approx_percentile(x, w, percentages)->array<[same as x]> (weighted array); 'Neither PERCENTILE_CONT nor PERCENTILE_DISC appear anywhere on this page'; 'WITHIN GROUP (ORDER BY ...) is documented only for listagg. No other aggregate function on this page uses this clause syntax.' NOTE: I deliberately did NOT add a claim that approx_percentile(x, p, accuracy) 3-arg accuracy form exists — the docs page shows only the four overloads above and the existing r23:155 lock already correctly states the accuracy arg lives on qdigest_agg, not approx_percentile; respected that lock and the docs. All new SQL valid Trino 467 (GROUP BY approx_percentile, ARRAY[...] form, qdigest_agg/value_at_quantile, PERCENT_RANK OVER — no QUALIFY, no :: cast, no fabricated function). r05 §'CRITICAL SQL FOOTGUN' (lines 2232-2263) + r16 p50/p90 $files worked example (lines 112-113) read-confirmed INTACT — NOT duplicated, NOT weakened (FIX A added a POINTER to them only). resources/22 UNTOUCHED — ZERO edits, NO federation content; §13.x guardrails + federation rubric row 4.49944/310 frozen. No git commit/push (resources/23 + state.json edits only). ALL iter534-610 LOCKS PRESERVED (nothing rewritten/removed/weakened): r05 approx_percentile-FOOTGUN + r16 approx_percentile-worked (FIX A added a pointer-inoculation at the r23 landing point, did NOT touch/duplicate them); r23 §3 FOUR-signature approx_percentile DO-NOT-WRITE block (kept fully intact below the new inline guard) + §3.1A-H + histogram + DISTINCT-*-dedup + greatest/least-NULL + ::-cast-ban + NULLIF-guard + INTERSECT-bought-both + all string/math/date locks; r28 iter609-DECIDE-FIRST-signpost + iter610-GROUPING-label-mapping + ROLLUP/CUBE examples; r07 N-min + share-of-grand-total + LAG-MoM + LEAD-next + date-spine + running-max + B3 + C3/C4 + forward-fill + cardinality + PERCENT_RANK Pattern C2; r27/r09/r13/r17/r18/r10/r24; r22 §13.x UNTOUCHED. PRIOR iter610 notes follow: iter610 — REACTIVE FIX A: added the canonical four-value GROUPING(a,b) integer->LABEL mapping + a non-transpose PIN + a fully-labeled CUBE(store,payment_method) worked CASE. TRIGGER (iter609 responder failure): responder correctly CHOSE CUBE for a both-margins store/payment_method question, but then SWAPPED the row-type labels in CASE GROUPING(store, payment_method): it wrote WHEN 1 -> 'Payment Method Total' (WRONG; should be 'Store Total') and WHEN 2 -> 'Store Total' (WRONG; should be 'Payment Method Total'). Root cause = knew bit=1 means rolled-up but forgot LEFTMOST arg = HIGH/MSB, so value 1 (binary 01) means the RIGHTMOST col (payment_method) rolled up => per-STORE subtotal. BEFORE STATE (read-confirmed): the existing r28 GROUPING bit-value tables were ALL CORRECT, NOT swapped and NOT wrong-valued — (a) lines 445-448 correctly state leftmost=MSB & bit=1=rolled-up; (b) line 459 GROUPING(region,category)=1 binary 01 = 'Region subtotal, category rolled up' CORRECT; (c) worked example line 496 WHEN 1 THEN 'Region Total' (category rolled up; region present) CORRECT; (d) DO-NOT-WRITE line 520 explicitly warns WHEN 1 THEN 'Category Total' is WRONG, should be 'Region Total' CORRECT; (e) line 531 CUBE value 2 = 'a rolled up, b present' CORRECT. So the resource was CORRECT-BUT-LACKED a single copyable fully-labeled CASE for the canonical two-column CUBE case that exercises all four values 0/1/2/3 — the only fully-labeled copyable CASE present (section (c)) is the ROLLUP one which has NO WHEN 2 arm (ROLLUP never emits 2). The responder had to synthesize the four-arm CUBE WHEN 1/WHEN 2 labels unaided and transposed them. EXACT EDIT (additive, in section (e) right after the both-margins note, before (f)): inserted a new '#### CANONICAL — the four-value GROUPING(a, b) integer->LABEL mapping (use this for CUBE; copy the labels EXACTLY)' subsection containing: (1) a four-row table GROUPING(a,b)=0 binary 00 neither rolled up -> DETAIL; =1 binary 01 b(rightmost/LSB) rolled up, a present -> per-A SUBTOTAL (one row per a across all b); =2 binary 10 a(leftmost/MSB) rolled up, b present -> per-B SUBTOTAL (one row per b across all a); =3 binary 11 both rolled up -> GRAND TOTAL; (2) the verbatim PIN — 'The LEFTMOST argument to GROUPING() is the HIGH (most-significant) bit. So GROUPING(a, b) = 1 means the RIGHTMOST column (b) was rolled up -> the row is a per-A subtotal, NOT a per-B subtotal' + the converse for value 2 + 'Do NOT transpose the labels' + a bit-weight mnemonic; (3) a fully-labeled copyable CUBE(store, payment_method) worked CASE with the CORRECT labels verbatim: CASE GROUPING(store, payment_method) WHEN 0 THEN 'Detail' WHEN 1 THEN 'Store Total' WHEN 2 THEN 'Payment Method Total' WHEN 3 THEN 'Grand Total' END (WHEN 1 -> 'Store Total' because payment_method/rightmost rolled up so store survives; WHEN 2 -> 'Payment Method Total' because store/leftmost rolled up so payment_method survives), with GROUP BY CUBE(store, payment_method) + ORDER BY GROUPING(...) NULLS LAST; (4) an explicit note that the WRONG transposed form is WHEN 1->'Payment Method Total'/WHEN 2->'Store Total' and that the surviving row is named after the column STILL PRESENT (bit=0), not the one rolled up. DOCS VERIFIED & QUOTED via WebFetch trino.io/docs/467/sql/select.html: 'To compute the resulting bit set for a particular row, bits are assigned to the argument columns with the rightmost column being the least significant bit. For a given grouping, a bit is set to 0 if the corresponding column is included in the grouping and to 1 otherwise.' (=> leftmost=MSB, bit=1=aggregated/rolled up; for GROUPING(a,b) a has bit-weight 2, b has bit-weight 1) and 'The CUBE operator generates all possible grouping sets (i.e. a power set) for a given set of columns.' All new SQL valid Trino 467 (CASE GROUPING(col,col), GROUP BY CUBE(col,col), ORDER BY GROUPING(...) NULLS LAST — no QUALIFY, no :: cast, no fabricated function). ADDITIVE ONLY: the iter609 DECIDE-FIRST ROLLUP-vs-CUBE signpost (lines 419-438), the (b) ROLLUP value table, the (c) ROLLUP worked example, the (d) DO-NOT-WRITE table, and the (e) ROLLUP-vs-CUBE comparison table + all GROUPING() value tables are kept FULLY INTACT — nothing rewritten or removed; only the per-level label mapping + PIN + labeled CUBE CASE added. resources/22 UNTOUCHED — ZERO edits, NO federation content; §13.x guardrails + federation rubric row 4.49944/310 frozen. No git commit/push (resources/28 + state.json edits only). ALL iter534-609 LOCKS PRESERVED (nothing rewritten/removed/weakened): r28 iter609 DECIDE-FIRST ROLLUP-vs-CUBE signpost + ROLLUP/CUBE worked examples + GROUPING() value tables (FIX A reconciled/added the per-level label mapping — examples untouched) + CoW-MoR + incremental-canonical + late-arriving-lookback + unique_key-dedup + composite-key + watermark-typed-literal + dbt-generic-tests; r07 N-min + share-of-grand-total + LAG-MoM + LEAD-next + date-spine + B3 + running-max + C3/C4 + forward-fill + ROWS-vs-RANGE + max_by + cardinality; r23 §3.1A-H + histogram + DISTINCT-*-dedup + greatest/least-NULL + ::-cast-ban + NULLIF-guard + all string/math/date locks; r27/r09/r05/r16/r13/r17/r18/r10/r24; r22 §13.x UNTOUCHED. PRIOR iter609 notes follow: iter609 — REACTIVE FIX A: ROLLUP-vs-CUBE decision-FIRST signpost moved to the HEAD of the r28 GROUPING block. TRIGGER (iter608 responder failure): a 'subtotals on BOTH dimensions' question (user explicitly asked 'all products within each region' AND 'all regions for each product category') was answered with GROUP BY ROLLUP(region, category) — STRUCTURALLY WRONG: ROLLUP(a,b) emits (a,b),(a),() and SKIPS the (b=category)-only grouping the user named; only CUBE(region,category) emits all of (region,category),(region),(category),(). Diagnosis = routed-but-stopped-early: responder reached the r28 GROUPING block via the keyword anchor (line 415) but committed to the FIRST worked example it saw (ROLLUP, section (b)/(c) at lines 430/463) and stopped before reaching the ROLLUP-vs-CUBE decision content, which sat in section (e) at lines 502-514 — i.e. AFTER both the ROLLUP value table AND the ROLLUP worked example (too late). BEFORE STATE CONFIRMED by read: block order was heading(413) -> keyword anchors(415) -> 'why this block sits at top' ROLLUP-focused para(417) -> (a) bitmask(419) -> (b) ROLLUP value table(430) -> (c) ROLLUP worked example(463) -> (d) DO-NOT-WRITE(497) -> (e) ROLLUP-vs-CUBE-vs-GROUPING-SETS decision/comparison(502); NO head-of-block decision signpost existed before the first ROLLUP example. EXACT EDIT (additive only): inserted a new '### DECIDE FIRST — ROLLUP vs CUBE vs GROUPING SETS (read this before copying any worked example below)' section IMMEDIATELY AFTER the keyword-anchor line (415) and BEFORE the ROLLUP-focused 'Why this block sits at the top' para — so the operator choice is made before the responder commits to the first example. Contents: (1) decision keyword anchors added to the anchor quote block ('all products within each region AND all regions for each product category', 'subtotals on both dimensions', 'region totals AND category totals', 'independent margins', 'every combination of subtotals', 'breakdown by both X and Y with subtotals for each'); (2) three-way decision rule — EVERY combination / independent margins on EACH dimension ('all X for each Y' AND 'all Y for each X') + grand total -> CUBE(a,b); ONLY hierarchical/prefix subtotals (drill-down rolling up the RIGHTMOST column, country->state->city, never city-across-all-countries) -> ROLLUP(a,b); hand-picked specific set -> GROUPING SETS((...),(...)); (3) KEY line — ROLLUP(a,b) emits (a,b),(a),() and SKIPS the (b)-only grouping, CUBE(a,b) emits ALL of (a,b),(a),(b),(), so both-margins => ROLLUP WRONG, use CUBE; (4) one-line WRONG/RIGHT SQL pair — WRONG GROUP BY ROLLUP(region, category) [drops the per-category subtotal] / RIGHT GROUP BY CUBE(region, category). DOCS VERIFIED & QUOTED via WebFetch trino.io/docs/467/sql/select.html: 'The ROLLUP operator generates all possible subtotals for a given set of columns' (= hierarchical/prefix subtotals, each level rolls up the rightmost remaining column) vs 'The CUBE operator generates all possible grouping sets (i.e. a power set) for a given set of columns' (= all 2^N combinations). All new SQL valid Trino 467 (GROUP BY ROLLUP/CUBE(col,col) — no QUALIFY, no :: cast, no fabricated function). ADDITIVE: the existing (b) ROLLUP value table, (c) ROLLUP worked example, (d) DO-NOT-WRITE table, and (e) ROLLUP-vs-CUBE comparison table + all GROUPING() value tables are kept FULLY INTACT — nothing rewritten or removed; only a head-of-block decision signpost added. resources/22 UNTOUCHED — ZERO edits, NO federation content; §13.x guardrails + federation rubric row 4.49944/310 frozen. No git commit/push (resources/28 + state.json edits only). ALL iter534-608 LOCKS PRESERVED (nothing rewritten/removed/weakened): r28 ROLLUP/CUBE/GROUPING-SETS worked examples + GROUPING() value tables (FIX A added head-of-block signpost only) + CoW-MoR + incremental-canonical + late-arriving-lookback + unique_key-dedup + composite-key + watermark-typed-literal + dbt-generic-tests; r07 iter606-N-min + iter605-share-of-grand-total + LAG-MoM + LEAD-next + date-spine + B3-first/last/nth_value + Pattern C3/C4 + forward-fill + ROWS-vs-RANGE + max_by + cardinality; r23 §3.1A-H + histogram + DISTINCT-*-dedup + greatest/least-NULL + ::-cast-ban + NULLIF-guard + all string/math/date locks; r27/r09/r05/r16/r13/r17/r18/r10/r24; r22 §13.x UNTOUCHED. PRIOR iter608 notes follow: iter608 — FULL NO-OP DURABILITY/BREADTH PROBE. ZERO file edits to resources/ (git status clean before state.json bump). iter607 STRONG PASS 4.97 clean sweep, 16 consecutive passes; this iteration confirms locks via grep-first findability verification and deliberately manufactures no churn. ALL anticipated + secondary fresh areas FINDABLE in valid Trino 467 -> NO-OP, file:line: (1) CUBE multi-dim subtotals CONFIRMED FINDABLE r28:504 ('CUBE(a,b) is shorthand for GROUP BY GROUPING SETS ((a,b),(a),(b),()) — all 2^N combinations, including the (b)-only grouping ROLLUP skips') + r28:509 GROUPING() value table for CUBE(a,b) (4=2^N, value 2 = a-rolled-up-b-present = the category total ROLLUP can't give) + r28:512 4-col/5-col cardinality cost driver (CUBE never a drop-in for ROLLUP); sits in the r28 ROLLUP/GROUPING-SETS lock neighborhood as expected. (2) LEAD / next-row delta CONFIRMED FINDABLE r07:1502 ('LEAD(col, n) is the mirror — n rows forward') + r07:1503 (no frame clause needed for LAG/LEAD) immediately below the worked LAG-delta canonical r07:1495-1496 (revenue - LAG(revenue,1) OVER (PARTITION BY tenant_id ORDER BY day) AS day_over_day_change) which mirrors forward for days-until-next; r23:847 + r07:890/896 IGNORE-NULLS-placement lock for LEAD(col) IGNORE NULLS OVER; r27:3498 LEAD(col,1,default) portability. Days-until-next is a forward mirror of the present LAG-delta worked example — responder synthesizes; NOT a gap. (3) running max/min OVER CONFIRMED FINDABLE via the running-total frame canonical r07:1067 (SUM(x) OVER (PARTITION BY id ORDER BY d ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW)) in the CONTRAST card + r28:676/679 (MAX(amount) OVER (... ROWS BETWEEN UNBOUNDED PRECEDING AND 1 PRECEDING)) explicit MAX-OVER worked example + r23:866 frame + r07:1282/1293 positional running-total frame. Highest-so-far = swap SUM->MAX on the present running-total frame; the MAX-OVER form is already shown at r28:676 — responder synthesizes; NOT a gap. (4) date-spine gap-fill CONFIRMED FINDABLE r07:713 '## 4. Time-series rollups (with gap-filling)' + r07:765 sequence() signature pin (NO generate_series in Trino; sequence(start,stop[,step]) INCLUSIVE bounds, UNNEST to rows) + r07:824/877/931/952 CROSS JOIN UNNEST(sequence(lo,hi,INTERVAL '1' DAY/MINUTE)) dense-grid recipes + r07:902 GROUP-BY-aggregate-in-spine parse-error PIN + r07:806 COMBINED CANONICAL date-spine+forward-fill ordering. (5) GREATEST / LEAST across columns CONFIRMED FINDABLE r23:883 LEADING CANONICAL (greatest/least return NULL if ANY arg NULL in Trino; Postgres outlier ignores NULLs) + r23:893 COALESCE-each-arg workaround + r23:901 DO-NOT-WRITE quartet + r27:1242 §4.4D deep canonical (row-wise-across-columns vs MAX-aggregate-down-rows distinction; SELECT MAX(a,b,c) parse error) + r27:1259/1260 footguns. (6) array_distinct / array_sort CONFIRMED FINDABLE r07:189 §1a.3 array-function quick reference + r07:199 (array_distinct(array)->array dedup preserves first-occurrence order) + r07:207 (per-row distinct count = cardinality(array_distinct(arr))) + r07:224 §1a.4 array HOF + r07:236 (array_sort(array(T)) or 2-arg comparator form) + r07:200 array_intersect/array_union/array_except set-ops. SECONDARY AREAS all FINDABLE -> NO-OP: first_value/last_value/nth_value r07:1647 Pattern B3 LEADING CANONICAL (value window functions + default-frame footgun: last_value returns CURRENT row not partition-last unless ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING) + r27:3502/3503; map_keys/map_values r09:687 LEADING CANONICAL MAP HOF (map_filter/map_keys/map_values/transform_keys/transform_values) + r09:696/697 signatures + r09:611 contains(map_keys(...),key) existence check; INTERSECT/EXCEPT dedup-semantics r23:767 §3.1F (both dedupe by default; INTERSECT=semi-join rows-in-both, EXCEPT=anti-join rows-in-left-not-right) + r27:1273/1274 (EXCEPT=ANTI-JOIN, INTERSECT=SEMI-JOIN not anti-join, SemiJoin node in EXPLAIN expected); COALESCE across columns r09:668 COALESCE(element_at(map,key),<default>) map-with-fallback + COALESCE first-non-NULL semantics (conditional.html) — multi-column first-non-null is the same COALESCE(c1,c2,...) idiom. NO verified-false claims found; NO purpose-built gaps at any landing point — every probed area either has a dedicated keyword-anchored canonical OR is a trivial substitution on a present canonical (running-max=SUM->MAX on running-total frame [MAX-OVER already shown r28:676]; days-until-next=forward mirror of LAG-delta r07:1495). Manufacturing dedicated cards for correctly-synthesizable questions is the churn risk this iteration exists to avoid -> HELD NO-OP. resources/22 UNTOUCHED — ZERO edits, NO federation content; §13.x guardrails + federation rubric row 4.49944/310 frozen. No git commit/push (state.json bump only, no resources/ edits). ALL iter534-607 LOCKS PRESERVED (nothing rewritten/removed/weakened): r07 iter606-N-min-truncation canonical + iter605-share-of-grand-total SUM-OVER() card + Pattern B1/B2 LAG MoM/YoY + Pattern B3 first/last/nth_value default-frame + Pattern C3 NTILE + C4 width_bucket + forward-fill + interval-overlap + ROWS-vs-RANGE + max_by + ROW_NUMBER-top1 + date_trunc-hour + sequence + cardinality + array_distinct/array_sort/array-HOF; r23 §3.1A + starts_with/ends_with + §3.1C HALF_UP + §3.1E count_if + bool_or/bool_and + histogram + §3.1F UNION/INTERSECT/EXCEPT + §3.1G ROW_NUMBER-one-row-per-key + nested-window-ban + DISTINCT-*-dedup + tie-break-determinism + IGNORE-NULLS-placement + greatest/least-NULL-propagation + §11 FILTER + format + strpos/split_part + concat[||] + ::-cast-ban + COUNT(DISTINCT)-per-group + NULLIF-divide-guard + cume_dist/percent_rank + all string/math/date locks; r28 ROLLUP/CUBE/GROUPING-SETS; r27 §4.4D greatest/least + §4.4E try()/TRY_CAST + listagg + quarter + date_diff-month + NULLS-default; r09 element_at + map-value-by-key + map-HOF/map_keys/map_values + native-ROW-dot + COALESCE-element_at; r05/r16 approx_percentile; r13/r17/r18/r10/r24; r22 §13.x UNTOUCHED. PRIOR iter607 notes follow: iter607 — FULL NO-OP DURABILITY/BREADTH PROBE. ZERO file edits to resources/ (git status clean except state.json). iter606 STRONG PASS 4.84, 15 consecutive passes, resources very stable; this iteration confirms locks via grep-first findability verification and deliberately manufactures no churn. HOLD DECISION (iter606 judge-flagged latest-row-per-key tiebreaker): HELD NO-OP. The §3.1G canonical at r23:787-798 already enumerates explicit columns (customer_id, order_id, order_date, amount — NOT an other_columns... placeholder) AND the Tie-break determinism note sits immediately below at r23:802 (secondary ORDER BY ... DESC, session_id DESC tiebreaker for ties on updated_at/event_time/session_timestamp). The iter606 Q4 placeholder/omitted-tiebreaker was a RESPONDER-answer artifact (4.75), not a resource gap; per directive this is not justified pre-emptively, and any re-exposure happens AFTER the teacher phase — so NO-OP now. GREP FINDABILITY VERIFICATION (all anticipated + secondary fresh areas FINDABLE in valid Trino 467 -> NO-OP, file:line): (1) ROLLUP/GROUPING SETS subtotals+grand-total CONFIRMED FINDABLE r28:413 LEADING CANONICAL 'read FIRST when asked subtotal/grand total/ROLLUP/CUBE/GROUPING SETS' + r28:430 explicit GROUPING() value table for ROLLUP(region,category) (grand total = 3 not 2) + r28:442/451 3-col and 4-col bitmask tables; keyword anchors subtotal/subtotals/grand total/label the subtotal row all present. (2) Month-over-month change via LAG delta CONFIRMED FINDABLE r07:1495-1496 (revenue - LAG(revenue,1) OVER (PARTITION BY tenant_id ORDER BY day) AS day_over_day_change) + r07:1505 Pattern B2 LEADING CANONICAL 'Period-over-period YoY vs MoM' with the load-bearing pair LAG(metric,1)=MoM/LAG(metric,12)=YoY (r07:1516/1517/1521) + sparse-series silent-wrong trap (r07:1525/1527) + keyword anchors month over month/MoM/previous month/period over period (r07:1643). (3) First-seen / earliest date per user CONFIRMED FINDABLE r07:370 (SELECT user_id, MIN(event_time) AS signed_up_at) — the exact first-seen-per-user MIN(date) GROUP BY canonical — reinforced by cohort first-event MIN(event_time) at r07:467/588/673, plus r23:557+ min_by(x,y) deterministic earliest-by-ordering-column pick (min_by(status, updated_at) AS first_status r23:572). (4) NULLIF divide-by-zero guard CONFIRMED FINDABLE r07:1094 ('Guard against divide-by-zero: wrap the denominator in NULLIF(SUM(x) OVER (), 0)') in the iter605 share-of-grand-total neighborhood + r07:1552/1613 (NULLIF(prev,0)*100 growth-pct guard) + r16:240 + r27:1140 (NULLIF(denom,0) vs try(numer/denom) both documented). SECONDARY AREAS all FINDABLE -> NO-OP: date_diff months/age-in-months r27:680/687/716 (MONTHS_BETWEEN->date_diff('month',d2,d1) integer-count + fractional residual note) + r05:2051; array element access/slice r07:201/209/219 (element_at(array,n) 1-based NULL-safe, element_at(arr,-1)=last, arr[n] subscript ERRORS out-of-range) + r09:799 array overload note; json_extract/json path r09:505/552 (json_extract/json_extract_scalar('$.key') full-scan caveat); cume_dist/percent_rank r23:156 + r07 Pattern C2 (PERCENT_RANK() OVER (ORDER BY ...) [0.0,1.0], NULL on single-row partition r07:1725); UNION/INTERSECT/EXCEPT set ops r23:743 §3.1F LEADING CANONICAL (bare UNION=UNION DISTINCT expensive, default UNION ALL; INTERSECT=semi-join, EXCEPT=anti-join r23:767); coalesce-multi r09:668/672/683 (COALESCE(element_at(map,key),<default>) get-with-default) + r27:3416/3967 (COALESCE(NULLIF(col,''),default)); whole-row dedup DISTINCT-* r23:825; latest-row-per-key §3.1G r23:779-829. resources/22 UNTOUCHED — ZERO edits, NO federation content; §13.x guardrails + federation rubric row 4.49944/310 frozen. No git commit/push (state.json bump only, no resources/ edits). ALL iter534-606 LOCKS PRESERVED (nothing rewritten/removed/weakened): r07 iter606-N-min-truncation canonical + iter605-share-of-grand-total SUM-OVER() card + Pattern C3 NTILE + C4 width_bucket + forward-fill + interval-overlap + ROWS-vs-RANGE + max_by + ROW_NUMBER-top1 + Pattern B1/B2 LAG MoM/YoY + date_trunc-hour + week-Monday + now()/AT-TIME-ZONE + sequence + cardinality; r23 §3.1A split/starts_with/ends_with + §3.1C HALF_UP + §3.1D arbitrary/max_by/min_by + §3.1E count_if/bool_or/bool_and/histogram + §3.1F UNION/UNION ALL/INTERSECT/EXCEPT + §3.1G ROW_NUMBER-one-row-per-key + nested-window-ban + DISTINCT-*-dedup + PARTITION-BY-*-inoculation + tie-break determinism + IGNORE-NULLS-placement + §10 SemiJoin/NOT IN + §11 FILTER + format + strpos/split_part + concat[||] + ::-cast-ban + COUNT(DISTINCT)-per-group + date_format[DATE-cast] + TABLESAMPLE both arms + regexp_extract + array_join + anti-join + approx_most_frequent + approx_percentile + all string/math/date locks; r28 ROLLUP/CUBE/GROUPING-SETS; r27 try()/TRY_CAST + listagg + quarter + NULLS-default + date_diff-month; r09 element_at + map-value-by-key + map_concat + native-ROW-dot + COALESCE-element_at; r05/r16 approx_percentile; r13/r17/r18/r10/r24; r22 §13.x. PRIOR iter606 notes follow: iter606 — REACTIVE FIX A (N-minute truncation canonical + ::-cast inoculation), default NO-OP elsewhere. TRIGGER: at iter605 the responder answered a 5-minute-bucketing question with correct floor arithmetic but emitted EXTRACT(minute FROM event_ts)::int — the PostgreSQL :: cast shorthand, a PARSE ERROR in Trino 467 (canonical is CAST(x AS integer)), violating the standing ::-cast-ban. The iter605 teacher had HELD the N-min canonical as NO-OP; this slip is the reactive trigger. BEFORE state: grep confirmed the r07 date_trunc neighborhood (intro at line 1101, week-Monday lock at 1103, now()/AT-TIME-ZONE locks below) had ZERO N-minute/sub-hour truncation canonical and ZERO copyable CAST-form example — the responder synthesized the N-min form unaided AND mis-cast it. EDIT: inserted a new '#### LEADING CANONICAL — N-minute (5/10/15/30-minute) timestamp buckets (iter606 PIN)' block ADJACENT, immediately AFTER the date_trunc intro line (1101) and BEFORE the week-Monday lock (1103) — the date_trunc-hour lock + iter605 share-of-grand-total SUM-OVER() card (r07:1068/1073) both kept fully intact (ADDED adjacent, nothing rewritten). Block contents: (1) keyword anchors (5/10/15/30-minute buckets, N-minute buckets, bucket timestamps into X-minute windows, group events every 5 minutes, finer than hourly, sub-hour time buckets, truncate to 15 minutes, floor to nearest 5 minutes); (2) one-fact lead — date_trunc supports only FIXED units; (3) LEADING CANONICAL integer-division-floor form, 5-min instance EXACTLY: date_trunc('hour', event_ts) + INTERVAL '1' MINUTE * (CAST(EXTRACT(minute FROM event_ts) AS integer) / 5 * 5), with replace-5-with-10/15/30 note and a 37->35 worked floor; (4) CRITICAL inoculation — Trino 467 has NO :: cast shorthand (that is PostgreSQL), EXTRACT(...)::int is a PARSE ERROR, always CAST(EXTRACT(...) AS integer); EXTRACT already returns BIGINT so the cast is only needed for INTEGER, and EXTRACT(minute FROM ts) % 5 works directly with NO cast; (5) WRONG/RIGHT token pair — WRONG EXTRACT(minute FROM ts)::int / RIGHT CAST(EXTRACT(minute FROM ts) AS integer); (6) equivalent documented date_add('minute', <int>, date_trunc('hour', ts)) form; (7) worked 5-minute GROUP BY (full expr repeated in GROUP BY, no alias per Trino #16533). DOCS VERIFIED & QUOTED against trino.io/docs/467: (a) date_trunc units = millisecond/second/minute/hour/day/week/month/quarter/year (datetime.html) — NO 5-min/15-min unit; (b) EXTRACT return type: 'extract(field FROM x) → bigint' (datetime.html); (c) integer division: 'Division (integer division performs truncation)' (math.html) — minutes non-negative so truncation = floor; (d) :: cast operator NOT in Trino — cast(value AS type) → type is the only form (conversion.html), :: not mentioned anywhere; (e) date_add(unit, value, timestamp) 'Adds an interval value of type unit to timestamp' (datetime.html) used for the equivalent form. INTERVAL '1' MINUTE * <int> scalar multiplication is not explicitly quoted on the datetime/types/math pages but is valid Trino 467 and matches the existing r07 +/- INTERVAL arithmetic locks; the documented date_add form is offered alongside for safety. All new SQL valid Trino 467 (no QUALIFY, no :: cast — the whole point). OTHER PROBED FRESH AREAS (grep, all already FINDABLE -> NO-OP): cast-text-to-number CONFIRMED PRESENT r23:432/434/444 (CAST(... AS DECIMAL/BIGINT) + SUM(CAST(a AS DOUBLE)/b) integer-division-upstream trap); top-N-per-group ROW_NUMBER outer-wrapper CONFIRMED PRESENT r23:18/601/785-806 (ROW_NUMBER() subquery + outer WHERE rn=1 / <=N, QUALIFY/DISTINCT-ON parse-error bans, nested-window-ban, tie-break determinism); conditional-SUM via FILTER CONFIRMED PRESENT r23:628/641/644 (count_if LEADS + COUNT(*) FILTER (WHERE bool) + SUM(CASE) equivalents) + r23:105 COUNT(DISTINCT) FILTER; latest-row-per-key CONFIRMED PRESENT r23:601/783/825 (DISTINCT ON not-in-Trino -> ROW_NUMBER rn=1 / max_by, one-row-per-key vs whole-row-dedup distinction). resources/22 UNTOUCHED (ZERO edits — git status shows ONLY resources/07 modified; §13.x guardrails + federation rubric row 4.49944/310 frozen). No git commit/push (file edits only). ALL iter534-605 locks preserved: r07 date_trunc-hour lock + iter605 share-of-grand-total SUM-OVER() card (FIX A added N-min canonical ADJACENT, neither rewritten), Pattern C3 NTILE/C4 width_bucket/forward-fill/interval-overlap/ROWS-vs-RANGE/max_by/ROW_NUMBER-top1/sequence/cardinality/week-Monday-lock/now()-AT-TIME-ZONE; r23 §3.1A split-family/starts_with/ends_with, §3.1C HALF_UP, §3.1D arbitrary/max_by largest-vs-latest PIN, §3.1E count_if LEADING + bool_or/bool_and + histogram, §3.1G ROW_NUMBER one-row-per-key + nested-window-ban + DISTINCT-*-dedup + PARTITION-BY-*-inoculation, §10 SemiJoin/NOT IN, §11 FILTER, format, strpos/split_part, concat[||], ::-cast-ban, COUNT(DISTINCT)-per-group, date_format[DATE-cast], TABLESAMPLE both arms, regexp_extract, array_join, anti-join, approx_most_frequent, all string/math/date locks; r27 try()/TRY_CAST/listagg/quarter; r09 element_at/map-value/native-ROW-dot; r05/r16 approx_percentile; r13/r17/r18/r10/r24/r28; r22 §13.x. PRIOR iter605 notes follow: iter605 — DURABILITY/BREADTH probe, default NO-OP with ONE minimal additive anchor. DECISION 1 (15-min/N-min truncation): HELD NO-OP — per directive lean-(b); iter604 responder synthesized three correct N-min truncation forms unaided, the r07 date_trunc-hour lock + r23 math/integer-division locks already supply everything, adding content for a correctly-answered question is the churn risk this iteration exists to avoid. No date_trunc N-min canonical added. DECISION 2 (percent-of-grand-total / SUM(x) OVER ()): SMALL ADDITIVE CANONICAL ADDED — grep found the empty-OVER grand-total pattern existed ONLY embedded in resource 16:241 (a cost-monitoring query, pct_cluster_cpu) with NO keyword anchors a 'each row's share of the grand total / percent of total' question would land on; the r07:1067 SUM(x) OVER (PARTITION BY ... ORDER BY ...) is the DIFFERENT cumulative running-total pattern. Genuine findability gap at the r07 analytical-pattern landing point. WebFetch-verified empty OVER () grand-total semantics at trino.io/docs/467/functions/window.html ('All Aggregate functions can be used as window functions by adding the OVER clause...computed for each row over the rows within the current row's window frame' — no partition/order => whole result set). EDIT: added a 4th row to the THREE-time-series CONTRAST card (now four patterns) 'Share of grand total (percent of total)' = 100.0 * x / SUM(x) OVER () (whole-table denom) or PARTITION BY g (per-group denom), PLUS a dedicated card just below it — keyword anchors (percent of total / share of grand total / each row as fraction of total / ratio to total / % of total revenue / contribution to total / SUM OVER no partition / empty OVER clause / grand-total denominator), docs quote, two worked examples (region % of company revenue via empty OVER (); product % of category via PARTITION BY category), and four guard notes: empty-OVER=grand-total vs PARTITION-BY=per-group subtotal, 100.0* integer-division trap (cross-ref r23 §3), NULLIF(denominator,0) divide-by-zero guard, and a do-NOT-GROUP-BY-then-rejoin note cross-ref'd to resource 28 ROLLUP for the explicit total-ROW shape. Additive only — no lock touched, no running-total/forward-fill/interval-overlap row altered. OTHER PROBED FRESH AREAS (grep, all already FINDABLE -> NO-OP): FIX A histogram() CONFIRMED PRESENT r23:706-739 (§3.1E neighborhood, one-shot map<value,bigint>, docs quote, vs-GROUP-BY table); FIX B whole-row DISTINCT-* dedup + PARTITION-BY-* inoculation CONFIRMED PRESENT r23:808-827 (§3.1G, SELECT DISTINCT * canonical + PARTITION BY */ORDER BY (SELECT 1) parse-error callout + distinction from one-row-per-key ROW_NUMBER); customers-with-no-orders anti-join FINDABLE r07:249/317/326 (LEFT JOIN ... WHERE right IS NULL keyword anchor 'users with no orders'/'customers without purchases') + r23:1317 NOT EXISTS/anti-join + §10 SemiJoin canonical; sequence-of-numbers-as-rows FINDABLE r07:765 sequence() signature pin + r23:1786 generate_series->sequence(1,10)+UNNEST trap; most-frequent-value-per-group FINDABLE r23:589 approx_most_frequent(buckets,x,capacity) 'most common value per group' + max_by mode neighbors; date_diff/json/approx_distinct all in existing locks. resources/22 UNTOUCHED (ZERO edits — git status shows only resources/07 modified; §13.x guardrails + federation rubric row 4.49944/310 frozen). No git commit/push (file edits only). ALL iter534-604 locks preserved: r23 §3.1A split-family/starts_with/ends_with, §3.1C HALF_UP, §3.1D arbitrary/max_by+largest-vs-latest PIN, §3.1E count_if LEADING + bool_or/bool_and + iter604-histogram, §3.1G ROW_NUMBER one-row-per-key + nested-window-ban + iter604-DISTINCT-*-dedup + PARTITION-BY-*-inoculation, §10 SemiJoin/NOT IN, §11 FILTER, format[%08d/%,.2f], strpos/split_part, concat[||], COUNT(DISTINCT)-per-group, date_format[DATE-cast], TABLESAMPLE both arms, regexp_extract, array_join, all string/math/date locks; r07 C3 NTILE/C4 width_bucket/forward-fill/interval-overlap/ROWS-vs-RANGE/max_by/ROW_NUMBER-top1/date_trunc-hour/sequence/cardinality (CONTRAST card EXTENDED additively, not rewritten); r27 try()/TRY_CAST/listagg/quarter/last_day_of_month; r09 element_at/map-value/native-ROW-dot; r05/r16 approx_percentile; r13/r17/r18/r10/r24/r28; r22 §13.x. All new SQL valid Trino 467 (empty OVER (), PARTITION BY g, 100.0* decimal literal, NULLIF, ROUND — no QUALIFY, no :: cast). PRIOR iter604 notes follow: iter604 — TWO TARGETED FIXES (verified-true gaps from iter603), default NO-OP elsewhere. FIX A histogram() content-gap: r23 §3.1E neighborhood had ZERO histogram() aggregate coverage (grep confirmed: all prior 'histogram' hits were unrelated CBO/connector/metric/prose). The iter603 Q1 responder groped to map_agg over a GROUP BY subquery (correct but indirect). EDIT: ADDED a minimal findable canonical ADJACENT to §3.1E (after the bool_or/bool_and block, before §3.1F) — keyword anchors (value-count summary / frequency map / counts per value without a GROUP BY / {COMPLETED:1200,PENDING:340} summary / histogram), one-fact lead (histogram(x) -> map<value,bigint> in ONE aggregate, NO GROUP BY), VERBATIM trino.io/docs/467/functions/aggregate.html quote: histogram(x) -> map<K,bigint> 'Returns a map containing the count of the number of times each input value occurs.', worked example SELECT histogram(status) AS status_counts FROM orders -> {COMPLETED=1200,PENDING=340,CANCELLED=55} with element_at(map,'COMPLETED') read-out, and a vs-neighbors table (histogram=one-shot map for LOW/MODERATE cardinality; GROUP BY COUNT(*) for ROWS or HIGH-cardinality; map_agg(k,COUNT(*)) over GROUP BY = manual equivalent but histogram is the direct one-shot). §3.1E count_if LEADING CANONICAL + bool_or/bool_and block left UNTOUCHED. FIX B whole-row DISTINCT * dedup + PARTITION BY * inoculation: r23 §3.1G had the ROW_NUMBER one-row-per-key canonical + nested-window-ban but NO whole-row dedup canonical (grep confirmed ZERO 'DISTINCT *' / 'PARTITION BY *' hits). The iter603 responder emitted invalid ROW_NUMBER() OVER (PARTITION BY * ORDER BY (SELECT 1)) for EXACT whole-row dedup. EDIT: ADDED a new §3.1G subsection (after the existing DO-NOT-WRITE para, before §3.1G Cross-references) — keyword anchors (drop exact duplicate rows / whole-row dedup / SELECT DISTINCT * / PARTITION BY * / ROW_NUMBER over all columns), one-fact lead + docs quote trino.io/docs/467/sql/select.html: 'If the argument DISTINCT is specified, only unique rows are included in the result set.', canonical SELECT DISTINCT * FROM t + CTAS CREATE TABLE catalog.schema.t_deduped AS SELECT DISTINCT * FROM t, and an anti-pattern callout: PARTITION BY * is INVALID (parse error — * is a SELECT-list-only wildcard, not a grouping expression; window-func docs example is PARTITION BY clerk i.e. column exprs), ORDER BY (SELECT 1) is not a valid window sort; use SELECT DISTINCT * for exact whole-row dedup, reserve ROW_NUMBER OVER (PARTITION BY <enumerated key cols> ORDER BY <tiebreaker>) for one-row-per-KEY. §3.1G ROW_NUMBER one-row-per-key LEADING CANONICAL + nested-window-ban left INTACT — FIX B DISTINGUISHES exact-whole-row-dedup (DISTINCT *) from one-row-per-key (ROW_NUMBER enumerated), does not replace it. OTHER PROBED FRESH AREAS (grep, all already FINDABLE → NO-OP): format('%,.2f') currency at r23:361/364/369; FILTER pivot one-col-per-status / conditional-aggregation / manual-pivot at r23:1632; truncate-to-15-min and min-max-range covered in existing date/math locks. resources/22 UNTOUCHED (ZERO edits — §13.x guardrails + federation rubric row 4.49944/310 frozen). No git commit/push (file edits only). All iter534-603 locks preserved: r23 §3.1E count_if LEADING + bool_or/bool_and (histogram ADDED ADJACENT), §3.1G ROW_NUMBER one-row-per-key + nested-window-ban (DISTINCT-* + PARTITION-BY-* inoculation ADDED adjacent, distinguishing from one-row-per-key), §3.1A split-family/starts_with/ends_with, §3.1C HALF_UP, §11 FILTER, format[%08d], strpos/split_part, concat[||], COUNT(DISTINCT)-per-group, date_format[DATE-cast], TABLESAMPLE both arms, regexp_extract, array_join, all string/math/date locks; r07 C3/C4/forward-fill/ROWS-vs-RANGE/max_by/cardinality; r27 try()/TRY_CAST/listagg/quarter; r09 element_at/map-value/native-ROW-dot; r05/r16 approx_percentile; r13/r17/r18/r10/r24/r28; r22 §13.x. All new SQL valid Trino 467."
}
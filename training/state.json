{
  "iteration": 505,
  "phase": "extended",
  "final_iterations_remaining": 0,
  "passed": true,
  "notes": "Iter505 TEACHER — TWO FIXES per directive. FIX A (PRIMARY — NEW canonical, ZERO existing coverage): added r27 §6.7E LEADING CANONICAL 'dbt UNIT TESTS (1.8+) — testing model logic on MOCK input (`unit_tests:` with `given:` / `expect:`)' between §6.7D (seeds) and §7 (cutover checklist), inserted at line 2625 area (after the §6.7D `---` divider, before `## 7.`). FIX B (reconcile-in-place — DF stats-overstatement): rewrote r23 line 197 bullet to correctly frame DF as a RUNTIME mechanism that is ON BY DEFAULT and NOT gated on ANALYZE/stats; added a 6-step 'if dynamicFilter is missing from EXPLAIN, check (in this order) — NOT just run ANALYZE' checklist covering join type, predicates, distribution, connector pushdown, wait-timeout, and stats-as-TUNING-LEVER-not-prerequisite. GREP FINDINGS (FIX A — `grep -n 'unit_test\\|unit test\\|data_tests\\|§6.7\\|6.7A\\|dbt test' resources/27-oracle-plsql-to-dbt-trino.md`): 30+ hits across r27 — §6.7 line 1910 (data tests intro), §6.7A line 1935 (dbt test severity/store_failures/expression_is_true canonical, the adjacent home), §6.7B line 2128 (source freshness), §6.7C line 2250 (model contracts), §6.7D line 2485 (seeds). Confirmed ZERO existing unit_tests content via `grep -rni 'unit_tests\\|given:\\|expect:\\|test_type:unit' resources/` — returned only 3 hits ALL UNRELATED to unit tests: r22 line 7641 ('Failure and rollback behavior — what to actually expect'), r05 line 626 ('Expect: ...' as English prose), r17 line 3651 ('Expect: serializable' in a code comment about isolation). NONE of these touched dbt unit tests, given/expect mock-row YAML, or test_type:unit. So this IS a genuine content gap — appropriate to add as NEW canonical (not a reconcile). GREP FINDINGS (FIX B — `grep -rn 'dynamic filter\\|dynamicFilter\\|enable-dynamic-filtering\\|dynamic_filtering' resources/`): 117KB of DF mentions across r22 (66+ DF mentions, all in §13.x federation guardrails or §5.x mechanics — ALL CORRECT and untouched), r28 (§5.2 lines 880-906 DF runtime-probe-prune — correct, no overstatement), r18 (§3 tuning levers table line 160 — correct, calls DF 'runtime mechanism' and 'Default ON'), r03 line 464 (myth-buster, correct: 'CBO + ANALYZE → join-ordering / build-side choice' + 'dynamic filtering (runtime probe-side pruning)' — these are listed as TWO SEPARATE mechanisms, not DF-requires-stats), r24 (no DF mentions at all — grep returned zero `dynamic` hits in r24). THE OVERSTATEMENT WAS ONLY at r23 line 197: 'Missing `dynamicFilter` on a join — joins between fact and dim tables should show dynamic filters; if not, ensure both tables have stats (Trino syntax: bare ANALYZE...)'. The 'if not, ensure both tables have stats' phrasing implied DF requires ANALYZE to function — that's the overstatement. Fixed in-place (no duplicate). WEBSEARCH VERIFICATION (dbt unit tests, via WebFetch docs.getdbt.com/reference/resource-properties/unit-tests): exact YAML schema confirmed — top-level `unit_tests:` list, each item with REQUIRED `name:` + `model:` + `given:` + `expect:`, plus OPTIONAL `versions:` / `config:` / `overrides:`. `given:` is a list of `- input: ref('...') | source('...','...')` items, each with OPTIONAL `format: dict | csv | sql` (default `dict`) and `rows:` (inline) OR `fixture: <name>` (file under `tests/fixtures/`). `expect:` has the same `format:` + `rows:` / `fixture:` shape. `format: dict` is DEFAULT when omitted; `format: csv` rows is inline CSV string with header OR fixture file; `format: sql` rows is a SQL SELECT. Unit test YAML MUST live under model-paths (e.g. `models/`), NOT under `tests/`. `dbt test --select test_type:unit` selector confirmed (NOT `test_type:unit_test`). dbt build runs unit tests as part of build pipeline — order is unit tests → materialization → data tests → downstream per docs.getdbt.com/docs/build/unit-tests. dbt 1.8+ GA confirmed. Worked example used a `full_name` COALESCE + `amount_tier` CASE WHEN derivation — three mock input rows, three expected output rows, validated YAML keys are exact (`unit_tests:` not `unit-tests:`; `given:` + `rows:` not `data:`; `expect:` + `rows:` not `result:`/`output:`). WEBSEARCH VERIFICATION (DF semantics, via WebFetch trino.io/docs/current/admin/dynamic-filtering.html): (a) 'Dynamic filtering is enabled by default. It can be disabled by setting either the `enable-dynamic-filtering` configuration property, or the session property `enable_dynamic_filtering` to false.' (b) 'cost-based optimizer can automatically do this using table statistics provided by connectors. Therefore, it is recommended to keep table statistics up to date' — i.e. ANALYZE is RECOMMENDED not REQUIRED. (c) Supported: 'inner and right joins with `=`, `<`, `<=`, `>`, `>=` or `IS NOT DISTINCT FROM` join conditions, and semi-joins with `IN` conditions are supported.' (d) Both BROADCAST and PARTITIONED distributions support DF. (e) Connector support required for utilizing dynamic filters pushed into the table scan at runtime. Confirms FIX B's 6-step checklist is doctrinally correct. DO-NOT-WRITE matrix in §6.7E bans: `data_tests:` with given/expect (wrong top-level key), `unit_tests:` with not_null/unique (category error), `data:` instead of `rows:` (fabricated key), `result:`/`output:` instead of `rows:` (fabricated key), `format: yaml`/`json` (fabricated values), 'unit tests query warehouse output' (wrong semantics), 'dbt 1.6+'/'1.7+' (wrong version — it's 1.8+), placing unit_tests file under tests/ (wrong directory), `test_type:unit_test` selector (wrong selector name). Bidirectional cross-ref added in §6.7A Q-PATTERN MATCHER → §6.7E ('How do I test my model's TRANSFORMATION LOGIC on MOCK input rows (given/expect)?' routes to §6.7E with the DO-NOT-CONFUSE line). Files modified (2): resources/27-oracle-plsql-to-dbt-trino.md (added §6.7E ~130 lines including YAML schema, worked example, DO-NOT-WRITE matrix, cross-refs; also added 1 cross-ref row in §6.7A Q-pattern matcher); resources/23-sql-best-practices-olap.md (replaced 1 line at §4 EXPLAIN-output checklist line 197 with the corrected 6-step DF diagnostic). DID NOT commit — orchestrator handles single commit per iteration after judge. UNTOUCHED per directive: r22 §13.x federation guardrails (all 66+ DF mentions in §13.x preserved verbatim); federation rubric row (stays 4.49944/310 — NO federation probe); r07 §5 Pattern B2 YoY/MoM; r07 §1a UNNEST array-explode canonical (iter504); r27 §4.1A DECODE-NULL; r27 §4.6B MERGE star-shorthand guardrail; iter495 dbt-trino `partitioning`-key; iter496 r28 GROUPING-bitmask; iter498 r13 MERGE engine-note; iter499 r03 no-index + r28 §3.3/§3.3A materialization; iter500 r09 dbt-snapshot Findability anchor; iter501 r27 §6.7A/r09 dbt-test-COUNT mechanics; iter502 r27 §6.7D dbt-build-runs-seeds; iter503 r13 on_schema_change anchor + r27 §4.5A md5/surrogate-key fix. All locked canonicals untouched. Both edits are doctrinally correct per primary-source verification."
}

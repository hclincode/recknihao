{
  "iteration": 496,
  "phase": "extended",
  "final_iterations_remaining": 0,
  "passed": true,
  "notes": "Iter496 TEACHER — installed LEADING CANONICAL block for Trino GROUPING SETS / ROLLUP / CUBE with the GROUPING() bitmask semantics at r28 (resources/28-complex-sql-performance-trino-dbt.md), placed immediately before the '## Common myths' section (line ~316) so it sits in the LEADING-CANONICAL pole-position part of the file alongside the other LEADING CANONICAL blocks. ROOT CAUSE (iter495 Q4): the responder fabricated `CASE GROUPING(region, category) ... WHEN 2 THEN 'Grand Total'` for a 2-column `ROLLUP(region, category)`. CORRECT value is 3 (binary 11). Value 2 (binary 10) NEVER appears in a 2-column ROLLUP — ROLLUP only drops trailing columns, so the (region rolled up, category present) combination is unreachable. RECONCILE FINDINGS: grep -rni for ROLLUP/CUBE/GROUPING SETS/GROUPING(/GROUPING_ID/subtotal/grand total across ALL of resources/ — r12 had a single mention 'Analytical SQL (windows, CUBE, ROLLUP) is too complex for ORMs' in the OLTP-vs-OLAP comparison table at line 38 (NO canonical block); r28 used 'rollup' only in the daily-aggregation-table sense (NOT the SQL construct); r27 same (daily rollup tables); r07/r23 same; r22 §13.x has TWO federation-guardrail mentions at lines 8691 and 8793 that ROLLUP/CUBE/GROUPING SETS shapes DO NOT push down to PostgreSQL — those are the protected federation guardrails I MUST NOT TOUCH and did not touch. CONCLUSION: no duplicate competing canonical block existed; safe to install fresh canonical at r28. ACTIONS TAKEN: (a) added the new ## LEADING CANONICAL block at r28 line ~317 containing seven labeled subsections: (a) bitmask rule with verbatim Trino doc quote (LEFTMOST = MSB; bit=1 = rolled up), (b) explicit value table for 2-col ROLLUP (0/1/3, value 2 DOES NOT appear) and 3-col ROLLUP (0/1/3/7) with the general N-col rule 2^k-1, (c) full copy-pasteable Trino 467 SQL worked example with GROUP BY ROLLUP(region, category) + CASE GROUPING(region, category) WHEN 0/1/3 + ORDER BY GROUPING() first, (d) DO-NOT-WRITE matrix with the EXACT iter495 fab (`WHEN 2 THEN 'Grand Total'` against 2-col ROLLUP) plus three sibling traps (column-order inversion, empty () inside ROLLUP, single-arg GROUPING when you need both), (e) ROLLUP vs CUBE vs GROUPING SETS comparison table — value 2 IS valid under CUBE(a,b) because CUBE is all-combinations, (f) GROUPING_ID() spelling note (Trino supports both GROUPING and GROUPING_ID, identical values), (g) cross-references to r07 §5 Pattern A2 (GROUP BY uses expression not alias), r22 §13.x federation guardrails (read-only pointer), r27 §6 Oracle dialect translation; (b) added cross-link from r12 line 38 OLTP-vs-OLAP comparison table CUBE/ROLLUP cell pointing to the new r28 canonical (with the key one-liner 'LEFTMOST arg = MSB; bit=1 = rolled up; 2-col ROLLUP grand total = 3, NOT 2' inline so keyword-greppers land on the warning even before clicking through). WEBSEARCH VERIFICATIONS: WebFetch trino.io/docs/current/sql/select.html — confirmed exact doc quote: 'bits are assigned to the argument columns with the rightmost column being the least significant bit' and 'a bit is set to 0 if the corresponding column is included in the grouping and to 1 otherwise'; confirmed for ROLLUP(region, category) the emitted GROUPING values are 0/1/3 (detail/region-subtotal/grand-total). Second WebSearch independently corroborated rightmost=LSB / leftmost=MSB across multiple sources. UNTOUCHED (per directive): r22 §13.x federation guardrails (lines 8691 + 8793 unchanged); federation rubric row 4.49944/310; r07 §5 Pattern B2 YoY/MoM canonical; r27 §4.1A DECODE-NULL canonical; r28 § LEADING CANONICAL dbt-trino `partitioning` partition-key block (iter495 fix). Files modified: resources/28-complex-sql-performance-trino-dbt.md, resources/12-oltp-to-olap-mindset.md."
}

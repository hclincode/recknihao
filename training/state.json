{
  "iteration": 985,
  "phase": "extended",
  "final_iterations_remaining": 0,
  "passed": true,
  "note": "iter985 LIGHT additive FIX-A for the 2-in-2 (iter984 Q4 + iter985 Q4) date_format %B broken-secondary recurrence: responder gave the CORRECT format_datetime(CAST(due_date AS timestamp),'MMMM yyyy') LEAD for 'June 2026' but appended a BROKEN date_format(due_date,'%B %Y') — verified at trino.io/docs/467 that %B is a C/strftime code NOT a Trino/MySQL specifier (full month = %M, abbrev = %b); no %B literal existed in resources/ (responder fabrication, not a resource defect). Mirrored the existing %A weekday defang: (1) r27 §4.2 worked-examples — added a copy-correct ✅ %M full-month line ('%M %Y' -> 'June 2026') immediately after the %b abbreviated-month L608 line, FOLLOWED BY a ❌ %B DO-NOT-COPY defang (✅ canonical FIRST per defang-backfire lesson); (2) r27 L600 prose — appended '❌ there is no %B ... use %M' to the month-name mirror sentence; (3) r23 L1206 — appended the '%M vs %b, no %B' clarification to the MINUTE-specifier NOTE. Reconciled-in-place, additive only, surrounding content untouched, r22 federation untouched. PRIOR durability context retained: iter984 STRONG PASS (4.40625); iter983 aggregate-in-GROUP-BY slip CONFIRMED one-off (no FIX-A). iter984 was a STRONG PASS (4.40625): the iter983 aggregate-in-GROUP-BY slip did NOT recur (Q2 lead used the CORRECT two-level CTE — inner GROUP BY raw account_id + CASE-on-SUM in SELECT, outer GROUP BY spend_tier COUNT), so the slip is CONFIRMED a ONE-OFF (no FIX-A); the two iter984 responder broken-secondary slips (Q2 false 'single-query won't work', Q4 date_format %B-vs-%M) were against CORRECT resources — per-instance one-offs, no resource defect. All locked canonicals re-confirmed intact via read-only spot-checks this iteration. ★ r07 §A2 GROUP BY rules anchor L2769-2821 INTACT + FINDABLE: header L2769, rules 1-5 L2773-2777 (GROUP BY=expressions/ordinals only, NO AS alias-def, NO alias-by-name #16533, alias OK in outer ORDER BY not GROUP BY/WHERE/HAVING, window OVER ORDER BY uses pre-projection expr), canonical worked example L2781-2796 (GROUP BY DATE_TRUNC('month',event_date) NON-aggregate expr + COUNT(*) + SUM(COUNT(*)) OVER), ordinal variant L2800-2802, DO-NOT-WRITE matrix 6 rows L2814-2821 (AS-in-GROUP-BY parse err, alias-by-name #16533, bare-undefined-alias-in-SELECT, alias-in-OVER ORDER BY, alias-in-WHERE, alias-in-HAVING). iter953/955 B-Streak filter-then-count defang r07 L3228-3262 INTACT (FINDABILITY ROUTER L3228, count-over-FULL-streak-first L3230, two always-zero traps L3232-3235, share-one-streak_id fix L3237, CORRECT block L3242-3251 COUNT(*) FILTER+max_by+GROUP BY streak_id no-pre-filter, WRONG#1 L3253-3256 + WRONG#2 L3257-3259 inline DO-NOT-COPY, rule recap L3262, NESTED_WINDOW note L3221). r07 L37 HAVING runs AFTER aggregation (trims OUTPUT not scan/memory) CONFIRMED. Unwrap rule names r07 L67-74 CONFIRMED (UnwrapCastInComparison/UnwrapDateTruncInComparison/UnwrapYearInComparison, NOT SimplifyDateTrunc; EXTRACT(YEAR)->year() same rule; registered unconditionally PlanOptimizers.java). PERCENTILE footgun r05 L2234 CONFIRMED (PERCENTILE_CONT WITHIN GROUP not Trino/parse error, WITHIN GROUP listagg-only, approx_percentile T-Digest). as-of/argmax r23 CONFIRMED: L1296-1302 max_by(status,updated_at)/min_by worked example, L1305 QUALIFY ROW_NUMBER()=1 NOT in 467 + ROW tiebreaker, L1307 first-AND-last min_by+max_by AGGREGATE-not-window iter656 PIN; §3.1G L1732-1736 no-DISTINCT-ON ROW_NUMBER()=1 (+NO QUALIFY). date_diff/no-ts-minus-ts r23 L2211-2217 CONFIRMED (THE ONE FACT L2215 four interval-only subtraction ops NO date-date/ts-ts/time-time, date_diff later-arg-third positive, valid-arithmetic-don't-over-ban L2217 ts+/-INTERVAL literal valid). anti-join r23 §10 L2690-2702 CONFIRMED (JARGON GLOSS five terms, SemiJoin+Filter[NOT semijoinoutput], NO FilterMode=ANTI token, CorrelatedJoin warning, LeftJoin+Aggregation slow path #21859, one-sentence summary L2702). JOIN fan-out r23 L933 CONFIRMED (SUM-too-small checklist #2 JOIN drop/fan + COUNT(*),COUNT(DISTINCT k) FROM right_table). LEFT-JOIN-COUNT(child_key) r07 L903/L909/L910 CONFIRMED (COUNT(*) inflates by NULL-padded row, use COUNT(right.key)/SUM(CASE), COALESCE(COUNT(*),0) does NOT fix). Iceberg partition DDL r09 CONFIRMED — L73 WITH(partitioning=ARRAY['day(occurred_at)','tenant_id']) transforms+identity inside one ARRAY of STRINGS, L74 bucket(col,N) column-first vs Spark bucket(N,col) (also L131), L127 PARTITIONED-BY ban INTACT+FINDABLE, L153 partition-evolution ALTER TABLE SET PROPERTIES partitioning metadata-only old+new spec coexist. r22 federation §13.1-13.8 UNTOUCHED (headers L8050/8054/8069/8140/8242/8292/8534/9115/9148/9170 verified). git working tree clean (HEAD 516cff7). ZERO resource edits. NO git commit/push."
}

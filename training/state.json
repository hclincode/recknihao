{
  "iteration": 456,
  "phase": "extended",
  "final_iterations_remaining": 0,
  "passed": true,
  "notes": "Iter456 TEACHER — PRIMARY FIX for iter455 Q4 FAIL (3.25; FAB-2 + FAB-3: native-Iceberg `write.parquet.compression-codec` keyed into a Trino WITH-clause + `properties = map(...)` wrapper as Trino syntax + string-literal LHS in SET PROPERTIES) AND the iter455 Q1 FAB-1 (`timestamp_ms` as a Trino `$snapshots` column). Root cause across three iterations: native-Iceberg/Spark/Java-API names spilling into the Trino dialect. Installed a consolidated meta-canonical guardrail.\n\n(1) COMPRESSION leading canonical worked example — installed in TWO load-bearing places: (a) resources/03-columnar-storage.md (the natural keyword home for 'compression' / 'Snappy' / 'Zstd' — placed as a new '### LEADING CANONICAL — How do I set Parquet compression on this stack (Trino 467 + Iceberg 1.5.2)?' subsection between the 'Other compressions you'll see' table and 'How compression makes queries faster' section, ~line 185), (b) resources/11-lakehouse-storage-sizing.md (reconciled IN PLACE at the existing '### Default Parquet compression codec' subsection, ~lines 185-208 BEFORE — which had two stale Trino-context examples using `'write.parquet.compression-codec'` and `SET TBLPROPERTIES`). Canonical Trino 467 forms taught: (i) CREATE TABLE iceberg.analytics.events (...) WITH (format = 'PARQUET', compression_codec = 'ZSTD', partitioning = ARRAY['day(occurred_at)']); — FLAT name=value pairs; (ii) ALTER TABLE iceberg.analytics.events SET PROPERTIES compression_codec = 'ZSTD'; — BARE identifier on LHS, string literal RHS; (iii) recompress existing files via ALTER TABLE ... EXECUTE optimize. Valid values: 'ZSTD','SNAPPY','GZIP','LZ4','NONE' verified via trino.io/docs/current/connector/iceberg.html. DO-NOT-WRITE matrix in both r03 and r11 quotes the iter455 Q4 fabrications verbatim with inline correction: (a) `WITH (..., properties = map('write.parquet.compression-codec', 'snappy'))` → wrong: Trino WITH takes FLAT pairs not properties-map; (b) `WITH (..., \"write.parquet.compression-codec\" = 'snappy')` → wrong: `write.parquet.compression-codec` is the native Iceberg property key not a Trino WITH property; (c) `ALTER TABLE ... SET PROPERTIES ('compression_codec' = 'ZSTD')` (string-literal LHS) → wrong: SET PROPERTIES takes bare identifiers; (d) `ALTER TABLE ... SET TBLPROPERTIES ('write.parquet.compression-codec' = 'zstd')` → wrong: that's Spark SQL + native-Iceberg key, not Trino.\n\n(2) $snapshots COLUMNS — scanned r17/r18/r10/r24/r25 and confirmed NO stale `timestamp_ms`-as-Trino-$snapshots-column usage existed in any resource (only legitimate Debezium `source.ts_ms` references in r13, which are correctly scoped to the CDC event envelope — NOT to Iceberg $snapshots). r17 already had the correct 6-column list (`committed_at, snapshot_id, parent_id, operation, manifest_list, summary`) at lines 423 and 435 — but no LEADING canonical 6-column-with-types block at the top of the metadata-table cheat sheet. NEW LEADING CANONICAL BLOCK inserted in r17 immediately before the existing metadata-table cheat sheet (between line 418 `Cross-engine pitfall` callout and line 419 `One-line use for X`): '### LEADING CANONICAL — Trino 467 `$snapshots` column list (6 columns, exact types)' with explicit type per column (committed_at TIMESTAMP(3) WITH TIME ZONE; snapshot_id BIGINT; parent_id BIGINT; operation VARCHAR; manifest_list VARCHAR; summary map(VARCHAR, VARCHAR)) verified via trino.io/docs/current/connector/iceberg.html. Includes the canonical ORDER BY committed_at DESC LIMIT 20 query. DO-NOT-WRITE matrix bans the iter455 Q1 fab (`timestamp_ms`), the related Iceberg-Java-API field-name variants (`epoch_ms`, `ts_ms`, `committed_at_ms`, `parent_snapshot_id`), and the column-placement-confusion fields (`is_current_ancestor`, `made_current_at` — both exist but on `$history` not `$snapshots`). Reproduces the iter455 Q1 fabrication verbatim with the precise correction (RIGHT vs WRONG side-by-side).\n\n(3) UNIFYING GUARDRAIL — '### LEADING CANONICAL — Trino dialect ↔ native-Iceberg name translation (meta-canonical)' inserted in r17 immediately after the $snapshots column-list block. 8-row translation table maps the most common Iceberg-on-Trino spillover failure surfaces: $snapshots commit timestamp (Trino `committed_at` ↔ Iceberg Java API `timestamp_ms`/`Snapshot.timestampMillis()`); $snapshots parent pointer (`parent_id` ↔ `parent_snapshot_id`/`parentId()`); Parquet compression (`compression_codec` ↔ `write.parquet.compression-codec`); file format (`format` ↔ `write.format.default`); target write file size (Trino session `iceberg.target_max_file_size` only — native `write.target-file-size-bytes` ignored by Trino 467 per trinodb/trino #28250); compaction file-size threshold (Trino `EXECUTE optimize(file_size_threshold => '256MB')` ↔ Spark `rewrite_data_files` `options=>map('target-file-size-bytes', ...)`); WITH-clause shape (FLAT `name = expression` pairs ↔ `properties = map(...)` / `TBLPROPERTIES(...)`); SET PROPERTIES LHS form (BARE identifier ↔ string-literal in Spark TBLPROPERTIES). Closing meta-rule (verbatim): 'in Trino, use Trino's dialect names — native Iceberg / Spark property and API names parse-error or no-op against the Trino Iceberg connector.' SECOND COPY of the translation table inserted as a top-level section in r11 ('## Trino dialect ↔ native-Iceberg name translation (meta-canonical)', after the 'Practical implications' subsection, ~line 252) so the keyword 'translation' / 'native Iceberg' is reachable from BOTH the compression-keyword path (r03/r11) AND the metadata-table-keyword path (r17).\n\nRECONCILE (NOT APPEND) — r11 lines 195-202 BEFORE: `ALTER TABLE iceberg.analytics.events SET TBLPROPERTIES ('write.parquet.compression-codec' = 'zstd');` (Spark form, OK label) AND `ALTER TABLE iceberg.analytics.events SET PROPERTIES \"write.parquet.compression-codec\" = 'zstd';` (labelled '-- Trino:' but using the native-Iceberg key and double-quoted-string LHS — the EXACT iter455 fabrication shape). AFTER: both forms removed and replaced with the LEADING CANONICAL block establishing `compression_codec` as the Trino property + the DO-NOT-WRITE matrix quoting the iter455 fabrications verbatim with inline correction. ALSO removed the stale verification line `SHOW CREATE TABLE iceberg.analytics.events; -- look for 'compression-codec' in the properties` (the property surfaces as `compression_codec` in the WITH clause, not `compression-codec` with a hyphen). Reconciled in place — no contradictory legacy text left in the file. No other resource files used the bad Trino-context forms (grep confirmed).\n\nFILES MODIFIED: resources/03-columnar-storage.md (new LEADING CANONICAL compression worked example + DO-NOT-WRITE between 'Other compressions' table and 'How compression makes queries faster' section), resources/11-lakehouse-storage-sizing.md (reconciled '### Default Parquet compression codec' subsection IN PLACE replacing stale Spark TBLPROPERTIES + Trino-context-with-native-key examples; added '## Trino dialect ↔ native-Iceberg name translation (meta-canonical)' top-level section), resources/17-iceberg-table-maintenance.md (new LEADING CANONICAL '$snapshots column list (6 columns, exact types)' block + new LEADING CANONICAL 'Trino dialect ↔ native-Iceberg name translation (meta-canonical)' block before the metadata-tables cheat sheet).\n\nWEBSEARCH VERIFICATION:\n- Trino Iceberg WITH-clause property list (format, compression_codec, partitioning, sorted_by, format_version, location, max_commit_retry, delete_after_commit_enabled, max_previous_versions, orc_bloom_filter_columns, orc_bloom_filter_fpp, parquet_bloom_filter_columns, object_store_layout_enabled, data_location, extra_properties) verified via trino.io/docs/current/connector/iceberg.html (Connector → Iceberg → Table properties).\n- Trino Iceberg compression_codec valid values ('NONE', 'SNAPPY', 'LZ4', 'ZSTD', 'GZIP') verified via same source.\n- Trino $snapshots metadata-table columns (committed_at TIMESTAMP(3) WITH TIME ZONE, snapshot_id BIGINT, parent_id BIGINT, operation VARCHAR, manifest_list VARCHAR, summary map(VARCHAR, VARCHAR)) verified via trino.io/docs/current/connector/iceberg.html (Metadata tables → $snapshots).\n- Trino SET PROPERTIES bare-identifier LHS syntax verified via trino.io/docs/current/sql/alter-table.html.\n- Trino CREATE TABLE flat WITH (property_name = expression) syntax verified via trino.io/docs/current/sql/create-table.html.\n- Iceberg Java API Snapshot.timestampMillis() field name verified via iceberg.apache.org/javadoc.\n\nNO FRESH UNVETTED CLAIMS — every new claim is either (i) doc-verified syntax/column form / property name / valid values from trino.io/docs/current/connector/iceberg.html or related Trino docs, (ii) directly quoted from Trino docs with hyperlink, or (iii) labelling a known-native-Iceberg/Spark name as such with the correct Trino-side equivalent.\n\n§13.X FEDERATION GUARDRAILS UNTOUCHED — `git diff --stat resources/22-trino-federation-postgresql.md` confirmed EMPTY; `grep -cE '^## 13\\.|^### 13\\.' resources/22-trino-federation-postgresql.md` returns 10 markers intact. Federation NOT probed this iter — recorded near-miss 4.49944/310 stays unchanged. Iteration bumped 455 → 456."
}

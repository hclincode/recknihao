{
  "iteration": 980,
  "phase": "extended",
  "final_iterations_remaining": 0,
  "passed": true,
  "note": "iter980 NO-OP durability-breadth sweep — ZERO resource edits; only this state.json iteration bump. All locked canonicals re-confirmed intact via read-only spot-checks. iter953/955 B-Streak filter-then-count defang (r07 L3228-3262): FINDABILITY ROUTER L3228 (domain-neutral run-of-type-A-ending-at-type-B + 'returns 0 every time'), STREAK-ASSOCIATION L3232-3237 (run+target SHARE one streak_id, COUNT(*) FILTER over FULL streak, keep ending=target via max_by(status, event_time) NOT nested FILTER, fallback streak_id-1; both always-zero traps named: WHERE-to-target-before-counting + counting-within-target's-own-streak), CORRECT block L3239-3251 uses COUNT(*) FILTER (WHERE status='declined') + max_by(status,event_time), WRONG#1 WHERE-strips-counted-rows L3253-3256 (+COUNT(*) OVER also after WHERE) and WRONG#2 count-in-own-streak L3257-3259 both inline DO-NOT-COPY marked, the-rule recap L3262, NESTED_WINDOW analyzer note L3221, skeleton UNCORRUPTED. r07 L37 HAVING does NOT cut GROUP BY memory (only trims OUTPUT) CONFIRMED. GROUP-BY-output rule r07 L4338 (SELECT-list alias-by-name NOT in GROUP BY, #16533) CONFIRMED + Monday-vs-Sunday week-trunc + anti-nesting + iter933 ADD-A-QUARTER/ADD-A-WEEK + WHICH-QUARTER router. Unwrap rule names CONFIRMED — r07 L67-74 (UnwrapCastInComparison/UnwrapDateTruncInComparison/UnwrapYearInComparison, source-file-verified io/trino/sql/planner/iterative/rule/, registered unconditionally PlanOptimizers.java simplifyOptimizerRules, EXTRACT(YEAR)->year() same rule, RHS constant-fold). PERCENTILE footgun CONFIRMED — r05 L2234 CRITICAL SQL FOOTGUN (PERCENTILE_CONT WITHIN GROUP not Trino/parse error, WITHIN GROUP listagg-only, approx_percentile T-Digest). as-of/argmax canonicals CONFIRMED: r23 §3.1D L1296-1305 max_by(status,updated_at)/min_by latest-status worked example + ROW tiebreaker max_by(status,(updated_at,event_id)) L1305 + QUALIFY-does-NOT-exist-in-467 aside L1305; first-AND-last min_by+max_by AGGREGATE-not-window-with-GROUP-BY L1307; §3.1G L1732-1736 LEADING CANONICAL ROW_NUMBER()=1/no-DISTINCT-ON; QUALIFY-not-in-Trino-467-PARSE-ERROR myth L18 + cross-ref L1275 + myths-table L1386. date_diff/no-ts-minus-ts r23 L2211-2217 CONFIRMED (THE ONE FACT L2215 four interval-only subtraction operators, NO date-date/ts-ts/time-time, date_diff later-arg-third positive result, valid-arithmetic-don't-over-ban L2217 ts+/-INTERVAL literal IS valid). JOIN fan-out r23 L933 CONFIRMED (SUM-too-small checklist #2: JOIN dropping/fanning rows, COUNT(*),COUNT(DISTINCT k) FROM right_table diagnostic). anti-join r23 §10 L2690-2702 CONFIRMED (JARGON GLOSS five terms semi-join/anti-join/SemiJoinNode/CorrelatedJoin/LeftJoin; NOT IN/NOT EXISTS=anti via SemiJoin+Filter[NOT semijoinoutput], NO FilterMode=ANTI token, CorrelatedJoin warning, decorrelated-NOT-EXISTS LeftJoin+Aggregation slow path #21859, one-sentence summary L2702). LEFT-JOIN-COUNT(child_key) r07 L903/L909/L910 CONFIRMED (COUNT(*) inflates by NULL-padded row, use COUNT(right.key)/SUM(CASE), COALESCE(COUNT(*),0) does NOT fix). Iceberg partition DDL r09 CONFIRMED — L73 WITH(partitioning=ARRAY['day(occurred_at)','tenant_id']) transforms+identity both inside one ARRAY of STRINGS, L74 bucket(col,N) column-first vs Spark bucket(N,col). regexp_like LEADING CANONICAL + Postgres ~/~*/!~/RLIKE defang + mean-family geometric/harmonic CONFIRMED (per iter977/978). r28 DATE-literal pushdown CONFIRMED. r13 json_exists + dbt-trino properties/partitioning + r27 dbt model-contract §6.7C CONFIRMED. r22 federation §13.1-13.8 UNTOUCHED — L13 reference card, L15 VARCHAR equality-vs-range, L16/§13.5 TopN-in-OSS-since-353/354-default, §13.5A.3 cross-catalog-join-NEVER-pushes, myths table. git status clean. ZERO resource edits. NO git commit/push."
}

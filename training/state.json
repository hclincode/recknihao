{
  "iteration": 539,
  "phase": "extended",
  "final_iterations_remaining": 0,
  "passed": false,
  "notes": "Iter539 TEACHER — PRIMARY: add a Trino DECIMAL-cast rounding canonical (HALF_UP, NOT banker's / NOT HALF_EVEN) adjacent to r23 §3.1B. Iter538 Q3 (Acc 2.0) claimed Trino casts DOUBLE/REAL → DECIMAL with 'banker's rounding (round-half-to-even)' — VERIFIED WRONG via Trino source: `core/trino-spi/src/main/java/io/trino/spi/type/DecimalConversions.java` and `core/trino-main/src/main/java/io/trino/type/DecimalCasts.java` both use `setScale(intScale(scale), HALF_UP)`. trino.io docs are SILENT on the mode — source is authoritative. SECONDARY: clarify on r27 §4.5C that Iceberg v2 DOES support DELETE FROM with predicate (row-level deletes); the ROWID-dedup limitation is specifically about expressing 'keep most recent' in a single predicate, NOT about DELETE being absent.\n\nGREP FINDINGS:\n  - r23 §3.1B at L249-278 — existing DECIMAL widening canonical (NUMERIC_VALUE_OUT_OF_RANGE overflow row at L274). No prior coverage of CAST rounding mode (HALF_UP vs HALF_EVEN). Confirmed absent — added §3.1C immediately after §3.1B (between L278 source list and the next `---` divider). Rounding canonical is now ADJACENT to the DECIMAL-sum canonical as directed.\n  - r07 §3 had no DECIMAL-cast-rounding content. No edit there (canonical belongs in r23 only).\n  - r17 L1463-1468 — existing GDPR 4-step DELETE runbook (CoW vs MoR). The current language ALREADY makes clear that `DELETE FROM iceberg.analytics.events WHERE user_id = 'gdpr-subject-42'` works on the production stack. No contradictory claim found in r17 — preventive note not needed there (the GDPR runbook already affirms DELETE FROM with predicate works on Iceberg v2 CoW and MoR).\n  - r27 §4.5C L1454 — the line 'Oracle ROWID dedup idiom has no direct rewrite on Trino 467' is correct but read in isolation could be misinterpreted as 'no DELETE on Iceberg'. ADDED a clarifying paragraph immediately below it: Iceberg v2 supports DELETE FROM with predicate; only 'delete duplicates keeping the most recent in a single predicate' is what's missing — direct quote from trino.io Iceberg connector docs included.\n\nWEBSEARCH/SOURCE VERIFICATION (2026-06-06):\n  - WebFetch github.com/trinodb/trino/blob/master/core/trino-spi/src/main/java/io/trino/spi/type/DecimalConversions.java — VERBATIM:\n    * DOUBLE→DECIMAL: `BigDecimal bigDecimal = BigDecimal.valueOf(value).setScale(intScale(scale), HALF_UP);`\n    * REAL→DECIMAL:   `BigDecimal bigDecimal = new BigDecimal(String.valueOf(floatValue)).setScale(intScale(scale), HALF_UP);`\n  - WebFetch github.com/trinodb/trino/blob/master/core/trino-main/src/main/java/io/trino/type/DecimalCasts.java — VERBATIM:\n    * `result = bigDecimal.setScale(DecimalConversions.intScale(scale), HALF_UP);` (in numberToShortDecimal and numberToLongDecimal)\n    * Overflow: `if (overflows(result, precision)) { throw new TrinoException(NUMERIC_VALUE_OUT_OF_RANGE, format(\"Cannot cast NUMBER '%s' to DECIMAL(%s, %s)\", ...)); }`\n  - WebFetch trino.io/docs/current/connector/iceberg.html — VERBATIM:\n    * 'The Data management functionality includes support for INSERT, UPDATE, DELETE, TRUNCATE, and MERGE statements.'\n    * 'Tables using v2 of the Iceberg specification support deletion of individual rows by writing position delete files.'\n    * 'For partitioned tables, the Iceberg connector supports the deletion of entire partitions if the WHERE clause specifies filters only on the identity-transformed partitioning columns, that can match entire partitions.'\n  - trino.io DECIMAL types/cast docs are SILENT on the rounding mode for DOUBLE→DECIMAL — confirmed via earlier WebSearch on Trino 481 decimal docs page. Source is authoritative; quoted in canonical.\n  - All SQL valid Trino 467 dialect (no QUALIFY, no PERCENTILE_CONT WITHIN GROUP, no Spark CALL syntax in Trino blocks).\n\nEDITS (2 in-place, no appended duplicate blocks, NO locked-canonical rewrites):\n\n  1. resources/23-sql-best-practices-olap.md — INSERTED new §3.1C between L278 (existing §3.1B Sources line) and the `---` divider preceding §4. The new section:\n     - HEADER: `## 3.1C. CAST(DOUBLE/REAL AS DECIMAL) uses HALF_UP rounding (NOT banker's / NOT HALF_EVEN) — billing-critical canonical`\n     - KEYWORD ANCHORS line covering: Trino DECIMAL cast rounding, banker's rounding Trino, round half to even Trino, round half up Trino, Trino billing decimal rounding, HALF_UP vs HALF_EVEN, cast double to decimal rounding, RoundingMode.HALF_UP Trino, cast real to decimal rounding.\n     - ONE-FACT paragraph: states HALF_UP per Trino source (DecimalConversions.java + DecimalCasts.java); explicitly notes trino.io docs are SILENT on the mode (source authoritative).\n     - TIE-CASE WORKED EXAMPLES SQL block (Trino 467, 7 lines): CAST(DOUBLE '0.5' AS DECIMAL(1,0)) → 1 (banker's: 0); CAST(DOUBLE '2.5' AS DECIMAL(2,0)) → 3 (banker's: 2); CAST(DOUBLE '0.005' AS DECIMAL(3,2)) → 0.01 (banker's: 0.00); CAST(DOUBLE '0.015' AS DECIMAL(3,2)) → 0.02; CAST(DOUBLE '0.025' AS DECIMAL(3,2)) → 0.03 (banker's: 0.02); plus 2 REAL examples for completeness.\n     - IN-LINE SIGNAL (iter534 strategy — corrective comment ON the line the responder will copy): billing example `CAST(amount_dbl AS DECIMAL(18, 2)) AS amount_billed  -- HALF_UP rounding (NOT banker's; 0.005 -> 0.01)` — embedded INSIDE a working SQL example so responder copying the line carries the corrective note inline.\n     - OVERFLOW paragraph: cites DecimalCasts.java NUMERIC_VALUE_OUT_OF_RANGE check (preserves the iter538 fact that overflow is a hard error, no silent wrap); reaffirms DECIMAL(18,2) as sensible billing default on this stack.\n     - DO-NOT-WRITE table (5 rows): bans 'banker's rounding' / 'round-half-to-even' / 'HALF_EVEN' claims; bans 'IEEE-754 round-half-to-even' claim; bans 'silent wrap on overflow'; clarifies that `ROUND(x, n)` is also HALF_UP (not a banker's-rounding escape hatch); notes Trino has no built-in HALF_EVEN cast function (must do in app code).\n     - SOURCES list: links to the two Trino source files on github.com/trinodb/trino, plus Trino docs language/types.html (with note that docs are silent on rounding mode), plus math.html for ROUND reference.\n\n  2. resources/27-oracle-plsql-to-dbt-trino.md — APPENDED a single `> Clarification` blockquote (4 lines) at §4.5C immediately after the existing 'The one fact' paragraph (L1454). Content: Iceberg v2 supports DELETE FROM with predicate (cites trino.io docs verbatim 'Tables using v2 of the Iceberg specification support deletion of individual rows by writing position delete files'); what's missing is expressing 'keep first per group' as a single predicate, not DELETE itself; bans the wrong claims 'Iceberg has no DELETE' / 'Trino has no row-level delete on Iceberg' (both FALSE); reaffirms Pattern A (READ-dedup) and Pattern B1/B2 (in-place dedup) as the correct routes. Did NOT rewrite the existing 'The one fact' line or any of Patterns A/B1/B2 — just added the clarification beneath.\n\nFILES MODIFIED (2): resources/23-sql-best-practices-olap.md (§3.1C HALF_UP rounding canonical inserted adjacent to §3.1B with tie-case examples + in-line signal + DO-NOT-WRITE table), resources/27-oracle-plsql-to-dbt-trino.md (§4.5C Iceberg-v2-DELETE clarification blockquote).\n\nUNTOUCHED per directive:\n  - resources/22 §13.x federation guardrails — ZERO edits; federation rubric row stays 4.49944/310; NO federation probe.\n  - r07 locked canonicals: §1a-§1a.4 + Pattern C4 + approx + date_trunc(return-type + week-Monday) + §5 B2/B3 + FILTER alias-must-match canonical (iter538) — all preserved.\n  - r09 locked canonicals: element_at + map-HOF + CAST-to-JSON + dbt-snapshot SCD2 (4-default) + COALESCE-default — preserved intact.\n  - r10 partition-evolution nuance canonical — preserved intact.\n  - r13 from_unixtime epoch-ms + JSON family — untouched.\n  - r17 CRITICAL QUOTING + $partitions + current-spec + RENAME + $snapshots ops + target-file-size + WAP + $refs + $files/$history JOIN + iter534 in-line signal + deleted_snapshot_id + 5d-retention — untouched. The existing GDPR 4-step runbook at L1463-1468 already correctly affirms DELETE FROM with predicate works on Iceberg v2 — no edit needed there.\n  - r23 §3.1A (string-split family) + §3.1B (DECIMAL SUM auto-widen) + §10 + DF + approx_percentile family (iter538) — all preserved intact. §3.1C is NEW (additive).\n  - r26 §4 — untouched.\n  - r27 §4.1A/§4.3 string table + §4.3-STR-FAMILY/§4.3A/§4.4*/§4.5A/§4.5B/§4.5D/§4.x/§4.6B/§7A.2A-B/§6.7A-J — untouched. §4.5C only EXTENDED with the new clarification blockquote below 'The one fact' line; Patterns A/B1/B2 + DO NOT WRITE table preserved intact.\n  - iter495-538 locks preserved.\n\nDID NOT run git commit or git push — orchestrator handles single commit per iteration after judge. DID NOT touch resources/22. All SQL valid Trino 467 dialect."
}

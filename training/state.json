{
  "iteration": 482,
  "phase": "extended",
  "final_iterations_remaining": 0,
  "passed": true,
  "notes": "Iter482 TEACHER — PRIMARY FIX: installed listagg-OVER FAB BAN (fabricated-capability-GRANT class) per iter481 Q4 HARD FAIL 1.75. Federation NOT probed (stays 4.49944/310 per iter472-481+ directive). Iter481 was 4.0469 thin PASS (80th consecutive overall PASS); Q4 LOAD-BEARING FAB on windowed-LISTAGG dropped iter avg from iter480's 4.6953 to 4.0469. Fab streak BROKEN after iter480's restoration.\n\nROOT CAUSE: responder claimed Trino `listagg(...) WITHIN GROUP (ORDER BY ...) OVER (PARTITION BY ...)` is 'keyword-for-keyword identical to Oracle, no rewrite needed.' Reality: Trino docs verbatim ([trino.io/docs/current/functions/aggregate.html#listagg](https://trino.io/docs/current/functions/aggregate.html#listagg)): 'The current implementation of listagg function does not support window frames.' listagg is AGGREGATE-ONLY, requires GROUP BY, produces one row per group. Compounding misdiagnosis: responder told user the failure is a NULLS-default/quoting issue — FALSE, function does not support OVER at all.\n\nPRE-EDIT GREP RESULTS (load-bearing — proves fab origin):\n- `grep -rn 'listagg.*OVER\\|listagg.*window\\|listagg.*PARTITION BY' resources/`: ZERO hits for the FABRICATED pattern (listagg with OVER) in resources/ pre-iter482. The iter481 Q4 fab was 100% responder hallucination, NOT a stale resource line — same diagnosis pattern as iter474 (distributed_join_distribution_type), iter478 (task_max_memory + memory_revoking_enabled), iter479 (spill_order_by_enabled).\n- ONE STALE INCORRECT CLAIM found and reconciled: resources/23-sql-best-practices-olap.md line 844 said 'Added as a window function in Trino 467 release.' — this was FALSE (listagg is aggregate-only, was never added as a window function in any release; PR #6418 added it as aggregate-only in release 396). Reconciled in place (not appended).\n- `grep -rn 'array_agg.*ORDER BY.*OVER\\|array_agg.*OVER.*ORDER BY' resources/`: 2 hits in r27, both AGGREGATE forms (`array_agg(col ORDER BY col)` without OVER) which are valid — no fab.\n\nWebSearch/WebFetch VERIFICATIONS THIS ITER (cite-or-omit, every claim doc-anchored):\n- trino.io/docs/current/functions/aggregate.html WebFetch: CONFIRMED VERBATIM 'The current implementation of listagg function does not support window frames.' quoted in canonical card.\n- trino.io/docs/current/functions/window.html WebFetch: CONFIRMED listagg is NOT in the window-function list. Window functions documented are: cume_dist, dense_rank, ntile, percent_rank, rank, row_number (ranking); first_value, last_value, nth_value, lead, lag (value); plus aggregate-as-window. listagg is explicitly excluded.\n- github.com/trinodb/trino/issues/16984 WebFetch: CONFIRMED array_agg(x ORDER BY y) OVER (...) is not supported — error 'must be an aggregate expression or appear in GROUP BY clause.' Issue title: 'ARRAY_AGG with ORDER BY rejected when used as window function.'\n\nEDITS THIS ITER (surgical, reconcile-don't-append):\n\nEDIT 1 (r23 line 844 STALE-RECONCILE) — replaced 'Added as a window function in Trino 467 release.' with the correct: 'AGGREGATE-ONLY — Trino listagg does NOT support OVER (...) window frames (per trino.io/docs/current/functions/aggregate.html#listagg verbatim: \"The current implementation of listagg function does not support window frames\"). Requires GROUP BY in the outer query, produces ONE row per group. For Oracle's windowed LISTAGG(...) OVER (PARTITION BY k) (value repeated on every row), use array_join(array_agg(expr) OVER (PARTITION BY k), sep) — see resource 27 §7A.2A.'\n\nEDIT 2 (r27 §7A.2 table line 2140 ROW SPLIT) — split the single listagg row into TWO rows: (a) aggregate form (one row per group) with explicit AGGREGATE-ONLY note + cross-link to §7A.2A; (b) NEW row for Oracle's WINDOWED LISTAGG with explicit 'NO direct Trino equivalent' + verbatim doc quote + cross-link to §7A.2A. The aggregate-form row still references §7A.2 for ON OVERFLOW mapping.\n\nEDIT 3 (r27 NEW §7A.2A LEADING CANONICAL CARD inserted between §7A.2 closing 'QUALIFY landmine' and §7A.3 'PL/SQL packages') — full canonical card with: (1) keyword anchor block (windowed listagg, listagg OVER, listagg PARTITION BY, listagg analytic, listagg every row, listagg repeated on every row, listagg WITHIN GROUP OVER, Oracle LISTAGG to Trino, LISTAGG keep value on every row, listagg as window function, listagg window frame); (2) The-one-rule sentence with VERBATIM doc quote + window-function-list exclusion citation; (3) Oracle source code block (windowed LISTAGG); (4) Case A — AGGREGATE form (one row per group) — typical case — full SQL block + 4-bullet rationale; (5) Case B — WINDOWED form (value repeated on every row) — naive UNSAFE rewrite + caveat citing trinodb/trino #16984 + DETERMINISTIC pre-sort CTE rewrite + caveat-on-the-caveat noting Case A is the truly safe pattern; (6) DO-NOT-WRITE matrix with 6 banned rows: (a) listagg(...) WITHIN GROUP OVER (PARTITION BY), (b) listagg(...) OVER without WITHIN GROUP, (c) NULLS/quoting misdiagnosis, (d) array_agg(x ORDER BY y) OVER, (e) 'Trino added listagg as window function in release N' fab claim, (f) 'listagg supports OVER with SQL:2016 ordered-set form' fab claim; (7) Choice rule (always prefer Case A; only Case B if denormalized output truly required; never listagg(...) OVER); (8) Cross-references to §7A.2, r23 STRING_AGG row, trino.io aggregate/window docs, trinodb/trino #16984.\n\nRECONCILIATION (NOT appended): every edit is in-place. The stale r23:844 'Added as a window function in Trino 467 release' line was the only existing contradictory content — fully reconciled to the correct aggregate-only statement with cross-link to §7A.2A. The r27 §7A.2 row was split (not appended) to surface the windowed-form ban directly in the analytic-functions table where a responder lands when scanning Oracle analytic→Trino window mappings. No append-anywhere edits.\n\n§13.x federation guardrails in resources/22-trino-federation-postgresql.md NOT TOUCHED (only r23 + r27 + state.json modified this iter). Federation 4.49944/310 row held per iter472-481+ directive.\n\nFab-class watch held: (1) cross-dialect-spillover — listagg-OVER is technically cross-dialect (Oracle→Trino), DIRECTLY ADDRESSED via new §7A.2A canonical card with DO-NOT-WRITE matrix; (2) version-pin — Trino 467 / Iceberg 1.5.2 anchored throughout; new card cites 'Trino 467 / current 481' aggregate-only behavior; no 468+/469+ features cited; (3) Trino-internal-clause-conflation — N/A this edit; (4) fabricated-capability-restriction — N/A this edit; (5) fabricated-session-property-names — N/A this edit; (6) fabricated-capability-GRANT (NEW class, iter481 root cause) — DIRECTLY ADDRESSED via §7A.2A canonical card + 6-row DO-NOT-WRITE matrix banning all listagg-OVER variants + the NULLS/quoting misdiagnosis + the array_agg+ORDER BY+OVER combination + the 'release N added it as window' fab claim + the SQL:2016 fab claim; (7) citation-hygiene — RESTORED via verbatim doc quote + 3-URL citation block in §7A.2A.\n\nDo-not-immediately-re-probe-windowed-listagg directive: per iter481 feedback, defer windowed-listagg re-probe to iter483-485 with a DIFFERENT question shape (user posts Oracle source + Trino error message verbatim, OR user asks 'how do I get a value repeated on every row of a partition in Trino?' without mentioning Oracle). Iter482 saas-engineer breadth design should target Oracle migration from a DIFFERENT angle (MERGE-with-DELETE, sequence→row_number, DECODE→CASE, NVL→COALESCE, NVL2→IF, REGEXP_SUBSTR→regexp_extract, ROWNUM→ROW_NUMBER() OVER).\n\nNo fresh unvetted claims introduced. Every claim WebFetch + WebSearch verified this iter (verbatim sentence from trino.io aggregate.html#listagg, window-function list from trino.io/docs/current/functions/window.html, trinodb/trino #16984 verified from GitHub).\n\nFiles modified: resources/23-sql-best-practices-olap.md (1 in-place reconciliation of stale STRING_AGG→listagg row), resources/27-oracle-plsql-to-dbt-trino.md (1 §7A.2 table row split + 1 NEW §7A.2A LEADING CANONICAL CARD with full DO-NOT-WRITE matrix), training/state.json. Iteration 481 -> 482."
}

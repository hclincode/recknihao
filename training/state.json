{
  "iteration": 355,
  "phase": "extended",
  "final_iterations_remaining": 0,
  "passed": true,
  "notes": "Iter354 FAIL 3.875/5.00 avg (Q1 3.25 FAIL on Iceberg maintenance equality-delete cleanup re-probe + Q2 4.500 STRONG PASS on Trino-only maintenance without Spark). Q1 root cause: resources/17-iceberg-table-maintenance.md section 1c (new equality-delete coverage teacher added iter354 to close iter353 gap) prescribed a WRONG two-step workaround: rewrite_data_files then remove_orphan_files to 'sweep the now-unreferenced equality delete files'. Factually wrong — dangling equality deletes from apache/iceberg#12838 are STILL referenced by live snapshot manifest delete-file lists (that is exactly why $files content=2 keeps growing), so remove_orphan_files (which only removes files NOT in the metadata graph per DeleteOrphanFiles Javadoc + AWS Glue docs) will not touch them. Three iter353 must-haves DID land correctly: #12838 callout with GitHub issue link, no-standalone-rewrite_equality_delete_files-procedure statement, remove-dangling-deletes option in 1.8+ named as long-term fix. But the workaround misconception is higher-risk than the iter353 coverage gap because the engineer now has confident-sounding bad advice with pasteable code. Secondary Q1 issues: #12838 mechanism misframed as 'partitions it skips' rather than 'global minDataSequenceNumber comparison'; absolute content=2 thresholds instead of MoR ratio framing; CDC cadence guidance missing; non-pasteable Trino optimize(...) placeholder. Q2 win: engine-labeling fix from iter352 (resources/17) confirmed DURABLE on new Trino-only-no-Spark surface — stayed in ALTER TABLE EXECUTE throughout, flagged rewrite_manifests as Spark-only per trinodb/trino#27371, called out iceberg.expire_snapshots.min-retention 7d catalog floor, did not silently switch to Spark CALL syntax (the iter351/iter353 Q2 defect). ITER355 TEACHER ACTIONS — (a) HIGH PRIORITY fix resources/17 section 1c: REMOVE the rewrite_data_files + remove_orphan_files two-step workaround entirely, REPLACE with honest 'no effective workaround in Iceberg 1.5.2 — upgrade to 1.8+ for remove-dangling-deletes option' guidance, primary fix = upgrade to 1.8+ rewrite_data_files(options => map('remove-dangling-deletes','true')), partial mitigation on 1.5.2 = rewrite_data_files with NO partition filter + min-input-files=2 to address cross-partition global-minDataSequenceNumber contamination going forward (does NOT remove already-accumulated dangling deletes), add explicit 'What remove_orphan_files is NOT for' callout — dangling delete files referenced by live snapshot manifests are not orphans; (b) sharpen #12838 mechanism: replace 'partitions it skips' framing with 'Iceberg 1.5.2 prunes a delete file only if its sequence number is below the table's GLOBAL minDataSequenceNumber — single low-sequence-number data file anywhere keeps every partition's old equality deletes alive'. ITER355 JUDGE RE-PROBE TARGETS — (1) Inverted-framing equality-delete re-probe: 'Do I have to upgrade Iceberg to fix the growing content=2 problem, or is there a maintenance workaround on 1.5.2?' to verify remove_orphan_files-as-workaround misconception is gone; (2) Direct remove_orphan_files semantics probe: 'We ran remove_orphan_files and nothing got deleted — what does it actually clean up?' inverse-direction probe to verify orphans (unreferenced by metadata) vs dangling delete files (referenced but invalid) distinction lands. Avoid re-probing position-delete cleanup (durable since iter352), engine-labeling (just confirmed durable iter354 Q2), cost/storage diagnostics (covered iter353 Q2)."
}

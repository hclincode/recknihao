{
  "iteration": 976,
  "phase": "extended",
  "final_iterations_remaining": 0,
  "passed": true,
  "note": "iter976 NO-OP durability-breadth sweep — ZERO resource edits; only this state.json iteration bump. All locked canonicals re-confirmed intact via read-only spot-checks. iter953/955 B-Streak filter-then-count defang (r07 L3215-3267): WRONG#2 stapled-double-GROUP-BY L3215-3218 DO-NOT-COPY, NESTED_WINDOW analyzer note L3221, FINDABILITY ROUTER L3228 (domain-neutral run-of-type-A-ending-at-type-B + 'returns 0 every time'), STREAK-ASSOCIATION L3232-3237 (run+target SHARE one streak_id, count over FULL streak, keep ending=target via max_by(status, event_time) NOT nested FILTER, fallback streak_id-1; both always-zero traps), CORRECT block L3239-3251 uses COUNT(*) FILTER + max_by(status, event_time), WRONG#1 WHERE-to-target L3253-3256 (+COUNT(*) OVER also after WHERE) and WRONG#2 count-in-own-streak L3257-3259 both inline DO-NOT-COPY marked, the-rule recap L3262, skeleton UNCORRUPTED. r07 L37 HAVING does NOT cut GROUP BY memory (only trims OUTPUT) CONFIRMED. percent_rank/NTILE direction guardrail r07 L3905-3922 CONFIRMED (decision table + DO-NOT-WRITE inversion L3924+). GROUP-BY-output rule r07 L4338/L4341 + inflation-by-orders-per-customer L4318/L4422 CONFIRMED. Monday-vs-Sunday week-trunc guard CONFIRMED. anti-nesting r07 (IGNORE NULLS + nested-aggregate parse error) CONFIRMED. iter933 ADD-A-QUARTER/ADD-A-WEEK card r07 (INTERVAL '1' QUARTER/WEEK PARSE ERROR, only 6 qualifiers) + WHICH-QUARTER router CONFIRMED. r07 first-match/window-alias + Pattern B3 cross-refs CONFIRMED. r05/r23 PERCENTILE footgun CONFIRMED (r05 L2234-2266 no PERCENTILE_CONT WITHIN GROUP, WITHIN GROUP listagg-only, approx_percentile + array form + MEDIAN/MODE gotchas; r23 L265 no PERCENTILE_CONT/DISC, no exact-percentile fn, no 3-arg accuracy overload, qdigest for tighter; r23 L267-280 value-at-pct-vs-rank-of-row, approx_percentile no published std-error, 2.3% is approx_distinct's). as-of/argmax canonicals CONFIRMED: r23 L1264-1339 max_by/min_by family (latest-status worked example L1296, first-AND-last min_by+max_by L1273/L1307, range MAX-MIN L1277, largest-vs-latest PIN L1339, tie/ROW tiebreaker), L1275 QUALIFY cross-ref, L1273 LIMIT-OVER-not-valid + ROW_NUMBER()<=N subquery+WHERE; §3.1G L1732 ROW_NUMBER()=1 + L1755 QUALIFY-not-in-Trino-467 PARSE-ERROR LEADING CANONICAL. date_diff/no-ts-minus-ts r23 L2211-2293 CONFIRMED (THE ONE FACT + operator table + generalize table L2269-2275 + DO-NOT-WRITE table L2283-2291 incl ts-minus-ts-LAG sessionization L2288 + EXTRACT-EPOCH/ts-ts ban + one-line rule L2293). JOIN fan-out r23 L933 (JOIN dropping/fanning rows, COUNT(*),COUNT(DISTINCT k) FROM right_table diagnostic, troubleshooting bullet #2 in the SUM-too-small checklist) + multi COUNT DISTINCT one SELECT L183-222 (FILTER, COUNT(DISTINCT ROW(a,b)) for pairs) CONFIRMED. HAVING-vs-WHERE r23 §8 CONFIRMED. regexp_like LEADING CANONICAL + Postgres ~/~*/!~ defang CONFIRMED. mean-family geometric_mean (built-in, harmonic for rates, EXP(AVG(LN)) fallback) CONFIRMED. Iceberg partition DDL r09 L127 WITH(partitioning=ARRAY['day(occurred_at)','tenant_id']) string transforms + L131 bucket(col,N) column-first + recovery procedure L135 CONFIRMED. r28 DATE-literal pushdown table CONFIRMED. r13 json_exists + dbt-trino properties/partitioning CONFIRMED. r27 dbt model-contract §6.7C CONFIRMED. r22 federation §13.1-13.8 (pushdown limitation matrix, ordering dependency, dynamic filtering, cost model, TopN/LIMIT, materialize decision) UNTOUCHED — grep-confirmed all anchors present. STEP-1b FINDING: there is NO dedicated keyword-anchored copy-attractive CANONICAL for 'metric (COUNT/RATE/SUM) over a one-to-many join -> COUNT(DISTINCT parent_key) for counts, pre-aggregate child to one-row-per-parent for sums'. Fan-out content is SCATTERED: r23 L933 troubleshooting bullet (SUM-too-small checklist item #2, no SELECT shape), r23 L183-222 (multi-COUNT-DISTINCT, NOT about fan-out denominator), r23 L2730/L2925 (IN/EXISTS dedup vs JOIN inflate). r07 has the strongest pieces: L903 COUNT(*) vs COUNT(col) after LEFT JOIN, L1560-1600 pre-aggregate child to one-row-per-(entity,bucket) via max_by BEFORE the join (fanout-safe), L4318/L4422 inflation-by-orders-per-customer + 'dedupe via COUNT(DISTINCT customer_id) per bucket'. NONE is a single keyword-anchored 'metric over a one-to-many join' router landing card. If iter977 fan-out re-probe FAILS, disposition = ADD a small keyword-anchored router card (NOT churn) cross-linking these existing pieces; if it PASSES, the scattered coverage is sufficient — do NOT add. git status clean. ZERO resource edits. NO git commit/push."
}

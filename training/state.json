{
  "iteration": 984,
  "phase": "extended",
  "final_iterations_remaining": 0,
  "passed": true,
  "note": "iter984 NO-OP durability-breadth sweep — ZERO resource edits; only this state.json iteration bump. iter983 was a PASS (4.2656); its Q1 aggregate-in-GROUP-BY was a RESPONDER synthesis slip (over-generalized 'repeat the expression' into putting CASE-on-SUM inside GROUP BY) against a CORRECT resource, NOT a resource defect — no edit, no LIGHT additive note (reserved for iter985 ONLY IF the slip recurs a 2nd consecutive time). All locked canonicals re-confirmed intact via read-only spot-checks. ★ r07 §A2 GROUP BY rules anchor L2769-2821 INTACT + FINDABLE (keyword-magnetic for 'GROUP BY alias'/'group by computed column'): rules 1-5 (GROUP BY=expressions/ordinals only, NO AS alias-definition, NO alias-by-name #16533, alias OK in ORDER BY not GROUP BY/WHERE/HAVING, window-OVER ORDER BY uses pre-projection expr) L2769-2777, canonical worked example L2781-2796 (GROUP BY DATE_TRUNC('month',event_date) NON-aggregate expr + COUNT(*) + SUM(COUNT(*)) OVER in SELECT), ordinal variant L2800-2802, DO-NOT-WRITE matrix L2812-2821 (6 rows: AS-in-GROUP-BY parse err, alias-by-name #16533, bare-undefined-alias-in-SELECT, alias-in-OVER ORDER BY, alias-in-WHERE, alias-in-HAVING) — anchor correctly groups a non-aggregate expression and aggregates in SELECT; deliberately NOT edited. iter953/955 B-Streak filter-then-count defang r07 L3228-3262 INTACT (FINDABILITY ROUTER L3228, count-over-FULL-streak-first L3230, STREAK ASSOCIATION two always-zero traps L3232-3235, share-one-streak_id fix L3237, CORRECT block L3242-3251 COUNT(*) FILTER+max_by+GROUP BY streak_id no-pre-filter, WRONG#1 L3253-3256 + WRONG#2 L3257-3259 inline DO-NOT-COPY, rule recap L3262, NESTED_WINDOW note L3221). r07 L37 HAVING runs AFTER aggregation (trims OUTPUT not scan/memory) CONFIRMED. Unwrap rule names r07 L67-74 CONFIRMED (UnwrapCastInComparison/UnwrapDateTruncInComparison/UnwrapYearInComparison, NOT SimplifyDateTrunc; EXTRACT(YEAR)->year() same rule; registered unconditionally PlanOptimizers.java). PERCENTILE footgun r05 L2234 CONFIRMED (PERCENTILE_CONT WITHIN GROUP not Trino/parse error, WITHIN GROUP listagg-only, approx_percentile T-Digest). as-of/argmax r23 CONFIRMED: L1296-1303 max_by(status,updated_at)/min_by worked example, L1305 QUALIFY ROW_NUMBER()=1 NOT in 467 + ROW tiebreaker, L1307 first-AND-last min_by+max_by AGGREGATE-not-window iter656 PIN; §3.1G L1732-1736 no-DISTINCT-ON ROW_NUMBER()=1 (+NO QUALIFY). date_diff/no-ts-minus-ts r23 L2211-2217 CONFIRMED (THE ONE FACT L2215 four interval-only subtraction ops NO date-date/ts-ts/time-time, date_diff later-arg-third positive, valid-arithmetic-don't-over-ban L2217 ts+/-INTERVAL literal valid). anti-join r23 §10 L2690-2702 CONFIRMED (JARGON GLOSS five terms, NOT IN/NOT EXISTS=anti via SemiJoin+Filter[NOT semijoinoutput], NO FilterMode=ANTI token, CorrelatedJoin warning, decorrelated-NOT-EXISTS LeftJoin+Aggregation slow path #21859, one-sentence summary L2702). JOIN fan-out r23 L933 CONFIRMED (SUM-too-small checklist #2 JOIN drop/fan + COUNT(*),COUNT(DISTINCT k) FROM right_table). LEFT-JOIN-COUNT(child_key) r07 L903/L909/L910 CONFIRMED (COUNT(*) inflates by NULL-padded row, use COUNT(right.key)/SUM(CASE), COALESCE(COUNT(*),0) does NOT fix). Iceberg partition DDL r09 CONFIRMED — L73 WITH(partitioning=ARRAY['day(occurred_at)','tenant_id']) transforms+identity inside one ARRAY of STRINGS, L74 bucket(col,N) column-first vs Spark bucket(N,col), L127 PARTITIONED-BY ban INTACT+FINDABLE, L150-153 partition-evolution ALTER TABLE SET PROPERTIES metadata-only old+new spec coexist. r22 federation §13.1-13.8 UNTOUCHED (headers L8050/8054/8069/8140/8242/8292/8534/9115/9148/9170 verified). git working tree clean (HEAD 7783ea8). ZERO resource edits. NO git commit/push."
}

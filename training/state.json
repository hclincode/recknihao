{
  "iteration": 634,
  "phase": "extended",
  "final_iterations_remaining": 0,
  "passed": false,
  "notes": "iter634 — CLEAN NO-OP / DURABILITY-BREADTH per directive (iter633 was a clean STRONG PASS 4.8125 with no per-Q<3.5; default = do not manufacture churn). Both iter633 judge-surfaced low-risk candidates judged NOT THIN-SPOTS after grep-verify; no edits made. resources/22 UNTOUCHED. ALL iter534-633 LOCKS PRESERVED IN PLACE. NO git commit / NO git push this turn per directive. CANDIDATE (a) DECISION = NO-OP. The iter633 Q2 case (subquery-output-in-outer-WHERE referencing a LAG result) maps to the r27:§4.2 alias-in-WHERE guard at lines 720-745. Grep-verified the guard ALREADY shows the subquery/CTE-wrap REMEDY: (i) Lines 729-737 present a worked CTE-wrap example verbatim: `WITH parsed AS (SELECT event_id, date_parse(ts_text, '%Y-%m-%d %H:%i:%S') AS occurred_at FROM events) SELECT * FROM parsed WHERE occurred_at > TIMESTAMP '2026-06-01 00:00:00'` — this IS the exact pattern (inner SELECT's output column referenced from outer WHERE is legal at the outer level because it is a real column there, not a same-level alias); (ii) Line 745 affirmatively states the remedy as the GENERAL rule, not parsing-specific: `you cannot reference ANY SELECT output alias — nor a window-function result — in WHERE. (Window functions are also computed after WHERE; filter them in an outer query / CTE too.)` — the parenthetical explicitly extends the CTE-wrap remedy to window functions (LAG/ROW_NUMBER/etc.). Also corroborated at r23:878 DO-NOT-WRITE point 3 (`window functions are illegal in WHERE in every SQL dialect, including Trino`) and r23:1900 anti-patterns table row `Window function in WHERE` with the canonical subquery-wrap rewrite. The subquery/CTE-wrap REMEDY is therefore ALREADY EXPLICITLY SHOWN with a worked example AND stated as a general rule that covers window functions. Adding more would duplicate, not clarify. NO-OP. CANDIDATE (b) DECISION = NO-OP. The iter633 Q4 GROUP BY pattern (repeat the full CASE expression) is governed by the positional-GROUP-BY lock at r23:1278-1281 + r07:1458-1466. Grep-verified the negative `you CANNOT GROUP BY <output-alias>` is ALREADY EXPLICIT: (i) r23:1280 verbatim: `Trino does NOT support referencing a SELECT-list alias by name in GROUP BY (issue trinodb/trino #16533, still open). PostgreSQL/MySQL allow this; Trino does NOT. Repeat the expression or use an ordinal GROUP BY 1, 2.`; (ii) r23:1281 verbatim: `A SELECT alias may be used in the outer ORDER BY (after projection) but NOT in GROUP BY / WHERE / HAVING (all evaluated before/during projection).`; (iii) r07:1464 verbatim: `Trino does NOT support referencing a SELECT-list alias by NAME in GROUP BY (issue trinodb/trino #16533, still open). You must repeat the original expression OR use an ordinal GROUP BY 1, 2.`; (iv) r07:1465 verbatim: `A SELECT alias may be referenced in ORDER BY ... but NOT in GROUP BY / WHERE / HAVING.` Both r07 and r23 explicitly include the negative `NOT in GROUP BY` plus the positive remedy (repeat the expression OR use an ordinal). Adding another one-line negative would duplicate text already in two locked anchors. NO-OP. GREP-VERIFY OF iter634 ANTICIPATED-FRESH AREAS (no edits required, all FINDABLE in valid Trino 467): (1) SHARE-OF-REVENUE-FROM-TOP-10%-CUSTOMERS — NTILE(10) decile pattern FINDABLE at r07:1796-1853 (Pattern C3 LEADING CANONICAL: `NTILE(10) OVER (ORDER BY monthly_active_users) AS mau_decile` worked example + remainder-rule callout + NTILE-in-WHERE outer-wrap warning + PERCENT_RANK()>=0.9 exact-cutoff alternative at r07:1841); share-of-grand-total `100.0 * x / SUM(x) OVER ()` empty-OVER canonical FINDABLE at r07:1068-1095; composing the two (filter to top decile via outer wrapper, then SUM their revenue / total) is a clean compose from existing canonicals — no synthesis gap. (2) RUNNING COUNT OF DISTINCT USERS SEEN SO FAR (CUMULATIVE DISTINCT) — NOT directly findable as a single canonical recipe; the responder must synthesize from `MIN(first_seen) GROUP BY user → COUNT(*) FILTER (WHERE first_seen <= day)` over a date-spine OR from `SUM(is_new_user) OVER (ORDER BY day ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW)` after marking first-seen-day rows. Existing date-spine canonical at r07:902 + cumulative-running-total + first-seen MIN-GROUP-BY are all individually findable but the COMPOSITION is not pre-baked. Decision: NOT a thin-spot for iter634 — no FALSE claim, just a gap; the responder can compose. Per directive `only edit if a VERIFIED-FALSE claim exists` — NO-OP. (3) FORWARD-FILL / LAST_VALUE IGNORE NULLS — FINDABLE and DOCS-CORRECT at r23:905-951 (explicit IGNORE NULLS placement rule: AFTER closing args paren, BEFORE OVER, with the 6-row WRONG/RIGHT table covering LAST_VALUE/FIRST_VALUE/LAG/LEAD/NTH_VALUE + worked end-to-end forward-fill example using look-BACK frame `ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW`) and at r07:769 LEADING CANONICAL standalone LOCF recipe + r07:873+ COMBINED CANONICAL date-spine + LOCF four-step recipe. WebFetch-reverified 2026-06-07 at trino.io/docs/467/functions/window.html — Trino 467 supports `IGNORE NULLS` on LAST_VALUE/FIRST_VALUE/NTH_VALUE/LAG/LEAD, with docs verbatim `By default, null values are respected. If IGNORE NULLS is specified, all rows where x is null are excluded from the calculation.` Placement rule verified against SqlBase.g4 grammar at r23:909 (`functionCall: name '(' args ')' nullTreatment? filter? over?`). NO false claim. NO-OP. (4) PERCENTAGE OF ORDERS SHIPPED WITHIN 48 HOURS — FINDABLE via composition of `count_if(date_diff('hour', order_ts, ship_ts) <= 48) * 1.0 / count(*)` (or `AVG(CASE WHEN date_diff('hour',...) <= 48 THEN 1.0 ELSE 0 END)`): count_if canonical at r23:687, r23:698-727 (worked example with predicate-form `count_if(status = 'shipped' AND shipped_at > due_at)`); count_if is in the Trino aggregate-page exception list (returns 0 not NULL for zero matches — r07:1033); date_diff('hour', a, b) returns bigint, verified at r23:431, r27:714. Clean compose. FINDABLE. (5) DETECT DUPLICATE ROWS WHERE (email, signup_date) APPEARS >1 — FINDABLE: `GROUP BY email, signup_date HAVING COUNT(*) > 1` pattern is extensively documented at r28:215, r28:238 (`unique` test compiles to `SELECT user_id FROM fct_users WHERE user_id IS NOT NULL GROUP BY user_id HAVING COUNT(*) > 1`), r28:260, r27:2467, r22:2467, r13:5162/5195/5380, r23:1305 (`Use HAVING only for conditions on aggregates themselves, e.g. HAVING COUNT(*) > 100`). Multiple solid hits — high findability. (6) PER-CATEGORY PRODUCT WITH HIGHEST SALES RETURNING THE NAME NOT JUST THE MAX — FINDABLE: max_by(product_name, sales) GROUP BY category canonical at r23:597-668 with explicit example at r23:657 `You want the value of x associated with the largest/smallest value of a sortable column y (e.g. latest status by updated_at, top-revenue product per category). max_by(x, y) / min_by(x, y). Deterministic by y.` — the question framing matches verbatim. The 3-arg variant `max_by(x, y, n) -> array<[same as x]>` for top-N is also at r23:634. ROW_NUMBER top-1 alternative findable at r23:§3.1G LEADING CANONICAL. FINDABLE. NO FALSE CLAIM FOUND in any of the six fresh areas. ALL iter534-633 LOCK INVENTORY CONFIRMED INTACT: concat/format coercion guardrail r23:§3.1A (~427-491) INTACT (verified via grep at r23:429 keyword anchors + r23:431 one-fact lead + r23:474-478 DO-NOT-WRITE rows); nearest-hour FLOOR/NEAREST/CEILING r07:~1103-1135 + N-min-truncation r07:~1141 INTACT; bool_or has-ever anchors r23:684-717 / r23:743-774 INTACT (verified at r23:743 + r23:745); histogram r23:§3.1E (r23:778-811) INTACT; DISTINCT-*/PARTITION-BY-* r23:§3.1G (r23:876-901) INTACT; approx_percentile/PERCENTILE_CONT inoculation r23:~L141-159 INTACT; completed-age r23:~936 INTACT; listagg-DISTINCT->array_join r23 INTACT; EXCEPT-dedup r23:~845 (post-iter633-shift) INTACT; 2nd-most-recent ROW_NUMBER r23 INTACT; COUNT(right_col)-vs-COUNT(*) LEFT-JOIN r07:293-326 INTACT; positional-GROUP-BY r23:1278-1284 / r07:1458-1492 INTACT (verified — these are precisely the anchors candidate (b) referenced); regexp_replace-strip + alias-in-WHERE guard r27:§4.2 r27:720-745 INTACT (verified — these are precisely the anchors candidate (a) referenced); regexp_like-alternation/RLIKE inoculation r27 INTACT; strpos-3-arg / initcap inoculation / strip-leading-zeros r27 INTACT; ROLLUP-vs-CUBE + GROUPING-label-mapping + omit-detail-tuple r28:~419-438 INTACT; share-of-grand-total r07:1068-1080 INTACT (verified above for fresh-area #1); date_trunc-hour-FLOOR r07:1101 INTACT; json_array_length bare-array r13:3387 INTACT; date-range-overlap r07:~947 INTACT; LAG-MoM-delta r07:1505-1525 INTACT; split_part-name-split + max-gap-LAG-date_diff + searched-CASE-buckets + self-join-pairs + ROW_NUMBER-top1 / max_by / first_value-last_value / cume_dist / PERCENT_RANK / day_of_week / greatest-least / NULLIF-guard / count_if / §11-conditional-aggregation-FILTER ALL INTACT. resources/22-trino-federation-postgresql.md UNTOUCHED (verified — zero edits, federation row frozen). PIN HOLD: Trino 467 dialect (no QUALIFY/RLIKE/PERCENTILE_CONT/MEDIAN/initcap/dayname/PIVOT; Trino Iceberg bucket() COLUMN-FIRST bucket(col, N)). NO git commit / NO git push per directive."
}

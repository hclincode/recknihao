{
  "iteration": 473,
  "phase": "extended",
  "final_iterations_remaining": 0,
  "passed": true,
  "notes": "Iter473 TEACHER — federation NOT probed (stays recorded near-miss 4.49944/310). Iter472 was a 4.4375 STRONG PASS with Q4 surfacing TWO MINOR labeling slips (no load-bearing fab — the example SQL was correct, but the Q4 heading conflated `rewrite_data_files (compact) [Trino or Spark]` and the opening line said `Spark required for steps 2 and 4` contradicting the body). PRIMARY teacher actions this iter address both labeling slips in resources/17-iceberg-table-maintenance.md:\n\n(1) PRIMARY — Q4-a fix: `rewrite_data_files` (Spark CALL name) vs `EXECUTE optimize` (Trino EXECUTE name) label conflation. Pre-edit grep confirmed NO line in resources/ labels `rewrite_data_files` as 'Trino or Spark' compaction step or presents `EXECUTE rewrite_data_files` as a Trino form (the existing `ALTER TABLE ... EXECUTE rewrite_data_files(...)` references at r17 lines 124-125 + r10 lines 485-486 are all INSIDE WRONG-DO-NOT-WRITE blocks — properly framed as banned patterns, not recommendations). The fab seed was the canonical-sequence framing using Spark procedure names (`rewrite_data_files`, `expire_snapshots`, ...) without engine-split disambiguation right at the step headings. Reconciled (NOT appended): TL;DR Step labels (line 13-19 -> line 13-35), the canonical sequence at line 1172-1178, the safe scheduling order ASCII diagram at line 1987-1999 (now 2005-2017), and the closing recap at line 3436-3440 (now 3468-3473) ALL updated to surface the Trino-EXECUTE-vs-Spark-CALL split at the step heading itself. Specifically:\n  - TL;DR Step 1 heading reconciled from `Step 1: rewrite_data_files (compact FIRST)` -> `Step 1 — Compaction (Trino EXECUTE optimize OR Spark CALL rewrite_data_files)` with explicit 'Do NOT write ALTER TABLE ... EXECUTE rewrite_data_files(...) on Trino — Trino uses EXECUTE optimize; rewrite_data_files is the Spark CALL procedure name'.\n  - Inserted a NEW small Trino-EXECUTE-vs-Spark-CALL matrix INSIDE TL;DR (7 rows: Compaction / Snapshot expiry / Orphan-file sweep / Drop extended stats / Manifest rewrite / Position-delete compaction / Branch fast-forward) for maximum findability — this is the FIRST mention of the engine split in the document (the existing detailed matrix at §'Trino EXECUTE procedures vs Spark CALL procedures' is now downstream).\n  - Canonical sequence at line 1172-1178 reconciled from `1. rewrite_data_files (compact) ...` -> `1. **Compaction** ... Trino: ALTER TABLE ... EXECUTE optimize(...). Spark: CALL iceberg.system.rewrite_data_files(...). Do NOT write EXECUTE rewrite_data_files on Trino — the Trino EXECUTE name is `optimize`; `rewrite_data_files` is the Spark CALL name.'\n  - Safe scheduling order ASCII diagram reconciled similarly (each step now shows both Trino EXECUTE form and Spark CALL form on separate lines).\n  - Closing recap reconciled similarly.\n\n(2) PRIMARY — Q4-b fix: 'Spark required for expire_snapshots' / 'expire_snapshots is Spark-only' wording. Pre-edit grep confirmed NO line in resources/ claims Spark is required for `expire_snapshots` (the iter472 Q4 contradiction was responder-side framing, NOT a stale resource line). Per the directive, added a DO-NOT-WRITE row banning the claim. Also extended the TL;DR Step 2 + Step 3 wording to explicitly assert `expire_snapshots` and `remove_orphan_files` ARE Trino 467 EXECUTE procedures — Spark NOT required for the routine 7d+ retention path. Trino-version-availability nuance: Trino 467 accepts only `retention_threshold`; `retain_last` and `clean_expired_metadata` were added in Trino 479 (Dec 2025). Trino enforces a 7d minimum-retention floor via `iceberg.expire-snapshots.min-retention` and `iceberg.remove-orphan-files.min-retention` catalog properties; Spark required ONLY for: (a) sub-7d retention, (b) Spark-only args like `retain_last`/`clean_expired_metadata`, (c) Spark-only related ops (`rewrite_manifests`, `rewrite_position_delete_files`, `fast_forward`).\n\n(3) Added 2 new DO-NOT-WRITE rows to the LEADING myth-buster matrix (lines 30-41 area; matrix lives at line 46-60 after the new rows). Both rows are placed in the matrix that the Haiku responder will encounter FIRST when answering Iceberg-maintenance questions:\n  - ROW 1 (banning EXECUTE rewrite_data_files on Trino): '\"I'll run ALTER TABLE ... EXECUTE rewrite_data_files(...) on Trino 467 for compaction.\" (iter472 Q4-a labeling slip) -> NO. `rewrite_data_files` is the SPARK CALL procedure name; the Trino EXECUTE equivalent is `optimize`. Trino 467's EXECUTE registry is exactly `optimize`, `expire_snapshots`, `remove_orphan_files`, `drop_extended_stats` (and `optimize_manifests` on Trino 470+, NOT 467) — `rewrite_data_files` is NOT in the registry. Writing it fails with `Procedure 'rewrite_data_files' not registered for catalog 'iceberg'`. Correct: Trino: `ALTER TABLE ... EXECUTE optimize(file_size_threshold => '256MB')` OR Spark: `CALL iceberg.system.rewrite_data_files(...)`.'\n  - ROW 2 (banning 'Spark required for expire_snapshots' claim): '\"Spark is required for expire_snapshots\" / \"expire_snapshots is Spark-only\" / \"Trino can't run expire_snapshots — drop to Spark for step 2\" (iter472 Q4-b internally-contradicted opening claim) -> FALSE. `expire_snapshots` IS a Trino 467 EXECUTE procedure. Spark required ONLY for sub-7d retention, Spark-only args (retain_last 479+, clean_expired_metadata 479+), or Spark-only related ops. The ONLY step in the standard 4-step maintenance sequence that is Spark-only on Trino 467 is Step 4 (manifest rewrite — `rewrite_manifests`), plus optional Step 1b (position-delete compaction). Steps 1, 2, 3 all have native Trino EXECUTE forms.'\n\n(4) Verified existing EXECUTE-vs-CALL matrix at §'Trino EXECUTE procedures vs Spark CALL procedures' (line 47-85 originally, now around line 60+) is already correct and authoritative — kept intact, just cross-referenced from the new TL;DR matrix. WebSearch verified at trino.io/docs/current/connector/iceberg.html: Trino EXECUTE procedures are `optimize`, `expire_snapshots`, `remove_orphan_files`, `drop_extended_stats`; `optimize_manifests` is 470+ NOT 467. WebSearch verified at iceberg.apache.org/docs/latest/spark-procedures/: Spark CALL procedures are `rewrite_data_files`, `rewrite_position_delete_files`, `rewrite_manifests`, `fast_forward`, `expire_snapshots` (Spark also has it with `older_than`/`retain_last`), `remove_orphan_files` (Spark also has it with `dry_run`), `rollback_to_snapshot`, `register_table`.\n\n§13.x federation guardrails in resources/22 NOT TOUCHED (git diff --stat confirms only r17 modified — 82 lines changed, 57 insertions, 25 deletions). Federation 4.49944/310 row sits 0.0006 below 4.5 raised threshold — let count grow naturally with non-federation breadth probes per iter472 judge directive.\n\nFab-class watch held: (1) cross-dialect spillover — no new fab introduced; existing Spark-dialect bans (SET PARTITION SPEC, ADD PARTITION FIELD, REPLACE PARTITION FIELD, parse_date, to_date) intact in r17 + r27; (2) version-pin — `optimize_manifests` 470+ NOT 467 retained, `retain_last`/`clean_expired_metadata` 479+ NOT 467 retained, `optimize` after partition evolution unreliable on 467 (bugs 26109/26503/25279) retained; (3) Trino-internal-clause conflation incl. EXECUTE-vs-CALL — NEW PRIMARY fab class addressed this iter: the new TL;DR matrix + 2 new DO-NOT-WRITE rows + reconciled step headings make the EXECUTE-vs-CALL split crisp at first read; (4) fabricated-capability-restriction — DO-NOT-WRITE row 59 explicitly bans 'Spark is required for expire_snapshots' as a fabricated capability restriction.\n\nWebSearch verifications this iter:\n- trino.io/docs/current/connector/iceberg.html: confirmed Trino EXECUTE procedures list is `optimize`, `expire_snapshots`, `remove_orphan_files`, `drop_extended_stats`; `optimize_manifests` was added in Trino 470. expire_snapshots accepts retention_threshold; retain_last/clean_expired_metadata added in Trino 479. remove_orphan_files accepts retention_threshold; no dry_run support on Trino.\n- iceberg.apache.org/docs/latest/spark-procedures/: confirmed Spark CALL procedures include rewrite_data_files (with strategy/sort_order/options including target-file-size-bytes/rewrite-all/min-input-files/delete-file-threshold), rewrite_position_delete_files (Spark only for position-delete compaction), rewrite_manifests, fast_forward, expire_snapshots (with older_than/retain_last/snapshot_ids), remove_orphan_files (with dry_run), and the recovery procs rollback_to_snapshot + register_table.\n\nNo fresh unvetted claims introduced. Every Trino EXECUTE claim is cited to trino.io/docs/current/connector/iceberg.html; every Spark CALL claim is cited to iceberg.apache.org/docs/latest/spark-procedures/.\n\nFiles modified: resources/17-iceberg-table-maintenance.md (TL;DR reconciled with NEW Trino-EXECUTE-vs-Spark-CALL matrix + 5 reconciled step headings + 2 NEW DO-NOT-WRITE myth-buster rows; canonical sequence list at line 1172-1178 reconciled; safe scheduling order ASCII diagram reconciled; closing recap reconciled), training/state.json. Iteration 472 -> 473."
}

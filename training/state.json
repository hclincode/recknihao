{
  "iteration": 454,
  "phase": "extended",
  "final_iterations_remaining": 0,
  "passed": true,
  "notes": "Iter454 TEACHER — PRIMARY FIX for citation-hygiene break at iter453 Q3 (Iceberg metadata-table FROM clause mismatch). Installed a LEADING CANONICAL snapshot-lookup + rollback + cleanup runbook block at the top of the Emergency-rollback section in resources/17-iceberg-table-maintenance.md (line ~2339). The block opens with the keywords engineers search for ('undo bad load', 'rollback Iceberg table', 'find snapshot id before bad load', 'list snapshots', '$snapshots metadata table'), shows the four-step paste-ready runbook (1) SELECT snapshot_id, committed_at, operation, summary FROM iceberg.analytics.\"events$snapshots\" ORDER BY committed_at DESC, (2) verify via SELECT count(*) FROM events FOR VERSION AS OF <id>, (3) CALL iceberg.system.rollback_to_snapshot('analytics','events',<id>) (Trino 467 positional CALL), (4) cleanup via ALTER TABLE events EXECUTE expire_snapshots(retention_threshold => '7d'). The leading block contains a DO-NOT-WRITE block banning six wrong patterns: (a) querying the BASE table for snapshot columns (the iter453 Q3 defect), (b) split quoting `events.\"$snapshots\"`, (c) Spark four-dot form `events.snapshots`, (d) Spark backticks `\\`events$snapshots\\``, (e) Trino 469+ ALTER TABLE EXECUTE form on Trino 467, (f) Spark named-arg form pasted into Trino CALL. Concludes with the meta-rule: 'the FROM clause MUST MATCH the explanatory comment — if the comment says $snapshots/$history/$files/$refs/$partitions/$manifests/$properties, the FROM must name the quoted \"table$<metatable>\", never the base table.' Includes a 7-row matrix mapping each metadata-table comment intent → correct quoted FROM clause → wrong base-table form.\n\nGREP-VERIFIED NO STALE BASE-TABLE SNAPSHOT-QUERY EXAMPLES IN RESOURCES — checked r17, r18, r22, r28. All existing snapshot-query examples already use the correct quoted form (`iceberg.analytics.\"events$snapshots\"`, `iceberg.analytics.\"feature_usage$snapshots\"`, `iceberg.analytics.\"usage_report$snapshots\"`, etc.). No before→after rewrites needed in source code — the defect was a responder copy-paste, not a teacher source bug. The new leading canonical block is a FINDABILITY + CANONICAL-FORM reinforcement aimed at preventing future responder copy-paste with the wrong FROM clause: the keyword-rich heading and DO-NOT-WRITE block sit immediately after `## Emergency rollback (the safest cleanup tool)` so a responder landing on 'rollback', 'undo bad load', 'find snapshot id' keyword paths lands on the correct form FIRST.\n\nSECONDARY FIX — Oracle DECODE NULL nuance added to resources/27-oracle-plsql-to-dbt-trino.md as new §4.1A ('Oracle DECODE → Trino CASE — the silent NULL-matching nuance'). Inserted immediately after the §4.1 translation table (DECODE row at line 183 now carries an inline pointer 'CRITICAL NULL-MATCHING NUANCE: see §4.1A below'). New §4.1A contents: (a) the trap statement quoting Oracle 19c docs 'DECODE considers two nulls to be equivalent' vs Trino simple CASE using `=` semantics where NULL = NULL is UNKNOWN; (b) source citations to docs.oracle.com/en/database/oracle/oracle-database/19/sqlrf/DECODE.html and trino.io/docs/current/functions/conditional.html; (c) WRONG simple-CASE-with-WHEN-NULL Trino translation example showing the silent fall-through to ELSE; (d) CORRECT searched-CASE Trino translation with explicit `WHEN status IS NULL THEN ...` first branch; (e) alternative COALESCE-wrap-with-sentinel form for code generators that must emit simple CASE; (f) DO-NOT-WRITE block banning (i) CASE col WHEN NULL — unreachable branch, (ii) reordering WHEN clauses hoping to catch NULL — irrelevant, every WHEN uses =, (iii) ELSE 'unknown' as a stand-in for NULL — catches ALL non-matching values, not just NULLs; (g) safe-default rule: if input is NULL-bearing, use searched CASE with IS NULL branch; if column is NOT NULL constrained, simple CASE is fine; (h) audit hint to grep Oracle source for `DECODE(<col>, NULL, ...)` literals — those calls SPECIFICALLY exploit Oracle's NULL=NULL and MUST be translated to searched CASE.\n\nWEBSEARCH/WEBFETCH VERIFICATION:\n- Trino Iceberg metadata-table quoting on trino.io/docs/current/connector/iceberg.html (Metadata tables section): VERIFIED. Exact form `SELECT * FROM \"test_table$snapshots\";` with the WHOLE table$metadata token inside one pair of double quotes. Base table cannot be queried for snapshot_id column (those columns exist ONLY on the $snapshots metadata table).\n- Oracle DECODE NULL-matching semantics on docs.oracle.com/en/database/oracle/oracle-database/19/sqlrf/DECODE.html: VERIFIED. 'DECODE considers two nulls to be equivalent. If expr is null, then Oracle returns the result of the first search that is also null.'\n- Trino simple-vs-searched CASE semantics on trino.io/docs/current/functions/conditional.html: VERIFIED. Simple form 'searches each value expression from left to right until it finds one that equals expression' — standard SQL equality, so NULL = NULL is UNKNOWN, hence WHEN NULL never matches in simple CASE.\n- CALL iceberg.system.rollback_to_snapshot('schema','table',<id>) Trino 467 positional form: VERIFIED via prior iter453 WebSearch (Starburst blog + trinodb/trino #12353 + PR #9921). Still the only Trino 467 form; PR #24580 (Trino 469) deprecates but does not remove.\n- ALTER TABLE EXECUTE expire_snapshots(retention_threshold => '7d') Trino 467: VERIFIED — `retention_threshold` is the only arg accepted on Trino 467; `retain_last` and `clean_expired_metadata` were added in Trino 479.\n\nNO FRESH UNVETTED CLAIMS — every new claim is either (i) a doc-verified syntax form (positional CALL rollback_to_snapshot, quoted $snapshots, searched CASE for NULL match, EXECUTE expire_snapshots retention_threshold), (ii) directly quoted from Trino / Oracle docs with hyperlink, or (iii) cross-referenced to an existing canonical guardrail in this repo (r17 metadata-table quoting section, r17 emergency-rollback section, r27 §4.1 translation table).\n\n§13.X FEDERATION GUARDRAILS UNTOUCHED — `git diff --stat resources/22-trino-federation-postgresql.md` confirmed EMPTY; `grep -cE '^## 13\\.|^### 13\\.' resources/22-trino-federation-postgresql.md` returns 10 markers intact.\n\nFEDERATION NOT PROBED — topic remains at recorded near-miss 4.49944/310 per iter453 directive.\n\nFILES MODIFIED: resources/17-iceberg-table-maintenance.md, resources/27-oracle-plsql-to-dbt-trino.md. Iteration bumped 453 → 454."
}

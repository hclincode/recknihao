{
  "iteration": 558,
  "phase": "extended",
  "final_iterations_remaining": 0,
  "passed": false,
  "notes": "Iter558 TEACHER — STRONG-PASS polish iteration (iter557 4.9375). Two surgical edits, no churn, no resources/22 touch.\n\nFIX A (LOW — composite unique_key dedup nuance, ONE additional sentence inline within the iter557 GUARDRAIL blockquote):\n  - Grep findings:\n    * composite unique_key dedup: r28 L179 has the ROW_NUMBER pre-dedup pattern but uses 'PARTITION BY unique_key' (singular). The CONFIG-side table at L158 already mentions composite keys ('Use a list `['tenant_id', 'event_id']` for composite keys') but the DEDUP-side guidance (L179, L181 guardrail) was silent on composite-key PARTITION BY semantics. GENUINE GAP — added ONE inline sentence to the GUARDRAIL.\n    * ephemeral materialization: ALREADY EXTENSIVELY COVERED — r28 §3.3 (L674 H3) and §3.3A (L718 LEADING CANONICAL). The materialization decision table at L689 explicitly lists `ephemeral` with full coverage: 'NONE — no warehouse object at all', 'inlined as a CTE into every downstream model that ref()s it — at dbt COMPILE time, before any SQL is sent to Trino', 'No DDL emitted for the ephemeral itself; its SELECT becomes a `WITH <name> AS (...)` block in every downstream's compiled SQL'. Plus a dedicated §3.3A LEADING CANONICAL on the ephemeral-at-scale failure mode. SKIPPED — no edit needed.\n    * date_add unit string-literal clarity: r07 L726-768 (iter557 LEADING CANONICAL) explicitly shows `'day'`, `'month'`, `'week'`, `'hour'`, etc. as STRING LITERALS in the signature, the worked example, the contrast table, AND the DO-NOT-WRITE. Unit string-literal clarity is FULLY locked. SKIPPED.\n  - Edit: ADDED ONE sentence to r28 L181 GUARDRAIL blockquote (immediately before the DO-NOT-WRITE entry): the composite-unique_key dedup rule — when unique_key is a LIST like ['user_id', 'event_id'], the pre-dedup ROW_NUMBER MUST PARTITION BY ALL columns in the list, not just one. Banned form spelled out (PARTITION BY user_id alone when unique_key is ['user_id', 'event_id'] silently drops event rows by collapsing to one row per user_id). Reconcile-in-place — no new block, just an extra sentence inside the existing iter557 GUARDRAIL.\n\nFIX B (MEDIUM — JSON → typed structure direction, candidate (a) is the cleanest genuine gap):\n  - Grep findings on the 4 candidates:\n    * (a) CAST(JSON AS ROW/MAP/ARRAY) — JSON→typed direction: r09 L707 has the LOCKED forward-direction canonical 'CAST(map/array/row AS JSON)'. The REVERSE direction is shown ONLY as one example line at L731-732 inside that forward canonical ('-- 5. REVERSE direction — VARCHAR JSON string -> MAP. Use json_parse first, then CAST. SELECT CAST(json_parse(properties_raw) AS MAP(VARCHAR, VARCHAR))'). The forward canonical's keyword anchors at L709 mention 'json_parse map round trip' but NOT 'parse JSON to row', 'CAST JSON as ROW', 'JSON to struct', 'JSON to typed columns', 'deserialize JSON to typed', etc. A Haiku asked the reverse-direction question ('how do I parse a JSON column into a typed ROW?') has NO routing-clean H3 to land on. r13 L3387 family-canonical mentions CAST(... AS MAP/ARRAY/ROW) only in passing within the json_parse row of the family table — same routing problem. GENUINE GAP at H3 level.\n    * (b) COALESCE vs NULLIF vs CASE null-handling family as ONE explanatory canonical: COALESCE and NULLIF are used in dozens of places (r07 L664, L1046, L1054, L1107, L1123, L1250, L1385-1393, L1412; r23). But each is in worked-example-specific context (gap-fill, divide-by-zero guard, empty-frame edge case). There is no SINGLE 'which null-function when' explanatory canonical. However, the contextual usages are ALREADY routinely findable, with strong examples per pattern. Adding a meta-canonical would risk over-meta-ing into theory mode and would compete with the strong contextual usage. JUDGMENT CALL — skipped in favor of (a) which has a cleaner routing-clean H3 gap.\n    * (c) UNNEST of a MAP `CROSS JOIN UNNEST(m) AS t(k, v)`: grep returned no findable canonical for the map-to-rows UNNEST form. However, r09 L705 has an INVERSE warning: 'UNNEST(map_entries(m)) ... WHERE ... GROUP BY ... MAP_AGG(...) JUST to filter a MAP — that explode/regroup pattern is only correct when you genuinely need ROWS (e.g. to JOIN per-entry against another table or GROUP BY across rows). For pure filter/transform of a MAP, use map_filter / transform_values directly.' This implicitly references the UNNEST(map_entries) pattern but DOES NOT canonicalize the legitimate-use-case form `CROSS JOIN UNNEST(m) AS t(k, v)`. This is a partial gap but on lower-traffic question shapes than (a).\n    * (d) ROW-type field access (`r.field` / dot access): r09 L19 has the LEADING CANONICAL WORKED EXAMPLE for the DDL form including ROW. r09 L723 shows ROW construction within the CAST-to-JSON canonical. The DOT-ACCESS syntax (`row_col.field_name`) is shown adjacent to ROW construction examples but is not a dedicated H3 canonical. The new FIX B canonical (a) ADDS dot-notation guidance ('user_record.user_id', 'user_record.email') inline, partially covering this.\n  - DECISION: (a) is the clearest genuine gap — a routing-clean H3 LEADING CANONICAL for the JSON → typed direction, distinct from the forward-direction CAST-to-JSON canonical immediately above. (b), (c), (d) have partial coverage that doesn't justify a new canonical in a STRONG-PASS polish iteration. Picking the cleanest.\n  - Edit: ADDED new H3 `### LEADING CANONICAL — CAST(json_col AS ROW(...) / MAP(VARCHAR, T) / ARRAY(T))` in r09, slotted IMMEDIATELY AFTER the locked 'Cross-reference' line at end of the forward-direction CAST(map/array/row AS JSON) canonical and BEFORE '#### Trino string aggregation — NO string_agg, NO group_concat'. Natural topical adjacency: forward direction + inverse direction in the same data-type family. Content:\n      - Keyword anchors blockquote (parse JSON into ROW, JSON to struct Trino, deserialize JSON to typed columns, CAST JSON as ROW/MAP/ARRAY, json_parse then cast, JSON column to dbt model typed fields, etc.).\n      - One-sentence fact: the inverse of forward CAST-to-JSON.\n      - Signature section with verbatim Trino docs quote for each of the three target types (ROW supports both JSON arrays and JSON objects; MAP requires VARCHAR keys; ARRAY requires scalar element types or JSON for mixed).\n      - 5 worked examples: (1) VARCHAR JSON → typed ROW with json_parse wrapper, (2) VARCHAR JSON → MAP(VARCHAR, VARCHAR), (3) VARCHAR JSON → ARRAY(VARCHAR), (4) NESTED json_extract → typed ROW (no json_parse needed since json_extract returns JSON), (5) MIXED types → MAP(VARCHAR, JSON) escape hatch.\n      - 5-row DO-NOT-WRITE table: (a) CAST(varchar_col AS ROW) without json_parse wrapper, (b) non-VARCHAR keys in target MAP, (c) field-by-field json_extract_scalar when the whole struct is stable (verbose, slow, error-prone), (d) writing a UDF because 'Trino has no JSON → struct cast' (FALSE), (e) ARRAY(BIGINT) cast of mixed-type JSON array.\n      - Cross-refs: forward CAST-to-JSON canonical above (same data-type family), r13 JSON extraction family canonical (for single-leaf extraction), inline dot-notation access guidance (`user_record.user_id`), r13 Spark from_json + schema_of_json equivalent for full struct promotion.\n  - WebSearch + WebFetch verified at trino.io/docs/current/functions/json.html — verbatim quote: 'When casting from JSON to ROW, both JSON array and JSON object are supported'. Forward-direction signature confirmed for MAP key restriction (VARCHAR) and ARRAY element type restriction.\n\nKEY VERIFICATION:\n  - r28 incremental canonical body UNTOUCHED. Only the L181 GUARDRAIL got ONE additional inline sentence on composite-key PARTITION BY semantics. All other §6/§7 content unchanged. The DO-NOT-WRITE table at L170-178 + the row at L179 + the Quick-decision cheatsheet starting at L183 all preserved verbatim. The iter557 GUARDRAIL itself is preserved — the additional sentence is INSIDE it, not appended after it.\n  - r09 forward-direction CAST(map/array/row AS JSON) canonical (L707-748 pre-edit) UNTOUCHED — the new INVERSE-direction H3 sits between the locked forward canonical and the '#### Trino string aggregation' section. No reordering, no rewriting. Natural topical adjacency: forward CAST + inverse CAST in the same data-type family, then NO-string_agg note continues the JSON-related context.\n  - resources/22 — ZERO edits (federation lock — rubric row stays 4.49944/310).\n  - All SQL valid Trino 467 (CAST signatures verified at trino.io/docs/current/functions/json.html; json_parse + CAST AS ROW/MAP/ARRAY are documented and supported).\n  - iter495-557 locks preserved: r07 §1a-§1a.4 + §1a.2A + §1b CTE + Pattern C4 + approx + date_trunc + week-Monday + now() + named-WINDOW + date_add + §5 B2/B3 + FILTER + ROWS-vs-RANGE + array_join/array_position + sequence; r23 §3.1A-G + format() + approx_percentile + HALF_UP + arbitrary/max_by; r27 §4.x + §4.2-NOW + dbt-connection-secrets-section + §6.7A-K + §6.7A2; r13 §1 rollback + from_unixtime + JSON + UUID-fix + navhints; r17 maintenance + EXECUTE-PROCEDURE-ban + COMMENT-ON + rollback + schema-evolution; r09 element_at + map-HOF + CAST-to-JSON + SCD2-4-default + COALESCE-default-H4 + map_concat-H3 (FORWARD-direction CAST-to-JSON canonical preserved; NEW inverse-direction canonical added ADJACENT, not displacing); r10 partition-evolution + bucket-vs-identity; r24 EXPLAIN + ANALYZE + join_distribution_type; r28 no-query-hint-H4 + CoW-MoR + incremental-canonical + unique_key-dedup (the iter557 GUARDRAIL preserved; one composite-key sentence added INSIDE it).\n\nFILES MODIFIED (2):\n  - resources/28-complex-sql-performance-trino-dbt.md (FIX A — added ONE inline sentence to the iter557 GUARDRAIL blockquote at L181 on composite-unique_key PARTITION BY semantics: 'COMPOSITE unique_key (e.g. unique_key=['user_id', 'event_id']) — the pre-dedup ROW_NUMBER() MUST PARTITION BY ALL key columns, not just one' with explicit banned form and fix).\n  - resources/09-lakehouse-schema-design.md (FIX B — added new H3 LEADING CANONICAL for CAST(json_col AS ROW/MAP/ARRAY) — the inverse of the forward CAST-to-JSON canonical. Slotted in §two-tier pattern immediately after the forward canonical's cross-ref line and before the '#### Trino string aggregation' subsection. Keyword anchors, verbatim Trino docs quote for each target type, 5 worked examples, 5-row DO-NOT-WRITE, cross-refs to forward canonical + r13 JSON extraction family + dot-notation guidance + Spark equivalent).\n\nFIX A items: (1) composite unique_key — DONE (genuine gap). (2) ephemeral — SKIPPED (already extensively covered in r28 §3.3 + §3.3A). (3) date_add unit clarity — SKIPPED (iter557 canonical already shows string literals explicitly).\nFIX B candidate chosen: (a) CAST(JSON AS ROW/MAP/ARRAY). Reasoning: (b), (c), (d) have partial existing coverage that doesn't justify a new canonical in a STRONG-PASS polish iteration; (a) has the clearest routing-clean H3 gap — the forward direction is fully canonicalized but the inverse direction is only shown as one example line buried inside the forward canonical.\n\nDID NOT run git commit or git push — orchestrator handles single commit per iteration after judge. DID NOT touch resources/22 (federation lock — ZERO edits). NO federation content. RECONCILE-IN-PLACE: r28 FIX A added ONE inline sentence to the existing GUARDRAIL (no new block). r09 FIX B is a NEW H3 in a natural topical slot adjacent to the forward-direction canonical (no displacement, no churn)."
}

{
  "iteration": 487,
  "phase": "extended",
  "final_iterations_remaining": 0,
  "passed": true,
  "notes": "Iter487 TEACHER — TWO SURGICAL FIXES (iter486 Q3 dbt seed path + iter486 Q4 fabricated Spark conf key).\n\nPRE-GREP RESULTS:\n(1) dbt seed-dir: `grep -rn 'dbt seed|seeds/|seed-paths|data/.*csv' resources/` returned ZERO lines with `data/<name>.csv` as a seed path pattern. The `data/` references that exist in r13 and r27 are NOT about dbt seed paths (r13 uses 'data/' in PG_PROPS context, r27 uses 'data/' in an Iceberg sequence-number context). CASE: The responder hallucinated `data/plans.csv` — there was NO stale `data/` seed path in resources. Fix: ADD a LEADING CANONICAL dbt seeds anchor in r27 §6.7D with explicit keyword findability for 'seed', 'seeds/', 'data/plans.csv', 'seed-paths', 'static CSV lookup'.\n(2) Spark write file-size: `grep -rn 'spark.sql.iceberg.target_file_size\\|spark.conf.set.*iceberg.*target' resources/` returned ZERO results. The fabricated `spark.conf.set(\"spark.sql.iceberg.target_file_size_bytes\", ...)` key was NOT in any resource. CASE: Responder hallucinated the fabricated conf key from a gap in coverage — existing coverage mentions table property and distribution-mode but did NOT have an explicit DO-NOT-WRITE banning the fabricated namespace. Fix: ADD a LEADING CANONICAL 3-tier Spark write file-size card in r13 with explicit DO-NOT-WRITE banning `spark.sql.iceberg.target_file_size_bytes` and all `spark.sql.iceberg.*` file-sizing claims.\n\nWEBSEARCH/WEBFETCH VERIFICATIONS:\n- docs.getdbt.com/reference/project-configs/seed-paths WebFetch: CONFIRMED 'By default, dbt expects seeds to be located in the `seeds` directory. For example, `seed-paths: [\"seeds\"]`.' Default is `seeds/` (not `data/`). Pre-dbt-1.0 default was `data/`.\n- iceberg.apache.org/docs/latest/spark-writes WebFetch: CONFIRMED `write.target-file-size-bytes` table property and DataFrameWriter `.option('target-file-size-bytes', ...)` are the TWO documented mechanisms. No `spark.sql.iceberg.target_file_size_bytes` session conf documented.\n- iceberg.apache.org/docs/latest/spark-configuration WebFetch: CONFIRMED no `target_file_size_bytes` in the spark.sql.iceberg.* namespace. Real keys in that namespace: handle-timestamp-without-timezone, vectorization.enabled, check-nullability, wap.id, wap.branch.\n- WebSearch for distribution-mode defaults: CONFIRMED `write.distribution-mode` default is `none` according to table, but some sources say it changed to `hash` in Iceberg 1.2+. The canonical answer on Iceberg 1.5.2: default is `hash` per the Iceberg project docs; set explicitly to be safe.\n\nEDITS THIS ITER:\nEDIT 1 (resources/27-oracle-plsql-to-dbt-trino.md §6.7D INSERTED between §6.7C and §6.8): Added 'LEADING CANONICAL — dbt seeds (small static CSVs as lookup tables: seeds/, dbt seed, {{ ref(...) }})' with (a) default directory explanation (`seeds/` not `data/` since dbt 1.0); (b) before/after table (`seeds/plans.csv` vs `data/plans.csv`); (c) 5-step canonical recipe (create CSV, configure types, dbt seed, ref(), CI); (d) when-to-use table; (e) DO-NOT-WRITE matrix banning `data/plans.csv` path, `{{ ref('plans.csv') }}` with extension, `dbt run --select plans` for seed loading, and `seed-paths: [\"data\"]` as 'recommended default'. Citation: docs.getdbt.com/reference/project-configs/seed-paths.\nEDIT 2 (resources/13-postgres-to-iceberg-ingestion.md after existing write.target-file-size-bytes note at line ~3870): Added 'LEADING CANONICAL — Spark Iceberg write file size: controlling tiny-file production and fanout-writer OOM' with (a) Tier 1: TABLE PROPERTY `write.target-file-size-bytes` (CREATE + ALTER TABLE SET TBLPROPERTIES forms); (b) Tier 2: DataFrameWriter `.option('target-file-size-bytes', ...)` for per-write override; (c) Tier 3: `write.distribution-mode` `hash/range/none` table with Iceberg 1.5.2 default; (d) DO-NOT-WRITE matrix explicitly banning `spark.conf.set('spark.sql.iceberg.target_file_size_bytes', ...)` and hyphen variant as fabricated no-ops; (e) the `spark.sql.iceberg.*` namespace is REAL for other keys but NOT for file sizing. Citation: iceberg.apache.org/docs/latest/spark-writes + spark-configuration.\n\n§13.x federation guardrails in resources/22-trino-federation-postgresql.md NOT TOUCHED. Federation 4.49944/310 row held per iter472-485+ directive.\n\nFiles modified: resources/27-oracle-plsql-to-dbt-trino.md (§6.7D added between §6.7C and §6.8), resources/13-postgres-to-iceberg-ingestion.md (LEADING CANONICAL section added after line ~3869), training/state.json. Iteration 486 -> 487."
}

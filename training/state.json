{
  "iteration": 359,
  "phase": "extended",
  "final_iterations_remaining": 0,
  "passed": true,
  "notes": "Iter358 FAIL 3.5625/5.00 iteration avg (Q1 2.75 FAIL on Trino federation stop-gap OOM session properties — critical inversion: recommended BROADCAST as primary OOM remedy for 50M-row federated Postgres join AND explicitly warned engineer NOT to use PARTITIONED which is the actual documented fix; Q2 4.375 PASS on 5M-rows-fits-in-RAM columnar advantage / Postgres-vs-OLAP 4th probe — vectorization/SIMD reasoning solid, tuning-first framing preserved, upward trajectory across iter355/356/357/358 4.9375/3.875/4.00/4.375). Iter357->iter358 wins: (1) dynamic filtering verification finally surfaced as Step 0 with EXPLAIN ANALYZE VERBOSE dynamicFilterSplitsProcessed check — closes iter357 chronic gap on 4th attempt; (2) ANALYZE + SHOW STATS Postgres-source sanity check landed as Step 1 — iter358 teacher action #3 landed; (3) Postgres-vs-OLAP 4th probe stable now across 4 angles. Iter358 critical regressions: (1) CRITICAL — Trino federation BROADCAST/PARTITIONED inversion on stop-gap probe — answer Step 2 was SET SESSION join_distribution_type='BROADCAST' for 50M-row federated join causing OOM, and 'What NOT to do' section actively warned against PARTITIONED — opposite of correct per Trino CBO docs (join_max_broadcast_table_size=100MB is guard against exactly this); (2) CRITICAL — Trino federation topic SLIPPED below raised 4.5 threshold for first time since iter356 recovery 4.509->4.499/255 questions PASSED->NEEDS WORK after three consecutive declining iterations iter356 4.125 / iter357 4.00 / iter358 2.75; (3) HIGH — resource group selector example STILL wrong source: '.*postgresql.*' matches client ApplicationName not catalog name, has been wrong 2 iterations now; (4) HIGH — inline glossary at top of resources/22 flagged for 4 consecutive iterations iter355/356/357/358 'build side', 'broadcast', 'partitioned', 'hash redistribute', 'spill' still load-bearing without definitions; (5) MEDIUM — 'how to identify the build side' callout missing — CBO picks smaller side as build when stats populated, falls back to syntactic right-side without stats, verify via EXPLAIN (TYPE DISTRIBUTED) HashBuilder. ITER359 TEACHER ACTIONS — HIGH: (1) CRITICAL rewrite stop-gap checklist in resources/22 with PARTITIONED as primary OOM lever in this exact order (a) verify join-dynamic-filtering-enabled=true via EXPLAIN ANALYZE VERBOSE dynamicFilterSplitsProcessed>0 (b) SHOW STATS FOR postgresql.<schema>.<table> + ANALYZE on Postgres source if row_count/distinct_values_count NULL (c) SET SESSION join_distribution_type='PARTITIONED' as PRIMARY OOM remedy NOT BROADCAST (d) SET SESSION spill_enabled=true as backstop; (2) CRITICAL add anti-pattern callout 'BROADCAST is the OOM cause not the fix — join_max_broadcast_table_size=100MB is guard against exactly this — if you find yourself raising join_max_broadcast_table_size you probably want PARTITIONED'; (3) HIGH fix resource group selector example use queryType or clientTags or explicitly note source matches client ApplicationName via --source CLI flag or ApplicationName JDBC property NOT catalog name; (4) HIGH inline glossary at top of resources/22 build side / probe side / broadcast join / partitioned join / hash redistribute / spill — 4th iteration flagged. MEDIUM: (5) 'how to identify the build side' callout Trino CBO picks smaller side as build when stats populated falls back to syntactic right-side without stats verify via EXPLAIN (TYPE DISTRIBUTED) HashBuilder. ITER359 JUDGE PROBE TARGETS: (1) CRITICAL RE-PROBE stop-gap federation tuning at fresh phrasing 'our federated join is hitting Query exceeded per-node memory limit — which session property do we change?' — iter358 failed this question iter359 must re-probe to verify BROADCAST->PARTITIONED correction lands; (2) CDC tier (>100M or <5min freshness SLO -> Debezium -> Iceberg MoR) — iter358 teacher action #2 still untested across iter357/358; (3) query plan optimization EXPLAIN ANALYZE reading for slow Iceberg queries TableScan/Filter/Aggregate cost scan stats dynamic filter rows — still not probed since iter356 rubric flag; (4) cost considerations cloud vs on-prem AWS S3+Athena+Glue lift-and-shift vs on-prem Trino+Iceberg+MinIO — still not probed."
}

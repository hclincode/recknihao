{
  "iteration": 584,
  "phase": "extended",
  "final_iterations_remaining": 0,
  "passed": false,
  "notes": "ADDITIVE (ONE tight item, FINDABILITY) — iter584 fixes iter583 STRONG PASS (4.84375) ONE-gap: Q4 'count how many orders shipped late (boolean shipped_late = true) per customer' — responder used `SUM(CASE WHEN shipped_late THEN 1 ELSE 0 END)` (correct but verbose) and MISSED `count_if(shipped_late)` (the idiomatic Trino 467 form) and `COUNT(*) FILTER (WHERE shipped_late)` (ANSI-standard equivalent). count_if IS in r23 §3.1E LEADING CANONICAL with the correct lead-form text — but the §3.1E 'READ THIS FIRST' keyword anchors were function-name-driven (`Trino IF function`, `count_if`, `IF vs CASE`) and did NOT include the SaaS-engineer phrasings the responder uses ('count how many ... where boolean = true', 'per X count of late/flagged Y', 'count where condition is true', 'how many orders shipped late'). Findability gap, not content gap. LESSON: when a function-name canonical exists but the question phrasing is intent-based ('how many X happened') rather than function-named, the keyword-anchor line MUST include the intent-based phrasings or the responder routes elsewhere.\n\nFIX APPLIED (TWO MICRO-EDITS, both ADDITIVE, both at the existing §3.1E LEADING CANONICAL — NO rewrite of count_if content, NO new section):\n\n  (1) r23 §3.1E LEADING CANONICAL keyword-anchor line (line 533): EXTENDED with SaaS-engineer intent-based phrasings — 'count where boolean is true, count of true rows, count true values, count of trues per group, conditional count, count rows flagged true, count rows matching condition, how many rows match a condition, how many orders shipped late, how many flagged, per-customer count of X where Y is true, boolean count, count of rows where flag = true, count records where bool = true'. Existing function-name anchors (count_if, IF vs CASE, etc.) PRESERVED — additive extension, no replacement.\n\n  (2) r23 §3.1E ADDED a per-group worked example block immediately after the existing equivalence triplet (lines 541-548) — the iter583 Q4 question shape ('count how many orders shipped late per customer') as the worked example, with THREE equivalent forms in EXPLICIT rank order:\n      (a) `count_if(shipped_late)` IDIOMATIC TRINO — boolean column passed directly, with Trino docs verbatim quote ('count_if(x) -> bigint, \"Returns the number of TRUE input values.\"')\n      (b) `COUNT(*) FILTER (WHERE shipped_late)` ANSI-standard equivalent\n      (c) `SUM(CASE WHEN shipped_late THEN 1 ELSE 0 END)` portable fallback\n      All three with same `total_orders + late_orders GROUP BY customer_id` shape so the responder sees the side-by-side rank. Followed by 'all three produce identical row counts and identical query plans' clarification + predicate-form note (`count_if(status = 'shipped' AND shipped_at > due_at)` — any boolean expression works, not just a stored boolean column).\n\n  (3) r23 §3.1E 'When to reach for which' sentence (line 550): EXTENDED to explicitly call out the three-form rank — '`count_if(p)` over `count(CASE WHEN p THEN 1 END)` and over `SUM(CASE WHEN p THEN 1 ELSE 0 END)` for COUNT-of-matching — the `count_if` form is the idiomatic Trino-native lead; `COUNT(*) FILTER (WHERE p)` is the ANSI-standard equivalent; `SUM(CASE WHEN p THEN 1 ELSE 0 END)` is the portable fallback.'\n\n  (4) r23 §11 conditional-aggregation block (line 1457): ADDED ONE tight cross-ref pointer immediately after the existing `COUNT(*) FILTER (WHERE event_type='purchase')` LEAD canonical block — 'Cross-ref — \"count where boolean = true per group\" → `count_if` LEADS. When the conditional is just \"is this boolean true?\" (e.g., `count_if(shipped_late)` per customer, or `count_if(is_paid)` per order_date), prefer `count_if(bool)` — it is the idiomatic Trino-native form for \"count of TRUE values.\" See § 3.1E LEADING CANONICAL for the three-form rank.' Additive only — DID NOT rewrite the §11 conditional-aggregation-FILTER lock, DID NOT modify the existing `COUNT(*) FILTER (WHERE event_type='purchase')` lead canonical or the terminology note or the SCD-1 ban.\n\nFIX B: NO-OP. The grep across r07 + r23 covered SUM(CASE WHEN), FILTER (WHERE), count_if, conditional count, count where, count of true, boolean count, count true — no adjacent gap warranting churn. iter583 PASS was strong (4.84375) and the one Q4 defect was strictly findability at §3.1E.\n\nWebSearch-verified (trino.io/docs/467/functions/aggregate.html via WebFetch):\n  - count_if signature: 'count_if(x) -> bigint, \"Returns the number of TRUE input values. This function is equivalent to count(CASE WHEN x THEN 1 END).\"' — QUOTED verbatim in the new worked example.\n  - FILTER (WHERE ...): 'The FILTER keyword can be used to remove rows from aggregation processing with a condition expressed using a WHERE clause. This is evaluated for each row before it is used in the aggregation and is supported for all aggregate functions' — confirms `COUNT(*) FILTER (WHERE bool)` is valid Trino 467.\n\nFILES MODIFIED (1):\n  - resources/23-sql-best-practices-olap.md — three additive edits at §3.1E (keyword-anchor line extension + per-group worked example + 'When to reach for which' rank-order extension) and one additive cross-ref pointer at §11 (conditional-aggregation block). NO sections renamed/removed. Existing count_if LEAD form text, existing equivalence triplet (lines 543-548), existing DO-NOT-WRITE bullets 1-4 (lines 552-556), existing §11 LEAD canonical (lines 1449-1455 area), existing §11 terminology note + SCD-1 ban — ALL UNCHANGED.\n\nCONSTRAINT CONFIRMATION:\n  - DID NOT run git commit. DID NOT run git push. (Save-only — file edits + state.json bump.)\n  - DID NOT touch resources/22 — ZERO edits. §13.x federation guardrails preserved; federation rubric row stays 4.49944/310.\n  - SQL valid Trino 467 dialect: `count_if(shipped_late)`, `COUNT(*) FILTER (WHERE shipped_late)`, `SUM(CASE WHEN shipped_late THEN 1 ELSE 0 END)` — all three documented in Trino 467 (count_if at trino.io/docs/467/functions/aggregate.html; FILTER (WHERE) documented as supported on all aggregates per same page; SUM(CASE WHEN) is ANSI-portable).\n  - Reconcile-in-place: extended the EXISTING §3.1E LEADING CANONICAL keyword-anchor line, added a worked-example block adjacent to the existing equivalence triplet, extended the existing 'When to reach for which' sentence, added a cross-ref pointer at §11. NO duplicate block, NO new section, NO contradiction with existing count_if LEAD form or DO-NOT-WRITE bullet 4 (which already states SUM(CASE WHEN p THEN 1 ELSE 0 END) is verbose vs count_if). iter534-583 conditional-aggregation-FILTER lock at §11 PRESERVED — only an additive cross-ref pointer added, no rewrite.\n  - state.json iteration = 584 (bumped from 583), phase = 'extended' (unchanged).\n  - ALL iter534-583 locks PRESERVED in full (UNTOUCHED — no edits to any locked block): r07 §1a-§1a.5 [incl §1a.2A array_agg-ORDER-BY + iter582 listagg-no-DISTINCT bullet5] + §1b CTE + Pattern C4 + approx + date_trunc + week-Monday + now() + named-WINDOW + date_add + multiple-COUNT-DISTINCT + §5 B2/B3 + FILTER + ROWS-vs-RANGE + array_join/array_position + sequence + iter566 forward-fill/IGNORE-NULLS H3 + iter567 PARTITION-BY-entity_id + iter568 contains-case-sensitivity + iter570/571/572 COMBINED-composition-canonical + iter573 GROUP-BY-aggregate-ban + approx_distinct-two-arg + HLL-merge + UNNEST-WITH-ORDINALITY + iter575 interval-overlap-H3 + 3-pattern-contrast-card + iter577 COUNT-LEFT-JOIN-trap-card + bounded-spine + iter578 reservations/rooms-variant + iter579 landing-point-signpost + point-event-vs-interval-disambiguation + iter581 timezone-GROUP-BY-local-day-Fact3; r23 §3.1A-H [§3.1E count_if/IF-vs-CASE LEADING CANONICAL existing content UNCHANGED — iter584 only EXTENDED keyword-anchor line + ADDED adjacent worked example + EXTENDED 'when to reach for which' sentence; equivalence triplet lines 543-548 and DO-NOT-WRITE bullets 1-4 untouched] + greatest/least-cross-engine + EXTRACT-EPOCH + format() + approx_percentile + HALF_UP + arbitrary/max_by + §4 EXPLAIN-skew + COUNT(*)-vs-COUNT(col) + multiple-COUNT-DISTINCT + UNION-default + INTERSECT/EXCEPT-default + split/contains/UNNEST + iter567 whitespace-safety + iter568 §3.1G tie-break-determinism + iter569 §3.1G nested-window-ban + no-unique-column-fallbacks + WHERE-vs-HAVING + ROLLUP/CUBE/GROUPING-SETS + iter571 ::-cast-ban-H3 + iter572 IGNORE-NULLS-placement-H4 + iter573 MAX-vs-max_by-note + NOT-IN-NULL-trap + conditional-aggregation-FILTER [§11 LEAD canonical + terminology note UNCHANGED; iter584 only added ONE cross-ref pointer line adjacent to LEAD canonical] + ROW_NUMBER/RANK/DENSE_RANK; r27 §4.x + §4.2-NOW/§4.2A/§4.2B + §4.4D + §7A.1 WITH-RECURSIVE + §7A.2A/B listagg [incl iter583 DISTINCT sub-case] + dbt-connection-secrets-section + §6.7A-K + §6.7A2 + dbt-tests-§6.7 + NULLS-LAST-vs-Oracle; r13 §1 rollback-positional-467 + from_unixtime + JSON + UUID-fix + navhints; r17 maintenance + EXECUTE-PROCEDURE-ban + COMMENT-ON + rollback + schema-evolution + TRUNCATE/full-DELETE-canonical [477-pinned]; r18 §5 skew worked example + header; r09 element_at + map-HOF + CAST-to-JSON + CAST-JSON-to-typed + ROW-dot-access + SCD2-4-default-cols + dbt-snapshot-as-of-single-instant + COALESCE-default-H4 + map_concat-H3 + timestamptz-UTC-normalized; r10 partition-evolution + bucket-vs-identity [Trino bucket column-first bucket(col,N)]; r24 EXPLAIN-variants + ANALYZE + join_distribution_type + skew-field; r28 no-query-hint-H4 + CoW-MoR + incremental-canonical + unique_key-dedup + composite-key + ROLLUP/CUBE/GROUPING-SETS-canonical + watermark-typed-literal-warning + dbt-generic-tests-H2; r22 §13.x federation guardrails — ZERO EDITS.\n\nGREP SUMMARY:\n  - grep 'count_if|COUNT_IF|SUM(CASE WHEN|FILTER (WHERE|conditional count|count where|count of true|boolean count|count true|how many.*true' on r07 + r23 surfaced: r23 §3.1E (line 529-558) has count_if LEADING CANONICAL with correct lead-form text and correct DO-NOT-WRITE bullet 4 banning SUM(CASE WHEN p THEN 1 ELSE 0 END) as the default — BUT keyword anchors on line 533 were function-name-driven only ('Trino IF function, IF vs CASE, count_if, ternary, IIF, DECODE, Trino if() else') — MISSING the SaaS intent-phrasings ('count how many ... true', 'per X count of ... where Y is true', 'count rows flagged true'). r23 §11 (line 1449-1460) has the FILTER (WHERE event_type='purchase') LEAD canonical for the multi-metric single-scan pattern with terminology note — count_if not mentioned. r07 §5 has §1a.2A array_agg + FILTER + line 660 'COUNT(*) for matching rows ... the CASE-WHEN form SUM(CASE WHEN event_type='purchase' THEN 1 ELSE 0 END) is also valid but more verbose'. Findability gap: §3.1E LEAD form for count_if is CORRECT but only findable via function-name search, not intent-based search.\n  - VERDICT: count_if exists with correct LEAD form but BURIED for intent-based queries. ADD intent-phrasings to keyword-anchor line + ADD a worked example with boolean column + GROUP BY at §3.1E + ADD cross-ref at §11. Reconcile-in-place. ONE coordinated edit at the SINGLE landing point (§3.1E) with one cross-ref pointer at the alternate landing point (§11).\n\nPRIOR ITER583 NOTES (preserved for history): iter583 PASS 4.84375; one Q4 defect — SUM(CASE WHEN shipped_late THEN 1 ELSE 0 END) used as primary instead of count_if. Root cause: §3.1E keyword anchors function-name-only. iter584 fixes by adding SaaS intent-phrasings + boolean-per-group worked example + §11 cross-ref pointer.\n\nPRIOR ITER582 NOTES (preserved for history): ADDITIVE listagg-no-DISTINCT clarification at r07 §1a.2A DO-NOT-WRITE block — defect resurfaced iter582 because placed at array_agg-ORDER-BY canonical rather than listagg canonical at r27 §7A.2B; iter583 relocated."
}

{
  "iteration": 591,
  "phase": "extended",
  "final_iterations_remaining": 0,
  "passed": false,
  "notes": "iter591 = RESOURCE DEFECT FIX (HIGH PRIORITY) — replacing FALSE 'ILIKE Supported' claim in r23 anti-patterns table at line 1576. iter590 Q2 case-insensitive match scored 2.25 because responder faithfully reproduced r23's false claim that 'ILIKE (case-insensitive LIKE) | PostgreSQL | Supported — Trino has ILIKE as a keyword' and produced `WHERE email ILIKE '%@gmail.com'` — which is a PARSE ERROR on a local Iceberg/Hive table (`mismatched input 'ilike'`). The false claim was a r22-PG-connector-context (legitimate — Postgres HAS ILIKE) leakage into r23 native-Trino best-practices.\n\nGREP RESULTS:\n  - `grep -n 'ILIKE\\|ilike\\|case.insensitive...' r23` → found false claim at line 1576 in the 'Trino 467 SQL-dialect anti-patterns' table. Also found benign r23:946 `WHERE LOWER(email) = 'me@x.com'` row (already correct).\n  - `grep -n 'ILIKE' r22-trino-federation-postgresql.md` → confirmed legitimate PG-CONNECTOR ILIKE PUSHDOWN content at r22 lines 68/2014/2328/2523/2571-2572/2586-2587/2600-2602/2661/2667/2677-2682/2698/2705-2710 (PG-pushdown context — accurate). DID NOT TOUCH r22.\n\nWEBSEARCH VERIFICATION (Trino 467 ILIKE absence):\n  - WebFetch trino.io/docs/current/functions/comparison.html: 'ILIKE is not documented in this page. Searching for ILIKE returns no results. The page mentions LIKE in the section Pattern comparison: LIKE but does not reference case-insensitive variants. Operators listed: <, >, <=, >=, =, <>, !=, BETWEEN, NOT BETWEEN, IS NULL, IS NOT NULL, IS DISTINCT FROM, IS NOT DISTINCT FROM, LIKE, NOT LIKE, IN, NOT IN, ALL, ANY, SOME. Functions: greatest(), least().'\n  - WebFetch trino.io/docs/current/functions/string.html: 'After searching the documentation page for both ILIKE and ilike, I can confirm that ILIKE is not mentioned on this page.'\n  - WebFetch trino.io/docs/current/language/reserved.html: 'ILIKE is not listed as a reserved keyword. The page documents LIKE as reserved but contains no mention of ILIKE.'\n  - WebSearch github.com/trinodb/trino/issues/2491: 'Issue #2491 was opened on January 13, 2020 with the title Add ILIKE function to support case-insensitive LIKE-like string matching... The issue is still open.' (OPEN since 2020, no PR merged.)\n\nFIX A DELIVERED (REPLACE-in-place, not append):\n  - r23:1576 OLD: `| **`ILIKE`** (case-insensitive LIKE) | PostgreSQL | **Supported** — Trino has `ILIKE` as a keyword. But **pushdown** to PostgreSQL is conditional on `enable_string_pushdown_with_collate=true` + compatible column collation (see resource 22). | Use `col ILIKE 'pat%'` freely in Trino-evaluated filters; verify EXPLAIN for pushdown if the col is in a JDBC catalog. |`\n  - r23:1576 NEW: `| **`ILIKE`** (case-insensitive LIKE) | PostgreSQL | **NOT supported in native Trino 467 SQL.** Parse error: `WHERE col ILIKE 'pat'` on a local Iceberg/Hive table fails with `mismatched input 'ilike'`. ILIKE is absent from trino.io/docs/current/functions/comparison.html, trino.io/docs/current/functions/string.html, and trino.io/docs/current/language/reserved.html. Feature request trinodb/trino #2491 (titled 'Add `ILIKE` function to support case-insensitive LIKE-like string matching') has been OPEN since January 2020 — no PR merged. Disambiguator: ILIKE DOES appear in the PostgreSQL CONNECTOR context (resource 22 §3.3) as a predicate pushed down to the remote PostgreSQL engine (Postgres has ILIKE natively) — that is federation/connector-pushdown behavior, NOT native Trino SQL you can run on a local Iceberg table. Do not conflate. | Case-insensitive LIKE/contains/starts-with on native Trino tables: lower both sides — `WHERE LOWER(col) LIKE LOWER('pat%')` (or `WHERE LOWER(email) LIKE '%@gmail.com'` when literal is already lowercase). Case-insensitive equality: `WHERE LOWER(col) = 'acme'`. Case-insensitive regex: `WHERE regexp_like(col, '(?i)pattern')` (Java regex inline flag). Ingest-time optimization: store `lower(col)` generated/computed column at ingest, filter on it directly — predicate pushes down to Iceberg as `col_lower = 'acme'`. DO NOT WRITE on local Trino tables: `WHERE col ILIKE 'pat'` — parse error. Keyword anchors: case-insensitive LIKE Trino, ILIKE Trino, case-insensitive match, match regardless of case, LOWER LIKE, ILIKE not supported Trino, case-insensitive equals/contains/starts-with. |`\n  - The false 'Supported' is GONE (replaced, not annotated). Reconcile-in-place satisfied. Disambiguator points to r22 §3.3 for the PG-connector ILIKE pushdown context so future responders don't re-conflate.\n\nFIX B SWEEP RESULT (sibling-claim check on the same anti-patterns table 1555-1580): VERIFIED-ACCURATE — NO-OP. Checked all 24 rows in the table:\n  - QUALIFY, SELECT * EXCEPT, SELECT * REPLACE, LIMIT N BY, TOP N, DISTINCT ON, GENERATE_SERIES, NOW AT TIME ZONE, EXTRACT EPOCH, ::cast, hints, TO_CHAR, ANALYZE TABLE, TIMESTAMPDIFF, DATE_FORMAT, STRING_AGG, ARRAY_AGG implicit ORDER BY, MERGE per-connector, RETURNING, Window in WHERE, ORDER BY NULLS default — all correctly marked NOT supported / Different semantics / Silently ignored / Per-connector gate, consistent with iter534-590 canonicals (QUALIFY ban, ::cast ban iter571, hint ban, NULLS-default lock iter587, STRING_AGG→listagg canonical, TO_CHAR→date_format/format_datetime canonical, EXTRACT EPOCH→to_unixtime canonical, ANALYZE TABLE→bare ANALYZE canonical).\n  - GROUP BY ALL marked SUPPORTED — verified accurate via trino.io/docs/current/sql/select.html: 'GROUP BY [ ALL | DISTINCT ] grouping_element [, ...]' (GROUP BY ALL is in SELECT grammar).\n  - FETCH FIRST N ROWS ONLY marked SUPPORTED — verified accurate via trino.io blog 2020/02/03 'Trino's FETCH FIRST clause supports either the FIRST or NEXT keywords and the ROW or ROWS keywords...If the argument ONLY is specified, the result set is limited to the exact number of leading rows.'\n  - The ILIKE row was the ONLY genuinely false claim. NO sibling false claims to fix. FIX B = NO-OP (no manufactured churn).\n\nCONSTRAINT CONFIRMATION:\n  - DID NOT touch resources/22 — ZERO edits to federation §13.x guardrails; federation rubric row stays 4.49944/310. (r22 §3.3 ILIKE PG-pushdown content remains intact as the legitimate disambiguator target.)\n  - DID NOT run git commit. DID NOT run git push. (Save-only — only r23 line 1576 + state.json edits.)\n  - state.json iteration = 591 (bumped from 590), phase = 'extended' (unchanged), final_iterations_remaining = 0.\n  - Reconcile-in-place: REPLACED the false r23:1576 line; did NOT append a contradictory block; the false 'Supported' is GONE.\n  - All SQL Trino 467 dialect valid: LOWER(col), LOWER('pat%'), LIKE, regexp_like(col, '(?i)pattern'), lower(col) generated column — all standard Trino 467.\n  - All iter534-590 locks PRESERVED in full (UNTOUCHED): r07 forward-fill H3 + interval-overlap H3 + signpost + COUNT-trap + reservations-variant + COMBINED-composition + timezone-Fact3 + ROWS-vs-RANGE [iter544 + iter587] + array_agg/element_at + self-join + correlated-subquery + cardinality; r23 §3.1A-H + §3.1E count_if + §11 conditional-aggregation-FILTER + count_if co-lead + greatest/least + EXTRACT-EPOCH + format + HALF_UP-round + arbitrary/max_by + EXPLAIN-skew + COUNT(*)-vs-COUNT(col) + UNION/INTERSECT/EXCEPT + split/contains/UNNEST + LENGTH-counts-chars + tie-break-determinism + §3.1G nested-window-ban + NOT-IN-NULL + ::-cast-ban + IGNORE-NULLS-placement + MAX-vs-max_by + ROW_NUMBER/RANK/DENSE_RANK + IS-DISTINCT-FROM + LIMIT-determinism + DOUBLE-binary-float [iter590] [iter591 REPLACED false ILIKE-Supported row at line 1576 with NOT-supported truth + LOWER LIKE / regexp_like (?i) canonical + r22 §3.3 cross-ref disambiguator — anti-patterns table otherwise UNTOUCHED]; r27 §4.x + §4.2-NOW/A/B + §4.4D + §7A.1 WITH-RECURSIVE + §7A.2A/B listagg [+DISTINCT sub-case] + dbt-secrets + §6.7A-K + §6.7A2 + dbt-tests + NULLS-LAST; r13 §1 rollback-positional-467 + from_unixtime + JSON + UUID-fix + navhints; r17 maintenance + EXECUTE-PROCEDURE-ban + COMMENT-ON + rollback + schema-evolution + TRUNCATE/full-DELETE [477] + time-travel-LEADING-CANONICAL [iter587 clause-arg signal]; r18 §5 skew; r09 element_at + map-HOF + CAST-JSON + CAST-JSON-to-typed-ROW + ROW-dot + iter589-native-ROW-dot-access-canonical + SCD2-4 + dbt-snapshot-as-of + COALESCE-default-H4 + map_concat-H3 + timestamptz-UTC; r10 partition-evolution + bucket-vs-identity [column-first bucket(col,N)]; r24 EXPLAIN-variants + ANALYZE + join_distribution_type + skew-field; r28 no-query-hint-H4 + CoW-MoR + incremental-canonical + late-arriving-lookback + unique_key-dedup + composite-key + ROLLUP/CUBE/GROUPING-SETS + watermark-typed-literal + dbt-generic-tests-H2; r22 §13.x UNTOUCHED.\n\nFILES MODIFIED:\n  - resources/23-sql-best-practices-olap.md — REPLACED line 1576 row (false 'ILIKE Supported') with NOT-supported truth + native-Trino fix (LOWER LIKE / regexp_like (?i) / lower() generated column) + r22 §3.3 disambiguator. NO other r23 edits.\n  - training/state.json — iteration 590 → 591.\n\nPRIOR ITER590 NOTES (preserved for history): iter590 PASS 4.3125 — Q2 case-insensitive match 2.25 = RESOURCE DEFECT (responder faithfully reproduced false r23 ILIKE claim). DOUBLE-binary-float Symptom→cause→fix added at end of §3.1C."
}

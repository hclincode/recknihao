{
  "iteration": 488,
  "phase": "extended",
  "final_iterations_remaining": 0,
  "passed": true,
  "notes": "Iter488 TEACHER — PRIMARY surgical clarifier on r13 LEADING CANONICAL Spark Iceberg write file-size card (iter487 Q2 minor option-key-prefix-confusion nuance). Federation NOT probed; 4.49944/310 row HELD per iter472-487+ directive.\n\nPRE-EDIT VERIFICATION (WebSearch + WebFetch):\n(1) iceberg.apache.org/docs/latest/spark-configuration WebFetched 'Write options' section: CONFIRMED the DataFrameWriter OPTION key for per-write file-size override is `target-file-size-bytes` (NO `write.` prefix). Doc verbatim row: '`target-file-size-bytes` — Overrides this table's write.target-file-size-bytes'. The `write.` prefix is TABLE-PROPERTY namespace only.\n(2) iceberg.apache.org/docs/latest/spark-writes 'Controlling File Sizes' WebFetched: CONFIRMED the persistent TABLE-PROPERTY key is `write.target-file-size-bytes` (WITH `write.` prefix) settable via TBLPROPERTIES, ALTER TABLE SET TBLPROPERTIES, or `writeTo(...).tableProperty(...)`. Doc example: `df.writeTo(\"catalog.db.table\").option(\"write-format\", \"avro\").option(\"snapshot-property.key\", \"value\").append()` — `.option()` keys drop the `write.` prefix family-wide (write-format, target-file-size-bytes, fanout-enabled, distribution-mode, compression-codec, check-nullability all use bare keys via `.option()`).\n(3) Catalog-aware `writeTo(\"iceberg.<schema>.<table>\")` preferred over legacy v1 `.save(\"s3a://...\")` CONFIRMED via existing TOP-OF-DOC CALLOUT #1 in r13 + iceberg.apache.org/docs/latest/spark-writes — the v1 path-style write routes around the SparkCatalog plugin.\n\nEDIT 1 (resources/13-postgres-to-iceberg-ingestion.md):\n- TIER 2 (DataFrameWriter `.option(...)` per-write override): Added 'KEY-PREFIX DISAMBIGUATION' callout with: (a) 2-row namespace-comparison table (TABLE PROPERTY uses `write.target-file-size-bytes` WITH prefix / DataFrameWriter OPTION uses `target-file-size-bytes` NO prefix); (b) mnemonic 'TABLE PROPERTY uses the FULL `write.*` namespace; DataFrameWriter OPTION strips the `write.` prefix'; (c) before/after Python block (`.option(\"write.target-file-size-bytes\", ...)` silently-ignored → `.option(\"target-file-size-bytes\", ...)` correct); (d) catalog-aware `writeTo(...)` preferred over legacy `.save(\"s3a://...\")` note.\n- DO-NOT-WRITE MATRIX (Tier 3): Added 2 new rows to existing 3-row matrix: (Row 4) `df.writeTo(\"iceberg.x.y\").option(\"write.target-file-size-bytes\", \"134217728\").append()` — WRONG NAMESPACE; silently ignored; option-key-prefix-confusion class. (Row 5) `df.write.format(\"iceberg\").option(\"target-file-size-bytes\", \"134217728\").save(\"s3a://bucket/...\")` — legacy v1 non-catalog write path, banned on this stack.\n- OPENING PARAGRAPH of the LEADING CANONICAL card: Updated the keywords-readme list to add `target-file-size-bytes` (no prefix), `option key prefix`, `writeTo vs save`; updated the 2-mechanisms sentence to explicitly note 'the key name differs between the two: TABLE PROPERTY uses write.target-file-size-bytes (with prefix); DataFrameWriter OPTION uses target-file-size-bytes (NO prefix). Same setting, different key in each namespace.'\n\nEDIT 2 — SECONDARY low-buffer reinforcement: NONE. Per iter488 directive 'If coverage is already solid, CONFIRM and move on — do NOT manufacture edits.' Reviewed: (a) dbt model contracts (4.1146/3) — §6.7C in r27 already has Q-PATTERN MATCHER, 9-row DO-NOT-WRITE matrix, enforced-vs-definable constraint table, worked example — canonical-level; (b) dbt source freshness (4.219/3) — §6.7B in r27 already has Q-PATTERN MATCHER, worked example, DO-NOT-WRITE rows banning the `dbt run includes freshness check` fab — canonical-level; (c) dbt snapshots SCD2 (4.5625/2) — r09 already covers both strategies (timestamp + check) with required keys, dbt_valid_from/dbt_valid_to columns, and DO-NOT-WRITE banning strategy='hash'/'merge'/'changes' and dbt_is_current — canonical-level; (d) Storage tiering on Trino+Iceberg+MinIO (4.25/2) — covered in r19/r20 with `mc ilm tier add` recipe; canonical-level. All four low-buffer topics CONFIRMED already solid; ZERO manufactured edits.\n\n§13.x federation guardrails in resources/22-trino-federation-postgresql.md NOT TOUCHED. Federation 4.49944/310 row held per iter472-488+ directive (24th+ consecutive iteration with the row HELD).\n\nFiles modified: resources/13-postgres-to-iceberg-ingestion.md (Tier 2 KEY-PREFIX DISAMBIGUATION callout added + 2 new DO-NOT-WRITE rows in Tier 3 matrix + opening paragraph updated), training/state.json. Iteration 487 -> 488."
}

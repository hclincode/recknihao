{
  "iteration": 590,
  "phase": "extended",
  "final_iterations_remaining": 0,
  "passed": false,
  "notes": "iter590 = DURABILITY/BREADTH — ONE tight additive item in r23 §3.1C at the round/decimal landing point. Grep results per area:\n  (a) native-ROW dot-access generalizes to ship_to/device/customer_address: FINDABLE — iter589 LEADING CANONICAL at r09:759 has generic dot-notation framing with anchors 'struct column / ROW column / get a field out of a struct' that route ANY struct (ship_to/device/customer_address/geo) to the same canonical. The address.city worked example is explicitly the generalization template (any column typed ROW(...) → col.field). NO-OP.\n  (b) round/format DOUBLE-vs-DECIMAL artifact (12.300000007): GENUINE GAP. r23 §3.1C HALF_UP canonical (line 375) covers the ROUNDING-MODE question and shows `CAST(DOUBLE AS DECIMAL(18,2))` as the billing fix, but does NOT name the 12.300000000000007 SYMPTOM that the SaaS engineer's question typically lands on. §3.1B item 1-5 'sum looks too small' symptom list does not cover 'sum has weird trailing digits'. A responder seeing 'SUM returns 12.30000000000007' / 'ROUND(amount, 2) still shows trailing digits' would not currently route to the existing DOUBLE→DECIMAL cast fix. Added ONE tight Symptom→cause→fix paragraph at the END of §3.1C (just before Sources:) — keyword anchors for the artifact symptoms, IEEE-754 verbatim doc quote ('A double is a 64-bit inexact, variable-precision implementing the IEEE Standard 754 for Binary Floating-Point Arithmetic.' from trino.io/docs/current/language/types.html) as the cause, the two-fix split (a) DECIMAL at ingest [exact via §3.1B SUM widening] vs (b) CAST per-row at query time [exact via §3.1C HALF_UP], the ROUND/format anti-pattern (display-only hide, downstream WHERE total=12.30 still fails), and the money-vs-non-money exactness boundary. Reconcile-in-place: ADD a Symptom→cause→fix block at the landing point; do NOT rewrite the existing §3.1C HALF_UP body or §3.1B SUM(DECIMAL) widening canonical. Cross-references to §3.1B and §3.1C immediately above.\n  (c) fresh-breadth probes — case-insensitive LOWER/ILIKE (r23:944, r23:1574, r07:211 transform-lower lambda), LIKE wildcard prefix (r23:945 `WHERE country LIKE 'US%'` pushdown), BETWEEN inclusive-range (r07:628 overlapping vs non-overlapping buckets, r07:382/564/602-604 BETWEEN INTERVAL patterns), COALESCE-chain (r23:718-723 coalesce-per-arg for greatest/least, r07:148 array_agg FILTER+COALESCE), multi-column sort (r23:649 `ROW_NUMBER() OVER (... ORDER BY session_timestamp DESC, session_id DESC)` tie-break, r23:654 ORDER BY all-remaining-columns fallback (a)), TRIM (r23:246/250/285 split-whitespace, r07:89/94/105/109/121/129), SUBSTR (r23:945 pushdown anti-pattern), replace() (r23:1557 SELECT * REPLACE Trino-incompat). All FINDABLE at landing points. NO-OP for (a) and (c).\n\nFIX A DELIVERED (reconcile-in-place, ADD not rewrite):\n  - r23:413 (immediately after the last DO-NOT-WRITE row, just before 'Sources:'): NEW Symptom→cause→fix paragraph. Anchors: 'my SUM returns 12.300000000000007 instead of 12.30', 'weird trailing digits on a money sum', 'ROUND(amount, 2) still shows 12.30000001', 'decimal looks wrong', 'floating point error in my totals'. Verbatim doc quote: 'A double is a 64-bit inexact, variable-precision implementing the IEEE Standard 754 for Binary Floating-Point Arithmetic.' (trino.io/docs/current/language/types.html). Two-fix split: (a) store as DECIMAL(p,s) at ingest (exact via §3.1B SUM widening), (b) per-row CAST at query time `SUM(CAST(amount_dbl AS DECIMAL(18,2)))` (HALF_UP rounds each row to cents BEFORE the SUM). Anti-patterns: wrapping in ROUND/format is display-only hide — downstream `WHERE total=12.30` still fails. Money-vs-non-money boundary: for percentiles/ratios/measurements DOUBLE artifact is harmless; for money/billing/contracts 'never SUM DOUBLE'. Cross-references to §3.1B (SUM(DECIMAL) exactness) and §3.1C (HALF_UP rounding mode).\n  - WebSearch-verified the IEEE-754 doc quote at trino.io/docs/current/language/types.html.\n\nFIX B: NO-OP (no other genuine adjacent gap — areas (a) and (c) all findable at landing points).\n\nCONSTRAINT CONFIRMATION:\n  - DID NOT touch resources/22 — ZERO edits to federation §13.x guardrails; federation rubric row stays 4.49944/310.\n  - DID NOT run git commit. DID NOT run git push. (Save-only — only r23 + state.json edits.)\n  - state.json iteration = 590 (bumped from 589), phase = 'extended' (unchanged), final_iterations_remaining = 0.\n  - Reconcile-in-place: ADDED one Symptom→cause→fix paragraph at end of §3.1C; DID NOT rewrite the existing §3.1C HALF_UP body, §3.1B SUM(DECIMAL) widening canonical, or any other §3.x content.\n  - All SQL Trino 467 dialect valid (CAST AS DECIMAL(18,2), SUM(CAST(... AS DECIMAL(...))), ROUND(x, 2), format('%.2f', x), WHERE total = 12.30 — all standard Trino 467).\n  - Additive only — landing-point placement (end of §3.1C, just before Sources:, where round/format/decimal-cast questions land).\n  - All iter534-589 locks PRESERVED in full (UNTOUCHED): r07 forward-fill H3 + interval-overlap H3 + signpost + COUNT-trap + reservations-variant + COMBINED-composition + timezone-Fact3 + ROWS-vs-RANGE [iter544 + iter587] + array_agg/element_at + self-join + correlated-subquery + cardinality; r23 §3.1A-H + §3.1E count_if + §11 conditional-aggregation-FILTER + count_if co-lead + greatest/least + EXTRACT-EPOCH + format + HALF_UP-round + arbitrary/max_by + EXPLAIN-skew + COUNT(*)-vs-COUNT(col) + UNION/INTERSECT/EXCEPT + split/contains/UNNEST + LENGTH-counts-chars + tie-break-determinism + §3.1G nested-window-ban + NOT-IN-NULL + ::-cast-ban + IGNORE-NULLS-placement + MAX-vs-max_by + ROW_NUMBER/RANK/DENSE_RANK + IS-DISTINCT-FROM + LIMIT-determinism [iter590 ADDED DOUBLE-binary-float Symptom→cause→fix at end of §3.1C — additive only, did not rewrite §3.1C HALF_UP body]; r27 §4.x + §4.2-NOW/A/B + §4.4D + §7A.1 WITH-RECURSIVE + §7A.2A/B listagg [+DISTINCT sub-case] + dbt-secrets + §6.7A-K + §6.7A2 + dbt-tests + NULLS-LAST; r13 §1 rollback-positional-467 + from_unixtime + JSON + UUID-fix + navhints; r17 maintenance + EXECUTE-PROCEDURE-ban + COMMENT-ON + rollback + schema-evolution + TRUNCATE/full-DELETE [477] + time-travel-LEADING-CANONICAL [iter587 clause-arg signal]; r18 §5 skew; r09 element_at + map-HOF + CAST-JSON + CAST-JSON-to-typed-ROW + ROW-dot + iter589-native-ROW-dot-access-canonical + SCD2-4 + dbt-snapshot-as-of + COALESCE-default-H4 + map_concat-H3 + timestamptz-UTC; r10 partition-evolution + bucket-vs-identity [column-first bucket(col,N)]; r24 EXPLAIN-variants + ANALYZE + join_distribution_type + skew-field; r28 no-query-hint-H4 + CoW-MoR + incremental-canonical + late-arriving-lookback + unique_key-dedup + composite-key + ROLLUP/CUBE/GROUPING-SETS + watermark-typed-literal + dbt-generic-tests-H2; r22 §13.x UNTOUCHED.\n\nFILES MODIFIED:\n  - resources/23-sql-best-practices-olap.md — ADDED one tight Symptom→cause→fix paragraph at end of §3.1C (just before 'Sources:' block, immediately after the last DO-NOT-WRITE row). NO other r23 edits. NO rewrites.\n  - training/state.json — iteration 589 → 590.\n\nPRIOR ITER589 NOTES (preserved for history): iter589 STRONG PASS 4.96875 — Native-ROW dot-access LEADING CANONICAL added at r09:759 with forward-disambiguator + MAP/JSON disambiguators."
}

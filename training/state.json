{
  "iteration": 471,
  "phase": "extended",
  "final_iterations_remaining": 0,
  "passed": true,
  "notes": "Iter471 TEACHER — federation NOT probed (stays recorded near-miss 4.49944/310). Iter470 was a thin pass at 3.71875 with three load-bearing fabrications across Q1 + Q3 (citation-hygiene streak broken). PRIMARY teacher actions this iter address the three fab classes:\n\n(1) Q3 DROP COLUMN fabricated-capability-restriction + column_order fabricated-table-property. **Pre-edit grep confirmed BOTH were responder hallucinations, NOT stale resource lines** — `grep -rn 'column_order' resources/` returned ZERO matches; `grep -rn 'cannot DROP COLUMN' resources/` returned ZERO matches; `grep -rn 'use Spark for DROP' resources/` returned ZERO matches. r13 line 3994 explicitly stated 'Both Trino 467 and Spark behave identically here: they share the same Iceberg metadata layer' for DROP COLUMN — so the resource set already correctly treated DROP COLUMN as Trino-native, and the responder simply hallucinated the restriction. The fix: install a LEADING canonical schema-evolution section at r17 right before the existing LEADING CANONICAL ADD COLUMN block, stating explicitly that DROP COLUMN + RENAME COLUMN + safe-widening SET DATA TYPE all run NATIVELY on Trino 467 (no Spark required, all metadata-only), with the four ALTER TABLE forms shown as copy-paste SQL. WebSearch-verified via trino.io/docs/current/sql/alter-table.html + trino.io/docs/current/connector/iceberg.html. Quoted DO-NOT-WRITE matrix entries: 'Trino 467 cannot DROP COLUMN — use Spark for DROP COLUMN' marked as FABRICATED CAPABILITY RESTRICTION; `ALTER TABLE iceberg.x.y SET PROPERTIES column_order = ARRAY[...]` marked as FABRICATED TABLE PROPERTY with the full supported-properties enumeration (format, compression_codec, partitioning, sorted_by, location, format_version, max_commit_retry, delete_after_commit_enabled, max_previous_versions, orc_bloom_filter_columns, orc_bloom_filter_fpp, parquet_bloom_filter_columns, object_store_layout_enabled, data_location, extra_properties) so the responder can verify against a known-good list. Column-reorder honesty: Trino 467 has NO reorder DDL; Spark uses `ALTER TABLE ALTER COLUMN col FIRST | AFTER other_col`; reorder is COSMETIC ONLY on Iceberg+Parquet (columnar layout reads only queried columns). Safe-widening promotion matrix: INTEGER→BIGINT, REAL→DOUBLE, DECIMAL precision-widen same-scale ONLY. Narrowing / scale-change / incompatible: rejected by Iceberg spec (not Trino limitation) — workaround is the 4-step add-new-col + backfill (CAST) + drop-old + rename pattern shown literally.\n\n(2) Q1 $refs vs $snapshots conflation + malformed metadata-table quoting. **Pre-edit grep confirmed `ref_name` does NOT appear anywhere in resources/ and the dotted `.$snapshots`/`.$refs` forms do NOT appear** — both responder hallucinations. The existing r17 §$refs worked-examples block (line 705-741) was correct (column list with `name`, NOT `ref_name`) but lacked an EXPLICIT $refs-vs-$snapshots contrast at the top of the metadata-tables section. Fix: install a LEADING canonical contrast block right before the existing dialect-translation meta-canonical, with: (i) 2-row table contrasting purpose / columns / has-ref-name-column for the two metadata tables; (ii) 6 copy-paste Trino 467 queries — list $snapshots, list $refs, branches-only, find branch by name, the CANONICAL JOIN ($refs to $snapshots ON snapshot_id), and all-branches-with-tip; (iii) 7-row DO-NOT-WRITE matrix with the iter470 verbatim fab `WHERE ref_name = 'staging'` on $snapshots called out by name, plus the dotted `iceberg.analytics.orders.$snapshots` malformed-quoting form and three other quoting-variant fails; (iv) cross-refs to the existing metadata-table quoting + $snapshots column-list canonicals. ALSO reconciled the WAP Step 3 worked example at r17 §2526 — it previously showed $snapshots ORDER BY committed_at LIMIT 10 as the DEFAULT to find an audit branch's tip with $refs as 'Or use $refs to find the branch tip directly'; flipped the priority so $refs is the DEFAULT pattern (one row, exact match) and the $snapshots ordered scan is shown only as the 'I don't know the branch name' fallback with an explicit `DO NOT add WHERE ref_name = ... here — that column does NOT exist on $snapshots` warning. WebSearch-verified $refs column list (name, type, snapshot_id, max_reference_age_in_ms, min_snapshots_to_keep, max_snapshot_age_in_ms) against trino.io/docs/current/connector/iceberg.html metadata-tables section.\n\n(3) Q2 dbt-framing for hard-delete Model 2. r27 §4.6A.1 Model 2 for SOFT-delete is correctly written as a dbt incremental MERGE model (SELECT body wrapped as MERGE INTO by `materialized='incremental'` + `incremental_strategy='merge'` + `unique_key`) — NOT the issue. The framing slip is in r27 §4.6A.4 row 4 (hard-delete via DELETE), which previously said 'Model 2 = standalone DELETE FROM fct_customers WHERE NOT EXISTS (...) (Trino DELETE on Iceberg)' without specifying WHERE this DML runs. Fix: (a) updated row 4 to explicitly say the raw DELETE 'runs as a `post_hook` on Model 1, or as a `dbt run-operation` macro, or as a dedicated operation file — NOT as a dbt model body'; (b) inserted a dbt-framing canonical note immediately below the decision table with three worked recipes — the `post_hook` form (literal config snippet on Model 1), the `dbt run-operation` macro form (literal macros/hard_delete_missing_customers.sql file), and the `on-run-end` operation-file form; (c) explicit 'what you DO NOT do' clarification: `models/fct_customers__hard_delete.sql` containing only `DELETE FROM ...` fails at dbt compile time with a parser error; (d) clarification that soft-delete Model 2 (§4.6A.1) is NOT affected because soft-delete is UPDATE-on-MERGE which dbt's incremental_strategy='merge' produces correctly from a SELECT — only hard-delete via raw DELETE needs hook/operation framing. ALSO added an inline note at the end of Model 2 'Notes on Model 2' bullet list re-stating the SELECT-only model body rule and pointing forward to §4.6A.4 + the dbt-framing canonical.\n\n§13.x federation guardrails in resources/22 NOT TOUCHED (git diff --stat will confirm only r17 + r27 + state.json modified). Federation 4.49944/310 row sits 0.0006 below 4.5 raised threshold — let count grow naturally with non-federation breadth probes.\n\nFab-class watch held: (1) cross-dialect spillover — new r17 schema-evolution DO-NOT-WRITE matrix explicitly bans 'Trino 467 cannot widen INT to BIGINT — use Spark' as FABRICATED capability restriction (same class as the column_order property fab); (2) version-pin spillover — the new schema-evolution section explicitly notes that `ADD COLUMN ... FIRST | AFTER` is Trino 469+ (not 467), and even on 469+ does not REORDER existing columns; (3) Trino-internal clause conflation — the new $refs-vs-$snapshots canonical disambiguates the two metadata tables by column list + purpose (not just name); (4) fabricated-capability-restriction — the LEADING schema-evolution DO-NOT-WRITE matrix explicitly catches the iter470 Q3 fab class with three banned 'cannot X — use Spark' claims (DROP COLUMN, RENAME COLUMN, SET DATA TYPE widening) each cited to the canonical Trino + Iceberg docs.\n\nWebSearch verifications this iter:\n- `Trino 467 Iceberg connector ALTER TABLE DROP COLUMN RENAME COLUMN SET DATA TYPE supported documentation` → trino.io/docs/current/connector/iceberg.html confirmed DROP COLUMN, RENAME COLUMN, SET DATA TYPE (widening only) are all supported ALTER TABLE operations for the Iceberg connector; Iceberg supports schema evolution with safe column add, drop, rename including nested structures; type updates limited to widening operations.\n- `Trino Iceberg connector $refs metadata table columns name type snapshot_id documentation` → confirmed $refs columns: name, type, snapshot_id, max_reference_age_in_ms, min_snapshots_to_keep, max_snapshot_age_in_ms.\n- `Trino 467 ALTER TABLE ADD COLUMN FIRST AFTER position column release notes` → confirmed ADD COLUMN FIRST/AFTER was added in Trino 469 (27 Jan 2025), NOT 467 — so the existing 'FIRST/AFTER is parse error on 467' claim in the ADD COLUMN LEADING CANONICAL section remains accurate.\n\nNo fresh unvetted claims introduced. Every schema-evolution claim is either (a) cited to trino.io/docs/current/sql/alter-table.html / trino.io/docs/current/connector/iceberg.html / iceberg.apache.org/docs/latest/evolution/, or (b) the explicit fall-back-pattern recipe (ADD + CAST + DROP + RENAME) which is mechanical Iceberg-DDL composition. The 'reorder is cosmetic on columnar storage' claim is grounded in the Parquet column-projection semantic that's already canonical across r03 (column-oriented storage).\n\nFiles modified: resources/17-iceberg-table-maintenance.md (NEW LEADING schema-evolution canonical at §line 154 + NEW LEADING $refs-vs-$snapshots canonical at §line 750 + RECONCILED WAP Step 3 worked example at §line 2526), resources/27-oracle-plsql-to-dbt-trino.md (NEW dbt-framing note appended to §4.6A.1 Model 2 'Notes' bullet list + RECONCILED §4.6A.4 row 4 hard-delete framing + NEW dbt-framing canonical recipe below the §4.6A.4 decision table), training/state.json. Iteration 470 -> 471."
}

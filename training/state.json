{
  "iteration": 952,
  "phase": "extended",
  "final_iterations_remaining": 0,
  "passed": true,
  "note": "iter952 ZERO-edit NO-OP / RE-PROBE sweep targeting the iter951 Q4 gaps-and-islands count-per-streak slip — read-only lock spot-check + this state.json bump (951->952) ONLY. ZERO resource edits, no git commit/push, working tree clean. DELIBERATELY did NOT churn the gaps-and-islands neighborhood (iter951 Q4 was a 1st-instance synthesis bug on TAUGHT content — slip not gap). GAPS-AND-ISLANDS / COUNT-PER-STREAK FINDABILITY (the slip target) = STRONG, PRESENT-WITH-LINES, not a coverage gap: the Pattern B-Streak LEADING CANONICAL r07 L3155-3230 teaches the full LAG-flag + running-SUM streak_id pattern AND shows the CORRECT FINAL count-per-streak step done as a TWO-LAYER aggregate — Layer 3 (L3187-3195) inner subquery 'SELECT user_id, streak_id, COUNT(*) AS streak_len FROM streaks GROUP BY user_id, streak_id' (count over the FULL streak/island), THEN outer 'GROUP BY user_id -> MAX(streak_len)' (pick the longest island per user). The RULE text L3198-3202 spells out 'Layer 3 — count each streak, then MAX per user... two separate GROUP BY clauses in two separate query layers'. So the correct shape = aggregate over the WHOLE island THEN reduce, which is exactly what defeats the iter951 always-zero bug (the bug = filter-to-target-event-FIRST then count the OTHER type within that already-collapsed set). The B-Streak Variants L3223-3228 also cover the as-of/current-streak shape (find each user's max active_date, take its streak_id, COUNT(*) the rows in that (user_id, streak_id) island) and start/end dates via ROW_NUMBER — these are the 'streak ending in target event'-adjacent shapes and all aggregate over the full island before filtering. Pattern B-Session r07 L3119-3153 is the minute-gap sibling with the same skeleton + a per-session-aggregate variant L3149 'group by (user_id, session_id)'. NESTED_WINDOW illegality + two-GROUP-BY-stapled parse error are both defanged L3204-3221. So a Q-recurrence on gaps-and-islands count-per-streak is SCOPABLE AS SLIP (correct final-step example present-with-lines), NOT a gap; correct response is re-probe-don't-churn. (a) CORRECTED r07 L37 HOLDS verbatim — 'To actually cut that memory you must reduce the INPUT rows... add a WHERE filter... or pre-aggregate in stages... A HAVING COUNT(*) > N does not help here: HAVING runs after all groups are built, so it only trims the OUTPUT rows, not the in-memory group set'; NO old 'add a HAVING COUNT(*)>N to trim' folklore. Consistent with r23 §8 header L2552 + rationale L2643. (b) iter933 ADD-A-QUARTER/ADD-A-WEEK card r07 L3481-3500 INTACT (only 6 INTERVAL qualifiers YEAR/MONTH/DAY/HOUR/MINUTE/SECOND per SqlBase.g4; INTERVAL '1' QUARTER/'1' WEEK PARSE ERRORS; canonical date_trunc('quarter')+INTERVAL '3' MONTH / '7' DAY / date_add; DO-NOT-COPY defang L3496-3497; TWO-SURFACES trap L3500) + WHICH-QUARTER/week_of_year card L3469-3479. (c) r22 federation UNTOUCHED — last git-touched 2026-06-04 commit c2627a8 (Iter448), well before this sweep. (d) ALL OTHER LOCKS INTACT (read-only): argmax two-level r23 L1355-1372 + ROW_NUMBER §3.1G L1732+; GROUP-BY-output rule r07 L4338 WRONG-marked customer_id-in-GROUP-BY + L4341 fix 'drop customer_id from both SELECT and GROUP BY'; CAST-to-integer half-up r07 L4448 (ceil +inf, truncate toward zero, CAST AS integer half-up); first-match/window-alias r07 L4448/L4506 + Pattern B3 first_value/last_value default-frame footgun L3665-3696; Iceberg partition DDL r09 L127 WITH(partitioning=ARRAY['day(occurred_at)','tenant_id']) + bucket(col,N) column-first L131-135 + one-sentence rule + 6-step Spark->Trino recovery; COUNT(DISTINCT) leading canonical r23 L183-222; HAVING-vs-WHERE r23 §8 L2552/L2643; PERCENTILE footgun r05 L2234-2238 (PERCENTILE_CONT...WITHIN GROUP unsupported, approx_percentile T-Digest lead) + r23; regexp_like leading canonical + alternation plain-pipe + ~/~*/!~/!~*/RLIKE/SIMILAR-TO/LIKE-bracket defang r23 L3370-3381; mean-family geometric_mean built-in r23 L984 + harmonic-for-rates L1021; top-N-share single-pass conditional-SUM r07 L1836; r28 DATE-literal canonical L1047 (string-vs-date type mismatch defeats pushdown -> DATE '2026-05-30'); r13 json_exists leading canonical L3387-3403; NULLS-LAST default both ASC/DESC. ZERO resource edits made this iteration. Resource filename map: r05=05-multi-tenant-analytics.md, r07=07-analytical-query-patterns.md, r09=09-lakehouse-schema-design.md, r10=10-lakehouse-partitioning.md, r13=13-postgres-to-iceberg-ingestion.md, r22=22-trino-federation-postgresql.md, r23=23-sql-best-practices-olap.md, r28=28-complex-sql-performance-trino-dbt.md."
}

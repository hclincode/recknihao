{
  "iteration": 538,
  "phase": "extended",
  "final_iterations_remaining": 0,
  "passed": false,
  "notes": "Iter538 TEACHER — NO-DEFECT POLISH iteration after iter537 4.813 STRONG PASS. Tight surgical additions per directive's FIX A (approx_percentile signature family + fab-absence pre-empt) and FIX B (FILTER alias-must-match-function rule). Did NOT touch resources/22 federation guardrails. Did NOT disturb any iter495-537 locked canonicals.\n\nGREP FINDINGS:\n  - approx_percentile canonical found at r23 L100-107 (the targeted ~L100-109 range). Single SQL block (L101-105) with two-percentile/multi-percentile examples; followed by a single dense Accuracy-note paragraph at L107 that lacked an explicit signature-family enumeration.\n  - FILTER conditional-aggregation canonical found at r07 L495-517 (the targeted ~L495-517 range). VERIFIED: the existing example uses `SUM(revenue) FILTER (WHERE quarter='Q1') AS q1_revenue` (alias matches SUM semantics — NO mislabel present). FIX B's IF-condition ('IF any example labels SUM as avg') was NOT met — but added a small alias-must-match-function rule paragraph to pre-empt any future fab.\n  - r23 also has a duplicate FILTER terminology block at L891-893 (already correct, untouched).\n  - r23 L850 has `AVG(amount) AS avg_amount` — function/alias match correctly (no edit).\n\nWEBSEARCH VERIFICATION (2026-06-06):\n  - trino.io/docs/current/functions/aggregate.html — confirmed FOUR approx_percentile signatures verbatim: (1) `approx_percentile(x, percentage) -> [same as x]` 'Returns the approximate percentile for all input values of x at the given percentage.'; (2) `approx_percentile(x, percentages) -> array<[same as x]>` 'Returns the approximate percentile for all input values of x at each of the specified percentages.'; (3) `approx_percentile(x, w, percentage) -> [same as x]` 'Returns the approximate weighed percentile for all input values of x using the per-item weight w.'; (4) `approx_percentile(x, w, percentages) -> array<[same as x]>` 'Returns the approximate weighed percentile for all input values of x using the per-item weight w at each given percentages.'\n  - CRITICAL: WebFetch directly searched the current Trino docs aggregate.html page for the literal word 'accuracy' — confirmed `accuracy` is NOT a parameter on any current approx_percentile overload. The word 'accuracy' appears on the same page ONLY in the qdigest family: `qdigest_agg(x, w, accuracy) -> qdigest([same as x])`. Older Presto (pre-Trino fork) had an accuracy overload on approx_percentile but current Trino 467/481 does NOT. This is a fab-absence that needs explicit pre-emption per the iter528 family strategy.\n  - FILTER (WHERE ...) clause: 'The FILTER keyword can be used to remove rows from aggregation processing with a condition expressed using a WHERE clause' — confirmed supported across all aggregate functions on Trino 467/481.\n  - All new SQL valid Trino 467 dialect (no QUALIFY, no Spark-isms, no PERCENTILE_CONT WITHIN GROUP).\n\nEDITS (2 in-place, no appended duplicate blocks, NO locked-canonical rewrites):\n\n  1. resources/23-sql-best-practices-olap.md L100-107 — STRENGTHENED the approx_percentile canonical IN PLACE. Replaced the single dense Accuracy-note paragraph at L107 with a structured block: (a) explicit 4-overload signature family table (FOUR official Trino 467/481 signatures, each with the doc-verbatim return type and short use-case caption); (b) explicit DO-NOT-WRITE pre-empt for the fab-absence on `accuracy` param on approx_percentile (pre-empts responder fabricating an accuracy parameter — correctly redirects to `qdigest_agg(x, w, accuracy)` + `value_at_quantile(qdigest, percentage)` if tunable accuracy is needed); (c) explicit DO-NOT-WRITE pre-empt for the Postgres/Snowflake `PERCENTILE_CONT WITHIN GROUP` syntax (NOT valid Trino — redirect to the four approx_percentile overloads or window `PERCENT_RANK()`); (d) preserved Accuracy note about NOT confusing with approx_distinct's 2.3% figure, but tightened wording — 'accuracy is fixed by the T-Digest sketch the function builds internally (you cannot tune it from approx_percentile's signature — see DO-NOT-WRITE above)' replaces the slightly-misleading 'accuracy depends on the T-Digest compression parameter' phrasing. Keyword anchors added inline: approx_percentile signature, approx_percentile weight parameter, approx_percentile array of percentages, multiple percentiles one pass, p50 p95 p99 Trino, exact percentile Trino, PERCENTILE_CONT Trino, approx_percentile accuracy parameter.\n\n  2. resources/07-analytical-query-patterns.md L517 — ADDED a tight 'Alias-must-match-function rule' callout (~3 sentences) at the end of the FILTER conditional-aggregation canonical block. The example in the canonical was already correct (SUM→q1_revenue) so FIX B's literal IF-condition was not met; instead, added a defensive pre-empt: 'SUM(revenue) FILTER (...) AS q1_revenue is correct (SUM produces a total). If you instead want the per-quarter average order value, switch the function — AVG(revenue) FILTER (WHERE quarter = 'Q1') AS q1_avg_order_value — do NOT keep SUM(...) and rename the alias to avg_* (that ships a wrong-by-a-factor-of-row-count number under an average label). FILTER (WHERE ...) is supported on every Trino aggregate function: SUM, AVG, COUNT, MIN, MAX, array_agg, approx_distinct, approx_percentile, etc.' Preserves the original SUM→q1_revenue worked example intact; only adds a defensive paragraph below it.\n\nFILES MODIFIED (2): resources/23-sql-best-practices-olap.md (approx_percentile signature family + fab-absence DO-NOT-WRITE pins for accuracy-on-approx_percentile and PERCENTILE_CONT-WITHIN-GROUP), resources/07-analytical-query-patterns.md (FILTER alias-must-match-function pre-empt added below the existing canonical).\n\nUNTOUCHED per directive:\n  - resources/22 §13.x federation guardrails — ZERO edits; federation rubric row stays 4.49944/310; NO federation probe.\n  - r17 iter534 in-line ON-clause comment / header / placeholder — preserved intact.\n  - r17 iter535 deleted_snapshot_id preventive pin + 5d-retention pin (iter536) — preserved intact.\n  - r07 locked canonicals: §1a-§1a.4 + Pattern C4 + approx + date_trunc return-type note + iter536 week-Monday canonical + §5 B2/B3 + iter537 locks — all preserved. FILTER canonical only EXTENDED with the new alias-must-match paragraph (no rewrite of the worked example).\n  - r09 locked canonicals: element_at + map-HOF + CAST-to-JSON + dbt-snapshot SCD2 (iter537 4-default fix) + iter532 COALESCE-default — preserved intact.\n  - r10 iter537 partition-evolution nuance canonical — preserved intact.\n  - r13 from_unixtime epoch-ms + JSON family — untouched.\n  - r17 CRITICAL QUOTING + $partitions + current-spec + RENAME + $snapshots ops + target-file-size + WAP + $refs + iter531-535/iter536 5d-retention pin — untouched.\n  - r23 §3.1A/§3.1B/§10/DF preserved; approx_percentile canonical at L100-107 STRENGTHENED in place (not rewritten — the SQL block is unchanged, only the prose Accuracy-note paragraph was upgraded to a structured signature family + DO-NOT-WRITE pins).\n  - r26 §4 — untouched.\n  - r27 §4.1A/§4.3 string table + §4.3-STR-FAMILY/§4.3A/§4.4*/§4.5*/§4.x/§4.6B/§7A.2A-B/§6.7A-J — untouched.\n  - iter495-537 locks preserved.\n\nDID NOT run git commit or git push — orchestrator handles single commit per iteration after judge. DID NOT touch resources/22. All SQL valid Trino 467 dialect."
}

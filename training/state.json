{
  "iteration": 491,
  "phase": "extended",
  "final_iterations_remaining": 0,
  "passed": true,
  "notes": "Iter491 TEACHER — ESCALATION: $snapshots split-quote regression THIRD recurrence cure. PRIMARY: added TOP-OF-FILE LEADING CANONICAL block in r17 (lines 42-115, inserted BETWEEN TL;DR and §Common-myths, the FIRST substantive content after the TL;DR) titled 'CRITICAL QUOTING RULE — read THIS FIRST before writing ANY metadata-table query' with full keyword anchoring in the header (list snapshots, snapshot history, show snapshots, $snapshots, $history, $files, $partitions, $manifests, $refs, $properties, metadata table, diff snapshots, what changed between versions, audit trail). Block contains: (a) the WHOLE-TOKEN-ONE-QUOTE-PAIR rule explicit and abstract; (b) the canonical 'list snapshot history' query VERBATIM `SELECT snapshot_id, committed_at, operation, summary FROM iceberg.analytics.\"events$snapshots\" ORDER BY committed_at DESC;`; (c) one example each for $files and $partitions; (d) full DO-NOT-WRITE bans covering split-quote (the recurring regression — `events.\"$snapshots\"`), bare-dollar (`events.$snapshots`), Spark-dotted (`events.snapshots`), and table-and-suffix-each-quoted (`\"events\".\"$snapshots\"`) — each with the precise Trino 467 parse-error explanation; (e) keyword anchors restated mid-block; (f) trino.io/docs/current/connector/iceberg.html verification cite; (g) cross-refs to the deeper mid-file canonical (§ Iceberg metadata tables cheat sheet) and the snapshot-DIFF section so the deeper content isn't duplicated. The intent: the responder's keyword path for ANY 'snapshot'/'metadata'/'history'/'$xxx' question hits the correct whole-token quoting BEFORE any other section. SECONDARY: added TYPE-ERROR DO-NOT-WRITE for `cardinality(element_at(map, key))` to r09-lakehouse-schema-design.md inside the existing § 'CRITICAL — use element_at(), NOT [], for MAP access in Trino' (new subsection 'Existence check on a MAP key — the CORRECT form, and a common TYPE ERROR to avoid') — element_at on MAP(K,V) returns V (scalar), cardinality() accepts only ARRAY/MAP not scalars; correct existence check is `element_at(map, key) IS NOT NULL`. Verified at trino.io/docs/current/functions/map.html (`element_at(map(K,V), key) -> V`) and trino.io/docs/current/functions/array.html (cardinality(x) on array/map only). Also added DO-NOT-WRITE for bracket-IS-NOT-NULL (errors on missing key) and contains(map_keys(x), k) (legal but slow). VERIFIED no live split-quote SQL remains in r17 — all 17 occurrences of `.\"$snapshots\"`/`.\"$files\"`/`.\"$partitions\"` etc are inside DO-NOT-WRITE / WRONG / FAILS / SQL error / MALFORMED contexts at lines 87-92, 106, 805-813, 967, 2473, 2595-2597, 3287. §13.x federation guardrails in r22 UNTOUCHED. Federation 4.49944/310 row NOT probed/disturbed. Files modified: resources/17, resources/09. No fresh unvetted claims. All edits WebSearch-verified at trino.io/docs/current before write."
}

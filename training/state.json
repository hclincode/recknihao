{
  "iteration": 510,
  "phase": "extended",
  "final_iterations_remaining": 0,
  "passed": true,
  "notes": "Iter510 TEACHER — OPTIONAL low-priority polish (iter509 was 4.94 STRONG PASS, zero defects). ONE tightly-scoped findability/correctness improvement. **GREP TRIAGE of the 3 candidates from directive**: (a) r17 `$partitions` current-spec caveat — GAP CONFIRMED at canonical home. `grep -n '\\$partitions\\|partition spec\\|partition evolution\\|#12323\\|current spec' resources/17-iceberg-table-maintenance.md` (1190-1192 area showed r17's existing two `$partitions` gotchas: #1 bucket-transform shows the BUCKET INTEGER not the original value; #2 stats reflect committed snapshots only). NEITHER gotcha mentioned the current-spec / partition-evolution caveat (#12323). The companion canonical at r05:2306 ALREADY has a clean current-spec callout — but r17 is the LEADING CANONICAL for `$partitions` per the iter509 `$partitions`-vs-`$files` callout (r17:1051-1080), so Haiku responders keyword-route to r17 first and missed this caveat there. **(b) r07 §1a.2 `array_agg(x ORDER BY x)` determinism** — ALREADY adequately covered at r23:921 (DO-NOT-WRITE matrix row: '`array_agg(col ORDER BY ts)` — always specify ORDER BY when order matters'). No urgent gap at r07 since the r23 row carries the canonical, and adding a duplicate at r07 would reduce findability not increase it (reconcile-don't-append guidance). **(c) MAP_AGG/MULTIMAP_AGG cross-apply** — ALREADY covered explicitly at r07:161 (last sentence of §1a.2 mental-model paragraph: 'Same fix applies to `MAP_AGG`, `MULTIMAP_AGG`, and any other collection aggregate over a LEFT-JOIN unmatched side'). Iter509 responder already cross-applied the FILTER fix to map_agg/multimap_agg correctly because the cross-note is present. No further edit needed. **CHOSE (a)** — the only candidate with a real gap at the canonical home + reconciles with existing r05 canonical (r05 had it, r17 didn't) + #12323 is well-documented + matches iter509's `$partitions` polish direction. **EDIT (reconcile-in-place at r17:1192-1194, NEW gotcha #3 inserted IMMEDIATELY after existing gotcha #2 and BEFORE the `Quick reference` heading)**: ADDED 'gotcha #3 `$partitions` reflects the table's CURRENT partition spec only' covering: (1) keyword-anchored sentence — after partition evolution, files written under an OLD spec may show NULL for newly-added partition fields and do NOT regroup under the new transforms; (2) trinodb/trino #12323 hyperlink reference; (3) cross-ref to existing § 'Evolving the partition spec on Trino 467' canonical at r17:396 (preserves the locked partition-evolution canonical untouched); (4) the mixed-spec mechanism — pre-evolution files keep their `spec_id` and appear in `$partitions` with NULLs in the new fields; (5) per-partition aggregates are NOT a true full-history breakdown until you rewrite old-spec files via Spark `CALL iceberg.system.rewrite_data_files(table => '...', options => map('rewrite-all', 'true'))`; (6) the fallback — use `$files` (one row per file regardless of spec) with `GROUP BY spec_id`, or query the data directly. Total +2 lines (1 new gotcha row + 1 blank line). Comfortably under the <=8 line budget. **WEBSEARCH VERIFICATION**: WebSearched 'Trino Iceberg $partitions metadata table current partition spec evolution NULL old spec files #12323'. CONFIRMED hit on github.com/trinodb/trino/issues/12323 'Iceberg `$partitions` metadata table only uses the current Spec'. Verbatim issue: 'The Iceberg table `$partitions` metadata table only shows partition values in the partition column based on the current PartitionSpec.' Confirms (1) the current-spec-only behavior, (2) #12323 is the canonical reference, (3) the NULL-for-new-partition-fields phenomenon for old-spec files. Also cross-checked trino.io/docs/current/connector/iceberg.html (Iceberg connector docs reachable from search results) and apache iceberg spec confirming the `partition-specs` array storing the historical spec list — Trino's `$partitions` view only joins against the current spec entry. All SQL in the edit is valid Trino 467 dialect (`SELECT ... GROUP BY spec_id FROM \"<t>$files\"`, Spark `CALL iceberg.system.rewrite_data_files(table => '...', options => map('rewrite-all', 'true'))`). **Files modified (1)**: resources/17-iceberg-table-maintenance.md (+2 lines at line 1192-1194: gotcha #3 — `$partitions` reflects current spec only / #12323). DID NOT run git commit/push — orchestrator handles single commit per iteration after judge. **UNTOUCHED per directive (full list)**: r22 §13.x federation guardrails (NO federation probe, federation rubric row stays 4.49944/310 — confirmed no edits to resources/22); r07 §5 Pattern B2 YoY/MoM; r07 §1a UNNEST + §1a.1 clause-order + §1a.2 array_agg-FILTER (locked iter509 canonical — confirmed no edits to resources/07); r17 CRITICAL QUOTING RULE (r17:42-113 — untouched, edit at line 1192 is far below) + iter509 `$partitions`-vs-`$files` callout (r17:1051-1080 — untouched, edit at 1192 is below); r23 §3.1A split-family; r27 §4.1A DECODE-NULL; r27 §4.x ADD_MONTHS/last_day_of_month; r27 §4.6B MERGE; r27 §6.7A dbt-test-COUNT; r27 §6.7D dbt-build-seeds; r27 §6.7E dbt-unit-tests; r23 DF checklist; iter495 dbt-trino partitioning-key; iter496 r28 GROUPING-bitmask; iter498 r13 MERGE engine-note; iter499 r03 no-index + r28 §3.3/§3.3A materialization; iter500 r09 dbt-snapshot anchor; iter503 r13 on_schema_change + r27 §4.5A md5/surrogate-key; iter508 r17 RENAME safe-rename (r17:344-area — untouched) + r07 date_trunc return-type. §13.x federation guardrails NOT TOUCHED."
}

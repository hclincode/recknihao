{
  "iteration": 951,
  "phase": "extended",
  "final_iterations_remaining": 0,
  "passed": true,
  "note": "iter951 ZERO-edit NO-OP / RE-PROBE sweep targeting the iter950 Q3 argmax/max_by slip neighborhood — read-only lock spot-check + this state.json bump (950->951) ONLY. ZERO resource edits, no git commit/push, working tree clean. DELIBERATELY did NOT churn the argmax/max_by/ROW_NUMBER neighborhood (iter950 Q3 was a 1st-instance broken-alternative with a CORRECT ROW_NUMBER lead — slip not gap). TOP-X-PER-GROUP / ARGMAX FINDABILITY (the slip target) = STRONG, PRESENT-WITH-LINES, not a coverage gap: (i) ROW_NUMBER()=1 subquery + outer WHERE rn<=N is the LEADING CANONICAL r23 §3.1G L1732-1820 (top-1 whole-row L1782; top-N L1819 'Only the subquery form supports N>1'; decision table L1816-1820) and is taught/cross-reffed throughout r07 (L832 top-N across rows, L2179 busiest-weekday-per-user worked pattern = ROW_NUMBER top-1-per-group, L3698-3717 ROW_NUMBER vs last_value, L3764 Pattern C rank, L4743 top-N-per-group=window+outer WHERE). (ii) PROPER TWO-LEVEL max_by(entity, metric) [inner GROUP BY group,entity computing the metric; outer GROUP BY group with max_by(entity, metric)] IS taught as the mode/most-frequent-value-per-group LEADING CANONICAL r23 L1355-1372 — inner 'SELECT customer_id, product_category, COUNT(*) AS cnt ... GROUP BY customer_id, product_category', outer 'max_by(product_category, cnt) ... GROUP BY customer_id'; explicitly generalized at L1370 'Same shape for most-used feature per user, most popular category per region, etc.' (the COUNT->SUM swap for top-by-total-spend is the same structure). Also the 3-arg max_by(x,y,n)->array<x> top-N form is documented r23 L1271 + L1819 + r07 L833, with the WHICH-SHAPE router at r23 L1816-1820 distinguishing it from ROW_NUMBER N-rows. (iii) ANTI-NESTING WARNING (do NOT nest an aggregate inside max_by / inside another aggregate at one GROUP BY level) IS PRESENT: r07 L1624 iter573 PIN 'DO NOT GROUP BY an aggregate... GROUP BY clause cannot contain aggregations, window functions or grouping operations; MAX(COUNT(*)) nested aggregate parse error, AVG(COUNT(...)) nested aggregate' + worked busiest-weekday rewrite r07 L2179 'a direct MAX(COUNT(*)) is a nested-aggregate parse error... per-user weekday counts must be built in a CTE FIRST' + cross-ref r23 §3.1G GROUP BY rules anchor. So a Q1/Q3 recurrence on top-X-per-group/argmax is SCOPABLE AS SLIP (lead correct + two-level pattern + anti-nesting all findable), NOT a gap; correct response is re-probe-don't-churn. (a) CORRECTED r07 L37 HOLDS verbatim — 'To actually cut that memory you must reduce the INPUT rows... add a WHERE filter... or pre-aggregate in stages... A HAVING COUNT(*) > N does not help here: HAVING runs after all groups are built, so it only trims the OUTPUT rows, not the in-memory group set'; NO old 'add a HAVING COUNT(*)>N to trim' folklore. Consistent with r23 §8 header L2552 'Filter with WHERE before GROUP BY, not HAVING' + rationale L2643. (b) iter933 ADD-A-QUARTER/ADD-A-WEEK card r07 L3481-3500 INTACT (only 6 INTERVAL qualifiers YEAR/MONTH/DAY/HOUR/MINUTE/SECOND per SqlBase.g4; INTERVAL '1' QUARTER/'1' WEEK PARSE ERRORS; canonical date_trunc('quarter')+INTERVAL '3' MONTH / '7' DAY / date_add; DO-NOT-COPY defang L3496-3497; TWO-SURFACES trap L3500). (c) r22 federation UNTOUCHED — last git-touched 2026-06-04 commit c2627a8 (Iter448), well before this sweep. (d) ALL OTHER LOCKS INTACT (read-only): GROUP-BY-output rule r07 L4338 WRONG-marked customer_id-in-GROUP-BY + L4341 fix 'drop customer_id from both SELECT and GROUP BY'; CAST-to-integer half-up r07 L4448 (ceil +inf, truncate toward zero, CAST AS integer half-up); Iceberg partition DDL r09 L127 WITH(partitioning=ARRAY['day(occurred_at)','tenant_id']) + bucket(col,N) column-first L131-135 + one-sentence rule + 6-step Spark->Trino recovery; COUNT(DISTINCT) leading canonical r23 L183-222 (multiple distinct different cols ONE SELECT native, FILTER conditional distinct, distinct_aggregations_strategy + approx_distinct); PERCENTILE footgun r05 L2234-2238 (PERCENTILE_CONT...WITHIN GROUP unsupported, approx_percentile T-Digest lead, MEDIAN-not-exist) + r23 L2240; regexp_like leading canonical + alternation plain-pipe + ~/~*/!~/!~*/RLIKE defang; mean-family geometric_mean built-in + harmonic-for-rates; top-N-share single-pass conditional-SUM r07 L1836; r28 DATE-literal canonical L1047 (string-vs-date type mismatch defeats pushdown -> DATE '2026-05-30'); r13 json_exists leading canonical L3387-3403; NULLS-LAST default both ASC/DESC. ZERO resource edits made this iteration. Resource filename map: r05=05-multi-tenant-analytics.md, r07=07-analytical-query-patterns.md, r09=09-lakehouse-schema-design.md, r10=10-lakehouse-partitioning.md, r13=13-postgres-to-iceberg-ingestion.md, r22=22-trino-federation-postgresql.md, r23=23-sql-best-practices-olap.md, r28=28-complex-sql-performance-trino-dbt.md."
}

{
  "iteration": 461,
  "phase": "extended",
  "final_iterations_remaining": 0,
  "passed": true,
  "notes": "Iter461 TEACHER — light cleanup of two iter460 MINOR imprecisions + reinforcement of two low-buffer PASSED topics (storage-sizing 4.5072/9 datapoints, cost-considerations 4.2079/18 datapoints — the two lowest-buffer PASSED rows). Federation NOT probed; stays at recorded near-miss 4.49944/310.\n\n(a) Iter460 MINOR-imprecision cleanup #1 — NULLS-default 'standard SQL' / 'ANSI' phrasing in r27/r23: GREP CONFIRMED no resource ever called NULLS-LAST-by-default 'the ANSI/standard SQL default' — the existing r27 line 169 (LEADING CANONICAL DO-NOT-WRITE table) already correctly says 'ANSI SQL leaves the NULLS-default implementation-defined.' The iter460 imprecision was the RESPONDER paraphrasing 'Trino (and standard SQL)' on its own — not a resource sourcing it. PROMOTED the ANSI-implementation-defined fact to a NAMED CALLOUT immediately after the DO-NOT-WRITE table for findability: 'Named callout — ANSI SQL does NOT pin a default for NULLS ordering' with verbatim quotes from postgresql.org/docs/current/queries-order.html ('By default, null values sort as if larger than any non-null value; that is, NULLS FIRST is the default for DESC order, and NULLS LAST otherwise.'), learn.microsoft.com (SQL Server NULLs as lowest value; no NULLS FIRST/LAST syntax support — verified), docs.snowflake.com (Snowflake DEFAULT_NULL_ORDERING parameter — configurable, not a fixed default — verified). Added EXPLICIT BAN row to the DO-NOT-WRITE table for 'Trino (and standard SQL) defaults to NULLS LAST' / 'Trino and standard SQL both default NULLS LAST' (the exact iter460 phrasing) with the correct framing: bind the claim to Trino docs, NOT to 'standard SQL'.\n\n(b) Iter460 MINOR-imprecision cleanup #2 — storage-sizing dimensionally-wrong formula in r11: GREP found r11 line 9 ('raw row bytes × row count ÷ compression ratio') and line 21 ('avg raw bytes per row × number of rows ÷ compression ratio') — BOTH technically correct (per-row bytes × row count = total raw bytes, the dimensionally-correct form) but PHRASED AMBIGUOUSLY enough that the responder paraphrased 'raw bytes' (total) × 'row count' (the wrong double-count form). RECONCILED IN PLACE (not appended): line 9 rewritten to present BOTH equivalent forms (Form A total-form: on_disk ≈ total_raw_bytes ÷ compression_ratio; Form B per-row-form: on_disk ≈ avg_bytes_per_row × row_count ÷ compression_ratio) with explicit 'these are algebraically identical because total_raw_bytes = avg_bytes_per_row × row_count' and an explicit ban on multiplying total_raw_bytes BY row_count. The '## The sizing formula' section (lines 21+) rewritten to present BOTH forms side-by-side with worked examples (50 GB CSV → 7.1 GB Iceberg using Form A; 200M rows × 200 B/row → 5.7 GB Iceberg using Form B); added a 5-row DO-NOT-WRITE table banning the 5 most-common dimensional errors (the iter460 double-count form, the ambiguous 'raw_bytes' word, missing × row_count, missing bytes term, treating compression_ratio as bytes/row); added a dimensional-sanity-check section showing how to verify units reduce to plain bytes.\n\n(c) Low-buffer-topic reinforcement: cost-considerations is the LOWEST-buffer PASSED topic (avg 4.2079, 18 datapoints — the lowest avg in the rubric). Added a NEW LEADING CANONICAL COST WORKED EXAMPLE for 'How do I find the most expensive single Trino queries (by CPU and bytes scanned)?' immediately before the existing '## Three cost layers' section. The recipe distinct from the per-tenant chargeback recipe (sorts by total_cpu_sec / gb_scanned per single query, not grouped by tenant). Uses ONLY documented Trino 467 columns: query_id, state, \"user\" (double-quoted — bare user is current_user builtin, silent-wrong), source, query, created (on system.runtime.queries); task_id, query_id, split_cpu_time_ms, physical_input_bytes (on system.runtime.tasks — JOINed). physical_input_bytes verified per Release 330 (Feb 2020) trino.io release notes. split_cpu_time_ms is documented in the system.runtime.tasks schema (queryable via DESCRIBE). Includes interpretation table (CPU-bound vs I/O-bound patterns + cross-refs to r18/r24/r28), the $/vCPU-second derivation (same as per-tenant section — bind to cluster fixed cost ÷ vCPU-seconds-available), and a 7-row DO-NOT-WRITE banning the most-frequently-invented columns (cost_usd, total_bytes_scanned on queries, peak_memory_bytes, cpu_time on queries directly, EXPLAIN ANALYZE for historical queries, sorting by elapsed_time instead of total_cpu_sec, using system.runtime.queries for >15 minute windows).\n\n(d) Storage-sizing reinforcement: the r11 rewrite (above, item b) is the storage-sizing reinforcement — it converts a single ambiguous formula into TWO clearly-named forms with worked examples for each input scenario (CSV file size, schema + row count). The DO-NOT-WRITE block targets the iter460 Q3 imprecision verbatim.\n\n(e) Reconciliation discipline (fix/remove stale, NOT append): r11's old single-formula 'on-disk size = (avg raw bytes per row × number of rows) ÷ compression ratio' (line 21) was REPLACED in place with the two-forms presentation — the prior text is gone, not stranded. The TL;DR bullet on line 9 was REPLACED with the structured two-forms version, not appended. r27's existing DO-NOT-WRITE table at line 169 was EXTENDED in place with a new row for the iter460 'Trino (and standard SQL)' phrasing (not a new table created). r16's '## Three cost layers' section was preserved verbatim — the new LEADING CANONICAL block was INSERTED immediately before it as a top-level section with its own '---' separator. No content duplicated or stranded.\n\n(f) §13.x FEDERATION GUARDRAILS UNTOUCHED: `git diff --stat resources/22-trino-federation-postgresql.md` returns EMPTY. ALL CLAIMS WEBSEARCH-VERIFIED: PostgreSQL ORDER BY NULLs default verified verbatim at postgresql.org/docs/current/queries-order.html ('By default, null values sort as if larger than any non-null value; that is, NULLS FIRST is the default for DESC order, and NULLS LAST otherwise.'). SQL Server NULLs-as-lowest-value behavior + no-NULLS-FIRST/LAST-syntax verified at learn.microsoft.com. Snowflake DEFAULT_NULL_ORDERING parameter (configurable, not a fixed default) verified at docs.snowflake.com/en/sql-reference/constructs/order-by. Trino default NULLS LAST regardless of direction verified verbatim at trino.io/docs/current/sql/select.html. Trino system.runtime.tasks.physical_input_bytes verified per trino.io/docs/current/release/release-330.html (added Feb 2020). NO FRESH UNVETTED CLAIMS: every fact in the named callout and the LEADING CANONICAL cost worked example is doc-verified. CITATION HYGIENE checked: no `::` cast, no `/*+ hints */`, no QUALIFY, no `ANALYZE TABLE` (only `ANALYZE table_name` Trino syntax), no native-Iceberg names (`write.parquet.compression-codec`, `properties=map()`, `TBLPROPERTIES`, `timestamp_ms`), no Trino 468+/479+ syntax (no ADD COLUMN ... DEFAULT, no ALTER COLUMN SET DEFAULT, no Iceberg format-v3 features) in any new content. All SQL is valid Trino 467 dialect — NULLS FIRST/NULLS LAST, JOIN, GROUP BY, INTERVAL, double-quoted user, system.runtime.queries/tasks JOIN. No SQL Server / Snowflake / PostgreSQL syntax leaked into new content. CROSS-DIALECT-SPILLOVER class GUARDED: §4.4B remains untouched; new content in the named callout explicitly CITES the engine + docs URL for each NULLs claim, never welds 'Trino' and 'standard SQL' together.\n\n(g) FILES MODIFIED: (i) resources/11-lakehouse-storage-sizing.md — TL;DR formula bullet rewritten with two-forms structure; '## The sizing formula' section rewritten as Form A / Form B with separate worked examples + DO-NOT-WRITE table + dimensional sanity check; existing `user_events` / `feature_usage` / `subscription_changes` worked examples below preserved verbatim. (ii) resources/16-cost-considerations.md — new '## LEADING CANONICAL COST WORKED EXAMPLE — most expensive single queries' section inserted before '## Three cost layers'; existing chargeback LEADING CANONICAL preserved verbatim. (iii) resources/27-oracle-plsql-to-dbt-trino.md — DO-NOT-WRITE table at line 169 extended with new ban for 'Trino (and standard SQL)' phrasing; new Named callout 'ANSI SQL does NOT pin a default for NULLS ordering' added immediately after the DO-NOT-WRITE table with the 5-engine matrix (Trino / Oracle / PostgreSQL / SQL Server / Snowflake) — each with verbatim doc citation; existing LEADING CANONICAL section content, §4.4B, §7A.2 callout preserved verbatim. NO OTHER FILES MODIFIED. r22 federation untouched per directive. Iteration bumped 460 → 461."
}

{
  "iteration": 460,
  "phase": "extended",
  "final_iterations_remaining": 0,
  "passed": true,
  "notes": "Iter460 TEACHER — PRIMARY FIX for iter459 Q4 cross-dialect-spillover fab (responder claimed 'Trino defaults NULLS LAST for ASC and NULLS FIRST for DESC' — that's Oracle's rule, NOT Trino's). Trino's documented default per trino.io/docs/current/sql/select.html verbatim: 'The default null ordering is NULLS LAST, regardless of the ordering direction.' Trino defaults NULLS LAST for BOTH ASC and DESC. Oracle defaults NULLS LAST for ASC, NULLS FIRST for DESC. This is the 3rd confirmed cross-dialect-spillover variant after iter456 Q2 (/*+ USE_HASH_JOIN */ Oracle/Spark hint) and iter456 Q4 (::VARCHAR Postgres cast). Federation NOT probed; stays at recorded near-miss 4.49944/310.\n\n(a) LEADING CANONICAL block 'Oracle vs Trino NULLS-default semantics in ORDER BY (read this BEFORE migrating any ORDER BY ... DESC query)' installed at TOP of resources/27-oracle-plsql-to-dbt-trino.md — inserted between the 'Common myths' table (ends line ~38) and the previous '## 1. The mindset shift' header (now line ~189). Section title verbatim: '## LEADING CANONICAL — Oracle vs Trino NULLS-default semantics in ORDER BY (read this BEFORE migrating any `ORDER BY ... DESC` query)'. Content: (i) the two-engine side-by-side facts table with WebSearch-verified verbatim quotes from trino.io/docs/current/sql/select.html and docs.oracle.com — Trino: 'The default null ordering is NULLS LAST, regardless of the ordering direction.' Oracle: 'if the null ordering is not specified, then the handling of the null values is NULLS LAST if the sort is ASC, NULLS FIRST if the sort is DESC.'; (ii) silent-wrong worked example with a small tasks table containing NULLs — same SQL `SELECT * FROM tasks ORDER BY priority DESC` produces NULLs at the TOP on Oracle and NULLs at the BOTTOM on Trino, no error message; (iii) defensive rule — ALWAYS write explicit NULLS FIRST / NULLS LAST when migrating Oracle ORDER BY, including inside window-function `OVER (... ORDER BY ...)` clauses (with a worked ROW_NUMBER() OVER (... ORDER BY ts DESC NULLS FIRST) example); (iv) 4-step migration audit checklist for grep'ing legacy Oracle ORDER BY ... DESC and rewriting Trino targets; (v) DO-NOT-WRITE table with 5 banned claims — 'Trino defaults NULLS FIRST for DESC' (Oracle's rule), 'Trino defaults NULLS LAST for ASC and NULLS FIRST for DESC' (Oracle's rule projected onto Trino — the EXACT iter459 Q4 fab), 'Trino NULLS-default behavior matches Oracle's', 'Trino follows ANSI SQL's default for NULLS ordering' (ANSI leaves it implementation-defined), 'You don't need NULLS FIRST/LAST because Trino does the right thing by default'; (vi) cross-ref table marking this as the 3rd cross-dialect-spillover variant after /*+ hints */ and :: cast — points to §4.4B for the consolidated table.\n\n(b) §4.4B CROSS-DIALECT-SPILLOVER GUARDRAIL consolidated table extended with a new row for NULLS-default ordering (silent-wrong failure mode, contrast with Oracle's direction-dependent default, points back to the LEADING CANONICAL section). Cross-reference chain footer extended to point at the new LEADING CANONICAL NULLS section.\n\n(c) §7A.2 (Oracle analytic functions → Trino window functions) callout added immediately after the section header — 'THE NULLS-DEFAULT LANDMINE — read the LEADING CANONICAL block at the top of this resource BEFORE migrating any window function whose OVER clause uses ORDER BY ... DESC.' Cross-refs the LEADING CANONICAL section; explains the ROW_NUMBER() OVER (... ORDER BY ts DESC) assigns rn=1 to a different row on Trino vs Oracle when ts has NULLs.\n\n(d) Common myths table at top of r27 extended with a new row — 'Trino's NULLS-default ordering matches Oracle's — ORDER BY ts DESC puts NULLs at the top in both engines.' marked FALSE with the WebSearch-verified Trino docs verbatim quote and pointer to the LEADING CANONICAL section. The 'Why these myths matter' wrap-up paragraph updated to call out the NULLS-default myth as the third most-dangerous silent-failure-mode myth (alongside empty-string-is-NULL and implicit-coercion).\n\n(e) resources/23-sql-best-practices-olap.md anti-patterns table — added a new row capturing 'Assuming ORDER BY ts DESC puts NULLs at the top (Oracle's default)' as a SILENT-WRONG row-ordering on Trino, with the verbatim Trino docs quote and pointer to resource 27 § LEADING CANONICAL.\n\nFACTS WEBSEARCH-VERIFIED on 2026-06-05 via trino.io/docs/current/sql/select.html and docs.oracle.com:\n  - Trino 467 default: NULLS LAST regardless of direction (verbatim: 'The default null ordering is NULLS LAST, regardless of the ordering direction.').\n  - Oracle default: NULLS LAST for ASC, NULLS FIRST for DESC.\n  - So ORDER BY ts DESC puts NULLs at top in Oracle, NULLs at bottom in Trino — silent row-ordering difference, no error message.\n  - ANSI SQL: leaves the NULLS-default implementation-defined (per docs.oracle.com and trino.io docs phrasing).\n\n(f) STALE WRONG-NULLS-DEFAULT lines reconciled: NONE existed to reconcile. Comprehensive grep `NULLS FIRST|NULLS LAST|null ordering|null.*order` across resources/27-oracle-plsql-to-dbt-trino.md, resources/23-sql-best-practices-olap.md, and resources/28-complex-sql-performance-trino-dbt.md PRIOR to my edits returned ZERO matches in NULLS-ordering-default context. Only one unrelated reference existed: r27 line 1320 about array_join/array_agg NULL handling — NOT about ORDER BY NULLS defaults. CONFIRM: no stale wrong-NULLS-default line required reconciliation; the iter459 Q4 responder fab was self-generated, NOT seeded by stale resource content. Post-edit grep confirms all NULLS-ordering content in the touched files consistently states the correct Trino default (NULLS LAST regardless of direction) and the correct Oracle default (NULLS LAST for ASC, NULLS FIRST for DESC).\n\n(g) §13.X FEDERATION GUARDRAILS UNTOUCHED: `git diff --stat resources/22-trino-federation-postgresql.md` returns EMPTY. The Trino-default NULLS-ordering fact is WebSearch-verified against trino.io/docs/current/sql/select.html (the canonical source). The Oracle-default rule is WebSearch-verified against docs.oracle.com and the Oracle SQL Language Reference. NO FRESH UNVETTED CLAIMS — every fact in the LEADING CANONICAL block is doc-verified. CITATION HYGIENE checked: no `::` cast, no `/*+ hints */`, no QUALIFY, no `ANALYZE TABLE`, no native-Iceberg names (`write.parquet.compression-codec`, `properties=map()`, `TBLPROPERTIES`, `timestamp_ms`), no Trino 468+/479+ syntax (no ADD COLUMN ... DEFAULT, no ALTER COLUMN SET DEFAULT, no Iceberg format-v3 features) in any new content. All SQL is valid Trino 467 / Iceberg 1.5.2 dialect. NULLS FIRST / NULLS LAST are standard ANSI SQL and supported by Trino 467.\n\n(h) FILES MODIFIED: (i) resources/27-oracle-plsql-to-dbt-trino.md — new LEADING CANONICAL section inserted at line ~43 (between Common Myths and §1); §4.4B table extended with NULLS-default spillover row; §4.4B cross-ref chain extended; §7A.2 callout for NULLS-default landmine added; Common Myths table extended with NULLS-default myth row + wrap-up paragraph updated; (ii) resources/23-sql-best-practices-olap.md — anti-patterns table extended with NULLS-default row + cross-ref to r27. Iteration bumped 459 → 460. NO OTHER FILES MODIFIED."
}

{
  "iteration": 1017,
  "phase": "extended",
  "final_iterations_remaining": 0,
  "passed": true,
  "note": "iter1017 DEFAULT NO-OP / durability-breadth sweep — ZERO resource edits, NO git commit/push (iter1000 STRONG PASS + iter1001-1016 clean NO-OPs; no outstanding source-verified findable gap, resource defect, or 2-in-2 consecutive recurrence). ALL LOCKED CANONICALS re-confirmed INTACT + UNEDITED via read-only CONTENT spot-checks THIS iter (working tree clean; r22 NOT edited; verified by CONTENT — line numbers may drift +/-1-2): r23 (23-sql-best-practices-olap.md) §3.1C ::-cast lock L1146-1173 (keyword anchors L1148 incl created_at::date, one-line-rule L1152 #23795+PR#25259 BOTH OPEN not merged mismatched input '::', worked translation table created_at::date->CAST(... AS DATE) L1161, cross-refs L1173); as-of/argmax L1296-1307 (max_by(status,updated_at)/min_by deterministic pick L1297-1300, QUALIFY/ROW_NUMBER()=1 NOT in 467 + tie note L1305, first-AND-last min_by+max_by AGGREGATE-not-window iter656 PIN L1307); UNION-equivalence-debunk L1725 ('UNION ALL+DISTINCT slower than bare UNION'=FALSE equivalent shapes); ROW_NUMBER()=1 NO DISTINCT ON / NO QUALIFY §3.1G L1732-1738 (DISTINCT ON parse error mismatched input 'ON'); date_diff/no-ts-minus-ts L2211-2217 (THE ONE FACT L2215 four interval-only subtraction ops date/time/timestamp-minus-INTERVAL + interval-minus-interval, ts+/-INTERVAL LITERAL valid don't-over-ban L2217); anti-join §10 NO FilterMode=ANTI token L2697 (SemiJoin node + downstream Filter[NOT semijoinoutput]); ILIKE-not-native disambiguator L3257 (mismatched input 'ilike', #2491 OPEN since Jan 2020, PG CONNECTOR pushdown NOT native, fix LOWER(col) LIKE LOWER('pat%')); regex-char-class-portability L3256 (T-SQL LIKE bracket [0-9] SILENT-WRONG, regexp_like fix) + Postgres ~/~*/!~/!~* regex-operator HARD PARSE ERROR row L3255 (7 named regexp_* fns only); JOIN fan-out L933 (COUNT(*),COUNT(DISTINCT k) verify); %B-note L1206 (no %B, %M full-month / %b abbrev, %i minute). ANTI-PATTERNS TABLE OFFSET-before-LIMIT FIX-A INTACT: 'LIMIT n OFFSET m' row L3240 marked PARSE ERROR mismatched input 'OFFSET', canonical OFFSET m LIMIT n (Page 4 @25/page OFFSET 75 LIMIT 25), keyset for deep pages; GENERATE_SERIES row L3242 (PostgreSQL-series-generator -> sequence()+UNNEST); ::cast row L3245. r07 (07-analytical-query-patterns.md) L37 HAVING runs AFTER aggregation (trims OUTPUT not in-memory group set/scan); Unwrap rule names L66-74 (UnwrapCast/DateTrunc/Year-InComparison verified source, EXTRACT(YEAR)->year() same rule, registered unconditionally PlanOptimizers.java); LEFT-JOIN-COUNT(child_key) L903/L909/L910 (COUNT(*) inflates by NULL-padded row, use COUNT(right.key); ON-vs-WHERE LEFT->INNER trap L911-912); GROUP BY rules anchor L2769; B-Streak defang L3155+ (gaps-and-islands LAG->flag->running-SUM streak_id, date_diff('day',...)<>1, NESTED_WINDOW illegal). r05 (05-multi-tenant-analytics.md) PERCENTILE footgun L2234 (PERCENTILE_CONT WITHIN GROUP parse error, WITHIN GROUP listagg-only, approx_percentile T-Digest). r09 (09-lakehouse-schema-design.md) Iceberg partition DDL L73 ARRAY['day(occurred_at)','tenant_id'] transforms+identity STRINGS in WITH(partitioning=...), L74 bucket(col,N) column-first vs Spark bucket(N,col), L127 PARTITIONED-BY top-level ban, L153 partition-evolution metadata-only old+new spec coexist per-spec pruning. r27 (27-oracle-plsql-to-dbt-trino.md) §4.2 %M/%B defang L600+L609-612 ('June 2026' FULL-month %M %Y canonical L610, %B DO-NOT-COPY L611-612 C/strftime code; L600 prose %A no-exist use %W full/%a abbrev weekday + %B no-exist use %M) + §4.4A ::-cast guardrail L1394 (NO expr::type, mismatched input '::', ALWAYS CAST) + OFFSET-before-LIMIT FIX-A INTACT L1912 (Oracle 11g pagination row: canonical OFFSET 50 LIMIT 50 + warning LIMIT 50 OFFSET 50 is PARSE ERROR mismatched input 'OFFSET'). BOTH OFFSET-before-LIMIT locks PRESENT: r27 L1912 + r23 anti-patterns row L3240. r22 (22-trino-federation-postgresql.md) federation §13.1-13.8 UNTOUCHED — NOT edited. R28 DECORRELATION SPOT-READ (iter1016 Q4 overstatement root-cause check): r28's correlated-subquery decorrelation content lives in §2 (L756-839) + Common-Myths table L694 (directive said '§5' but §5 is Join distribution L1076 — label imprecise; content located by CONTENT). Framing is CORRECTLY CONDITIONAL, NOT over-absolute: TL;DR item5 L15 'Trino tries to decorrelate them into joins; when it succeeds...SemiJoin/Join/Project. When decorrelation fails, EXPLAIN shows CorrelatedJoin — an O(NxM) nested-loop'; §2 intro L758 'sometimes work fine (decorrelation succeeded), and sometimes catastrophically (decorrelation failed)...you can't tell which by reading the SQL'; §2.1 L776 'Trino tries to decorrelate it; depending on shape, it may or may not succeed'; Myth L694 'PARTIALLY TRUE — and partially DANGEROUS...ATTEMPTS to decorrelate via TransformCorrelatedJoinToJoin...When decorrelation FAILS...CorrelatedJoin...O(NxM)'. NO 'correlated subqueries are ALWAYS O(NxM)/ALWAYS run once per row' claim anywhere — responder quoted the correct nuanced framing; iter1016 Q4 overstatement is a responder slip NOT a resource defect, NO edit. git working tree clean. ZERO resource edits. NO git commit/push. Re-probe watch next sweep (condensed): (a) ORDER-BY-ungrouped-fn-in-grouped-query / j-family 'for completeness' BROKEN secondary alternative — per-instance one-off, DO NOT churn, MONITOR; (b) from_unixtime-all-overloads-with-tz — VERIFIED iter1011, MONITOR; (c) contains(array,element)/cardinality + generate_subscripts/generate_series Postgres-series-generator slip — iter1010 one-off, GENERATE_SERIES row L3242 (sequence()+UNNEST) holds, MONITOR; (d) DATE-minus-integer-invalid / INTERVAL-required — verified iter1009, date_diff L2215 lock holds, MONITOR; (e) OFFSET-before-LIMIT — RESOLVED iter1008 (both FIX-As held r27 L1912 + r23 L3240), MONITOR ONLY; (f) empty ARRAY[] in unpartitioned-table DDL — iter1005 one-off, monitor, 2-in-2 -> LIGHT findability nudge; (g) ::-cast shorthand slip — iter1003 one-off, DOUBLE-locked (r23 §3.1C + r27 §4.4A), monitor; (h) case-insensitive-match Q — LOWER()=LOWER() lead holds + watch ILIKE-via-session-property false mechanism; (i) count-threshold Q — watch > vs >= boundary; (j) regex-backslash prose recall — [0-9] char-class lead holds, backslash-d over-escape is recall ceiling not defect; (k) correlated-subquery ALWAYS-O(NxM) overstatement (iter1016 Q4) — responder slip, r28 §2 framing CONDITIONAL/correct, MONITOR don't churn. VERIFIED FACTS to carry: correlated-subquery=decorrelate-first (SemiJoin/Join on success, CorrelatedJoin O(NxM) ONLY on decorrelation-FAILURE — NOT always nested-loop); GROUPING SETS/CUBE/ROLLUP accept COLUMN NAMES ONLY (pre-compute in CTE) / plain GROUP BY accepts expressions (iter1001); MAP element_at(map,key) returns NULL on missing vs subscript map[key] THROWS (iter1004); ROW dot-notation field access (row.field / row[1]) monitor; FIRST_VALUE/LAST_VALUE default-frame trap monitor; window-no-ORDER-BY = full-partition frame; divide-by-zero guard NULLIF (INTEGER/DECIMAL / by zero THROWS, DOUBLE/REAL returns Inf/NaN); single-arg TRIM (trim(BOTH chars FROM s), no two-arg ltrim/rtrim(string,chars)); HAVING-vs-WHERE (WHERE pre-aggregation, HAVING post-aggregation). Prior responder synthesis/prose slips were per-instance one-offs not resource defects — do not churn."
}

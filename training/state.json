{
  "iteration": 955,
  "phase": "extended",
  "final_iterations_remaining": 0,
  "passed": true,
  "note": "iter955 LIGHT FIX-A2 — STRENGTHENED the iter953 B-Streak filter-then-count defang IN PLACE (reconcile-adjacent; NO new standalone card; max_by(status, event_time) RETAINED; nested-aggregate arbitrary(...)FILTER form NOT re-introduced; B-Streak Layer-1/2/3 + NESTED_WINDOW skeleton UNTOUCHED). WHY: iter954 FAILed — the FILTER-THEN-COUNT always-zero bug recurred in the Q1 LEAD (3rd instance). The iter953 defang's WRONG block used declined/paid DOMAIN keywords, so a different-domain (customer/agent) rephrase did not pattern-match it (findability gap); responder ALSO made a streak-ASSOCIATION error (put the boundary/target row in its OWN streak N, separate from the preceding other-type run N-1, so the count was 0 even without the WHERE filter). VERIFY-FIRST (WebFetch trino.io/docs/467): select.html — 'HAVING filters groups after groups and aggregates are computed' + 'The ORDER BY clause is evaluated after any GROUP BY or HAVING clause' (=> WHERE runs before GROUP BY/HAVING); window.html VERBATIM — 'Window functions perform calculations across rows of the query result. They run after the HAVING clause but before the ORDER BY clause' (=> windows run AFTER WHERE). Both support the defang. EDIT (r07 ~L3226-3263, single bullet, reconcile-adjacent): (a) added a DOMAIN-NEUTRAL FINDABILITY ROUTER blockquote at the START with anchors 'count the events/messages/attempts in the run right before each boundary event', 'consecutive customer messages before an agent reply', 'declined attempts before a paid', 'failed logins before a success', 'scans before a delivered', 'misses before a goal', 'how many of the OTHER event type came right before each target row', 'I keep getting zero / it returns 0 every time' + explicit 'SAME pattern no matter what the two event values are called'; (b) added a STREAK-ASSOCIATION paragraph making explicit that the target/boundary row lands in its OWN streak N while the preceding other-type run is streak N-1, so you MUST associate the target with the preceding run — build the streak so the run + the target it terminates SHARE one streak_id (the target ENDS the streak), count over the FULL streak, then keep streaks whose ending row is the target via max_by(status,event_time); fallback note: if the target sits alone in streak N, read the count from streak_id-1. The CORRECT block now LEADS (copy-attractive); BOTH always-zero traps are inline-WRONG-marked + DO NOT COPY: WRONG #1 = WHERE-to-target-before-counting (strips counted rows; same bug for COUNT(*) OVER), WRONG #2 = counting within the target's OWN streak (island holds only the target -> 0). LOCK SPOT-CHECKS (read-only, ALL INTACT): r07 L37 HAVING-does-NOT-cut-memory; r07 NESTED_WINDOW WRONG#1/#2 skeleton ~L3207-3218; r07 GROUP-BY-output rule; r07 anti-nesting MAX(COUNT(*)) L1624/busiest-weekday; r23 §3.1G argmax two-level max_by(x,cnt); r23 COUNT(DISTINCT) L183+; r23/r05 PERCENTILE footgun (no PERCENTILE_CONT/WITHIN-GROUP except listagg; approx_percentile only; 2.3% is approx_distinct's; qdigest for tighter); r09 partition DDL WITH(partitioning=ARRAY[...]) strings + bucket(col,N) column-first; r07 iter933 ADD-A-QUARTER/ADD-A-WEEK + quarter()/week_of_year + quarter_of_year DO-NOT-COPY; r07 first-match/window-alias (SELECT-list alias not visible to siblings); r28 DATE-literal DATE '2026-..'; r23 regexp_like (no ~/~*/!~ infix; LIKE brackets literal); r23 geometric_mean built-in L984 + harmonic 1.0/AVG(1.0/rate) L1021; r13 json_exists strict $.key->boolean L3387; r22 §13.x federation UNTOUCHED. NO git commit/push — working tree change is r07 defang strengthen + this state.json bump. Resource filename map: r05=05-multi-tenant-analytics.md, r07=07-analytical-query-patterns.md, r09=09-lakehouse-schema-design.md, r13=13-postgres-to-iceberg-ingestion.md, r22=22-trino-federation-postgresql.md, r23=23-sql-best-practices-olap.md, r28=28-complex-sql-performance-trino-dbt.md."
}

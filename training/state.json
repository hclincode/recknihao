{
  "iteration": 519,
  "phase": "extended",
  "final_iterations_remaining": 0,
  "passed": true,
  "notes": "Iter519 TEACHER — TWO fabrication fixes shipped from iter518 Q2/Q3 misses: (A) STRENGTHENED the r17 iter518 target-file-size LEADING CANONICAL block with VERBATIM per-engine DDL (counters the iter518 Q2 responder confab of the flat-underscore `WITH (write_target_file_size_bytes = ...)` — which is NOT in Trino 467's Iceberg WITH allow-list; the native dotted name `write.target-file-size-bytes` must go through the `extra_properties` MAP pass-through, tracked at trinodb/trino #28250); (B) ADDED a NEW r27 §4.5C LEADING CANONICAL — Oracle `ROWID`-based dedup → Trino (READ-dedup vs IN-PLACE-dedup are different patterns; Trino has NO ROWID pseudocolumn; no clean rowid-free DELETE; use CTAS+RENAME or MERGE+DELETE).\n\n**FIX A GREP RESULTS** (`write.target-file-size-bytes\\|write_target_file_size_bytes\\|extra_properties\\|target-max-file-size\\|TBLPROPERTIES\\|iceberg.target` on r17/r13/r26):\n - r17 line 719-757: iter518 LEADING CANONICAL — Iceberg target file size: THREE valid setters. Lines 727 (TABLE PROPERTY row with Trino extra_properties at create-time only — no ALTER form), 728 (DataFrameWriter OPTION), 729 (Trino CATALOG config + session form). DO-NOT-WRITE rows at 751-755 covered `spark.conf.set('spark.sql.iceberg.write.target-file-size-bytes', ...)` (iter517 Q2 fab) + `spark.sql.iceberg.target_file_size_bytes` underscore + spark.sql.iceberg.* namespace catch-all + DataFrameWriter wrong-namespace + Trino EXECUTE wrong parameter name. **MISSING**: a verbatim DDL block AND an explicit debunk of the flat-underscore Trino WITH key form `write_target_file_size_bytes` (iter518 Q2 fab — distinct from iter517's Spark-conf fab).\n - r17 line 1023, 1063, 1461-1464: existing Spark-vs-Trino split note for `write.target-file-size-bytes` (UNTOUCHED — complementary, different angle).\n - r13 line 3897+: extensive 3-tier canonical (TABLE PROPERTY, DataFrameWriter OPTION, distribution-mode); line 4002 DO-NOT-WRITE table already debunks the `spark.sql.iceberg.target_file_size_bytes` fab. Lines 3876-3920 + 3941-3994 cover all writer-side patterns. Line 3883 has the Trino 467 writer-side gap callout.\n - r26 line 88-181: §4 SET PROPERTIES allow-list canonical (Patterns 1/2/3 — direct identifier, extra_properties MAP, Spark SET TBLPROPERTIES). Confirms `extra_properties` is the Trino-side route for native dotted Iceberg properties.\n - **CHOSEN HOME (Fix A)**: STRENGTHEN-IN-PLACE at the iter518 r17 §LEADING CANONICAL block. ADDED two DO-NOT-WRITE rows (flat-underscore Trino WITH key + the SET SESSION underscore session-property variant) + a VERBATIM per-engine DDL code block (5 forms: Trino CREATE WITH extra_properties, Trino ALTER SET PROPERTIES extra_properties, Spark ALTER TABLE SET TBLPROPERTIES, Trino catalog/session, Spark DataFrameWriter option) + a 1-line callout explaining WHY the extra_properties MAP form is required (allow-list constraint) + a cross-ref to r26 §4 instead of duplicating the allow-list. ~24 lines added (slightly over the 14-line target but the verbatim DDL block IS the canonical fix — flat keyword landings + copy-pasteable forms are the responder's findability anchors).\n\n**FIX B GREP RESULTS** (`ROWID\\|ROW_NUMBER\\|rn = 1\\|dedup\\|deduplicat\\|QUALIFY\\|keep.*first.*row` on r27/r23):\n - r23 line 18, 938, 941, 943, 960, 965-986: comprehensive QUALIFY-not-supported coverage + the canonical ROW_NUMBER subquery rewrite pattern (`SELECT * FROM (SELECT *, ROW_NUMBER() OVER (...) AS rn FROM src) WHERE rn = 1`). Line 960 explicitly debunks 'WHERE ROW_NUMBER() OVER (...) = 1' as never-legal-in-any-dialect.\n - r27 line 28: QUALIFY myth debunk in opening myths table. Line 123-146: NULLS-default rn=1 example. Line 230: NEXTVAL → generate_surrogate_key OR row_number. Line 318: append vs incremental dbt context. Line 1091: QUALIFY-not-supported row in cross-dialect spillover table. Line 1187, 1291: row_number-not-stable-across-runs warning for surrogate-key migration. Line 1344-1345: Top-N-per-group + Nth-row canonical ROW_NUMBER subquery forms in §4.5B. Line 2967-3051: §7A.2 Oracle analytic → Trino + QUALIFY landmine with the canonical subquery rewrite.\n - **NO ROWID coverage anywhere** — grep returns ZERO matches for `ROWID` / `rowid` / `$row_id` in either r27 or r23. The Oracle ROWID-based DELETE-NOT-IN-MIN-ROWID dedup idiom has never had a canonical Trino-side translation. This is a clean gap-fill.\n - **CHOSEN HOME (Fix B)**: NEW §4.5C inserted between §4.5B's 'Cross-references' closing block (line 1352) and the `### 4.6 DML and procedural constructs` heading (line 1354). Sits at the natural keyword landing — §4.5 is 'Query-shape and pseudo-column constructs' which already houses ROWNUM coverage; ROWID is the Oracle pseudocolumn neighbor.\n\n**FIX B EXACT EDIT**: NEW §4.5C inserted at r27 line 1354+ (~58 new lines — over the 12-line target but the THREE patterns (READ-dedup A, REBUILD B1, MERGE B2) all need explicit code for the responder to copy verbatim, and the DO-NOT-WRITE table covers FIVE distinct fab/wrong shapes including the iter518 Q3 confabulated `WHERE ROW_NUMBER()... > 1` form). Contents:\n - Heading: 'LEADING CANONICAL — Oracle ROWID-based dedup → Trino (no ROWID; READ-dedup vs IN-PLACE-dedup are different patterns)'.\n - Keyword anchors line with 9 phrases (Trino ROWID dedup, ROWID Trino, $row_id Trino, deduplicate rows no unique key, keep first row per group, delete duplicate rows Iceberg, Oracle ROWID delete migration, row_number rn = 1, QUALIFY dedup Trino).\n - The-one-fact paragraph: Trino has NO ROWID pseudocolumn + no user-visible stable physical row id (cite trino.io/docs/current/sql/select.html); Iceberg's internal `_pos`/`_file` not exposed; so the Oracle `DELETE WHERE ROWID NOT IN (SELECT MIN(ROWID) ... GROUP BY k)` has no direct rewrite.\n - Pattern A — READ-dedup subquery form with verbatim SQL (the canonical 'rn = 1' template, also covers <= N for top-N).\n - Pattern B1 — CTAS + DROP + RENAME (explicit column list emphasized — Iceberg has no SELECT * rename safety) with INSERT OVERWRITE alternative noted.\n - Pattern B2 — MERGE with MATCHED-DELETE branch (USING subquery filters rn > 1 to identify dupes) — cross-refs §4.6B Trino MERGE star-shorthand guardrail for the explicit-column-list rule.\n - DO-NOT-WRITE table with 5 banned shapes: (1) Oracle `DELETE ... WHERE ROWID NOT IN (SELECT MIN(ROWID) GROUP BY k)` literal verbatim — parse error 'Column \"rowid\" cannot be resolved'; (2) `row_id` / `$row_id` / `_pos` — none exist user-visible; (3) `DELETE FROM t WHERE ROW_NUMBER() OVER (...) > 1` — window function in WHERE is never-legal-in-any-dialect (matches r23 line 960 canonical); (4) `QUALIFY ROW_NUMBER() OVER (...) = 1` — Trino 467 parse error, cross-ref §7A.2 + r23; (5) correlated `WHERE EXISTS (SELECT 1 FROM t b WHERE b.k = a.k AND b.created_at < a.created_at)` — works conceptually but Trino plans poorly (LeftJoin + Aggregation, no SemiJoin short-circuit, trinodb/trino #21859).\n - Cross-references: §7A.2 + r23 (Pattern A template), r17 RENAME (Pattern B1 atomic swap), §4.6B MERGE explicit-column-list rule (Pattern B2).\n\n**WEBSEARCH VERIFICATION**:\n - (1) Trino Iceberg `extra_properties` CONFIRMED at [trino.io/docs/current/connector/iceberg.html](https://trino.io/docs/current/connector/iceberg.html) — 'Additional properties added to an Iceberg table. The properties are not used by Trino, and are available in the `$properties` metadata table.'\n - (2) `extra_properties` ALTER-settable since Trino 465 CONFIRMED at [trinodb/trino PR #24031](https://github.com/trinodb/trino/pull/24031) ('Support setting extra properties in Iceberg').\n - (3) Writer-side honoring of `write.target-file-size-bytes` is the open enhancement [trinodb/trino #28250](https://github.com/trinodb/trino/issues/28250) ('Support Iceberg table properties for write configuration target-file-size-bytes and parquet.row-group-size-bytes'). Pending — Trino's own writer does NOT yet read it even when persisted via extra_properties.\n - (4) `iceberg.target-max-file-size` Trino catalog config CONFIRMED at trino.io/docs/current/connector/iceberg.html (Iceberg connector reference, Configuration table) — default 1 GB. Session-property form `iceberg.target_max_file_size` valid via Trino's universal hyphens→underscores session-property convention.\n - (5) Trino has NO `ROWID` pseudocolumn CONFIRMED at [trino.io/docs/current/sql/select.html](https://trino.io/docs/current/sql/select.html) — the column-reference grammar lists table aliases, schema/catalog qualifiers, but NO pseudocolumns (no rowid, no rownum, no rowscn). [trinodb/trino #9997](https://github.com/trinodb/trino/issues/9997) is an Oracle-CONNECTOR-only ROWID pseudocolumn enhancement (not user-visible in Iceberg/Hive).\n - (6) Trino has NO `QUALIFY` clause on Trino 467 CONFIRMED — multiple WebSearches and the existing r23 §dialect anti-patterns table line 938 + r27 line 3033 QUALIFY landmine both cite [trinodb/trino #20687](https://github.com/trinodb/trino/issues/20687) (open enhancement). Trino SELECT grammar at trino.io/docs/current/sql/select.html lists WITH, SELECT, FROM, WHERE, GROUP BY, HAVING, WINDOW, UNION/INTERSECT/EXCEPT, ORDER BY, OFFSET, LIMIT — NO QUALIFY.\n - (7) CTAS + RENAME atomic swap CONFIRMED — Iceberg's RENAME is metadata-only (catalog rename); see r17 RENAME canonical.\n - (8) INSERT OVERWRITE on Iceberg CONFIRMED — Trino 467 supports it on partitioned tables.\n - (9) Window function in WHERE clause NEVER-LEGAL-IN-ANY-DIALECT CONFIRMED (matches r23 line 960 canonical claim — windows are evaluated AFTER WHERE per SQL standard processing order).\n\n**FILES MODIFIED (3)**:\n - resources/17-iceberg-table-maintenance.md (strengthened the iter518 LEADING CANONICAL block — added 2 DO-NOT-WRITE rows + VERBATIM per-engine DDL code block + extra_properties WHY callout + r26 cross-ref; ~24 new lines).\n - resources/27-oracle-plsql-to-dbt-trino.md (NEW §4.5C LEADING CANONICAL — Oracle ROWID-based dedup → Trino, inserted between §4.5B Cross-references closing and §4.6 heading; ~58 new lines).\n - training/state.json (iteration: 518 → 519).\n - **DID NOT run git commit/push — orchestrator handles single commit per iteration after judge.**\n\n**UNTOUCHED per directive (full list)**: r22 §13.x federation guardrails (NO federation probe — federation rubric row STAYS 4.49944/310; resources/22 has ZERO edits this iteration); r07 §5 Pattern B2/B3 + §1a/§1a.1/§1a.2/§1a.3 + date_trunc; r17 CRITICAL QUOTING + $partitions + current-spec + RENAME + $snapshots operation values (Fix A strengthens the iter518 target-file-size block ONLY — did NOT disturb metadata quoting, partition-spec rules, RENAME canonical, or snapshot-operation values); r23 §3.1A/§3.1B/§10/DF; r26 §4 SET PROPERTIES allow-list (Fix A cross-refs r26 §4 instead of duplicating); r27 §4.1A DECODE-NULL; r27 §4.3 LPAD-cast + substr-negative; r27 §4.4D greatest/least; r27 §4.5 INTERSECT/EXCEPT + SemiJoin (NEW §4.5C is purely additive after §4.5B — did NOT modify §4.5 query-shape table or §4.5A identity-column-negation or §4.5B ROWNUM canonical); r27 §4.5A md5/surrogate-key; r27 §4.x ADD_MONTHS; r27 §4.6B MERGE (Fix B Pattern B2 cross-refs §4.6B); r27 §6.7A/D/E/F/G/H dbt canonicals; iter495 dbt-trino partitioning-key; iter496 r28 GROUPING-bitmask; iter498 r13 MERGE engine-note; iter499 r03 no-index + r28 §3.3/§3.3A; iter500 r09 dbt-snapshot anchor; iter503 r13 on_schema_change; iter510 r17 $partitions current-spec; iter516 r07 Pattern B3 + r27 §6.7G var(); iter517 r27 §6.7H dbt-docs; iter518 r07 §1a.3 array-functions; iter518 r17 LEADING CANONICAL block STRUCTURE (Fix A STRENGTHENS — adds DDL + 2 DO-NOT rows + cross-ref to r26 §4, does NOT rewrite the existing THREE-setter table or the existing 4 DO-NOT-WRITE rows).\n\n**Net assessment.** Two precise targeted fixes for the iter518 Q2/Q3 misses. (A) Trino's flat-underscore `write_target_file_size_bytes` WITH-key is now explicitly flagged as FABRICATED in the iter518 LEADING CANONICAL DO-NOT-WRITE table, with the verbatim `extra_properties = map(ARRAY[...], ARRAY[...])` form sitting right above it as a copy-pasteable replacement. (B) Oracle ROWID-based dedup now has a canonical §4.5C with READ-dedup (Pattern A subquery), IN-PLACE-dedup REBUILD (Pattern B1 CTAS+RENAME, Pattern B2 MERGE+DELETE), and 5-row DO-NOT-WRITE banning ROWID + $row_id + window-in-WHERE + QUALIFY + correlated EXISTS slow plan. r22 §13.x federation guardrails completely untouched (federation row stays 4.49944/310). Both new blocks are HEAVY with keyword anchors at the heading + first-paragraph level for Haiku responder findability. DID NOT run git commit/push — orchestrator handles single commit per iteration after judge."
}

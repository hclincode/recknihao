{
  "iteration": 541,
  "phase": "extended",
  "final_iterations_remaining": 0,
  "passed": false,
  "notes": "Iter541 TEACHER — Three fixes (1 HIGH + 1 MED + 1 LOW). FIX A (HIGH, PRIMARY — iter540 Q4 3.25 lowest): added a new LEADING CANONICAL to r10 (immediately after the `Setting the partition spec` section, before `Common myths`) — 'bucket(N, tenant_id) vs identity(tenant_id): which partition transform for a multi-tenant Iceberg table (iter541 PIN)'. The canonical fixes the iter540 wrong recommendation of `identity(tenant_id)` for high-cardinality tenant_id (10k+ tenants × day partitions = partition/tiny-file explosion). Block includes: (1) DECISION RULE — identity only for LOW cardinality (≲ a few hundred: status, region, plan_tier); bucket(N, tenant_id) with N=16/32/64 for HIGH cardinality. (2) IN-LINE SIGNAL — full multi-tenant Trino DDL example carrying the corrective comment directly on the `partitioning = ARRAY[...]` line: `-- bucket(N) for high-cardinality tenant_id; NOT identity(tenant_id) (10k tenants -> partition/tiny-file explosion)`. (3) Pruning-still-works paragraph quoting trino.io verbatim ('The data is hashed into the specified number of buckets. The partition value is an integer hash of x, with a value between 0 and nbuckets - 1 inclusive.') to refute the common worry that bucket() defeats `WHERE tenant_id = 'acme'` pruning. (4) DO-NOT-WRITE 4-row table banning: 'identity(tenant_id) is default best-practice' (FALSE), 'bucket() defeats pruning' (FALSE — Iceberg hashes the literal to its bucket), Hive-ism 'Iceberg adds partition directories like ADD PARTITION' (WRONG vocab — Iceberg tracks via manifests), 'bigger N is safer' (WRONG — bucket count IS partition count). (5) Cross-refs to existing r10 Choosing-partition-columns, Bucket-partitioning-footguns, r17 small-files/compaction, iter537 PIN for migrating an existing identity(tenant_id) live spec to bucket(N, tenant_id).\n\nFIX B (MEDIUM — dbt 1.10+ freshness YAML nesting): tightened r27 §6.7B 'Declaring freshness on a source' paragraph at L2389. Updated 'introduced in dbt 1.9+' to 'In dbt 1.10+, BOTH `loaded_at_field:` AND `freshness:` are nested under `config:` as siblings on the source (and on each `table:` for per-table overrides)' with WebFetch-verified canonical YAML shape and the parenthetical note '(The keys moved under `config:` in dbt v1.10; the older top-level / sibling-of-`config:` form still parses but emits a `PropertyMovedToConfigDeprecation` warning — prefer the `config:`-nested form in new code.)'. The §6.7B YAML example at L2434-2457 already correctly nests both keys under `config:` — no example rewrite needed. Did NOT touch §6.7K command-card per directive.\n\nFIX C (LOW — dbt contract framing): added a one-paragraph clarification block to r27 §6.7C 'What a contract is and when it fires' subsection (inserted after the existing 'build-time check ... not query-time check' paragraph, before 'Why it matters on this stack'). The clarification distinguishes the dbt CONTRACT (STRUCTURAL check at COMPILE time — column name + data_type + declared constraint DDL; does NOT scan data values) from the NOT-NULL DATA rejection (fires at INSERT/MERGE inside Trino via the Iceberg NOT NULL column DDL). Both can fail `dbt build` but by DIFFERENT mechanisms — STRUCTURAL preflight (no data scan) vs Iceberg write-side DATA rejection. Quotes verbatim from docs.getdbt.com/reference/resource-configs/trino-configs ('only constraints with `type` as `not_null` are supported') confirming primary_key / unique are metadata-only on dbt-trino. Did NOT rewrite the existing §6.7C constraint canonical (preserved verbatim — the new line is a single inserted paragraph before 'Why it matters on this stack').\n\nGREP FINDINGS:\n  - r10: existing partition-evolution iter537 PIN at L57-82 preserved verbatim; existing 'Common myths' table mentions bucket-vs-identity at L45-47 but didn't have a leading canonical with the in-line signal — the iter540 Q4 wrong-recommendation gap. New canonical inserted at L34 between the 'Setting the partition spec' tail (L32) and 'Common myths' (L36 was original; now L36 area). Existing 'Choosing partition columns' at L789 and 'Bucket partitioning — the two production footguns' at L841 preserved verbatim, now cross-referenced from the new canonical.\n  - r27: §6.7B at L2374 freshness canonical; the 'under `config:` wrapper introduced in dbt 1.9+' line at L2389 was tightened in-place to dbt 1.10+ + both-keys-under-config. The §6.7B YAML example at L2442-2456 is already correctly nested under `config:` per WebFetch verification of the canonical shape — no example edit needed. §6.7C at L2496 — single-paragraph clarification inserted at L2520-area (between the existing build-time-vs-query-time paragraph and 'Why it matters on this stack'). §6.7K command-card at L3199 preserved verbatim per directive.\n\nWEBSEARCH/DOC VERIFICATION (2026-06-06):\n  - WebFetch trino.io/docs/current/connector/iceberg.html — VERBATIM bucket transform: 'The data is hashed into the specified number of buckets. The partition value is an integer hash of x, with a value between 0 and nbuckets - 1 inclusive.' Trino syntax: 'bucket(x, nbuckets)'. ALTER TABLE syntax: 'ALTER TABLE table_name SET PROPERTIES partitioning = ARRAY[<existing partition columns>, ...]'. system.bucket('trino', 16) SQL function exposed for inspection. Docs are silent on explicit high-cardinality identity anti-pattern but the partition-count math (days × distinct_values) is self-evident from the partitioning model.\n  - WebFetch docs.getdbt.com/reference/resource-properties/freshness — VERBATIM dbt 1.10+ YAML shape: under `sources: -name: ... config: freshness: warn_after: ... error_after: ... filter: ... loaded_at_field: ...` — BOTH keys nested under `config:` as siblings; per-table override under `tables: -name: ... config: freshness: ... loaded_at_field: ...`. v1.10 also adds `loaded_at_query` as alternative to `loaded_at_field`.\n  - WebFetch docs.getdbt.com/reference/resource-configs/trino-configs — VERBATIM: 'The `dbt-trino` adapter supports model contracts. Currently, only constraints with `type` as `not_null` are supported. Before using `not_null` constraints in your model, make sure the underlying connector supports `not null`, to avoid running into errors.' Confirms primary_key / unique are metadata-only on dbt-trino.\n  - All SQL / YAML valid for Trino 467 + dbt-trino 1.9+/1.10+ + Iceberg 1.5.2.\n\nFILES MODIFIED (2): resources/10-lakehouse-partitioning.md (new LEADING CANONICAL — bucket(N, tenant_id) vs identity(tenant_id) decision block with in-line signal + 4-row DO-NOT-WRITE table), resources/27-oracle-plsql-to-dbt-trino.md (§6.7B dbt 1.10+ both-keys-under-config tightening + §6.7C contract-vs-NOT-NULL-data-rejection framing paragraph).\n\nUNTOUCHED per directive:\n  - resources/22 §13.x federation guardrails — ZERO edits; federation rubric row stays 4.49944/310; NO federation probe.\n  - r07 §1a-§1a.4 + Pattern C4 + approx + date_trunc(return-type + week-Monday) + §5 B2/B3 + FILTER alias canonical — preserved.\n  - r09 element_at + map-HOF + CAST-to-JSON + dbt-snapshot SCD2 (4-default) + COALESCE-default — preserved.\n  - r10 iter537 PIN partition-evolution nuance, Common myths table, Choosing partition columns, Bucket partitioning footguns — preserved (new canonical is additive at top; cross-refs to existing sections).\n  - r13 from_unixtime epoch-ms + JSON family — untouched.\n  - r17 CRITICAL QUOTING + $partitions + current-spec + RENAME + $snapshots ops + target-file-size + WAP + $refs + $files/$history JOIN + iter534 in-line signal + deleted_snapshot_id + 5d-retention — untouched.\n  - r23 §3.1A/§3.1B/§3.1C HALF_UP/§10/DF + approx_percentile family + §4 EXPLAIN-variants table — preserved.\n  - r24 EXPLAIN coverage — preserved.\n  - r26 §4 — untouched.\n  - r27 §4.1A/§4.3 string table + §4.3-STR-FAMILY/§4.3A/§4.4*/§4.5*/§4.x/§4.6B/§7A.2A-B/§6.7A/§6.7D-J/§6.7K — preserved. §6.7B is tightened in-place (one-paragraph rewrite at L2389; YAML example unchanged). §6.7C has a single inserted paragraph; the existing canonical body (Required YAML keys, Constraints-on-dbt-trino table, Failure mode, Worked example, When-to-use, DO-NOT-WRITE) is preserved verbatim.\n  - iter495-540 locks preserved.\n\nDID NOT run git commit or git push — orchestrator handles single commit per iteration after judge. DID NOT touch resources/22. All SQL/YAML/bash valid for Trino 467 / dbt-trino 1.10+ / Iceberg 1.5.2."
}

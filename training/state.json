{
  "iteration": 502,
  "phase": "extended",
  "final_iterations_remaining": 0,
  "passed": true,
  "notes": "Iter502 TEACHER — OPTIONAL LOW-priority polish after iter501 4.80 STRONG PASS. PRIMARY: fixed dbt-seeds-vs-build-vs-run framing per directive. CORRECT semantics confirmed via WebSearch on docs.getdbt.com/reference/commands/build and docs.getdbt.com/docs/build/seeds: `dbt build` runs seeds + models + snapshots + tests TOGETHER in DAG order by default (no --select needed); `dbt seed` runs seeds only; `dbt run` runs models only and does NOT include seeds. GREP FINDINGS: `grep -rn 'dbt seed|dbt build|dbt run|seeds/|seed ' resources/` returned the canonical site at resources/27-oracle-plsql-to-dbt-trino.md §6.7D dbt-seeds CANONICAL (lines 2480-2577). Within that section, lines 2550-2556 (Step 5) and the DO-NOT-WRITE table row at line 2574 contained the bug: line 2550 said `dbt seed is NOT run by dbt run or dbt build by default` (half-wrong — correct for run, WRONG for build); line 2553 comment `# or: dbt seed && dbt build` implied build needs pre-seeding (FALSE); line 2556 `single dbt build with --select seeds+ to include seeds and their dependents` implied build needs explicit seeds selection (FALSE — DAG order by default); line 2574 row claimed `plain dbt build or dbt run without explicit seed selection does NOT re-run dbt seed` (WRONG re: build). NOTE: line 2230 in the SAME file (in §6.7B source-freshness DO-NOT-WRITE table) was already CORRECT — it says `dbt build runs models + tests + snapshots + seeds, but NOT dbt source freshness` (lists seeds — consistent with the fix). Also grepped r28 for dbt build/seed mentions — r28 only mentions `dbt build` in performance/cost framing contexts (lines 172, 1134), no build-vs-seed semantics to fix. OPTIONAL SECONDARY (bare-integer date subtraction): `grep -rn 'current_date - |parse error|bare integer' resources/` returned r07 §405-407 which is ALREADY CORRECT — labels `current_date - 30` as 'parse/type error' with explicit `parse/type` framing (line 405) + reinforcement that the only banned form is the bare integer (line 407), and r07 §761 labels the analogous `cur.month - 12` as 'Type error — TIMESTAMP - INTEGER is not a defined operation in Trino'. Both correct, no edit needed. EDITS (2, both reconcile-in-place per directive — NO new sections, NO duplicate blocks, tight 1-2 line corrections): EDIT 1 — resources/27-oracle-plsql-to-dbt-trino.md lines 2550-2556 (Step 5 in §6.7D): REPLACED the misleading 'dbt seed is NOT run by dbt run or dbt build by default' framing with the CORRECT 'dbt build runs seeds + models + snapshots + tests together in DAG order; dbt run runs models only and does NOT include seeds' framing, including the docs.getdbt.com/reference/commands/build citation, a 3-command bash block (dbt build = recommended; dbt seed && dbt run = run-only path; dbt seed alone = load seeds only). EDIT 2 — resources/27-oracle-plsql-to-dbt-trino.md line 2574 (DO-NOT-WRITE row in §6.7D): REPLACED the WRONG row 'Seeds are loaded automatically during dbt run or dbt build' / 'dbt build does include seeds IF you pass --select seeds+' with the CORRECT row 'dbt build does NOT run seeds — you have to pass --select seeds+' as the banned claim, with the verdict 'Wrong per docs.getdbt.com/reference/commands/build — plain dbt build (no --select) runs seeds + models + snapshots + tests together in DAG order across the whole project — seeds are loaded automatically; the half-truth is dbt run alone (models only, no seeds)'. WEBSEARCH VERIFICATION (2 fetches): (1) docs.getdbt.com/reference/commands/build confirmed verbatim 'dbt build runs models, tests tests, snapshots snapshots, seeds seeds, builds user-defined functions — In DAG order, for selected resources or an entire project' AND showed the example output `1 of 7 START seed file dbt_jcohen.my_seed... [RUN]` then `1 of 7 OK loaded seed file dbt_jcohen.my_seed... [INSERT 2 in 0.09s]` followed by view model — proving plain `dbt build` (no --select) loads seeds first as part of the DAG. (2) docs.getdbt.com/docs/build/seeds confirmed `dbt seed` is a separate command that loads CSVs, `dbt run` runs models only (does NOT include seeds), and showed the `dbt seed --select country_codes` + `dbt run --select country_codes+` patterns for selective execution. Both edits Trino 467 / dbt-trino dialect-clean (only command-name strings in code blocks; no SQL changed). Files modified (1): resources/27-oracle-plsql-to-dbt-trino.md (§6.7D lines 2550-2556 + line 2574). UNTOUCHED (per directive): r22 §13.x federation guardrails (file unchanged, 66 §13.x refs preserved); r07 §5 Pattern B2 YoY/MoM; r27 §4.1A DECODE-NULL; r27 §4.6B MERGE star-shorthand guardrail; iter495 dbt-trino `partitioning`-key canonical; iter496 r28 GROUPING-bitmask canonical; iter498 r13 MERGE engine-note; iter499 r03 no-index + r28 §3.3/§3.3A materialization canonicals; iter500 r09 dbt-snapshot Findability anchor + cross-refs; iter501 r27 §6.7A / r09 dbt-test-COUNT mechanics (lines 1932-2113 in r27 + r09 line 899 — both pre-existing iter501 edits left intact). r27 §6.7B line 2230 source-freshness DO-NOT-WRITE row already CORRECT (`dbt build runs models + tests + snapshots + seeds, but NOT dbt source freshness`) — left untouched, consistent with the fix. No federation probe; no new sections; no duplicate seed blocks created."
}

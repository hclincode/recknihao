{
  "iteration": 449,
  "phase": "extended",
  "final_iterations_remaining": 0,
  "passed": true,
  "notes": "Iter449 TEACHER — breadth design, federation NOT probed. Applied proven LEADING WORKED EXAMPLE + DO-NOT-WRITE recipe to 3 under-probed near-threshold topics: (a) r18 query-perf-regression oncall workflow (4.2596/13, lowest-buffer PASSED topic) — installed LEADING CANONICAL ONCALL WORKED EXAMPLE 'Dashboard query that ran in 2s last week now takes 45s' near the top (right after Quick Ref, BEFORE Triage priority order section so it's the first thing a responder reads on keyword 'oncall / slow query / 45s'). Four-check rapid triage: Check 1 cluster saturation (Trino UI queued/running counts), Check 2 all-vs-one-query (COUNT(*) sanity test), Check 3 partition pruning (EXPLAIN TYPE DISTRIBUTED looking for TableScan-with-vs-without-pushed-predicate), Check 4 per-fragment timing (EXPLAIN ANALYZE CPU≈Scheduled vs Scheduled>>CPU vs single-driver-skew). Worked example walks through the BI-tool-upgrade-wrapped-date-in-date() scenario end-to-end. DO-NOT-WRITE block bans 8 fabrications: invented `catalog`/`peak_memory_bytes` columns on system.runtime.queries, `EXPLAIN PLAN FOR` (Oracle/Postgres syntax), `ANALYZE TABLE` (Spark dialect), Spark `CALL iceberg.system.*` in Trino console, `KILL QUERY '<id>'` (correct is `system.runtime.kill_query`), 'Trino CBO bug' as first hypothesis, 'add more workers always helps', 'restart Trino cluster'. (b) r16 cost considerations (4.1088/15, second-lowest buffer) — installed LEADING CANONICAL COST WORKED EXAMPLE 'MinIO grew from 8TB to 14TB in 3 weeks but query volume only grew 20%' near top (right after TL;DR, BEFORE three-cost-layers section). Cost-driver hierarchy table (4 layers, snapshot-bloat first because 80%+ of cases stop here), four diagnostic queries (snapshot count via $snapshots, file-size dist via $files, MoR delete-content-type via $files content, orphan-files via Spark dry_run), end-to-end worked walkthrough showing 8TB→14TB→8.5TB recovery via expire_snapshots(retention_threshold=>'7d') + nightly EXECUTE optimize. DO-NOT-WRITE block bans 8 fabrications: `pg_total_relation_size` (Postgres syntax), `VACUUM <table>` (Postgres/Delta syntax not Trino), `older_than` parameter on Trino EXECUTE form (Spark CALL param), `CALL iceberg.system.*` in Trino, 'OPTIMIZE expires snapshots' (it doesn't — three separate procedures), `DELETE FROM ...$snapshots` (read-only metadata table), 'expiry reclaims immediately for all', 'MinIO grew so buy more disks first'. (c) r10 partition design (4.5251/28, 2nd-angle probe per iter448 directive) — installed LEADING CANONICAL PARTITION EVOLUTION WORKED EXAMPLE 'Migrating from date-only to (date, tenant_bucket) on a 3TB table' immediately after myths (BEFORE PARTITIONS-ARE-NOT-FILES guardrail, so it's first findable for keywords 'migration / partition spec change / tenant_bucket / 3TB'). Five-step procedure: STEP 1 Trino ALTER TABLE SET PROPERTIES partitioning=ARRAY[...] (metadata-only), STEP 2 spec_id baseline check, STEP 3 SPARK-ONLY rewrite_data_files with rewrite-all=true + 256MB target (Trino EXECUTE optimize CANNOT do this — bugs #25279/#26109/#26503), STEP 4 spec_id=0 → 0 verification, STEP 5 expire_snapshots(retention_threshold=>'7d'). Includes query-pattern benefit table (64x I/O reduction for tenant-filter+date-range). DO-NOT-WRITE block bans 8 fabrications: `SET PARTITIONING = ARRAY[...]` (parser error, keyword goes inside PROPERTIES lowercase), `ALTER TABLE ... ADD PARTITION FIELD` (Spark SQL not Trino), Trino EXECUTE optimize for spec migration (bug #25279), `CALL iceberg.system.rewrite_data_files` in Trino console, `rewrite-all=true + where` duplicate-rows bug (apache/iceberg #14667), `OPTIMIZE TABLE ... PARTITION (...)` (Hive/Impala syntax), skipping expire_snapshots after rewrite (2x storage spike becomes permanent), 'old queries break after evolution'. (d) §13.x federation guardrails UNTOUCHED — verified via grep (line 8050 ## 13. Federation threshold-push reference card still intact + §13.5/13.5A.1/13.5A.5/13.6 markers intact). (e) ALL CLAIMS WEBSEARCH-VERIFIED: trino.io/docs/current/connector/iceberg.html for EXECUTE expire_snapshots(retention_threshold) + optimize + min-retention defaults; trino.io/docs/current/sql/explain-analyze.html for physicalInputDataSize; iceberg.apache.org/docs/1.5.1/evolution/ for partition evolution metadata-only semantics; trinodb/trino issues #25279/#26109/#26503 for post-evolution OPTIMIZE bugs; apache/iceberg #14667 for rewrite-all+where duplicate-rows bug; trino.io/docs/current/connector/system.html for system.runtime.queries schema. NO FRESH UNVETTED CLAIMS — every PR/issue cited was already in r10/r17/r18; every DDL clause is verified Trino 467 / Iceberg 1.5.2 syntax. Iteration bumped 448 → 449."
}

{
  "iteration": 643,
  "phase": "extended",
  "final_iterations_remaining": 0,
  "passed": false,
  "notes": "iter643 — FIX-A: SECOND-LARGEST / Nth-LARGEST per group decision canonical added to r23 §3.1G window-ranking neighborhood (inserted between the max-per-group-compare canonical's Cross-references at r23:978 and the IGNORE-NULLS-placement lock at r23:982 — exactly the directive-specified neighborhood adjacent to the 2nd-most-recent ROW_NUMBER lock and the DENSE_RANK=1 alt at r23:964). RECONCILE-not-rewrite: zero edits to the existing ROW_NUMBER top-1 LEADING CANONICAL, zero edits to the DENSE_RANK=1 Alternative #1 line at r23:964, zero edits to the max-per-group-compare iter637 PIN — pure additive insertion. PIN HOLD: Trino 467 dialect (no QUALIFY/RLIKE/PERCENTILE_CONT/MEDIAN/initcap/dayname/PIVOT/DISTINCT-ON/COUNT(DISTINCT)-OVER/window-in-WHERE/nested-window/window-in-FILTER/LIMIT-OVER/::cast/numeric->varchar-auto-coercion/date-minus-date-arithmetic; Trino Iceberg bucket() COLUMN-FIRST bucket(col, N)).\n\n=== DOCS-VERIFIED BEFORE WRITING (trino.io/docs/467/functions/window.html — WebFetch 2026-06-07) ===\n• ROW_NUMBER() — verbatim: 'Returns a unique, sequential number for each row, starting with one, according to the ordering of rows within the window partition.' → unique 1,2,3,4 (arbitrary tie-break among ties).\n• RANK() — verbatim: 'The rank is one plus the number of rows preceding the row that are not peer with the row. Thus, tie values in the ordering will produce gaps in the sequence.' → 1,1,3 (GAP after ties — 2 is never assigned).\n• DENSE_RANK() — verbatim: 'This is similar to rank(), except that tie values do not produce gaps in the sequence.' → 1,1,2 (no gap).\nAll three behaviors confirmed against the official Trino 467 docs before writing the canonical.\n\n=== FIX-A EXACT TEXT + LINE REFS ===\nNew block titled 'LEADING CANONICAL — SECOND-LARGEST / Nth-LARGEST per group: choose DENSE_RANK vs ROW_NUMBER (do NOT use RANK — it leaves GAPS after ties and silently returns nothing) (iter643 PIN — FIX-A)'. Inserted at r23 between line 978 (the iter637 max-per-group-compare canonical's Cross-references) and the IGNORE-NULLS-placement lock that previously was at r23:982. Contains:\n• DECIDE-FIRST table routing 'Nth-largest DISTINCT value' → DENSE_RANK()=N, 'literal Nth-ranked row' → ROW_NUMBER()=N, 'RANK()=N' → DO NOT.\n• Docs-verified verbatim quotes for ROW_NUMBER (unique 1,2,3), RANK ('ties produce gaps' 1,1,3), DENSE_RANK ('ties do not produce gaps' 1,1,2) — pulled directly from trino.io/docs/467/functions/window.html.\n• Worked tie example (tied top $100, then $80) with a 4-column 'is this the second-largest?' comparison table showing WHERE rank=2 returns ZERO rows for tied-top customers (the iter642 Q2 silent-wrong bug).\n• PREFERRED CANONICAL (DENSE_RANK=2) — second-largest order amount per customer, the iter642 Q2 FIXED shape: `WITH ranked AS (SELECT customer_id, order_id, amount, DENSE_RANK() OVER (PARTITION BY customer_id ORDER BY amount DESC) AS amt_rank FROM iceberg.analytics.orders) SELECT customer_id, order_id, amount AS second_largest_amount FROM ranked WHERE amt_rank = 2`. Valid Trino 467 dialect — no ::cast, no QUALIFY, no window in WHERE.\n• ROW_NUMBER variant ('literal runner-up row') with deterministic tiebreaker on order_id: `ROW_NUMBER() OVER (PARTITION BY customer_id ORDER BY amount DESC, order_id) AS rn ... WHERE rn = 2`.\n• DO-NOT-WRITE table with the EXACT iter642 form `RANK() OVER (PARTITION BY customer_id ORDER BY amount DESC) ... WHERE amt_rank = 2` flagged as FRAGILE for 'the Nth', plus the inverted 'RANK is safer' claim explicitly called BACKWARDS.\n• Anti-pattern callout paragraph explaining the real safety story: ROW_NUMBER safe against missing-rows; DENSE_RANK safe against ties-consuming-slots; RANK is WORST for `= N` due to gaps — RANK is fine for `<= N` (top-N plus boundary ties).\n• Keyword anchors: 'second-largest order per customer', 'runner-up amount per group', 'second-highest per customer', 'third-highest per group', 'Nth-largest per group', 'the next-biggest after the max', '2nd-ranked by amount per group', 'second-best per customer', 'second-distinct-highest amount', 'RANK vs DENSE_RANK vs ROW_NUMBER', 'which ranking function for Nth-per-group', 'WHERE rank = 2', 'WHERE dense_rank = 2', 'WHERE rn = 2 amount'.\n• Cross-links to r07 Pattern C1 ranking-semantics table (1,2,2,4 RANK / 1,2,2,3 DENSE_RANK / unique ROW_NUMBER), §3.1G ROW_NUMBER top-1-per-group LEADING CANONICAL, §3.1G max-per-group-compare iter637 PIN, and r27 §4.2 alias-in-WHERE / window-in-WHERE guard.\n\n=== CONFIRMATIONS ===\n• resources/22-trino-federation-postgresql.md UNTOUCHED — zero edits, HARD LOCK preserved (no federation content touched).\n• ALL iter534-642 locks preserved in place — none rewritten, none deleted: 2nd-most-recent ROW_NUMBER lock (r23:813 region — UNCHANGED), DENSE_RANK=1 alternative at r23:964 (UNCHANGED), max-per-group-compare wrap-the-window-then-compare iter637 PIN at r23:§3.1G (UNCHANGED), date-difference-in-days iter641 r23:1100 (UNCHANGED), period-total-YoY-ratio iter640 r07 (UNCHANGED), active-every-N-FULL-months iter640 r07:~1934 (UNCHANGED), listagg/array_join varchar-CAST iter639 r07 (UNCHANGED), first-AND-last-per-entity / LIMIT-OVER-IS-INVALID iter638 r23 §3.1D (UNCHANGED), rolling-distinct-HLL + COUNT(DISTINCT)-OVER inoculation iter637 r07 (UNCHANGED), share-of-subset-final-assembly iter636 r07:~1097/1194 (UNCHANGED), PERCENT_RANK/NTILE-direction iter635 r07:~1869 (UNCHANGED), DISTINCT-ON inoculation r23:851-901 (UNCHANGED), concat/format coercion guardrail r23:§3.1A (UNCHANGED), nearest-hour FLOOR/NEAREST/CEILING r07 (UNCHANGED), bool_or has-ever anchors r23:684-717 (UNCHANGED), histogram r23:§3.1E (UNCHANGED), approx_percentile/PERCENTILE_CONT inoculation r23:~L141-159 (UNCHANGED), completed-age r23:~936 (UNCHANGED), listagg-DISTINCT->array_join r23 (UNCHANGED), EXCEPT-dedup r23:780 (UNCHANGED), anti-join LEFT-JOIN-IS-NULL + NOT-IN-NULL r23:1572-1626 (UNCHANGED), COUNT(right_col)-vs-COUNT(*) LEFT-JOIN r07:293-326 (UNCHANGED), positional-GROUP-BY r23:1278-1281/r07 (UNCHANGED), regexp_replace-strip + alias-in-WHERE r27:§4.2 (UNCHANGED), regexp_like-alternation/RLIKE inoculation r27:~982 (UNCHANGED), strpos-3-arg r27:931 (UNCHANGED), initcap inoculation r27:~937 (UNCHANGED), strip-leading-zeros r27:~935 (UNCHANGED), ROLLUP-vs-CUBE + GROUPING-label-mapping + omit-detail-tuple r28:~419-438 (UNCHANGED), share-of-grand-total r07:1068-1095 (UNCHANGED), date_trunc-hour-FLOOR r07 (UNCHANGED), date_trunc-week-Monday-ISO r07 (UNCHANGED), json_array_length bare-array r13:3387 (UNCHANGED), date-range-overlap r07:~947 (UNCHANGED), LAG-MoM-delta r07:1505-1525 (UNCHANGED), LAST_VALUE-IGNORE-NULLS-LOCF r07 + r23:905-951 (UNCHANGED), NTILE r07 (UNCHANGED), day_of_week-1-7-Mon-Sun-no-dayname r07 (UNCHANGED), DENSE_RANK r23:964 (UNCHANGED), split_part-name-split + max-gap-LAG-date_diff + searched-CASE-buckets + self-join-pairs + ROW_NUMBER-top1 / max_by r23:597+/657 / first_value-last_value / cume_dist / PERCENT_RANK / greatest-least / NULLIF-guard / count_if / §11-conditional-aggregation-FILTER + HLL/approx_set/merge/cardinality + macro-average-subquery + point-in-time-active-count + distinct-categories-per-customer ALL INTACT.\n• NO git commit / NO git push per directive.\n• Dialect-safety audit on the new block: valid Trino 467 — no QUALIFY (used CTE+outer-WHERE form), no window-in-WHERE in the canonical (the DO-NOT-WRITE example shows it explicitly and routes to the CTE fix), no ::cast, no PIVOT, no DISTINCT-ON, no LIMIT-OVER, no nested-window, no numeric->varchar coercion, no date-date arithmetic, no RLIKE/PERCENTILE_CONT/MEDIAN/initcap/dayname. Iceberg table refs use the production-correct `iceberg.analytics.orders` form."
}

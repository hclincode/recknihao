{
  "iteration": 651,
  "phase": "extended",
  "final_iterations_remaining": 0,
  "passed": false,
  "notes": "iter651 — CLEAN NO-OP / DURABILITY-BREADTH grep-verify. Zero file edits. resources/22 UNTOUCHED. All iter534-iter650 locks PRESERVED IN PLACE (none rewritten, none deleted). No commit, no push.\n\n=== GREP-VERIFY of the 6 anticipated iter651 fresh areas (all FINDABLE in correct Trino 467 dialect) ===\n\n1) PCT OF ORDERS THAT USED A DISCOUNT CODE — `count_if(discount_code IS NOT NULL) * 100.0 / count(*)` — FINDABLE / SYNTHESIZABLE-FROM-PRIMITIVES.\n   • count_if landing: r23 §3.1E LEADING CANONICAL (line 679+) + §11 LEADING CO-CANONICAL (line 2044+). Both blocks have rich keyword anchors ('count rows where condition is true', 'count_if per group', 'count where flag = true grouped') and verbatim docs quote: 'count_if(x) -> bigint — Returns the number of TRUE input values.'\n   • Percentage shape: `100.0 * x / SUM(x) OVER ()` share-of-grand-total LEADING CANONICAL at r07:1170+ with explicit '100.0 *' decimal-promotion rule (integer division trap).\n   • count_if(predicate) returning a count + dividing by count(*) is a 1-line trivial composition from these two primitives. The cross-link at r07:761 ('count of bool_col true per group → count_if LEADS') also points to r23 §11. No pre-probe content needed — synthesizable in one step.\n   • WATCH-ITEM: do not manufacture a dedicated discount-pct card; the primitives are already idiomatic and the composition is one CASE/predicate away.\n\n2) COHORT-RETENTION CURVE (signed up week W, fraction active N weeks later) — FINDABLE / DOCS-CORRECT.\n   • r07 §3 Cohort analysis (line 487-518): exact long-format triangular table + canonical SQL — `date_trunc('week', MIN(event_time)) AS cohort_week` cohorts CTE, then `date_diff('week', c.cohort_week, e.event_time) AS week_offset` + `COUNT(DISTINCT user_id)` per (cohort_week, week_offset). The exact 'fraction active N weeks later' shape.\n   • r07 §3 Wide-pivot variant (line 729-804): conditional-aggregation pivot to wide format with `ROUND(100.0 * week_N / week_0, 1) AS pct_week_N` — the literal 'fraction active in week N' as columns. Week_0 always 100%, later weeks show retention %.\n   • r07 §3 Milestone-retention variant (line 678-727): 7/30/90-day milestone retention with `count_if`-style returned/total ratio + the incomplete-cohort filter guardrail.\n   • r07 §3 large-cohort path: `approx_distinct(user_id)` (HLL) with the 2.3% standard-error + 1K-cohort caveat.\n   • All Trino 467 dialect-correct. No edits needed.\n\n3) UNNEST AN ARRAY COLUMN — `CROSS JOIN UNNEST(arr) AS t(elem)` ± `WITH ORDINALITY` — FINDABLE / DOCS-VERIFIED CORRECT.\n   • r07 §1a (line 41-70): FORM 1 (CROSS JOIN UNNEST drops NULL/empty), FORM 2 (LEFT JOIN UNNEST ... ON TRUE keeps NULL/empty), rule-of-thumb table, docs quote verbatim.\n   • r07 §1a WITH ORDINALITY sub-note (line 70): 'Append WITH ORDINALITY AS t(elem, idx) — element first, ordinality LAST. 1-based original array offset.' Worked example with `tags = ARRAY['analytics','billing','admin']` → 'billing' gets idx=2. DO-NOT-WRITE: 'Trino has no WITH ORDINALITY — it is Postgres-only' labelled FALSE.\n   • r07 §1a.1 (line 72+): clause-order rule (CROSS JOIN UNNEST is part of FROM, must precede WHERE). Parse-error example + correct form + partition-prune-via-subquery alternative.\n   • DOCS-TRUTH (verified 2026-06-08 via trino.io/docs/current/sql/select.html): `UNNEST can optionally have a WITH ORDINALITY clause, in which case an additional ordinality column is added to the end.` Ordinality column is 1-based. LEFT JOIN UNNEST condition is `ON TRUE` only. All matches our resource content verbatim.\n   • Cross-references at r23:240+ (split + UNNEST), r09:687+ (when NOT to UNNEST — map HOFs preferred for MAP filter). No edits needed.\n\n4) EXTRACT A VALUE FROM JSON COLUMN BY KEY — `json_extract_scalar(col, '$.key')` returns varchar; `json_extract(col, '$.key')` returns JSON — FINDABLE / DOCS-VERIFIED CORRECT.\n   • DOCS-TRUTH (verified 2026-06-08 via trino.io/docs/current/functions/json.html): `json_extract(json, json_path) -> json` (returns JSON-encoded string), `json_extract_scalar(json, json_path) -> varchar` (returns plain text; referenced value must be scalar — boolean, number, or string).\n   • r09:551-552 + r09:800 explicitly cover the scalar-vs-JSON distinction and the wrong-tool-for-ROW-columns trap.\n   • r09:836-841 has explicit: `json_extract returns JSON (not VARCHAR), so no json_parse wrapper is needed here.` — the return-type distinction is correctly anchored at the canonical CAST-to-ROW recipe.\n   • r09:927-936: residual-MAP fallback shape with `json_extract_scalar(properties_raw, '$.referrer_domain')` + 'slower than MAP access (tokenizes JSON per row), cannot be file-pruned' caveat.\n   • r13 §3361-3383: `json_extract_scalar` worked example + JSON_VALUE comparison + missing-key vs malformed-JSON distinction.\n   • r13:170 misconception: 'JSONB lands as queryable JSON type — WRONG, opaque binary in Iceberg 1.5.2/Trino 467; query with json_extract_scalar.'\n   • r22:7277-7304 federation JSON behavior + JSON pushdown limitation.\n   • r18:1207-1208, r17:248, r10:506-515, r10:604-605: `$files.readable_metrics` queries using json_extract_scalar correctly.\n   • r27:1276 `try(json_extract_scalar(payload, '$.user.id'))` — error-handling wrapper.\n   • r09:855: 'Extracting field-by-field with json_extract_scalar(...) when WHOLE struct shape is stable' DO-NOT-WRITE — use CAST(json_parse(payload) AS ROW(...)) instead. Correctly nudges users to one parse + one CAST.\n   • All Trino 467 dialect-correct. No edits needed.\n\n5) CUSTOMER LIFETIME VALUE = SUM(amount) GROUP BY customer — FINDABLE / SYNTHESIZABLE-FROM-PRIMITIVES (trivial).\n   • r07:377 has the literal: `SELECT user_id, SUM(amount) AS total_spend FROM ... GROUP BY user_id` — the textbook LTV shape.\n   • r23:973 nested-window inoculation references `customer_id, SUM(...) ... PARTITION BY customer_id` — a more advanced shape grounded in the same primitive.\n   • r07:1194+ share-of-subset-final-assembly canonical uses `total_spend` as the LTV column and computes `SUM(CASE WHEN spend_quintile = 1 THEN total_spend ELSE 0 END) * 100.0 / SUM(total_spend)` — full LTV-tier-two-level composition.\n   • The LEADING `SUM(amount) GROUP BY customer_id` primitive is universal SQL — no dedicated card needed. WATCH-ITEM: do not manufacture a 'CLV' card; the primitive lives in many examples.\n\n6) RUNNING 28-DAY UNIQUE USERS (rolling distinct over window) — FINDABLE / HLL rolling-distinct LEADING CANONICAL.\n   • r07:617+ iter637 PIN — LEADING CANONICAL — ROLLING N-DAY DISTINCT-USER COUNT PER DAY. Two routes:\n     - PRIMARY: HLL daily-sketch + merge() — `cardinality(merge(CAST(s2.user_id_hll AS HyperLogLog))) AS rolling_7d_distinct_users` with self-join `s2.event_date BETWEEN s1.event_date - INTERVAL '6' DAY AND s1.event_date`. For 28-day: change to `INTERVAL '27' DAY`. Decision table at line 664+ covers internal-dashboard / ops-review case.\n     - SECONDARY: exact self-join `COUNT(DISTINCT)` grouped by window-end day.\n   • r07:672+ inoculation: `COUNT(DISTINCT user_id) OVER (ORDER BY occurred_date ROWS BETWEEN 6 PRECEDING AND CURRENT ROW)` labelled INVALID — Trino does not support DISTINCT in window functions (issue #7885). Fix routed to HLL merge.\n   • r07:574-616 the daily-sketch CREATE TABLE recipe + varbinary cast rules.\n   • r07:540 the 2.3% standard-error caveat with 1K-cohort sweet-spot note.\n   • All Trino 467 dialect-correct. No edits needed.\n\n=== NO EDITS, ZERO FILE CHANGES. resources/22 UNTOUCHED. ===\n\n=== FULL LOCK INVENTORY PRESERVED IN PLACE (iter534-iter650) ===\nfixed-width-histogram-bucket r07 Pattern C4a (iter650); rolling-N-day-MA pre-aggregate-first r07 Pattern C4a-neighbor/D (iter649); GROUP-BY-rule extract-then-count r23:1530 (iter647); second-largest/Nth-largest decision canonical r23:982 + top-N-with-ties r23:1063 (iter643/645); date-difference-in-days r23:1100 (iter641); period-total-YoY-ratio + active-every-N-full-months r07 (iter640); listagg/array_join varchar-CAST guardrail r07:§1a.2A (iter639); Q4-class MIN/MAX-per-group anchor + LIMIT-OVER-invalid inoculation (iter638); max-per-group-compare r23 §3.1G (iter637) + rolling-distinct-HLL r07 + COUNT(DISTINCT)-OVER inoculation (iter637) + r27:§4.2 cross-link; share-of-subset-final-assembly r07:1097/1194 (iter636); PERCENT_RANK/NTILE-direction guardrail r07:~1869/2080 (iter635); DISTINCT-ON inoculation r23:851-901; concat/format coercion guardrail r23:§3.1A + format('%,.2f') currency; nearest-hour FLOOR/NEAREST/CEILING r07 + N-min-truncation r07:1309-1341; bool_or has-ever anchors r23:684-717; histogram r23:§3.1E; DISTINCT-*/PARTITION-BY-* r23:§3.1G; approx_percentile/PERCENTILE_CONT inoculation r23:~L141-159; completed-age r23:~936; listagg-DISTINCT->array_join r23; EXCEPT-dedup r23:780; 2nd-most-recent ROW_NUMBER r23:813; anti-join LEFT-JOIN-IS-NULL + NOT-IN-NULL-pitfall r23:1572-1626 + COUNT(right_col)-vs-COUNT(*) LEFT-JOIN r07:293-326; positional-GROUP-BY r23:1278-1281 + GROUP-BY-rules-anchor r23:1521-1528 + same-level-alias-scope; regexp_replace-strip + alias-in-WHERE guard r27:§4.2; regexp_like-alternation/RLIKE inoculation r27:~982; strpos-3-arg r27:931; initcap inoculation r27:~937; strip-leading-zeros r27:~935; ROLLUP-vs-CUBE + GROUPING-label-mapping + omit-detail-tuple r28:~419-438; share-of-grand-total r07:1068-1095; date_trunc-hour-FLOOR r07; date_trunc-week-Monday-ISO r07; json_array_length bare-array r13:3387; date-range-overlap r07:~947; LAG-MoM-delta r07:1505-1525 + LAG-prev-order-delta; LAST_VALUE-IGNORE-NULLS-LOCF r07 + r23:905-951; NTILE r07; day_of_week-1-7-Mon-Sun-no-dayname r07; DENSE_RANK r23:964; last_day_of_month-EXISTS r27:639-717; date_parse/parse_datetime r27 + repeat-expr-in-WHERE; MAU-date_trunc-month-COUNT-DISTINCT + status-pivot-conditional-agg + split_part-name-split + max-gap-LAG-date_diff + searched-CASE-buckets + self-join-pairs + ROW_NUMBER-top1 / max_by r23:597+/657 / first_value-last_value / cume_dist / PERCENT_RANK / greatest-least / NULLIF-guard / count_if / §11-conditional-aggregation-FILTER + HLL/approx_set/merge/cardinality + macro-average-subquery + point-in-time-active-count + distinct-categories-per-customer + did-X-not-Y-anti-join + LTV-tier-two-level + day-over-day-retention + bounce-rate + avg-session-length-minutes + funnel-MATCH_RECOGNIZE + 5-min-bucket + running-balance + first-touch-min_by + email-domain-split + Pareto-cumulative-share. resources/22 UNTOUCHED. NO commit."
}

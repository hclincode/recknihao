{
  "iteration": 696,
  "phase": "extended",
  "final_iterations_remaining": 0,
  "passed": false,
  "notes": "iter696 — NO-OP CONFIRMATION. Re-read of the iter695 QUALIFY inoculation card per directive STEP-A. Both required shapes are ALREADY clearly covered with copy-attractive ✅ blocks + keyword anchors. ZERO resource edits made. State.json bumped to 696 only.\n\n=== STEP-A RE-READ FINDING ===\nFile: resources/23-sql-best-practices-olap.md.\n\nShape (1) — FIRST-AND-LAST in one row (two aggregates, one GROUP BY pass, NOT QUALIFY, NOT two self-joins): CONFIRMED at r23:1030-1038. Block header (r23:1030): `-- Trino 467 — FIRST AND LAST amount per customer in ONE row (iter695 ✅ COPY THIS — Q2 canonical):`. Body (r23:1031-1037): `SELECT customer_id, min_by(amount, order_date) AS first_amount, max_by(amount, order_date) AS last_amount, MIN(order_date) AS first_order_date, MAX(order_date) AS last_order_date FROM iceberg.analytics.orders GROUP BY customer_id;` — exactly the two-aggregates-one-GROUP-BY-pass shape the directive requires. Inline same-line comments document the semantics: `-- value of amount at MIN(order_date) per customer` and `-- value of amount at MAX(order_date) per customer`. Cross-ref bridge at r23:744 (§3.1D iter638 quick-route anchor) also reaches this card: `Full inoculation card with defanged QUALIFY snippets + both correct forms (min_by/max_by AGGREGATE and ROW_NUMBER() SUBQUERY+WHERE) lives at §3.1G LEADING CANONICAL — QUALIFY is NOT a Trino 467 clause just below.`\n\nShape (2) — TOP-N-per-group with N>1 (ROW_NUMBER subquery + outer WHERE rn <= N, NOT QUALIFY): CONFIRMED at r23:1042-1056. The fenced ```sql block at r23:1042-1056 contains both the rn=1 canonical AND the explicit N>1 generalization on the SAME line: `-- For TOP-N (e.g. latest 3 per device): just change the outer filter to WHERE rn <= 3.` (r23:1055). The block above it (r23:1014) explicitly says `Top-N-per-group (not just top-1)? Same pattern with WHERE rn <= N.` Decision table at r23:1080 contains a dedicated row for `\"TOP-N rows per group\" (N > 1)` → `ROW_NUMBER() subquery + outer WHERE rn <= N` → `Only the subquery form supports N > 1. max_by(x, y, n) 3-arg form returns an array<x>, not N rows — different shape.` Keyword anchors at r23:1016 include `top-N per group dedup`.\n\nDefanged QUALIFY snippets — CONFIRMED at r23:1058-1069. Three fenced ```sql lines each carry inline SAME-LINE `-- ❌ WRONG: ... DO NOT COPY` markers per iter694 defang lesson: (a) r23:1061-1062 bare `SELECT * FROM device_readings QUALIFY ROW_NUMBER() OVER (...) = 1` with `-- ❌ WRONG: Trino 467 has NO QUALIFY clause (parse error: mismatched input 'QUALIFY') — Snowflake/BigQuery/Databricks/Teradata/DuckDB-only — DO NOT COPY:`; (b) r23:1064-1065 dbt-model `SELECT * FROM {{ ref('orders') }} QUALIFY ...` with `-- ❌ WRONG: same parse error in dbt models targeting Trino — DO NOT COPY:`; (c) r23:1067-1068 window-in-WHERE `SELECT * FROM orders WHERE ROW_NUMBER() OVER (...) = 1` with `-- ❌ WRONG: window functions are illegal in WHERE in EVERY SQL dialect — parse error — DO NOT COPY:`. All three banned lines are self-documenting-broken on the same line — they are NOT copy-attractive. The ✅ COPY THIS blocks above them ARE copy-attractive.\n\nKeyword anchors at r23:1016 include both shapes' route triggers: `first row per group`, `latest row per customer`, `latest row per device`, `top-N per group dedup`, `top-1 per group`, `one row per group keep latest`, `first and last per customer one row`, `first and last per user one row`, plus the `Snowflake QUALIFY in Trino` / `BigQuery QUALIFY in Trino` / `Databricks QUALIFY in Trino` / `Teradata QUALIFY in Trino` / `DuckDB QUALIFY in Trino` dialect-muscle-memory anchors. Decision table at r23:1075-1081 maps question shape → form: first-AND-last-in-one-row (Q2 canonical) → min_by/max_by; latest-a-few-cols → max_by; latest-WHOLE-row → ROW_NUMBER subquery; TOP-N N>1 → ROW_NUMBER subquery (with rn <= N); first-AND-last-TIMESTAMP-ITSELF → plain MIN/MAX (iter638 PIN cross-ref).\n\n=== STEP-B DECISION — NO-OP ===\nThe card already covers BOTH required shapes with copy-attractive ✅ blocks AND has keyword anchors covering every variant in the directive (`first and last per customer one row`, `top-N per group dedup`, `top-1 per group`, etc.). Defanged ❌ snippets are intact at r23:1058-1069 with inline same-line markers per iter694 defang lesson. NO additive edit needed. NO resource files touched.\n\n=== DOCS-TRUTH RE-VERIFIED (2026-06-08) ===\n[trino.io/docs/467/functions/aggregate.html](https://trino.io/docs/467/functions/aggregate.html) WebFetch result quoted verbatim:\n• `max_by(x, y) -> [same as x]`: \"Returns the value of x associated with the maximum value of y over all input values.\"\n• `min_by(x, y) -> [same as x]`: \"Returns the value of x associated with the minimum value of y over all input values.\"\n• `max_by(x, y, n) -> array<[same as x]>` and `min_by(x, y, n) -> array<[same as x]>` 3-arg forms confirmed for top/bottom-N.\n• QUALIFY: \"No, the term 'QUALIFY' does not appear anywhere in the provided documentation.\" — confirms QUALIFY is absent from the aggregate-functions page.\n• Combined with the iter695 docs-truth verification of [trino.io/docs/467/sql/select.html](https://trino.io/docs/467/sql/select.html) (QUALIFY absent from the documented SELECT grammar), both correct forms in §3.1G LEADING CANONICAL match the official Trino 467 documentation.\n\n=== HELD FIXES — ALL UNTOUCHED ===\n• iter695 QUALIFY inoculation card r23:744 + r23:1014-1071 — HELD (NO-OP this iter, FRESH judge-recommended HOLD).\n• iter693/694 Pattern A4 r07:2054-2136 — UNTOUCHED (responder-limit lock).\n• iter689 Pattern-A3 r07:1938-2052 — UNTOUCHED.\n• iter688 storage-tiering r16:501 + r17-xref + exposures r27:3420 — UNTOUCHED.\n• iter686 timezone-CAST-trap r07:1594 — UNTOUCHED.\n• iter684 snapshot unique_key r09:366/386/401/410 — UNTOUCHED.\n• iter682 dbt-incremental/EXPLAIN/tests — UNTOUCHED.\n• iter681 CREATE-TABLE-constraints r03:465/r23:27/r27:1681-1735 — UNTOUCHED.\n• iter679 max_recursion_depth r27:33/r27:3593/r07:456 — UNTOUCHED.\n• iter676 slice() r07:281 §1a.4A — UNTOUCHED.\n• iter674 string-funcs — UNTOUCHED.\n• iter673 MAP/JSON — UNTOUCHED.\n• iter671 ts-diff r23:1221+ + r07:1860 — UNTOUCHED.\n• iter670 MoR-vs-CoW — UNTOUCHED.\n• iter668 r27:4122 rollback-CALL — UNTOUCHED.\n• iter667 DataSize/ROWS-vs-RANGE — UNTOUCHED.\n• iter666 r12 maintenance — UNTOUCHED.\n• iter665 day_of_week-name r07:1343-1399 — UNTOUCHED.\n• iter656 min_by/max_by + first-AND-last per ticket PIN at r23:733-790 — PRESERVED.\n• iter638 first-AND-last-per-entity MIN/MAX PIN at §3.1D — PRESERVED.\n• Full iter534-695 lock inventory (~260 locks across r03/r05/r07/r08/r09/r10/r11/r12/r13/r16/r17/r18/r21/r23/r27/r28) — PRESERVED.\n• resources/22-trino-federation-postgresql.md — UNTOUCHED (HARD LOCK preserved through 248 consecutive iterations since iter448).\n\n=== FILES CHANGED ===\nZERO resource files touched. Only training/state.json bumped from 695 → 696 with NO-OP confirmation note.\n\n=== NO COMMIT, NO PUSH ===\nPer directive — state.json bump ONLY. Orchestrator commits."
}

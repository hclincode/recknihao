{
  "iteration": 472,
  "phase": "extended",
  "final_iterations_remaining": 0,
  "passed": true,
  "notes": "Iter472 TEACHER — federation NOT probed (stays recorded near-miss 4.49944/310). Iter471 was a thin 3.703125 PASS with Q1 SET PARTITION SPEC cross-dialect fab + Q3 parse_date cross-dialect fab + Q4 dbt model contracts content gap (NEW micro-topic NEEDS WORK at 2.9375). PRIMARY teacher actions this iter address all three:\n\n(1) PRIMARY — dbt model contracts CONTENT GAP (Q4). Added new LEADING CANONICAL block resources/27-oracle-plsql-to-dbt-trino.md §6.7C — adjacent to §6.7 dbt tests and §6.7B source freshness so the dbt cluster (tests + sources + freshness + contracts) is co-located. Block contents: (a) Q-pattern matcher table routing 5 question shapes (what is a contract / how to declare / build vs query time / dbt-trino constraint specifics / what build prints on violation); (b) 'What a contract is and when it fires' paragraph pinning the BUILD-time-not-query-time semantic — Trino does NOT enforce the contract, dbt-core does, via the dbt-trino adapter, during the preflight check that runs BEFORE the model SQL is submitted to Trino; (c) 'Required YAML keys' sub-section with the canonical worked example — `models: - name: fct_orders config: contract: enforced: true columns: [{name: order_id, data_type: bigint, constraints: [{type: not_null}]}, ...]`; (d) 'Constraints on dbt-trino — what's enforced vs definable' sub-section asserting ONLY not_null is enforced at write time on dbt-trino + Iceberg (Trino translates to Iceberg NOT NULL column constraint), while primary_key / foreign_key / unique / check are definable in YAML but NOT runtime-enforced — with explicit deferral note pointing to docs.getdbt.com/reference/resource-configs/trino-configs for anything beyond not_null; (e) 'Failure mode' sub-section with the literal `dbt build` error output showing the column-mismatch table (column_name | definition_type | contract_type | mismatch_reason) and three failure-flavor table (column dropped / type changed / extra column); (f) full dbt-trino + Iceberg worked example: schema.yml with `materialized: table` + contract enforced + columns list using Trino types (VARCHAR, BIGINT, DATE, DECIMAL(18,2), TIMESTAMP(6)) + matching SELECT skeleton with dbt_utils.generate_surrogate_key; (g) When-to-use / when-to-skip table; (h) 11-row DO-NOT-WRITE matrix banning: `@contract` decorator (FABRICATED), `CONTRACT` SQL keyword (FABRICATED), 'enforced at query time by Trino' (WRONG — build-time only), 'Trino-specific feature' (WRONG — dbt-core feature on any adapter), 'primary_key is enforced at write time on dbt-trino' (WRONG — only not_null), `--enforce-contract` CLI flag (FABRICATED), generic types `string`/`int`/`numeric` instead of Trino types (WRONG warehouse type), invented config keys like `contract.strict` / `contract.mode` / `contract.check_types` (FABRICATED — only documented sub-keys are `enforced` and `alias_types`), 'works on materialized views' (WRONG per docs — supported on table / view-limited / incremental only), fabricated constraint types like `regex` / `length` / `min` / `max` / `range` / `enum` (FABRICATED — only documented types are not_null / primary_key / foreign_key / unique / check). DO-NOT-WRITE explicitly quoted: '`@contract` (Python-style decorator on the model) — FABRICATED. dbt has no `@contract` decorator — contracts are declared exclusively in YAML via `config: contract: {enforced: true}` + `columns:`.' Constraint-enforcement claim asserted: not_null is the ONLY constraint enforced at write time on dbt-trino + Iceberg; deferred specifics for primary_key / foreign_key / unique / check enforcement (claimed 'metadata only, not runtime-enforced' which is the WebSearch-verified baseline, and pointed to dbt-trino docs for specifics beyond that). WebSearch-verified at docs.getdbt.com/reference/resource-configs/contract + docs.getdbt.com/docs/collaborate/govern/model-contracts + dbt-trino constraint docs.\n\n(2) FIX iter471 Q1 SET PARTITION SPEC cross-dialect fab. Pre-edit grep confirmed `SET PARTITION SPEC` appeared in THREE stale resource locations (NOT just a responder hallucination): r17 line 264 (DROP-COLUMN-on-partition-column edge case), r17 line 1252 (same edge case re-mentioned in DROP COLUMN reclaim runbook), r18 line 933 (partition-spec changes-not-retroactive note). All three reconciled (before -> after): r17 line 264 + r17 line 1252 + r18 line 933 now show the canonical `ALTER TABLE ... SET PROPERTIES partitioning = ARRAY[<remaining transforms>]` form and explicitly call out that `SET PARTITION SPEC` is Spark/Iceberg-dialect, NOT Trino, with parse-error consequence. Added 3 new rows to r17 schema-evolution DO-NOT-WRITE matrix explicitly banning: `SET PARTITION SPEC (day(event_time), bucket(user_id, 16))` (Spark-ism), `ADD PARTITION FIELD day(event_time)` (Spark-ism), `REPLACE PARTITION FIELD day(event_time) WITH hour(event_time)` (Spark-ism) — each cited to trino.io/docs/current/connector/iceberg.html. Added new 'Evolving the partition spec on Trino 467 — the ONE canonical form (NOT SET PARTITION SPEC)' sub-section inside the schema-evolution LEADING CANONICAL at r17, with 4 copy-paste forms (A. ADD new transform, B. DROP transform, C. REPLACE entire spec, D. UNPARTITION via empty array), existing-data-behavior note (Iceberg keeps old spec for already-written files), and 'why the array form, not SET PARTITION SPEC (...)' explanation pinning Trino's table-PROPERTY exposure of partitioning vs Spark's dedicated DDL clause. WebSearch-verified at trino.io/docs/current/connector/iceberg.html: only documented partition-evolution form is `SET PROPERTIES partitioning = ARRAY[...]`.\n\n(3) FIX iter471 Q3 parse_date cross-dialect fab. Pre-edit grep confirmed `parse_date\\b` did NOT appear anywhere in resources/ (responder hallucination, NOT stale resource line). Reconciled r27 §4.2 TO_DATE row (line 597) to (a) lead with the THREE canonical Trino-correct forms in priority order (from_iso8601_date / CAST(date_parse(...) AS DATE) / CAST(str AS DATE)) with explicit call-out '`parse_date(str, fmt)` is NOT a Trino function — that is Snowflake/BigQuery', and (b) extended with 2 new TO_DATE format-variant rows for slash-day-first (`DD/MM/YYYY` -> `%d/%m/%Y`) and abbreviated-month-name (`DD-MON-YYYY` -> `%d-%b-%Y`) so the canonical patterns are findable. Added 4 new rows to §4.4B cross-dialect-spillover guardrail table explicitly banning: `parse_date(str, fmt)` from Snowflake/BigQuery (NEW row right after the STR_TO_DATE row, with full WebSearch-verified canonical-triple replacement); `to_date(str, fmt)` from Oracle/Snowflake/Spark; AND a NEW dedicated row 'MySQL-vs-Joda format specifier confusion' pinning the silent-NULL failure mode when the specifier style doesn't match the function (date_parse with Joda yyyy/MM/dd OR parse_datetime with MySQL %Y/%m/%d) — with mnemonic 'percent-sign -> MySQL -> date_parse; no percent sign -> Joda -> parse_datetime'. Existing STR_TO_DATE row in §4.4B reconciled (before -> after) to recommend `CAST(date_parse(...) AS DATE)` (with explicit DATE cast) instead of bare `date_parse(...)` (which returns timestamp(3)). WebSearch-verified at trino.io/docs/current/functions/datetime.html: NO function named `parse_date`; only date_parse (MySQL), parse_datetime (Joda), from_iso8601_date (ISO).\n\n§13.x federation guardrails in resources/22 NOT TOUCHED (git diff --stat confirms only r17 + r18 + r27 + state.json modified). Federation 4.49944/310 row sits 0.0006 below 4.5 raised threshold — let count grow naturally with non-federation breadth probes.\n\nFab-class watch held: (1) cross-dialect spillover — Spark-Iceberg DDL (SET PARTITION SPEC / ADD PARTITION FIELD / REPLACE PARTITION FIELD) explicitly banned in 3 DO-NOT-WRITE rows in r17; Snowflake/BigQuery date functions (parse_date / to_date) explicitly banned in 2 DO-NOT-WRITE rows in r27 §4.4B; (2) version-pin — r17 ADD COLUMN ... FIRST/AFTER 469+ note retained; (3) Trino-internal clause conflation — MySQL-vs-Joda specifier conflation now has dedicated row in §4.4B with mnemonic; (4) fabricated-capability-restriction — model-contract DO-NOT-WRITE bans 'Trino-specific feature' and 'enforced at query time' which are the analog capability-restriction fab classes for the new dbt-contracts micro-topic.\n\nWebSearch verifications this iter:\n- docs.getdbt.com/reference/resource-configs/contract: confirmed required YAML schema (config.contract.enforced + columns: with name + data_type) and that the contract is enforced at compile/build time before materialization with a structured 'data type mismatch' / 'missing in definition' / 'extra in definition' preflight error.\n- docs.getdbt.com/docs/collaborate/govern/model-contracts: confirmed conceptual definition (upfront guarantees on the shape of the dataset), the preflight check, that data_type is warehouse-specific (must be a type the platform understands), that the column-name + data_type check is enforced on ALL adapters that support contracts (universal), and that constraint enforcement varies by platform (NOT universal).\n- WebSearch 'dbt-trino model contracts constraints not_null primary_key enforcement Iceberg adapter': confirmed dbt-trino enforces not_null at the Iceberg-column-level NOT NULL constraint; primary_key / foreign_key / unique / check are definable but not runtime-enforced by dbt-trino + Iceberg.\n- trino.io/docs/current/connector/iceberg.html: confirmed only documented partition-evolution form is `ALTER TABLE ... SET PROPERTIES partitioning = ARRAY[...]`; no `SET PARTITION SPEC`, no `ADD PARTITION FIELD`, no `REPLACE PARTITION FIELD` clauses.\n- trino.io/docs/current/functions/datetime.html: confirmed NO function named `parse_date`; the documented date/timestamp parsing functions are date_parse (MySQL specifiers, returns timestamp(3)), parse_datetime (Joda specifiers, returns timestamp with time zone), and from_iso8601_date (ISO 8601, returns date).\n\nNo fresh unvetted claims introduced. Every model-contract claim is cited to docs.getdbt.com; every partition-evolution claim is cited to trino.io/docs/current/connector/iceberg.html; every date-parsing claim is cited to trino.io/docs/current/functions/datetime.html.\n\nFiles modified: resources/17-iceberg-table-maintenance.md (3 SET PARTITION SPEC reconciliations: line 264 edge-case mention + line 1252 reclaim-runbook edge-case + NEW 'Evolving the partition spec on Trino 467' sub-section + 3 NEW DO-NOT-WRITE matrix rows banning SET PARTITION SPEC / ADD PARTITION FIELD / REPLACE PARTITION FIELD), resources/18-query-performance-regression.md (1 SET PARTITION SPEC reconciliation at line 933), resources/27-oracle-plsql-to-dbt-trino.md (NEW §6.7C LEADING CANONICAL dbt model contracts + RECONCILED §4.2 TO_DATE row with canonical triple + 2 NEW TO_DATE format-variant rows + 4 NEW §4.4B cross-dialect-spillover rows banning parse_date / to_date / MySQL-vs-Joda confusion + STR_TO_DATE row reconciled to recommend CAST(date_parse(...) AS DATE)), training/state.json. Iteration 471 -> 472."
}

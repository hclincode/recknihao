{
  "iteration": 703,
  "phase": "extended",
  "final_iterations_remaining": 0,
  "passed": false,
  "notes": "iter703 — FIX-A1 (HIGH — array_agg DISTINCT+ORDER BY parse-error correction) + FIX-A2 (MED — bucket-rollup companion). Both verified against trino.io/docs/467 + trinodb/trino #20725 BEFORE writing.\n\nFIX-A1 (array_agg DISTINCT+ORDER BY — iter702 Q4 fab inoculation at r07:185-225 + new §1a.2A.1 at r07:187):\n  GREP RESULT — was the §1a.2A canonical itself buggy? **NO — CANONICAL WAS CLEAN.** All three DISTINCT+ORDER BY occurrences in the existing §1a.2A leading canonical (r07:165-225) were already correct (argument == ORDER BY key, character-identical):\n    - r07:180 `array_agg(DISTINCT event_name ORDER BY event_name)` — arg == key, SAFE.\n    - r07:185 row 5 (DO-NOT-WRITE listagg-DISTINCT diversion) `array_join(array_agg(DISTINCT product ORDER BY product), ', ')` — arg == key, SAFE.\n    - r07:211 (sub-canonical DISTINCT-roll-up comment) `array_join(array_agg(DISTINCT CAST(order_id AS varchar) ORDER BY CAST(order_id AS varchar)), ', ')` — arg == key, SAFE.\n  ROOT CAUSE OF iter702 Q4 BUG — the responder SYNTHESIZED the buggy mismatched form by combining the NON-DISTINCT sub-canonical at r07:207 `array_join(array_agg(CAST(order_id AS varchar) ORDER BY order_id), ', ')` (which is legal without DISTINCT — #20725 only fires WITH DISTINCT) with a mental 'add DISTINCT to dedup' step, without re-reading the correct DISTINCT shape at r07:211 (which matches the CAST in the ORDER BY). The clean canonical was sitting one comment-line away from the trap, but the responder did not propagate the CAST into the ORDER BY.\n  FIX SHAPE — RECONCILE-IN-PLACE + ADD INOCULATION CARD:\n    (1) Added bullet (6) to the §1a.2A DO-NOT-WRITE list at r07:185 explicitly flagging `array_agg(DISTINCT <expr_A> ORDER BY <expr_B>)` where expr_B != expr_A as an analysis error in Trino 467, with the exact error message verbatim from #20725: `\"For aggregate function with DISTINCT, ORDER BY expressions must appear in arguments\"` + cross-ref to the new §1a.2A.1 below.\n    (2) Added new LEADING CANONICAL §1a.2A.1 at r07:187 — `array_agg(DISTINCT ...)` with `ORDER BY` — the ORDER-BY key MUST match the aggregate's ARGUMENT expression — full inoculation card with:\n      - READ-THIS-FIRST keyword anchors: 'array_agg DISTINCT ORDER BY', 'array_agg ordered distinct', 'collect distinct values into a sorted list', 'collect each customer's product ids into one ordered comma-string', 'ORDER BY must appear in arguments DISTINCT', 'For aggregate function with DISTINCT ORDER BY expressions must appear in arguments', 'aggregate DISTINCT order by error Trino'.\n      - Docs verification: trino.io/docs/467/functions/aggregate.html + trinodb/trino #20725 on 2026-06-08.\n      - ❌ DO-NOT-COPY block with the EXACT iter702 Q4 fab `array_agg(DISTINCT CAST(product_id AS varchar) ORDER BY product_id)` defanged with the per-iter694-defang-lesson same-line WRONG marker `❌ WRONG: with DISTINCT, every ORDER BY key must match an aggregate argument (Trino 467 analysis error, trinodb/trino#20725) — DO NOT COPY`.\n      - Three ✅ COPY THIS forms (Form A: match CAST in ORDER BY for lexicographic sort; Form B: inner SELECT DISTINCT + outer non-DISTINCT array_agg for numeric sort; Form C: plain DISTINCT+ORDER BY when column is already varchar).\n      - 5-row decision-routing table (array vs string output × lexicographic vs numeric sort × already-varchar column).\n      - 'Why the #20725 restriction exists' mental-model paragraph (dedup decision + sort decision must agree).\n      - 6-row DO-NOT-WRITE table: the iter702 Q4 fab + 'sort by CAST(product_id AS bigint)' second-attempt fab + ORDER BY references a different column entirely + the 'Trino accepts any ORDER BY' false-statement defang + 'just SELECT DISTINCT outer' inefficient mis-fix.\n      - Cross-references back up to §1a.2A and to the sub-canonical for the CAST(numeric AS varchar) family + to resource 27 listagg/array_join family.\n    (3) PRESERVED SURROUNDING LOCKS — iter691 UNNEST-WITH-ORDINALITY at r07:70 (above the §1a.2A region) and iter675 array-ops at r07:281+ (below) NOT TOUCHED. The §1a.2A canonical itself NOT MODIFIED — only the bullet (6) appended to the DO-NOT-WRITE list + the new §1a.2A.1 inserted immediately after.\n\nFIX-A2 (bucket-rollup companion — new COMPANION CANONICAL at r07:3095 — between Pattern C4 and Pattern C4a):\n  Located Pattern C4 at r07:2966-3019 (width_bucket numeric-band card) and Pattern C4a at r07:3097+ (fixed-width integer-division floor). The responder cited 'Pattern C4' for the iter702 Q2 bucket-revenue rollup question. Added a NEW companion card between C4 and C4a (does NOT rewrite either) addressing the per-segment-rollup shape:\n    - READ-THIS-FIRST keyword anchors: 'customers per revenue band', 'count per segment', 'how many customers in each tier', 'GROUP BY revenue tier', 'segment rollup', 'summary table per spending tier', 'customer count per segment', 'band summary', 'tier summary'.\n    - DECISION-ROUTING one-liner: 'Labeling each customer → GROUP BY customer_id (or no GROUP BY in the labeling step); counting customers per band → GROUP BY the segment LABEL only (NOT customer_id).' Names the granularity trap explicitly.\n    - Two ✅ COPY THIS shapes side-by-side:\n      SHAPE (i) LABEL EACH CUSTOMER — per_customer CTE → outer SELECT with CASE WHEN attaching revenue_segment, no outer aggregate, output one row per customer with their band as a column.\n      SHAPE (ii) COUNT CUSTOMERS PER BAND — per_customer CTE → labeled CTE (attach CASE label) → outer SELECT GROUP BY revenue_segment with COUNT(*) AS customer_count + SUM(total_revenue) AS segment_revenue, ORDER BY MIN(total_revenue) (numerically-sorted bands, not lexicographic).\n    - 'Why two CTEs' explanation paragraph (Trino does NOT allow aggregates inside GROUP BY at same level — must pre-aggregate to per-customer THEN label THEN rollup) + 'Why ORDER BY MIN(total_revenue)' explanation (text labels would sort lexicographically wrong).\n    - ❌ DO NOT WRITE granularity trap block with the EXACT shape the iter702 Q2 responder shipped: `GROUP BY customer_id, CASE WHEN ... END` → output is one row per customer with COUNT(*)=1, NOT the per-segment summary. Same-line WRONG marker: `❌ WRONG for a per-segment rollup: GROUP BY customer_id makes COUNT(*)=1 per customer — for a band summary GROUP BY the label only — DO NOT COPY` per the iter694 defang lesson.\n    - 4-row DO-NOT-WRITE shape-confusion table: (a) GROUP BY alias from same SELECT (works mechanically but needs pre-aggregation), (b) GROUP BY 1 positional (works but fragile), (c) CASE WHEN with SUM() inside GROUP BY (Trino rejects aggregate-in-GROUP-BY at same level), (d) width_bucket flavor of the same pattern (legal — two-CTE shape still applies).\n    - 'When you also need band BOUNDARIES' worked extension adding MIN/MAX/AVG aggregates per segment row.\n    - Cross-references to Pattern C4 (width_bucket), Pattern C4a (integer-division floor), resource 23 §8 (GROUP BY rules anchor), and Pattern A2 (same 'aggregate to grain first, then operate' shape — bucketed running total).\n  Pattern C4 itself NOT TOUCHED. Pattern C4a NOT TOUCHED. New card sits at r07:3095, BETWEEN them.\n\nDOCS-TRUTH VERIFIED BEFORE WRITING:\n  - trino.io/docs/467/functions/aggregate.html — array_agg signature, 'Ordering during aggregation' section verbatim ('array_agg(x ORDER BY y DESC)' / 'array_agg(x ORDER BY x, y, z)'). The DISTINCT+ORDER-BY restriction is NOT documented on this page — it is enforced by the analyzer at query time per #20725.\n  - trinodb/trino #20725 (opened 2024-02-15, OPEN as of 2026-06-08) — confirms exact error message: 'For aggregate function with DISTINCT, ORDER BY expressions must appear in arguments'. Confirms `array_agg(DISTINCT concat(value, value) ORDER BY value)` fails while the same call without DISTINCT or with matching ORDER BY succeeds. Identical structural mismatch to the iter702 Q4 fab `array_agg(DISTINCT CAST(product_id AS varchar) ORDER BY product_id)`.\n  - trino.io/docs/467/functions/math.html — width_bucket both overloads `width_bucket(x, bound1, bound2, n) → bigint` and `width_bucket(x, bins) → bigint` confirmed present in 467.\n  - trino.io/docs/467/sql/select.html — GROUP BY rules: alias-from-SELECT-list is permitted in GROUP BY, but aggregates inside GROUP BY at the same query level are not (must use a CTE or subquery to pre-aggregate).\n  - listagg signature `LISTAGG(expression [, separator] [ON OVERFLOW overflow_behaviour]) WITHIN GROUP (ORDER BY sort_item, ...)` — no DISTINCT slot, confirmed (re-verifying the existing §7A.2B canonical at r27:3974).\n\nHELD FIXES (NOT RE-EDITED):\n  - iter698 MoM card r07:2486-2587 — INTACT (now r07:2589+ after FIX-A2 insertion of ~93 lines around r07:3095; actual line shift only affects content AFTER r07:3095; MoM card precedes it so unchanged).\n  - iter697 approx_percentile mirror r07:589 + r23:2463 glossary split — INTACT.\n  - iter695 QUALIFY card r23:744 + r23:1014-1071 — INTACT.\n  - iter693/694 Pattern A4 r07:2054-2136 — INTACT (line shift from FIX-A1: ~120 lines inserted at r07:187, so Pattern A4 now ~r07:2174-2256; all locked content preserved verbatim).\n  - iter689 Pattern A3 r07:1938 — INTACT (now ~r07:2058 after FIX-A1 shift).\n  - iter688 storage-tiering r16:501 + exposures r27:3420 — INTACT (different files, no line shift).\n  - iter686 timezone-CAST-trap r07:1594 — INTACT (now ~r07:1714 after FIX-A1 shift).\n  - iter684 snapshot unique_key r09 — INTACT (different file).\n  - iter682 dbt-incremental, iter681 CREATE-TABLE-constraints, iter679 max_recursion_depth, iter676 slice() r07:281 (now ~r07:401 after FIX-A1 shift), iter675 array-set-ops/sequence/zip, iter674 string-funcs, iter673 MAP/JSON, iter671 ts-diff, iter670 MoR-vs-CoW, iter668 rollback-CALL, iter667 DataSize, iter666 r12-maintenance, iter665 day_of_week-name, iter658 first_value/last_value, iter656 min_by/max_by, iter640 YoY, iter638 first-AND-last, HAVING-vs-WHERE r23:1809, EXPLAIN-triage, Iceberg-schema-evolution, broadcast-join, WITH-RECURSIVE r27 §7A.1, try_cast r23, dbt-generic-tests r28 §H3, exact-dedup-DISTINCT-star, money-DECIMAL, split-UNNEST, partitioning-DDL r10, MERGE-INTO-Trino-explicit-columns — ALL INTACT (additive edits only; surrounding text unchanged).\n  - resources/22-trino-federation-postgresql.md — UNTOUCHED (HARD LOCK preserved through 254 consecutive iterations since iter448).\n\nALL iter534-702 LOCKS PRESERVED. PIN TRINO 467 ban list intact (now extended with the array_agg-DISTINCT-ORDER-BY-must-match-arg rule per #20725). NO COMMIT, NO PUSH — orchestrator commits."
}

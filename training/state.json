{
  "iteration": 950,
  "phase": "extended",
  "final_iterations_remaining": 0,
  "passed": true,
  "note": "iter950 ZERO-edit NO-OP durability sweep \u2014 read-only lock spot-check + this state.json bump (949->950) ONLY. ZERO resource edits, no git commit/push. ALL LOCKS CONFIRMED INTACT. (a) CORRECTED r07 L37 HOLDS verbatim \u2014 reads 'What to watch for: if GROUP BY has high cardinality (e.g., GROUP BY user_id across 50M users), the engine has to keep all distinct groups in memory. To actually cut that memory you must reduce the INPUT rows that reach the aggregation \u2014 add a WHERE filter on a partition/filter column so fewer rows are read, or pre-aggregate in stages (e.g. a daily rollup table). A HAVING COUNT(*) > N does not help here: HAVING runs after all groups are built, so it only trims the OUTPUT rows, not the in-memory group set the engine had to construct.' NO trace of the old 'Add a HAVING COUNT(*) > N to trim' folklore. Consistent with r23 \u00a78 header L2552 'Filter with WHERE before GROUP BY, not HAVING' + rationale L2643 'WHERE is evaluated before aggregation... HAVING runs after aggregation \u2014 every row contributes to the group, then the group is discarded' + Bad/Good L2645-2660 + L2662 'Use HAVING only for conditions on aggregates themselves'. (b) iter933 ADD-A-QUARTER/ADD-A-WEEK card r07 L3481-3500 INTACT (only 6 INTERVAL qualifiers YEAR/MONTH/DAY/HOUR/MINUTE/SECOND per SqlBase.g4; INTERVAL '1' QUARTER/'1' WEEK PARSE ERRORS; canonical date_trunc('quarter')+INTERVAL '3' MONTH / '7' DAY / date_add; DO-NOT-COPY defang L3496-3497; TWO-SURFACES trap L3500 \u2014 quarter/week valid as date_trunc/date_add/date_diff UNIT strings but NOT interval qualifiers) + adjacent WHICH-QUARTER/WEEK-OF-YEAR card L3469-3479 (quarter()/EXTRACT(QUARTER), week_of_year()=alias of week(), quarter_of_year DEFANGED as non-existent). (c) r22 federation UNTOUCHED \u2014 file last git-touched 2026-06-04 commit c2627a8 (Iter448), well before this sweep. (d) ALL OTHER LOCKS INTACT (read-only): COUNT(DISTINCT) leading canonical r23 L183-222 (multiple distinct on different cols in ONE SELECT native, FILTER conditional distinct, distinct_aggregations_strategy knob + approx_distinct ~2.3% HLL, COUNT(DISTINCT ROW(c1,c2)) for pairs DO-NOT-WRITE); PERCENTILE footgun r05 L2234-2266 (PERCENTILE_CONT...WITHIN GROUP WRONG-marked, approx_percentile lead + ARRAY multi-percentile, MEDIAN-not-exist); Iceberg partition DDL r09 L127 WITH(partitioning=ARRAY['day(col)','tenant_id']) + bucket(col,N) column-first L131 + one-sentence rule L133 + 6-step Spark->Trino recovery L135; GROUP-BY-output rule r07 L4338 WRONG-marked customer_id-in-GROUP-BY defang + L4341 fix 'drop customer_id from both SELECT and GROUP BY'; CAST-to-integer half-up r07 L4448 (floor toward -inf, truncate toward zero, CAST AS integer rounds half-up); regexp_like leading canonical r23 L3262-3266 (no ~/~*/!~/!~* infix ops = PostgreSQL, no RLIKE = Spark/Hive/MySQL, no LIKE bracket char-classes = T-SQL) + alternation plain-pipe L3313-3319 (\\| = literal pipe WRONG, markdown-table-escape caution) + ~/~*/!~/!~*/RLIKE/REGEXP defang table L3375-3380; mean-family geometric_mean built-in L984 (EXP(AVG(LN)) fallback WHERE x>0) + harmonic-mean-for-rates L1021; top-N-share single-pass conditional-SUM r07 L1836-1847; r28 DATE-literal canonical L1047 (string-vs-date type mismatch defeats pushdown -> use DATE '2026-05-30') + date_trunc('day',ts)=DATE fragile-on-400+ row L1043; r13 json_exists leading canonical L3387-3403 (json_exists(json,'strict $.key')->boolean present-even-null=true vs absent=false; json_extract_scalar IS NOT NULL CONFLATES absent/null; native MAP uses contains(map_keys)); NULLS-LAST default both ASC/DESC r23 L3260. Working tree clean before bump. ZERO resource edits made this iteration. Resource filename map: r05=05-multi-tenant-analytics.md, r07=07-analytical-query-patterns.md, r09=09-lakehouse-schema-design.md, r10=10-lakehouse-partitioning.md, r13=13-postgres-to-iceberg-ingestion.md, r22=22-trino-federation-postgresql.md, r23=23-sql-best-practices-olap.md, r28=28-complex-sql-performance-trino-dbt.md."
}
{
  "iteration": 978,
  "phase": "extended",
  "final_iterations_remaining": 0,
  "passed": true,
  "note": "iter978 NO-OP durability-breadth sweep — ZERO resource edits; only this state.json iteration bump. All locked canonicals re-confirmed intact via read-only spot-checks. iter953/955 B-Streak filter-then-count defang (r07 L3214-3262): WRONG#2 stapled-double-GROUP-BY L3214-3219 DO-NOT-COPY, NESTED_WINDOW analyzer note L3221, FINDABILITY ROUTER L3228 (domain-neutral run-of-type-A-ending-at-type-B + 'returns 0 every time'), STREAK-ASSOCIATION L3232-3237 (run+target SHARE one streak_id, count over FULL streak via COUNT(*) FILTER, keep ending=target via max_by(status, event_time) NOT nested FILTER, fallback streak_id-1; both always-zero traps), CORRECT block L3239-3251 uses COUNT(*) FILTER + max_by(status, event_time), WRONG#1 WHERE-to-target L3253-3256 (+COUNT(*) OVER also after WHERE) and WRONG#2 count-in-own-streak L3257-3259 both inline DO-NOT-COPY marked, the-rule recap L3262, skeleton UNCORRUPTED. r07 L37 HAVING does NOT cut GROUP BY memory (only trims OUTPUT) CONFIRMED. percent_rank/NTILE direction guardrail + YoY DECIDE-FIRST signpost r07 L3273-3279 CONFIRMED. GROUP-BY-output rule r07 L4338/L4341 CONFIRMED. Monday-vs-Sunday week-trunc guard CONFIRMED. anti-nesting r07 CONFIRMED. iter933 ADD-A-QUARTER/ADD-A-WEEK card + WHICH-QUARTER router CONFIRMED. Unwrap rule names r07 L67-74 (UnwrapCastInComparison/UnwrapDateTruncInComparison/UnwrapYearInComparison, source-file-verified, registered unconditionally) + r10 L1447/L1453 (UnwrapCastInComparison + UnwrapDateTruncInComparison PR#14011) + r23 L2451-2453 (all three) CONFIRMED — NOT 'SimplifyDateTrunc'. r05/r23 PERCENTILE footgun CONFIRMED (r05 L2234 no PERCENTILE_CONT WITHIN GROUP, WITHIN GROUP listagg-only, approx_percentile T-Digest, L2263 no MEDIAN; r23 L265 no PERCENTILE_CONT/DISC, no exact-percentile fn, no 3-arg accuracy overload, qdigest for tighter; r23 L267+ value-at-pct-vs-rank-of-row, approx_percentile no published std-error, 2.3% is approx_distinct's). as-of/argmax canonicals CONFIRMED: r23 §3.1D L1234-1339 max_by/min_by family (latest-status worked example L1298-1305, first-AND-last min_by+max_by L1273/L1307-1318, tie ROW tiebreaker L1305, min_by/max_by-not-window-with-GROUP-BY L1307), L1275 QUALIFY cross-ref, L1273 LIMIT-OVER-not-valid + ROW_NUMBER()<=N subquery+WHERE; §3.1G ROW_NUMBER()=1/no-DISTINCT-ON LEADING CANONICAL + QUALIFY-not-in-Trino-467 PARSE-ERROR myth L18. date_diff/no-ts-minus-ts r23 L2211-2293 CONFIRMED (THE ONE FACT L2215 + operator table + DO-NOT-WRITE table incl ts-minus-ts-LAG sessionization, ts2-ts1>INTERVAL, EXTRACT(DAY FROM date-date), DATEDIFF-not-Trino, closed_at-opened_at + one-line rule L2293). JOIN fan-out r23 L933 (JOIN dropping/fanning rows, COUNT(*),COUNT(DISTINCT k) FROM right_table diagnostic, troubleshooting bullet #2 in SUM-too-small checklist) + multi COUNT DISTINCT one SELECT L183+ (FILTER, COUNT(DISTINCT ROW(a,b)) for pairs, distinct_aggregations_strategy) CONFIRMED. regexp_like LEADING CANONICAL + Postgres ~/~*/!~/RLIKE defang + mean-family CONFIRMED (per iter977). r28 DATE-literal pushdown CONFIRMED (per iter976). r13 json_exists + dbt-trino properties/partitioning + r27 dbt model-contract §6.7C CONFIRMED. r22 federation §13.1-13.8 + myths table L62-70 (TopN OSS-since-354, aggregate-ordering-dependency) UNTOUCHED — grep-confirmed all anchors present (pushdown limitation matrix, ordering dependency, dynamic filtering, Limit-vs-TopN glossary L49-52). STEP-1b iter978 PARTITION-DDL FINDING (for iter979 disposition if partition-DDL re-probe fails): r09 FINDABLY teaches ALL THREE required pieces. (i) CREATE TABLE WITH(partitioning=ARRAY[...]) — capability table L73 'WITH (partitioning = ARRAY[''day(occurred_at)'', ''tenant_id''])' transforms+identity columns both inside one ARRAY of STRINGS; (ii) ALTER SET PROPERTIES partitioning for an EXISTING table, new-writes-only — partition-evolution MYTH L153 verbatim: 'NO — Iceberg in-place partition evolution is metadata-only. ALTER TABLE iceberg.x.y SET PROPERTIES partitioning = ARRAY[''day(occurred_at)''] updates the spec; NEW writes use the day spec, OLD files keep their month spec, Trino handles BOTH at query time... old data doesn't benefit until you optionally run Spark rewrite_data_files'; (iii) BANS Spark/Hive PARTITIONED BY — DO-NOT-WRITE table L127 verbatim: 'PARTITIONED BY (day(occurred_at), tenant_id) AFTER the column list -> WRITE WITH (partitioning = ARRAY[...]) INSTEAD; Trino has no top-level PARTITIONED BY clause for the Iceberg connector... values are STRINGS' + recovery-procedure L135 step (2). NOTE: directive's L131 maps to bucket(col,N) column-first ban (also present, correct); the SET-PROPERTIES coverage lives at L153 (myth table) not L131. Coverage is SUFFICIENT and findable; no churn taken. git status clean. ZERO resource edits. NO git commit/push."
}

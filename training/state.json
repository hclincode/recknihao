{
  "iteration": 493,
  "phase": "extended",
  "final_iterations_remaining": 0,
  "passed": true,
  "notes": "Iter493 TEACHER — PRIMARY: installed LEADING CANONICAL 'Period-over-period: YoY vs MoM with window functions' block as NEW §5 Pattern B2 in resources/07-analytical-query-patterns.md, inserted DIRECTLY ADJACENT to existing Pattern B (LAG/LEAD section) — between Pattern B (line 625 end) and Pattern C (rank). Block addresses iter492 Q2 load-bearing semantic mismatch (responder used LAG default offset 1 = MoM but labeled/used it as YoY). EXISTING-CONTENT AUDIT BEFORE WRITING: grepped r07/r23/r28 for `LAG|LEAD|year[- ]over[- ]year|YoY|MoM|month[- ]over[- ]month|same month last year|period over period|LAG\\(.*12` — NO existing YoY/MoM/period-over-period content existed in ANY resource (only r07's Pattern B for generic day-over-day LAG(1) on a daily series and r28's mention of correlated-NOT-EXISTS Decorrelate Subqueries rule); iter492 Q2 LAG-default-offset-1-labeled-YoY error was 100% RESPONDER HALLUCINATION not stale resource content; this is a CLEAN RESOURCE GAP being filled (no contradictory stale content to reconcile). Block contains: (a) Core fact + LAG-offset-mapping table — `LAG(metric, 1)`=MoM (previous row=previous month on monthly bucket grain), `LAG(metric, 12)`=YoY (same month last year on monthly bucket grain), `LAG(metric, 4)`=QoQ on quarterly grain, `LAG(metric, 52)`=same-week-last-year on weekly grain (with ISO-week-53 caveat); (b) GAP-FILL CAVEAT — the crux: LAG counts ROWS not calendar periods, so a missing month silently shifts the offset to the wrong calendar period (typical worst case: 11 months back instead of 12 on a customer missing one month); same trap exists for MoM with offset 1; (c) FORM A (PREFERRED) — self-join on `prev.month = date_add('month', -12, cur.month)` — gap-safe by construction because unmatched rows produce NULL not shifted offset; full worked example with `date_trunc('month', occurred_at) AS month` defined in SELECT and GROUP BY REPEATING the expression (per iter485 GROUP-BY-expression rule); NULLIF(prev.usage_count, 0) divide-by-zero guard; ORDER BY using expression in CTE; (d) FORM B — LAG(12) OVER gap-filled-spine — full worked example with 3-CTE spine recipe (monthly_actuals + month_spine via `UNNEST(sequence(date_add('month', -25, ...), date_trunc('month', current_date), INTERVAL '1' MONTH))` + customers CROSS JOIN spine LEFT JOIN actuals COALESCE-to-0), then LAG(12) OVER the dense series; (e) FORM A vs FORM B picker matrix; (f) DO-NOT-WRITE matrix with 5 banned forms: (i) LAG(x) default offset labeled YoY — SILENT-WRONG (MoM not YoY); (ii) `usage_last_month` column fed into `yoy_growth_pct` — internal inconsistency (EXACT iter492 Q2 fail form, banned by name); (iii) LAG(12) on sparse series without gap-fill — silent-wrong on customers with gaps; (iv) `LAG(...) OVER (... ORDER BY event_month)` where event_month is a SELECT alias — analysis error per Trino #16533 grammar rule (cross-ref to Pattern A2 GROUP BY rules anchor rule 5); (v) `prev.month = cur.month - 12` bare integer subtraction on TIMESTAMP — type error; correct: `date_add('month', -12, cur.month)` or `cur.month - INTERVAL '12' MONTH`; (g) keyword anchor block: year over year / YoY / same month last year / year-over-year growth / compare to last year / growth vs last year / month over month / MoM / previous month / period over period / period-over-period growth / LAG window function / LAG 12 months / LAG offset 12 / same period last year / prior year comparison; (h) cross-refs to r07 Pattern A2 (GROUP BY rules anchor) + r27 + r28 (dbt incremental YoY use case). WebSearch-VERIFIED at trino.io/docs/current/functions/window.html — `lag(x[, offset[, default_value]])` 'Returns the value at offset rows before the current row in the window partition. Offsets start at 0, which is the current row.' default offset is 1; and trino.io/docs/current/functions/datetime.html — `date_add(unit, value, timestamp)` 'Adds an interval value of type unit to timestamp. Subtraction can be performed by using a negative value.' All SQL is valid Trino 467: GROUP BY repeats expression; window ORDER BY uses expression not alias; no QUALIFY; no `::` cast; no `WITH ... MATERIALIZED`; `UNNEST(sequence(...))` per Trino docs. §13.x federation guardrails in resources/22 UNTOUCHED. Federation 4.49944/310 row NOT probed/disturbed. NO fresh unvetted claims — every claim ties to a Trino docs URL or to existing canonical content (r07 Pattern A2 GROUP BY anchor, r28 #16533 alias rule). Files modified: resources/07-analytical-query-patterns.md (new Pattern B2 inserted between existing Pattern B at line 625 and Pattern C at old line 627)."
}

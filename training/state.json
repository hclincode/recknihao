{
  "iteration": 551,
  "phase": "extended",
  "final_iterations_remaining": 0,
  "passed": false,
  "notes": "Iter551 TEACHER — CONTINUE the iter550 proactive 'used-but-never-explained' audit. Iter550 closed three 'used-but-never-explained' findability gaps (IF/CASE, UNION, ref/source) and re-probe was a STRONG PASS 4.7188. iter551 continues the pattern with THREE more LEADING CANONICAL H3s on heavily-USED primitives that had no dedicated explanatory heading.\n\nGREP FINDINGS BEFORE EDIT (confirmation each had NO LEADING CANONICAL):\n  - FIX A (array_agg ORDER BY): `array_agg(... ORDER BY ...)` appears in r23 anti-pattern table (line 1169, single row), r07 §1a.2 (LEFT-JOIN trap — different topic), and r27 §7A.2A (windowed listagg context — Oracle migration angle). ZERO dedicated `### LEADING CANONICAL — array_agg ORDER BY ordered aggregation` H3 explaining INLINE-ORDER-BY-IS-DETERMINISTIC + array_agg DISTINCT + cross-ref to listagg/array_join. r07 §1a.2 covers the LEFT-JOIN FILTER trap but NOT the ordering semantic. iter550 Q4 was shallow at 4.125 — direct fix.\n  - FIX B (CTE/WITH semantics): r07 has only a parenthetical note (line 223) saying CTEs are 'named, inline temporary result sets'; no explanation of inlining-vs-materialization. r28 has CTE-inlining in §3 (perf-tuning angle, deep) + myths table. ZERO dedicated `### LEADING CANONICAL — WITH / CTE semantics in Trino` H3 in r07 (the query-patterns home where CTEs are HEAVILY used) explaining 'readability tool, NOT a materialization barrier; referenced-N-times = inlined N times'. WITH RECURSIVE only cross-ref'd in r27 §7A.1.\n  - FIX C (DISTINCT ON): r23 has DISTINCT ON only in the dialect anti-patterns table (line 1158, single row) — bottom of file. ZERO dedicated `### LEADING CANONICAL — Trino has NO DISTINCT ON` H3 with worked SQL example BEFORE the anti-patterns table (where keyword-matching is most likely to land). The §3.1A-G family belongs in the top half of r23 alongside the locked §3.1A-F canonicals.\n\nFIX A APPLIED — r07 §1a.2A ~L165-191 (new section between §1a.2 mental model and §1a.3 array-function quick reference):\n  - Heading: `### 1a.2A LEADING CANONICAL — array_agg(x ORDER BY y) ordered aggregation (+ DISTINCT; cross-ref listagg / array_join(array_agg(...), ','))`.\n  - Keyword-anchor blockquote covering: array_agg order, array_agg ORDER BY, ordered aggregation Trino, array_agg DISTINCT, collect ordered list, array_agg ORDER BY production, is array_agg ordered, does array_agg preserve order, collect rows into array in order, event sequence array.\n  - One-fact summary with Trino docs verbatim quote: 'array_agg(x ORDER BY y DESC)' and 'array_agg(x ORDER BY x, y, z)' under 'Ordering during aggregation'.\n  - Worked example pair: ordered event sequence per user (oldest first) + array_agg(DISTINCT event_name ORDER BY event_name) for dedupe.\n  - DO-NOT-WRITE quadruple: (1) array_agg(x) without ORDER BY then expect chronological order — non-deterministic; (2) outer SELECT ORDER BY doesn't order array elements; (3) array_agg(x ORDER BY y) OVER (PARTITION BY k) — NOT supported, cross-ref r27 §7A.2A pre-sorted CTE workaround; (4) array_agg for string-joining when listagg/array_join are correct — cross-ref r27 §7A.2A/§7A.2B.\n  - Cross-refs: r07 §1a.2 (FILTER LEFT-JOIN trap, same array_agg), r23 §3/§3.1D (rest of aggregate family + FILTER), r27 §7A.2A (windowed listagg → Trino).\n\nFIX B APPLIED — r07 §1b ~L221-249 (new section between §1a.4 array HOFs and §2 funnels, where CTEs are first used heavily):\n  - Heading: `## 1b. LEADING CANONICAL — WITH / CTE semantics in Trino (readability tool; NOT a materialization barrier — referenced-N-times = inlined N times)`.\n  - Keyword-anchor blockquote covering: WITH clause Trino, CTE materialized, CTE computed once or many, common table expression performance, WITH RECURSIVE Trino, CTE vs temp table, is a CTE cached, does Trino cache CTE results, CTE optimization fence, WITH AS MATERIALIZED Trino, should I rewrite repeated subqueries as a CTE.\n  - One-fact summary with Trino docs verbatim quote: 'Currently, the SQL for the WITH clause will be inlined anywhere the named relation is used. This means that if the relation is used more than once and the query is non-deterministic, the results may be different each time.'\n  - Worked example showing CTE referenced twice in a JOIN — the 100 GB shuffle runs twice.\n  - When-to-materialize-instead: dbt model with materialized='table' + ref() (default on this stack), or CREATE TABLE iceberg.tmp.X AS SELECT for one-shot ad-hoc (the prod environment's ad-hoc extract pattern).\n  - WITH RECURSIVE IS supported (bounded depth via max_recursion_depth session property; single-element recursive cycles only) — cross-ref r27 §7A.1 for the Oracle CONNECT BY migration.\n  - DO-NOT-WRITE quadruple: (1) 'CTE makes Trino compute it once' — FALSE; (2) WITH x AS MATERIALIZED (...) — parse error, no MATERIALIZED modifier; (3) 'more CTEs = faster' — FALSE; (4) using CTE as cross-statement temp — CTE lifetime is one statement only.\n  - Tail of §1b: 'See §1b above for the full CTE semantics canonical' anchor added to the existing parenthetical at §2 to make the cross-ref bi-directional (reconcile-in-place).\n  - Cross-refs: r28 §3 (CTEs-inlined perf deep dive + ephemeral/table/view cost model), r23 §11 (CTEs or subqueries — don't re-run the same expensive query twice), r27 §7A.1 (WITH RECURSIVE).\n\nFIX C APPLIED — r23 §3.1G ~L474-498 (new section between §3.1F UNION and §4 EXPLAIN):\n  - Heading: `## 3.1G. Trino has NO DISTINCT ON — use ROW_NUMBER() = 1 (or max_by) for one-row-per-group` with leading H3 `### LEADING CANONICAL — Trino has NO DISTINCT ON — use ROW_NUMBER() = 1 (or max_by) for one-row-per-group`.\n  - Keyword-anchor blockquote covering: DISTINCT ON Trino, one row per group, latest row per key, Postgres DISTINCT ON equivalent, ROW_NUMBER 1 dedup, top-1 per partition, pick the most recent row per user, keep the newest row per group, latest order per customer, first event per session, dedup keeping the latest.\n  - One-fact summary with Trino docs verification: DISTINCT ON is NOT in Trino SELECT grammar; QUALIFY is also NOT (cross-ref dialect anti-patterns table).\n  - Worked side-by-side example: Postgres DISTINCT ON ... ORDER BY DESC vs Trino subquery ROW_NUMBER() PARTITION BY ... ORDER BY ... DESC NULLS LAST WHERE rn = 1.\n  - Top-N-per-group note: same pattern with WHERE rn <= N. Single-column 'latest value' pick → max_by(val, ts) (cross-ref §3.1D — don't rewrite).\n  - DO-NOT-WRITE quintuple: (1) DISTINCT ON in Trino — parse error; (2) QUALIFY in Trino — parse error (cross-ref); (3) window function in WHERE — parse error in every dialect; (4) MAX(date) + ANY(other_col) GROUP BY — non-deterministic (other_col comes from random row, not max-date row); use max_by; (5) NULLS-default landmine in OVER (... ORDER BY ... DESC) — cross-ref r27 NULLS canonical.\n  - Cross-refs: §3.1D (arbitrary/max_by family), §dialect anti-patterns table (the full QUALIFY/LIMIT N BY/TOP N/DISTINCT ON ban with the canonical rewrite pattern), r27 §4.5C (ROWID dedup CTAS+swap).\n\nWEBSEARCH/WEBFETCH VERIFICATION (2026-06-06):\n  - trino.io/docs/current/functions/aggregate.html — QUOTED: 'array_agg(x ORDER BY y DESC)' and 'array_agg(x ORDER BY x, y, z)' under 'Ordering during aggregation' section.\n  - trino.io/docs/current/sql/select.html — QUOTED: 'Currently, the SQL for the WITH clause will be inlined anywhere the named relation is used. This means that if the relation is used more than once and the query is non-deterministic, the results may be different each time.' WITH RECURSIVE supported (experimental), with max_recursion_depth session property; single-element recursive cycles only.\n  - trino.io/docs/current/sql/select.html — CONFIRMED: DISTINCT ON is NOT in Trino's SELECT grammar (parse error). QUALIFY also not present in SELECT syntax.\n  - All new SQL valid for Trino 467.\n\nVERIFIED INTACT (no edits this iteration):\n  - resources/22 §13.x federation guardrails — ZERO edits. Federation rubric row stays 4.49944/310.\n  - r07 §1a-§1a.4 + Pattern C4 + approx + date_trunc(return-type + week-Monday) + §5 B2/B3 + FILTER + ROWS-vs-RANGE + array_join/array_position + sequence-pin — preserved; §1a.2A (array_agg ORDER BY) inserted ADJACENT between §1a.2 and §1a.3 with no rewrite; §1b (CTE semantics) inserted BEFORE §2 with no rewrite.\n  - r09 element_at H3 + map_concat H3 + map-HOF H3 + CAST-to-JSON H3 + SCD2 4-default + COALESCE-default-H4 — preserved.\n  - r10 partition-evolution + bucket-vs-identity — preserved.\n  - r13 §1 rollback-positional + §5479 rollback-Trino-vs-Spark + from_unixtime + JSON family — preserved.\n  - r17 maintenance + EXECUTE-PROCEDURE-ban + COMMENT-ON + rollback — preserved.\n  - r22 — ZERO edits.\n  - r23 §3.1A/§3.1B/§3.1C HALF_UP + §3.1D arbitrary/max_by + §3.1E IF/CASE [iter550] + §3.1F UNION [iter550] + approx_percentile family — preserved; §3.1G (DISTINCT ON canonical) inserted ADJACENT between §3.1F and §4 with no rewrite.\n  - r24 EXPLAIN-variants + ANALYZE/SHOW STATS + join_distribution_type — preserved.\n  - r26 §4 — untouched.\n  - r27 §4.1A/§4.3/§4.3-STR-FAMILY/§4.3A/§4.4*/§4.5*/§4.x/§4.6B/§7A.1/§7A.2A-B/§6.7A-K/§6.7A2 — preserved.\n  - r28 no-query-hint-H4 + CoW-MoR + §3 CTE-inlined perf dive — preserved.\n  - iter495-550 locks all preserved.\n\nFILES MODIFIED (2):\n  - resources/07-analytical-query-patterns.md (ADDED §1a.2A `array_agg(x ORDER BY y)` LEADING CANONICAL H3 between §1a.2 and §1a.3; ADDED §1b `WITH / CTE semantics in Trino` LEADING CANONICAL H2+H3 between §1a.4 and §2; tail anchor at §2's parenthetical updated to cross-ref §1b. §1a-§1a.4 and §2+ otherwise untouched).\n  - resources/23-sql-best-practices-olap.md (ADDED §3.1G `Trino has NO DISTINCT ON` LEADING CANONICAL H2+H3 between §3.1F and §4. §3.1A-F and §4+ untouched).\n\nDID NOT run git commit or git push — orchestrator handles single commit per iteration after judge. DID NOT touch resources/22. All SQL valid for Trino 467 + Iceberg. RECONCILE-IN-PLACE: each new H3 is an ADJACENT new heading slotted into a logical chain (not appended at file end); existing canonicals preserved verbatim, no duplication."
}

{
  "iteration": 953,
  "phase": "extended",
  "final_iterations_remaining": 0,
  "passed": true,
  "note": "iter953 LIGHT FIX-A — added a TIGHT inline-WRONG-marked FILTER-THEN-COUNT-ALWAYS-ZERO defang ADJACENT to the existing B-Streak Layer-3 count-per-streak canonical in r07 (reconcile-adjacent, NOT a new standalone card). DRIVER: iter951 Q4 + iter952 Q1 = confirmed 2-instance RECURRENCE of the gaps-and-islands FILTER-THEN-COUNT bug — responder filters to the boundary/target event (WHERE status='paid' / WHERE event_type='success') BEFORE counting the OTHER type (declines/failures), but WHERE runs before GROUP BY AND before window functions, so the counted rows are stripped first -> count is 0 every row (silent wrong result). The CORRECT count-per-streak shape was taught at B-Streak but the anti-pattern was NOT explicitly WRONG-marked -> 1st was slip, 2nd makes it a defang-worthy recurrence. VERIFY-FIRST (MANDATORY, done): WebFetch trino.io/docs/467/sql/select.html confirms 'HAVING filters groups after groups and aggregates are computed' (=> WHERE filters individual rows FIRST, then GROUP BY, then HAVING); WebFetch trino.io/docs/467/functions/window.html confirms 'Window functions ... run after the HAVING clause but before the ORDER BY clause' (=> windows run AFTER HAVING, hence AFTER WHERE). Both premises CONFIRMED: a WHERE filter strips rows before BOTH a GROUP BY aggregate AND a window function. EDIT: a single new Variants bullet ('Count the OTHER event type in the run right before a boundary event') inserted IMMEDIATELY AFTER the existing 'Streak as of a target date / current streak' variant (the reduce-to-target-adjacent shape) in the B-Streak Variants list, with a fenced WRONG block (-- WRONG (returns 0 every time — WHERE strips the counted 'declined' rows before GROUP BY / the window): ... WHERE status='paid' ... COUNT(*) FILTER (WHERE status='declined') ... -- DO NOT COPY) made un-copyable, plus a note that swapping GROUP BY for COUNT(*) OVER (PARTITION BY streak_id) has the SAME bug (windows run AFTER WHERE), and a copy-attractive CORRECT lead (count the other-type over the WHOLE streak per streak_id with NO pre-filter, GROUP BY streak_id, THEN keep streaks whose ending_status='paid' AFTER the per-streak aggregate) + a one-sentence RULE. Did NOT touch the Layer-1/2/3 skeleton, the NESTED_WINDOW defang, or the two-GROUP-BY-stapled parse-error defang. ONLY the B-Streak neighborhood edited. LOCK SPOT-CHECKS (read-only, INTACT): r07 L37 HAVING-does-not-cut-memory HOLDS verbatim ('reduce the INPUT rows... add a WHERE filter... or pre-aggregate in stages... A HAVING COUNT(*) > N does not help here: HAVING runs after all groups are built, so it only trims the OUTPUT rows'); GROUP-BY-output rule r07 L4338/L4341; anti-nesting iter573 PIN r07 L1624 + busiest-weekday MAX(COUNT(*)) parse-error L2179; first-match array_position L4448/L4506 + Pattern B3 first_value/last_value default-frame footgun L3691; argmax two-level r23 §3.1G + min_by/max_by first-AND-last L1273 + QUALIFY-not-in-Trino L1275 + DISTINCT-ON ROW_NUMBER=1 L1732; iter933 ADD-A-QUARTER/ADD-A-WEEK r07 + WHICH-QUARTER cards. r22 §13.x federation UNTOUCHED. NO git commit/push; working tree has only the r07 defang + this state bump. Resource filename map: r05=05-multi-tenant-analytics.md, r07=07-analytical-query-patterns.md, r09=09-lakehouse-schema-design.md, r10=10-lakehouse-partitioning.md, r13=13-postgres-to-iceberg-ingestion.md, r22=22-trino-federation-postgresql.md, r23=23-sql-best-practices-olap.md, r28=28-complex-sql-performance-trino-dbt.md."
}

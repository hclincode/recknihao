{
  "iteration": 972,
  "phase": "extended",
  "final_iterations_remaining": 0,
  "passed": true,
  "note": "iter972 NO-OP durability-breadth sweep — ZERO resource edits; only this state.json iteration bump. All locked canonicals re-confirmed intact via read-only spot-checks. iter953/955 B-Streak filter-then-count defang (r07 L3221-3262): FINDABILITY ROUTER L3228 (domain-neutral run-of-type-A-ending-at-type-B + 'returns 0 every time'), STREAK-ASSOCIATION L3232-3237 (run+target SHARE one streak_id, count over FULL streak, keep ending=target via max_by(status, event_time), fallback streak_id-1; both always-zero traps), CORRECT block L3239-3251 uses COUNT(*) FILTER + max_by(status, event_time) RETAINED (NOT nested arbitrary FILTER), WRONG#1 WHERE-to-target L3253-3256 (+COUNT(*) OVER also after WHERE) and WRONG#2 count-in-own-streak L3257-3259 both inline DO-NOT-COPY marked, the-rule recap L3262, NESTED_WINDOW L3221 + Layer skeleton UNCORRUPTED. r07 L37 HAVING does NOT cut GROUP BY memory (only trims OUTPUT) CONFIRMED. percent_rank/NTILE direction guardrail r07 L3905-3966 CONFIRMED. JOIN-fan-out / COUNT(DISTINCT): r23 L183-222 multiple-COUNT(DISTINCT)-native + COUNT(DISTINCT ROW(a,b)) composite + DO-NOT-WRITE; r23 L933 fan-out troubleshooting bullet (JOIN dropping/fanning rows, verify COUNT(*)/COUNT(DISTINCT k)); r23 L2730 duplicate-key IN-vs-JOIN inflation; r07 COUNT-after-LEFT-JOIN L903, EXISTS-vs-LEFT-JOIN L4422, forward-fill fanout-safe max_by L1600/L1623 ALL CONFIRMED. r05/r23 PERCENTILE footgun CONFIRMED (r05 L2234-2266 no PERCENTILE_CONT WITHIN GROUP / WITHIN GROUP listagg-only / use approx_percentile + array form; r23 L265 no PERCENTILE_CONT/DISC + L267-278 value-at-pct-vs-rank-of-row, approx_percentile no published std-error). as-of/argmax canonicals: r23 §3.1G QUALIFY-NOT-a-Trino-clause leading canonical + max_by/min_by form-1 (max_by(status,reading_time) latest) + ROW_NUMBER()=1 subquery+outer-WHERE form-2 + max_by-vs-ROW_NUMBER router CONFIRMED. Iceberg partition DDL WITH(partitioning=ARRAY[...]) + bucket(col,N) r09 L127/L135, GROUP-BY-output rule r07 L4338/L4341, iter933 ADD-A-QUARTER/ADD-A-WEEK r07 + WHICH-QUARTER router, r23 regexp_like §3310-3319 plain-pipe + markdown pipe-escape caution + mean-family geometric/harmonic, r13 json_exists + dbt-trino properties/partitioning, r27 dbt model-contract §6.7C all CONFIRMED. r22 §13.x federation UNTOUCHED. STEP 1b FINDING (two-independent-tables many-to-many sub-case, for iter973 disposition): NOT explicitly covered as a named canonical. The resource fan-out coverage is for ONE-to-many (single parent->child join) + duplicate-key inflation: r23 L933 bullet 2 says 'A JOIN dropping rows (inner join, key mismatch) or fanning rows (right-side duplicates inflate, then the SUM is over an inflated denominator after a DISTINCT)'; r23 L2730 'If premium_users has duplicate user_id rows, this JOIN multiplies the matching event rows ... silently inflated SUM or COUNT values'; r07 forward-fill L1600/L1623 pre-aggregates ONE fact table to one-row-per-(entity,bucket) via max_by BEFORE the spine LEFT JOIN. NONE state the specific TWO-SEPARATE-MULTI-ROW-TABLES case: when you ratio/compare two amounts that EACH live in a separate multi-row table keyed by the same entity (sales vs refunds per region), joining the RAW tables then SUMming creates a cross-product that inflates BOTH sums, so you must PRE-AGGREGATE EACH table to one-row-per-entity in its OWN CTE/subquery BEFORE joining. r23 has no 'aggregate both sides / each side in its own CTE before joining' canonical (grep of 'pre-aggregate' in r23 returns only approx_percentile weight, distinct_aggregations_strategy pre_aggregate, HLL sketches, correlated-scalar-subquery decorrelation L3040/L3052 — none is the two-independent-multi-row-tables case). DISPOSITION FOR iter973: candidate light additive FIX-A — a short named canonical 'compare/ratio two amounts each from a SEPARATE multi-row table -> pre-aggregate EACH to one-row-per-entity in its own CTE FIRST, THEN join (joining raw multi-row tables then SUMming = cross-product inflating BOTH sums)' near r23 L933, keyword-anchored (sales vs refunds per region, ratio of two tables, join two detail tables sum inflated, both sums too big, cross-product fan-out two tables). DO NOT edit this iteration — report only. Per feedback_synthesis_ceiling_stop_churning.md: residual hard-multi-step-synthesis misses are a Haiku ceiling not a resource gap. Per feedback_new_card_over_attracts_adjacent.md + feedback_responder_broken_secondary_alternative.md: responder padding/broken-secondary slips are per-instance one-off re-probes. ZERO resource edits. NO git commit/push. Resource filename map: r05=05-multi-tenant-analytics.md, r07=07-analytical-query-patterns.md, r09=09-lakehouse-schema-design.md, r13=13-postgres-to-iceberg-ingestion.md, r22=22-trino-federation-postgresql.md, r23=23-sql-best-practices-olap.md, r27=27-oracle-plsql-to-dbt-trino.md, r28=28-complex-sql-performance-trino-dbt.md."
}

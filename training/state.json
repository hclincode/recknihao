{
  "iteration": 635,
  "phase": "extended",
  "final_iterations_remaining": 0,
  "passed": false,
  "notes": "iter635 — FIX-A (PRIMARY) IMPLEMENTED + FIX-B = NO-OP. NO git commit / NO git push per directive. resources/22 UNTOUCHED (zero edits, federation row frozen). PIN HOLD: Trino 467 dialect (no QUALIFY/RLIKE/PERCENTILE_CONT/MEDIAN/initcap/dayname/PIVOT/DISTINCT ON; Trino Iceberg bucket() COLUMN-FIRST bucket(col, N)). DOCS-VERIFICATION 2026-06-07: (1) trino.io/docs/467/functions/window.html — percent_rank() definition CONFIRMED VERBATIM: `(r - 1) / (n - 1) where r is the rank of the row and n is the total number of rows in the window partition` — so first-row-of-ORDER-BY = 0.0, last-row = 1.0. Under ORDER BY metric DESC, the highest metric is the FIRST row and gets percent_rank 0.0; the lowest metric is the LAST row and gets 1.0. The iter634 silent-wrong (`>= 0.9` under DESC for `top 10%`) thus selects the BOTTOM decile — verified WRONG. (2) Same page — ntile(n) CONFIRMED VERBATIM: `Divides the rows for each window partition into n buckets ranging from 1 to at most n` with remainder distributed to first buckets. Bucket assignment follows ORDER BY position: bucket 1 = first rows in ORDER BY → under DESC bucket 1 = highest metric (top decile); under ASC bucket 1 = lowest metric. (3) trino.io/docs/467/sql/select.html — Trino 467 grammar is `SELECT [ ALL | DISTINCT ] select_expression [, ...]` — NO `DISTINCT ON (col_list)` extension. PostgreSQL-only syntax parse-errors in Trino. FIX-A IMPLEMENTED: PERCENT_RANK / NTILE direction guardrail at r07. (a) Tightened r07:1759 PERCENT_RANK bullet to lead with `FIRST row in ORDER BY gets 0.0; LAST row gets 1.0 — top/bottom depends on sort direction` and cross-link to the guardrail sub-section. (b) NEW LEADING CANONICAL sub-section inserted at r07:1794 immediately after Pattern C2 CUME_DIST line and before Pattern C3 NTILE — titled `LEADING CANONICAL — PERCENT_RANK / NTILE direction guardrail (iter635 — top X% by metric inversion trap)`; includes (i) keyword-anchor READ-THIS-FIRST line with `top 10% of customers by spend`, `share of revenue from the top decile`, `highest-spending 10%`, `top N percent by a metric`, `what percent of revenue comes from the top X%`, `bottom decile by revenue`, `customers in the top quartile by ARR`, `PERCENT_RANK >= 0.9 vs <= 0.1`, `which side of percent_rank is the top`, `NTILE bucket 1 vs bucket 10`; (ii) ONE-FACT lead naming the iter634 inversion explicitly; (iii) DECISION TABLE 6 rows covering all four direction×top/bottom×PERCENT_RANK plus the two NTILE direction variants (DESC bucket=1 / ASC bucket=N); (iv) mnemonic `percent_rank = 0.0 = FIRST row in the sort; pick the threshold for the side of the sort where your target rows land — never assume >= 0.9 means the top`; (v) DO NOT WRITE block with the exact iter634 silent-wrong SQL labeled WRONG (selects bottom 10%) + three RIGHT alternatives (DESC flip threshold, ASC keep >=0.9, NTILE(10) DESC = 1 count-based); (vi) DO NOT WRITE prose row inoculating against `percent_rank 0.0 = bottom, 1.0 = top` direction-naive explanation; (vii) PERCENT_RANK-vs-NTILE selection table for `top X%` with the share-of-grand-total composition path. (c) Also tightened r07:1914 (`This matters for SaaS metrics` paragraph) and r07:1938 (NTILE-vs-PERCENT_RANK selection table row) — both originally said `PERCENT_RANK() >= 0.9` direction-naive; both now read with `direction-correct threshold from the direction guardrail`: `<= 0.10` under DESC, `>= 0.90` under ASC. Added warning `Do NOT write the direction-naive PERCENT_RANK() >= 0.9 without checking the sort direction (under DESC, >= 0.9 selects the BOTTOM decile).` Also tightened r07:1792 CUME_DIST sibling note to flag CUME_DIST is similarly sort-direction-dependent under DESC. FIX-A LOCATION REFS — r07:1759 (tightened bullet), r07:1792 (tightened CUME_DIST sibling), r07:1794 (NEW LEADING CANONICAL guardrail sub-section, ~125 lines), r07:1914 (reconciled paragraph), r07:1938 (reconciled table row). FIX-B DECISION = NO-OP (well-covered). Grep-verified comprehensive DISTINCT ON inoculation ALREADY EXISTS at r23 §3.1G (lines 851-901): (i) Section title `Trino has NO DISTINCT ON — use ROW_NUMBER() = 1 (or max_by) for one-row-per-group`; (ii) LEADING CANONICAL block with keyword anchors `DISTINCT ON Trino`, `Postgres DISTINCT ON equivalent`, `one row per group`, `latest row per key`; (iii) ONE-FACT verbatim: `Postgres SELECT DISTINCT ON (k) ... FROM t ORDER BY k, ts DESC is NOT in Trino 467 — it raises a parse error: mismatched input ON`; (iv) Side-by-side Postgres-vs-Trino-ROW_NUMBER rewrite example at r23:861; (v) DO NOT WRITE row at r23:878 #1: `SELECT DISTINCT ON (k) ... in Trino — parse error. Always rewrite to the ROW_NUMBER() subquery (or max_by for a single-column pick)`; (vi) Anti-patterns table row at r23:1883: `DISTINCT ON (col) (Postgres' one row per group) | PostgreSQL | NOT supported. Parse error. | ROW_NUMBER() subquery + outer WHERE rn = 1 (same pattern as QUALIFY)`; (vii) Cross-references at r13:5392/5419 (`If duplicates have already landed in Iceberg ... you cannot use DISTINCT ON (user_id) in Trino — that is PostgreSQL-only syntax`), r27:1229, r27:1372. Plus `max_by(product_name, total_sales) GROUP BY category` (top product NAME per category one-liner) is ALREADY ANCHORED at r23:657 verbatim: `You want the value of x associated with the largest/smallest value of a sortable column y (e.g. latest status by updated_at, top-revenue product per category). max_by(x, y) / min_by(x, y). Deterministic by y.` Adding more inoculation would duplicate existing locked anchors — NO-OP. RECONCILE-IN-PLACE adherence: tightened/replaced existing stale text in r07 (not appended) — three direction-naive `PERCENT_RANK() >= 0.9` references reconciled to point at the new direction guardrail (one in C2 bullet, one in C3 SaaS-metrics paragraph, one in C3 selection table). New LEADING CANONICAL block placed adjacent to existing PERCENT_RANK / NTILE neighborhood (between Pattern C2 and Pattern C3) — judge cited ~r07:1841 Pattern C3 vicinity; the guardrail now anchors directly between the two. ALL iter534-634 LOCK INVENTORY PRESERVED IN PLACE: concat/format coercion guardrail r23:§3.1A (~427-491); nearest-hour FLOOR/NEAREST/CEILING r07:~1103-1135 + N-min-truncation r07:~1141; bool_or has-ever anchors r23:684-717; histogram r23:§3.1E; DISTINCT-*/PARTITION-BY-* r23:§3.1G (also serves as FIX-B coverage); approx_percentile/PERCENTILE_CONT inoculation r23:~L141-159; completed-age r23:~936; listagg-DISTINCT->array_join r23; EXCEPT-dedup r23:780; 2nd-most-recent ROW_NUMBER r23:813; COUNT(right_col)-vs-COUNT(*) LEFT-JOIN r07:293-326; positional-GROUP-BY r23:1278-1281/r07:1462-1492; regexp_replace-strip + alias-in-WHERE guard r27:§4.2; regexp_like-alternation/RLIKE inoculation r27:~982; strpos-3-arg r27:931; initcap inoculation r27:~937; strip-leading-zeros r27:~935; ROLLUP-vs-CUBE + GROUPING-label-mapping + omit-detail-tuple r28:~419-438; share-of-grand-total r07:1068-1095; date_trunc-hour-FLOOR r07:1101; json_array_length bare-array r13:3387; date-range-overlap r07:~947; LAG-MoM-delta r07:1505-1525; LAST_VALUE-IGNORE-NULLS-LOCF r07:769/873 + r23:905-951; NTILE r07:Pattern C3 (now shifted by FIX-A insert, still INTACT below the new guardrail); split_part-name-split + max-gap-LAG-date_diff + searched-CASE-buckets + self-join-pairs + ROW_NUMBER-top1 / max_by r23:597+ / first_value-last_value / cume_dist / PERCENT_RANK r07 (now tightened at 1759 + guardrail at 1794) / day_of_week / greatest-least / NULLIF-guard / count_if / §11-conditional-aggregation-FILTER ALL INTACT. resources/22-trino-federation-postgresql.md UNTOUCHED (zero edits). NO QUALIFY/RLIKE/PERCENTILE_CONT/MEDIAN/initcap/dayname/PIVOT/DISTINCT-ON/::cast in any new edit (verified via grep). All new SQL is valid Trino 467 dialect (PERCENT_RANK, NTILE, FILTER, SUM OVER, subquery-WHERE-on-window-alias pattern). Trino Iceberg bucket() COLUMN-FIRST PIN held (not touched this iteration). NO git commit / NO git push per directive."
}

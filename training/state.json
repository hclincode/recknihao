{
  "iteration": 536,
  "phase": "extended",
  "final_iterations_remaining": 0,
  "passed": false,
  "notes": "Iter536 TEACHER — TWO edits per directive: FIX A (HIGH, PRIMARY) added new `date_trunc('week') = MONDAY` ISO-8601 canonical to r07 directly adjacent to the locked date_trunc return-type note (iter535 Q4 was 2.75 — responder honestly declined on Monday-vs-Sunday, THEN invented a WRONG workaround `date_trunc('week', d + INTERVAL '1' DAY) - INTERVAL '1' DAY` to 'force Monday', which actually produces SUNDAY-start). FIX B (LOW, SECONDARY) added preventive exact-value pin to r17 §Snapshot-retention block making `history.expire.max-snapshot-age-ms = 432000000 ms = 5 days` (NOT 7 days) explicit + banning the 7-day-Iceberg-default conflation.\n\nGREP FINDINGS:\n  - `grep -rn \"date_trunc('week'|week start|day_of_week|ISO week\" resources/07 resources/27`: r07 has week SQL at L322/L443/L524 (cohort week buckets), the locked date_trunc return-type note at L610, and `LAG(metric, 52)` ISO-week YoY at L821; r27 has day_of_week at L589/L692/L697/L700/L718 (NEXT_DAY canonical). Confirmed Monday/ISO fact NOT pinned anywhere; week-start direction was a content gap.\n  - `grep -rn \"history.expire.max-snapshot-age\" resources/17`: present at L1967 (definition), L2040 (`Iceberg's actual default ... is 5 days`), L2042 (`default 5 days`), L2050, L2060 (30-day operator example), L2065, L2072 (`Iceberg's 5-day ... default`), L2806 (`defaults to 5d ... no explicit older_than`). NO existing 7-day claim for the Iceberg table-level default — all 7-day mentions correctly attribute to Trino's `iceberg.expire-snapshots.min-retention` floor (separate setting). FIX B is preventive — adds the EXACT `432000000` ms value + explicit ban on conflation.\n  - `grep -n 'date_trunc|week' resources/07`: line 610 is the locked date_trunc return-type note. New Monday/ISO canonical inserted IMMEDIATELY AFTER L610, BEFORE the `---` separator and `## 5.` Window functions header — preserves locked content intact, places new content at the keyword-anchor for 'week start / Monday / date_trunc week'.\n\nWEBSEARCH VERIFICATION (2026-06-06):\n  - trino.io/docs/current/functions/datetime.html — `day_of_week(x)` returns 'the ISO day of the week from x, with values ranging from 1 (Monday) to 7 (Sunday)'. ISO 8601 standard, Monday is day 1. `week(x)` returns ISO week of year 1..53. `date_trunc(unit, x)` truncates to unit — ISO-8601 alignment means 'week' rounds DOWN to Monday. Worked example: `date_trunc('week', DATE '2020-01-01')` (a Wednesday) returns `2019-12-30` (Monday of that week) — Monday-start, no workaround needed.\n  - iceberg.apache.org/docs/latest/configuration/ — `history.expire.max-snapshot-age-ms` documented default `432000000` ms = **5 days**. Quote: 'specifies the default maximum age of snapshots to keep while expiring snapshots.' Companion `history.expire.min-snapshots-to-keep` default `1`. Confirms Iceberg table-level default is 5 days, NOT 7 days; 7-day is Trino-specific floor (`iceberg.expire-snapshots.min-retention`).\n  - All SQL in new r07 §Monday canonical is valid Trino 467 dialect (no QUALIFY, no `::` cast, no Spark-only `+/-` interval arithmetic that Trino rejects). The `event_ts + INTERVAL '1' DAY` form is Trino-valid for timestamp/date + interval.\n\nEDITS (2 in-place, no appended duplicate blocks):\n\n  1. resources/07-analytical-query-patterns.md — NEW `#### date_trunc('week', ts) ALWAYS starts the week on MONDAY (ISO-8601) — Trino canonical (iter536 PIN)` subsection inserted between locked L610 (date_trunc return-type note) and L612 (`---` separator before `## 5. Window functions`). Block contents (~30 lines): (a) keyword anchors line (date_trunc week Monday, Trino week start day, weekly report week start, ISO week Trino, Sunday vs Monday week, first day of week, European Monday, US Sunday); (b) TRUTH paragraph stating Monday/ISO-8601 + day_of_week 1=Monday..7=Sunday + NO locale/first_day_of_week setting — verified at trino.io docs; (c) WORKED EXAMPLE: `date_trunc('week', DATE '2020-01-01')` = `2019-12-30` (the Monday); (d) IN-LINE SIGNAL canonical SQL block — `SELECT date_trunc('week', event_ts) AS week_start,  -- Trino weeks ALWAYS start MONDAY (ISO-8601); no Sunday-start option, no workaround needed` (corrective on the line the responder copies, per iter534 strategy); (e) DO-NOT-WRITE ban on `date_trunc('week', d + INTERVAL '1' DAY) - INTERVAL '1' DAY` as a Monday fix — explicit explanation that the interval-shift produces SUNDAY-start; (f) Sunday-start recipe ONLY framed as the SUNDAY recipe (US-style report) with explicit framing 'rare — only when the report spec explicitly demands Sunday'; (g) default-to-Monday-when-unspecified guidance. Does NOT rewrite L610 (locked iter533 date_trunc return-type note kept verbatim).\n\n  2. resources/17-iceberg-table-maintenance.md — preventive exact-value pin inserted between L2806 (`defaults to 5d`) and the L2808 separator paragraph. Block contents (3 lines, blockquote): '**Exact-value pin (iter536):** the documented default of `history.expire.max-snapshot-age-ms` is **`432000000` ms = 5 days** (verified at iceberg.apache.org/docs/latest/configuration/). **NOT 7 days** — the 7-day number applies to the Trino-only `iceberg.expire-snapshots.min-retention` catalog floor (a separate setting in a separate config layer). Do **not** state \"Iceberg's table-level snapshot retention default is 7 days\" — that conflates the two settings. Iceberg = **5d / 432000000 ms** (table property); Trino floor = **7d** (catalog property).' Reinforces existing L2040/L2042/L2072/L2806 5-day fact with the EXACT millisecond value and explicit conflation ban.\n\nFILES MODIFIED (2): resources/07-analytical-query-patterns.md (NEW Monday/ISO canonical inserted after L610 locked date_trunc note), resources/17-iceberg-table-maintenance.md (preventive 432000000 ms / 5d exact-value pin after L2806).\n\nUNTOUCHED per directive: resources/22 §13.x federation guardrails — ZERO edits; federation rubric row stays 4.49944/310; NO federation probe. r17 iter534 in-line ON-clause comment / header / placeholder — preserved intact. r17 iter535 deleted_snapshot_id preventive pin — preserved intact. r07 locked canonicals (§1a-§1a.4 + Pattern C4 + approx + date_trunc return-type note at L610 + §5 B2/B3) — all preserved (new Monday canonical placed ADJACENT to L610, not rewriting it). r09 element_at + map-HOF + CAST-to-JSON + dbt-snapshot + iter532 COALESCE-default — untouched. r13 from_unixtime epoch-ms + JSON family — untouched. r17 CRITICAL QUOTING + $partitions + current-spec + RENAME + $snapshots ops + target-file-size + WAP + $refs + iter531-535 $files/$history/$snapshots JOIN canonical + in-line signal + deleted_snapshot_id pin — untouched (only added the new L2807 exact-value pin to the existing snapshot-retention block). r23/r26/r27 — untouched. iter495-535 locks preserved.\n\nDID NOT run git commit or git push — orchestrator handles single commit per iteration after judge. DID NOT touch resources/22. All SQL valid Trino 467 dialect."
}
